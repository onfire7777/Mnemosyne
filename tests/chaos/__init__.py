"""Phase-2 Task 11 — DST/chaos harness, differential oracle, lane invariants.

Everything under ``tests/chaos/`` is GATED behind ``MNEMOSYNE_CHAOS=1`` by
``conftest.py`` (collection-time), so a plain ``pytest`` run neither executes
nor counts these tests. See ``conftest.py`` for the mechanism and the Task-11
report for the rationale.
"""
