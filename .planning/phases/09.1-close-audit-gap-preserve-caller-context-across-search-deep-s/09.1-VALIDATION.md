---
phase: 09.1
slug: close-audit-gap-preserve-caller-context-across-search-deep-s
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-10
---

# Phase 09.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest via locked `uv` environment; Ruff for static checks |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `uv run --locked python -m pytest tests/test_access_policy_enforcement.py tests/test_parity_mcp.py tests/test_cli_runtime_tools.py tests/test_runtime_surfaces.py -q` |
| **Full suite command** | `uv run --locked python -m pytest && uv run --locked ruff check . && git diff --check && gsd-sdk validate consistency` |
| **Estimated runtime** | Quick feedback under 180 seconds; full local gate depends on optional backend availability |

---

## Sampling Rate

- **After every task commit:** Run the smallest affected test nodes, then the four-file quick suite before advancing.
- **After every plan wave:** Run the full local gate.
- **Before `$gsd-verify-work`:** Full local gate, CBM freshness, gbrain health, and required GitHub CI jobs must be green.
- **Max feedback latency:** 180 seconds for task-level automated feedback.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 09.1-01-01 | 01 | 1 | REQ-003, REQ-004, REQ-007, NFR-004 | T-09.1-01 | Missing or mismatched caller context filters protected records without revealing their existence, while legacy positional search/deep-search calls retain their established meaning. | red-first contract | Collect the Phase 09.1 selector in all four suites, then require the selected pytest run to exit exactly `1`; see `09.1-01-PLAN.md`. | ✅ | ⬜ pending |
| 09.1-01-02 | 01 | 1 | REQ-003, REQ-004, REQ-007, NFR-004 | T-09.1-01 | All public read operations use the canonical fail-closed engine filter, preserve search trust-tier precedence, and add fields without positional reinterpretation. | unit + integration | `uv run --locked python -m pytest tests/test_access_policy_enforcement.py tests/test_cli_runtime_tools.py -q && uv run --locked ruff check src/mnemosyne/mcp_tools.py src/mnemosyne/cli.py tests/test_access_policy_enforcement.py tests/test_cli_runtime_tools.py` | ✅ | ⬜ pending |
| 09.1-01-03 | 01 | 1 | REQ-003, REQ-004, REQ-007, NFR-004 | T-09.1-01 | Generated MCP, JSON-RPC, and SDK surfaces expose semantically identical optional context fields regardless of order, and denied results leak no protected metadata. | contract + transport + closure | Full local gates plus the executable source-scope, staged-secret, commit/push, exact-SHA CI, CBM, gbrain, and clean-sync closure gate in `09.1-01-PLAN.md`. | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing pytest, CLI subprocess, JSON-RPC, optional official MCP SDK, and Ruff infrastructure covers every phase requirement. No new dependency, fixture framework, or test runner is required.

---

## Manual-Only Verifications

All phase behaviors have automated verification. GitHub CI, CBM index freshness, and gbrain source health are operator-observed completion gates rather than substitutes for behavioral tests.

---

## Validation Sign-Off

- [x] All tasks have automated verification commands.
- [x] Sampling continuity: every task has a closest-layer automated check.
- [x] Wave 0 covers all referenced test infrastructure.
- [x] No watch-mode flags.
- [x] Task feedback latency target is under 180 seconds.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** approved 2026-07-10 under the user's no-compromise execution authorization.
