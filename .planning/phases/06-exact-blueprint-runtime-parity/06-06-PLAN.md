---
phase: 06-exact-blueprint-runtime-parity
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mnemosyne/parametric.py
  - tests/test_learning_and_attack_suite.py
autonomous: true
requirements: [FR-21]
must_haves:
  truths:
    - "A real LoRA / test-time-training trainer + protected-suite + rollback path exists as authorable code with local isolated validation."
    - "The trainer runs in isolation (no GPU/cloud required for the local validation) and enforces the same rails parametric-trainer-check expects."
    - "Production GPU trainer deploy + production rollback evidence remains operator-run under row 9."
  artifacts:
    - path: "src/mnemosyne/parametric.py"
      provides: "Real LoRA/TTT trainer + protected-suite + rollback code"
      contains: "lora"
    - path: "tests/test_learning_and_attack_suite.py"
      provides: "Local isolated validation of trainer/protected-suite/rollback"
  key_links:
    - from: "src/mnemosyne/parametric.py"
      to: "protected regression suite + rollback rails"
      via: "trainer enforces protected-case pass + margin-over-noise + authorized rollback before artifact accept"
      pattern: "protected|rollback|rail"
---

<objective>
CURRENT STATUS after `7f8fd99`: complete locally. `06-06-SUMMARY.md`,
`.planning/STATE.md`, `src/mnemosyne/parametric.py`, and
`tests/test_learning_and_attack_suite.py` show the command-backed LoRA/TTT
trainer, protected-suite enforcement, rail checks, and rollback drill are
implemented with local isolated validation. Do not reopen this as new source
work; only Tier B operator evidence remains for deployed GPU trainer and
production rollback proof.

Historical objective: Implement FR-21 real LoRA / test-time-training (D-12): author the trainer / protected-suite / rollback CODE with LOCAL isolated validation. The existing `parametric.py` already exposes a command-backed LoRA/TTT proposal/evaluation/rollback boundary with rails (operator-grade auth, mutation-rate bounds, external-reward, monotonic-trust/sink restrictions, exact artifact/gate matching, protected-case pass, margin-over-noise). This plan completes the real trainer/TTT code path and validates it in isolation (no GPU, no cloud). Real GPU trainer deploy + production rollback evidence stays operator-run under parity row 9 (plan 06-09); this overlaps that row and the ROLLBACK.md drill (plan 06-03).

Purpose: FR-21 is one of the optional items the operator opted into this phase (D-02/D-12). Completing the trainer/protected-suite/rollback code + local validation makes the parametric tier code-complete so only deployed-trainer operator evidence remains.
Output: real LoRA/TTT trainer + protected-suite + rollback code, locally isolated-validated.

HISTORICAL COORDINATION HAZARD (D-14): at planning time an autonomous session owned `src/`. VERIFY the current `parametric.py` command-provider boundary and rails first, COMPLETE the trainer/TTT path additively behind the existing operator-authorization + rail gates (do not weaken or rewrite the rails), and NEVER `git add -A` — stage only the two named files. Verify the tree is not mid-edit before committing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@docs/ROADMAP-TO-100.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the real LoRA/TTT trainer + protected-suite enforcement</name>
  <files>src/mnemosyne/parametric.py, tests/test_learning_and_attack_suite.py</files>
  <read_first>
    - src/mnemosyne/parametric.py (existing command-backed LoRA/TTT proposal/evaluation/rollback boundary; the rail set: operator auth, mutation-rate bounds, external-reward metadata, monotonic-trust/sink restrictions, exact artifact/gate matching, protected-case pass, margin-over-noise)
    - tests/test_learning_and_attack_suite.py (the learning/attack test surface to extend with isolated trainer/protected-suite cases)
    - .planning/STRICT-BLUEPRINT-PARITY-AUDIT.md (Parametric tier row — what parametric-trainer-check validates: trainer/protected_suite/gate/rollback/deployment/rail_report sections, canonical protected-suite IDs/counts/non-synthetic source/tier coverage, margin floors, no promoted rollback branch)
    - docs/ROADMAP-TO-100.md (A11/A12/FR-21 notes — FR-21 overlaps Tier B B8 GPU trainer; local isolated validation here, production GPU evidence operator-run)
  </read_first>
  <behavior>
    - Test (trainer produces artifact): the LoRA/TTT trainer runs in isolation and produces a persisted artifact with a deterministic/hashable URI; no GPU required for the local run.
    - Test (protected-suite gate): an artifact that fails any protected case or falls below the margin-over-noise floor is REJECTED (not promoted).
    - Test (rails enforced): operator authorization required; external-reward-only, monotonic-trust, no-system-prompt-sink, no eval-source overlap rails block a violating artifact.
  </behavior>
  <action>
    Complete the real LoRA/TTT trainer path in `src/mnemosyne/parametric.py` so it produces a real (isolated, non-synthetic-source) artifact and runs the protected suite + margin-over-noise floor before acceptance, behind the existing operator-authorization + rail gates. Keep the rails intact — the trainer must fail closed exactly as `parametric-trainer-check` validates. Add isolated local tests to `tests/test_learning_and_attack_suite.py` for the three behaviors above (no GPU, no cloud).
  </action>
  <verify>
    <automated>grep -niE "lora|test.time|ttt" src/mnemosyne/parametric.py | head -1 && .venv/bin/python -m pytest -q tests/test_learning_and_attack_suite.py -k "parametric or lora or trainer or ttt or protected" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/mnemosyne/parametric.py` has a real LoRA/TTT trainer path producing a persisted artifact (grep `lora` or `ttt` matches a real path).
    - Protected-suite gate + margin-over-noise floor reject a failing artifact (test passes).
    - Operator-auth + reward/trust/sink/eval-overlap rails enforced; violating artifacts fail closed (test passes).
    - Existing rails are not weakened or removed.
  </acceptance_criteria>
  <done>The real LoRA/TTT trainer + protected-suite enforcement is code-complete with isolated local validation and intact rails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the rollback path + local rollback-drill validation</name>
  <files>src/mnemosyne/parametric.py, tests/test_learning_and_attack_suite.py</files>
  <read_first>
    - src/mnemosyne/parametric.py (the rollback boundary + rollback-suite snapshot persistence + protected-case rollback metrics)
    - tests/test_learning_and_attack_suite.py (extend with the rollback-drill case)
    - .planning/ROLLBACK.md scope (plan 06-03 — the parametric-trainer-check rollback drill this code must support; cross-reference, do not author the runbook here)
  </read_first>
  <behavior>
    - Test (authorized rollback): an operator-authorized rollback reverts to the prior artifact, persists the rollback suite snapshot + protected-case rollback metrics, and promotes no rollback branch.
    - Test (unauthorized rollback denied): a non-operator / low-trust rollback attempt fails closed.
    - Test (rollback-drill shape): the rollback produces the evidence shape parametric-trainer-check expects (rollback drill verification, authorized rollback, no promoted rollback branch).
  </behavior>
  <action>
    Complete the rollback path in `src/mnemosyne/parametric.py` so an authorized rollback reverts to the prior artifact, records the rollback suite snapshot + protected-case rollback metrics, promotes no rollback branch, and fails closed for unauthorized callers — matching what `parametric-trainer-check` validates and what the ROLLBACK.md drill (plan 06-03) describes. Add the isolated local rollback-drill tests to `tests/test_learning_and_attack_suite.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_learning_and_attack_suite.py -k "rollback or parametric or trainer" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Authorized rollback reverts to prior artifact, persists rollback suite snapshot + metrics, promotes no rollback branch (test passes).
    - Unauthorized/low-trust rollback fails closed (test passes).
    - Rollback evidence shape matches parametric-trainer-check expectations and the ROLLBACK.md drill.
  </acceptance_criteria>
  <done>The parametric rollback path is code-complete with authorized-revert, no-promoted-branch, fail-closed denial, and isolated local rollback-drill validation.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/test_learning_and_attack_suite.py` passes locally for trainer + rollback (isolated, no GPU/cloud).
- Rails intact and fail-closed (D-12); production GPU/rollback evidence stays operator-run under row 9.
- Only the two named files staged; no `git add -A` (D-14).
</verification>

<success_criteria>
The real LoRA/TTT trainer, protected-suite gate, and rollback path are code-complete and isolated-locally-validated, leaving only deployed-GPU operator evidence (row 9) for full FR-21 parity.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-06-SUMMARY.md` when done.
</output>
