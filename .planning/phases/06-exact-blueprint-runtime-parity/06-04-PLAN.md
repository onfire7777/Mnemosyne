---
phase: 06-exact-blueprint-runtime-parity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/schema.sql
  - src/mnemosyne/postgres_engine.py
  - tests/test_shared_engine_contract.py
autonomous: true
requirements: [FR-12]
must_haves:
  truths:
    - "A materialized cached-PPR column + refresh path exists on the Postgres engine and is default-off."
    - "When the cached-PPR path is inactive, retrieval output is byte-identical to the current recursive-PPR behavior on both Local and Postgres engines."
    - "A refresh path can materialize the cached-PPR column and the shared Local/Postgres contract covers the active and inactive paths."
  artifacts:
    - path: "sql/schema.sql"
      provides: "Raw-SQL migration adding the materialized cached-PPR column (additive, no ORM)"
      contains: "ppr"
    - path: "src/mnemosyne/postgres_engine.py"
      provides: "Cached-PPR read path + refresh path, default-off"
    - path: "tests/test_shared_engine_contract.py"
      provides: "Shared Local/Postgres coverage for cached-PPR active + inactive (byte-identical when off)"
  key_links:
    - from: "src/mnemosyne/postgres_engine.py"
      to: "sql/schema.sql cached-PPR column"
      via: "reads/refreshes the materialized column when enabled"
      pattern: "ppr"
---

<objective>
Implement A12 (D-10): add a materialized cached-PPR column plus a refresh path to the Postgres engine. The change is ADDITIVE, DEFAULT-OFF, and BYTE-IDENTICAL when inactive — no retrieval-quality change when disabled. The schema migration is raw SQL in `sql/schema.sql` (no ORM). Shared Local/Postgres coverage proves both the active refresh path and that the inactive path matches current recursive-PPR output exactly. Production cached-PPR claims stay gated behind the real-infra evidence pass (row 1, plan 06-09).

Purpose: A12 is the one optional retrieval-code item the operator opted into this phase (D-02/D-10). Closing it removes the last optional Tier-A retrieval gap.
Output: cached-PPR column + refresh path + shared contract coverage.

HISTORICAL COORDINATION HAZARD (D-14): at planning time an autonomous session owned `src/` and `infra/`. This task EXTENDED `sql/schema.sql` and `postgres_engine.py` additively. VERIFY the current state of both files before editing, ADD only the cached-PPR column/method/refresh (do not refactor or rewrite existing recursive-PPR code), and NEVER `git add -A` — stage only the three named files. Verify the tree is not mid-edit before committing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@docs/ROADMAP-TO-100.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the materialized cached-PPR column + raw-SQL migration</name>
  <files>sql/schema.sql, tests/test_shared_engine_contract.py</files>
  <read_first>
    - sql/schema.sql (the full raw-SQL schema — identify the relations/graph tables the recursive PPR traverses; add the new column/materialized structure additively, default-off)
    - src/mnemosyne/postgres_engine.py (locate the existing recursive graph/PPR query path — `graph_ppr` and active-only relation validity; the cached column must mirror its output when refreshed)
    - tests/test_shared_engine_contract.py (the shared Local/Postgres contract pattern to extend)
    - docs/ROADMAP-TO-100.md (A12 row: "Add cached-PPR column + refresh path", default-off, gated behind real-infra evidence)
  </read_first>
  <behavior>
    - Test (inactive/default-off): with the cached-PPR feature disabled, `graph_ppr` / fused retrieval output on Postgres is byte-identical to the current recursive-PPR output for a fixed seed set.
    - Test (schema additive): applying `sql/schema.sql` on a fresh database succeeds and exposes the new cached-PPR column without altering existing column semantics.
    - Test (parity): Local and Postgres produce the same retrieval result when the feature is off (extends existing cross-engine parity expectations).
  </behavior>
  <action>
    Add the cached-PPR materialized column to `sql/schema.sql` as raw SQL (a nullable/default-empty column or a dedicated materialized table keyed to the graph node, default-off semantics — choose the additive shape that does not change existing recursive-PPR query behavior). Add forcing-function tests in `tests/test_shared_engine_contract.py` for the three behaviors above. Do NOT change the default retrieval path: the column is unused unless explicitly enabled.
  </action>
  <verify>
    <automated>grep -niE "ppr" sql/schema.sql | head -1 && .venv/bin/python -m pytest -q tests/test_shared_engine_contract.py -k "ppr or cached or parity" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `sql/schema.sql` contains an additive cached-PPR column/structure (grep `ppr` matches a newly added definition) and applies cleanly on a fresh DB.
    - The default (feature-off) retrieval path is unchanged: new and existing parity tests pass with the feature disabled.
    - No existing recursive-PPR SQL or column is rewritten or removed.
  </acceptance_criteria>
  <done>The cached-PPR column exists in sql/schema.sql as additive raw SQL, default-off, with shared-contract tests proving byte-identical inactive behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the cached-PPR read + refresh path on PostgresEngine</name>
  <files>src/mnemosyne/postgres_engine.py, tests/test_shared_engine_contract.py</files>
  <read_first>
    - src/mnemosyne/postgres_engine.py (the recursive graph/PPR method; add a default-off cached read + a refresh method beside it without altering the recursive default)
    - sql/schema.sql (the cached-PPR column added in Task 1)
    - tests/test_shared_engine_contract.py (extend with the active-path test)
  </read_first>
  <behavior>
    - Test (refresh): calling the refresh path materializes the cached-PPR column from the current graph, and a subsequent cached read returns the same ranked node set the recursive path would return for the same seeds.
    - Test (active vs recursive equivalence): when enabled-and-refreshed, cached-PPR retrieval equals recursive-PPR retrieval for a fixed seed set (no retrieval-quality change).
    - Test (still default-off): without enabling, the recursive path runs and the cached column stays unused.
  </behavior>
  <action>
    Add a cached-PPR read path and a refresh path to `src/mnemosyne/postgres_engine.py`, both gated behind an explicit default-off flag/config. The refresh materializes the column added in Task 1; the cached read is only consulted when the feature is enabled and the column is fresh. Preserve the recursive-PPR default exactly. Add the active-path tests to `tests/test_shared_engine_contract.py`. Keep adapter/scope fail-closed behavior consistent with existing graph hits (tenant/branch scope validation).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_shared_engine_contract.py -k "ppr or cached or refresh or parity" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/mnemosyne/postgres_engine.py` has a cached-PPR read path AND a refresh path, both default-off.
    - When enabled+refreshed, cached-PPR result equals recursive-PPR result for a fixed seed set (test passes).
    - When disabled (default), retrieval uses recursive PPR and tests confirm byte-identical output vs pre-change behavior.
    - Cached hits honor tenant/branch scope validation (fail-closed) like existing graph hits.
  </acceptance_criteria>
  <done>Cached-PPR read + refresh implemented default-off on PostgresEngine, equivalent to recursive PPR when active, byte-identical when inactive, covered by the shared contract.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/test_shared_engine_contract.py` passes locally (feature-off path).
- With `MNEMOSYNE_POSTGRES_DSN` set, the same file passes against compose-Postgres (covered fully in plan 06-07 Lane G sweep).
- A12 is additive, default-off, byte-identical when inactive (D-10).
- Only sql/schema.sql, src/mnemosyne/postgres_engine.py, tests/test_shared_engine_contract.py staged; no `git add -A` (D-14).
</verification>

<success_criteria>
The materialized cached-PPR column + refresh path exists and is provably inert when disabled, so it adds the optional A12 capability without risking any retrieval-quality regression.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-04-SUMMARY.md` when done.
</output>
