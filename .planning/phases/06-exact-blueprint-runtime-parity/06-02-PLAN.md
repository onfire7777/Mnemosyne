---
phase: 06-exact-blueprint-runtime-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ENV-AND-SECRETS.md
autonomous: true
requirements: [FR-7, FR-9]
must_haves:
  truths:
    - "An operator can read .planning/ENV-AND-SECRETS.md and know every environment variable Mnemosyne consumes for production, what it is, and where its value comes from — without any secret value being printed."
    - "Vault, Keycloak, and KMS wiring + provisioning steps are documented by cross-reference to the existing infra/ assets, not by restating them."
  artifacts:
    - path: ".planning/ENV-AND-SECRETS.md"
      provides: "Env-var catalog + Vault/Keycloak/KMS wiring + provisioning steps, no secret values"
      contains: "MNEMOSYNE_POSTGRES_DSN"
      min_lines: 60
  key_links:
    - from: ".planning/ENV-AND-SECRETS.md"
      to: "infra/vault/, infra/keycloak/, infra/c2pa/"
      via: "cross-reference to existing provisioning assets"
      pattern: "infra/(vault|keycloak|c2pa)"
    - from: ".planning/ENV-AND-SECRETS.md"
      to: "src/mnemosyne/cli.py provider/secret flags"
      via: "documents the env vars the CLI/provider boundary reads"
      pattern: "MNEMOSYNE_|MNEME_"
---

<objective>
Author Lane D: `.planning/ENV-AND-SECRETS.md` — the env-var catalog plus Vault/Keycloak/KMS wiring and provisioning steps for a production Mnemosyne deployment. NO secret values appear anywhere in the document; secrets are described by source (environment, file, or command provider) only (D-04). The file cross-references the existing `infra/keycloak/`, `infra/vault/`, and `infra/c2pa/` assets rather than restating them.

Purpose: Lane C runbooks (plan 06-01) and the operator-evidence pass (plan 06-09) reference env-var names and provisioning steps; this is the single catalog that owns them so no runbook restates secret provisioning (OPS-HANDOFF section 4: D-vs-C seam).
Output: `.planning/ENV-AND-SECRETS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@.planning/OPS-HANDOFF-AND-OWNERSHIP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the env-var catalog + IdP/Vault/KMS wiring</name>
  <files>.planning/ENV-AND-SECRETS.md</files>
  <read_first>
    - .planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md (D-04 env/secrets contract; canonical-refs list of infra assets)
    - .planning/OPS-HANDOFF-AND-OWNERSHIP.md (section 2 Lane D scope; section 4 D-vs-C seam — Lane D owns the catalog, runbooks only reference it)
    - infra/README.md and infra/PRODUCTION-EVIDENCE.md (existing operator docs — cross-reference, do not duplicate)
    - infra/vault/providers.json, infra/vault/mnemosyne-transit-policy.hcl, infra/keycloak/realm-mnemosyne.json (the concrete assets the catalog points at)
    - src/mnemosyne/cli.py lines 95-180 (the frozen DEPLOYMENT_SOAK_GLOBAL_OPTIONS and PRODUCTION_RELEASE_REQUIRED_PROVIDER_CHECKS — these name the provider/secret surfaces the env vars feed)
  </read_first>
  <current_status>
    Superseded implementation note (2026-06-28): this historical action text
    still lists `--runtime-state` among deployment-soak placement options.
    The shipped catalog in `.planning/ENV-AND-SECRETS.md` is authoritative:
    current placement options are `--backend`, `--queue-backend`,
    `--queue-tenant`, `--object-store`, and `--parametric-artifact-store`.
  </current_status>
  <action>
    Create `.planning/ENV-AND-SECRETS.md` with: (1) a hard banner stating no secret VALUES are recorded here — only names, purpose, and source (env / file / command provider). (2) An env-var catalog table with columns: variable, purpose, source, consumed-by. Cover at minimum `MNEMOSYNE_POSTGRES_DSN`, `MNEME_BACKEND`/`MNEME_STORE`, the embedding/reranker provider URL+model+API-key flags (`--embedding-provider/--embedding-url/--reranker-provider/--reranker-url` env equivalents), object-store + object-key/KMS provider settings, residency labels, session-secret command-provider settings, OIDC/JWKS issuer/audience/JWKS-URL, the deployment-soak global placement options (`--backend`, `--queue-backend`, `--queue-tenant`, `--runtime-state`, `--object-store`, `--parametric-artifact-store`), and `MNEMOSYNE_EXPECTED_RELEASE_FINGERPRINT` / `MNEMOSYNE_PRODUCTION_SOAK_CHECK_TIMEOUT` used by `infra/scripts/capture-production-evidence.sh`. (3) A Keycloak (IdP/JWKS) wiring section that cross-references `infra/keycloak/realm-mnemosyne.json` + `infra/scripts/setup-keycloak.sh` for provisioning and names the issuer/audience/JWKS env vars (no values). (4) A Vault (session-secret custody + transit KMS) section cross-referencing `infra/vault/{providers.json,mnemosyne-transit-policy.hcl,vault-object-key-provider.py}` + `infra/scripts/setup-vault.sh`. (5) A KMS / object-key section describing the command-backed KMS/Vault-style key provider boundary and key-shred-after-legal-delete expectation. (6) A "secrets MUST come from environment, files, or command providers; never inline in soak-manifest args" rule that matches the sensitive-option rejection list in `capture-production-evidence.sh`. Keep all of this as catalog + cross-reference; do not paste realm JSON, HCL, or key material.
  </action>
  <verify>
    <automated>test -f .planning/ENV-AND-SECRETS.md && grep -q "MNEMOSYNE_POSTGRES_DSN" .planning/ENV-AND-SECRETS.md && grep -Eq "infra/vault" .planning/ENV-AND-SECRETS.md && grep -Eq "infra/keycloak" .planning/ENV-AND-SECRETS.md && grep -q "MNEMOSYNE_EXPECTED_RELEASE_FINGERPRINT" .planning/ENV-AND-SECRETS.md && [ "$(grep -vc '^$' .planning/ENV-AND-SECRETS.md)" -ge 60 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ENV-AND-SECRETS.md` exists, is at least 60 non-blank lines, and opens with a no-secret-values banner.
    - Contains the literal `MNEMOSYNE_POSTGRES_DSN`, `MNEMOSYNE_EXPECTED_RELEASE_FINGERPRINT`, and the six deployment-soak global placement option names.
    - Cross-references `infra/vault`, `infra/keycloak`, and `infra/c2pa` by path.
    - Contains no JWT, password, private key, base64 secret, or `vault token` value (verified by a grep for obvious secret patterns returning nothing meaningful — the file is names + sources only).
    - States the "secrets via env/file/command-provider, never inline in manifest args" rule that mirrors capture-production-evidence.sh.
  </acceptance_criteria>
  <done>The env/secrets catalog exists with all production env vars named (not valued), IdP/Vault/KMS wiring cross-referenced to existing infra assets, and the no-inline-secrets rule documented.</done>
</task>

</tasks>

<verification>
- `.planning/ENV-AND-SECRETS.md` exists with the full env catalog and IdP/Vault/KMS wiring.
- No secret values present; all secrets described by source only (D-04).
- Existing infra assets cross-referenced, not restated (OPS-HANDOFF section 4).
</verification>

<success_criteria>
An operator can provision every production secret and set every required env var from this one catalog without it leaking a single secret value.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-02-SUMMARY.md` when done.
</output>
