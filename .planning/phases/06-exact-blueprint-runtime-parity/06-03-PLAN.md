---
phase: 06-exact-blueprint-runtime-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROLLBACK.md
autonomous: true
requirements: [FR-21]
must_haves:
  truths:
    - "An operator can read .planning/ROLLBACK.md and execute a revert, a canary abort, or a rollback drill for any deployed surface without authoring new procedure."
    - "The parametric-trainer-check rollback drill is documented as a concrete drill an operator can run."
  artifacts:
    - path: ".planning/ROLLBACK.md"
      provides: "Revert + canary-abort + rollback-drill procedures incl. parametric-trainer rollback drill"
      contains: "parametric-trainer-check"
      min_lines: 50
  key_links:
    - from: ".planning/ROLLBACK.md"
      to: "gstack land-and-deploy"
      via: "references the deploy/rollback flow (not restated)"
      pattern: "land-and-deploy"
    - from: ".planning/ROLLBACK.md"
      to: ".planning/runbooks/row-09-parametric-tier.md"
      via: "cross-link for the parametric-trainer rollback drill"
      pattern: "row-09|parametric"
---

<objective>
Author Lane F: `.planning/ROLLBACK.md` — revert procedures, canary-abort, and rollback drills for the deployed surfaces, including the `parametric-trainer-check` rollback drill (D-05). It references (does not restate) the `gstack land-and-deploy` deploy/rollback flow and cross-links the parametric row runbook. Forward deploy steps belong to Lane C runbooks; this file owns recovery/abort only (OPS-HANDOFF section 4: F-vs-C seam).

Purpose: The operator-evidence pass for rows 2, 4, 7, and 9 (plan 06-09) requires rollback-drill evidence; this file is the single owner of those procedures so no runbook restates them.
Output: `.planning/ROLLBACK.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@.planning/OPS-HANDOFF-AND-OWNERSHIP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author rollback, canary-abort, and rollback-drill procedures</name>
  <files>.planning/ROLLBACK.md</files>
  <read_first>
    - .planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md (D-05 rollback contract)
    - .planning/OPS-HANDOFF-AND-OWNERSHIP.md (section 2 Lane F scope: revert/canary-abort/rollback drills + parametric-trainer-check rollback drill; section 4 F-vs-C seam; section 5 deploy/land flow via gstack land-and-deploy)
    - .planning/STRICT-BLUEPRINT-PARITY-AUDIT.md (parametric tier row — `parametric-trainer-check` validates rollback drill verification, authorized rollback, no promoted rollback branch)
  </read_first>
  <action>
    Create `.planning/ROLLBACK.md` with: (1) a scope banner stating Lane F owns recovery/abort only; forward deploy steps live in `.planning/runbooks/`. (2) A general revert procedure that references the `gstack land-and-deploy` flow (dry-run -> pre-merge gate -> deploy strategy -> canary verification -> deploy report) for how forward deploys happen, and documents the reverse: how to roll a surface back to the prior release. (3) A canary-abort procedure: when canary verification tripwires fire, how to abort the canary without promoting. (4) Per-surface rollback drills for the surfaces whose rows require rollback evidence (rows 2 auth, 4 consolidation worker, 7 privacy/erasure, 9 parametric) — each drill: trigger, steps, expected post-state, evidence to capture (redacted). (5) The `parametric-trainer-check` rollback drill in detail: it must demonstrate rollback drill verification, authorized rollback, and that no rollback branch is promoted — matching what `parametric-trainer-check` validates; cross-link `.planning/runbooks/row-09-parametric-tier.md` (plan 06-01) and note this overlaps FR-21 (D-12, plan 06-06). Document procedures only; do not restate the land-and-deploy internals or include secrets.
  </action>
  <verify>
    <automated>test -f .planning/ROLLBACK.md && grep -q "parametric-trainer-check" .planning/ROLLBACK.md && grep -q "land-and-deploy" .planning/ROLLBACK.md && grep -Eiq "canary" .planning/ROLLBACK.md && [ "$(grep -vc '^$' .planning/ROLLBACK.md)" -ge 50 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROLLBACK.md` exists, at least 50 non-blank lines, and opens with the Lane-F-owns-recovery-only scope banner.
    - Contains the literal `parametric-trainer-check` within a dedicated rollback-drill section describing rollback drill verification, authorized rollback, and no-promoted-rollback-branch.
    - References `gstack land-and-deploy` for the deploy/rollback flow rather than restating it.
    - Contains a canary-abort procedure (literal "canary").
    - Cross-links `.planning/runbooks/row-09-parametric-tier.md` or the parametric row.
  </acceptance_criteria>
  <done>ROLLBACK.md exists with revert, canary-abort, per-surface rollback drills, and the parametric-trainer-check rollback drill, referencing (not restating) the land-and-deploy flow.</done>
</task>

</tasks>

<verification>
- `.planning/ROLLBACK.md` exists with revert + canary-abort + rollback-drill procedures.
- Parametric-trainer rollback drill documented (D-05).
- gstack land-and-deploy referenced, not restated (OPS-HANDOFF section 4 F-vs-C seam).
</verification>

<success_criteria>
An operator facing a bad deploy or a failed canary can recover any surface — including the parametric trainer — using only this document plus the referenced land-and-deploy flow.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-03-SUMMARY.md` when done.
</output>
