---
phase: 06-exact-blueprint-runtime-parity
plan: 09
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-07", "06-08"]
files_modified: []
autonomous: false
requirements: [FR-3, FR-7, FR-9, FR-12, FR-18, FR-19, FR-20, FR-21]
must_haves:
  truths:
    - "Each of the 10 parity rows has an authored runbook (plan 06-01), and plan 06-08 records a global local-staging harness proof for evidence mechanics plus representative local services. This is setup/readiness proof, not per-row production proof."
    - "These 10 operator gates are explicit and NON-autonomous: they require real cloud infrastructure + operator hands; the executor does NOT mark any row Done."
    - >
      A row flips Partial->Done ONLY when the operator runs
      release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json"
      --require-production-validated --require-provider-forbid-local
      against real infra with that command's output shape present and findings empty.
  artifacts: []
  key_links:
    - from: "each operator gate"
      to: ".planning/runbooks/row-NN-*.md + infra/scripts/capture-production-evidence.sh"
      via: "the authored runbook + verified harness the operator follows"
      pattern: "capture-production-evidence.sh|release-audit"
---

<objective>
Make the path to literal 100% explicit: one NON-autonomous operator gate per parity row (10 gates). These exist so the phase's remaining work is legible and runnable, NOT so the executor completes them. Each gate is blocked on real cloud infrastructure + operator hands and flips its row Partial->Done only via `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local` against real infra with that command's output shape present and `findings` empty (D-13, OPS-HANDOFF section 5 DoD).

CRITICAL: The executor MUST NOT claim real-infra completion for any gate. The acceptance criterion for the executor is that each row's runbook (plan 06-01) is authored and the global local-staging harness proof (plan 06-08) is recorded — the readiness, NOT the production result. Each gate pauses for the operator.

Purpose: This plan converts the deferred operator-evidence pass into 10 named, runbook-backed checkpoints so nothing is silently dropped and the literal-100% path is explicit (D-13/D-15).
Output: 10 operator checkpoints, each pointing at its runbook + the verified harness; zero src/infra changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@.planning/OPS-HANDOFF-AND-OWNERSHIP.md
@.planning/runbooks/README.md
@.planning/runbooks/LOCAL-STAGING-DRY-RUN.md
</context>

<terminology>
When a checkpoint row below says "dry-run proven", it means the global
local-staging harness proof recorded in plan 06-08 with
`production_validated=false`. It does not mean every row has per-row production
evidence or that any row can flip Partial->Done without the operator-only
production capture.
</terminology>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 1: Production Postgres retrieval (retrieval-ops-check)</name>
  <read_first>
    - .planning/runbooks/row-01-production-postgres-retrieval.md
    - infra/scripts/capture-production-evidence.sh
    - .planning/ENV-AND-SECRETS.md
  </read_first>
  <what-built>Runbook row-01 authored (plan 06-01); harness verified + local-staging dry-run proven (plan 06-08). Gate commands frozen: retrieval-ops-check + provider-check retrieval_backends.</what-built>
  <action>EXECUTOR (autonomous): confirm `.planning/runbooks/row-01-production-postgres-retrieval.md` exists and the plan 06-08 local-staging dry-run is proven, then PAUSE — do NOT attempt the production capture (no paid cloud; D-13). OPERATOR (real infra): perform the row-1 capture per how-to-verify below. The executor never marks row 1 Done.</action>
  <how-to-verify>
    Operator only, against real infra (ParadeDB BM25 + Apache AGE + pgvector + non-local embedding/reranker): follow .planning/runbooks/row-01-production-postgres-retrieval.md to capture redacted gate evidence, then run infra/scripts/capture-production-evidence.sh with a production-scoped manifest so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes with retrieval-ops-check output present and findings empty. The executor cannot do this (no paid cloud); it stops here.
  </how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-01 runbook exists and the local-staging dry-run (plan 06-08) is proven. This is the ONLY thing the executor verifies.
    - Operator-side (not executor): release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes with retrieval-ops-check output present, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-1 evidence captured" after release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for row 1, or "skip operator gates" to defer the production pass.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 2: Tenant isolation & auth (auth-ops-check + TLS/IdP suite)</name>
  <read_first>
    - .planning/runbooks/row-02-tenant-isolation-and-auth.md
    - .planning/ENV-AND-SECRETS.md
    - .planning/ROLLBACK.md
  </read_first>
  <what-built>Runbook row-02 authored; harness + dry-run proven. Frozen gates: auth-ops-check + idp-jwks-live-check + idp-authz-policy-rollout-check + tls-cert-check + tls-rotation-plan-check + tls-lifecycle-ops-check.</what-built>
  <action>EXECUTOR (autonomous): confirm row-02 runbook exists + dry-run proven, then PAUSE — do NOT attempt the production auth capture (D-13). OPERATOR (real Keycloak/Vault/TLS): perform the row-2 capture per how-to-verify. The executor never marks row 2 Done.</action>
  <how-to-verify>Operator only, against real Keycloak IdP/JWKS + Vault + real TLS certs/rotation: follow row-02 runbook to capture redacted live IdP/JWKS/TLS + tenant-RLS evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for the row-2 commands with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-02 runbook exists; local-staging dry-run proven (plan 06-08).
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for auth-ops-check + IdP/TLS sub-checks, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-2 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 3: CLI/MCP runtime coverage (mcp-ops-check) [A11]</name>
  <read_first>
    - .planning/runbooks/row-03-cli-mcp-runtime-coverage.md
    - .planning/ENV-AND-SECRETS.md
  </read_first>
  <what-built>Runbook row-03 authored; A11 local readiness confirmed (plan 06-07) — StreamableHTTP/SSE locally validated. Frozen gates: mcp-ops-check + mcp-http-soak + mcp-streamable-http-soak.</what-built>
  <action>EXECUTOR (autonomous): confirm row-03 runbook exists + A11 local readiness confirmed (plan 06-07) + dry-run proven, then PAUSE — do NOT attempt the hosted-endpoint capture (D-13). OPERATOR (hosted MCP endpoint w/ TLS): perform the row-3 capture per how-to-verify. The executor never marks row 3 Done.</action>
  <how-to-verify>Operator only, against a hosted MCP endpoint with TLS: follow row-03 runbook to capture hosted JSON-RPC + StreamableHTTP soak evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for mcp-ops-check with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-03 runbook exists; A11 local readiness confirmed (plan 06-07); dry-run proven.
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes with mcp-ops-check output present, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-3 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 4: Consolidation role pipeline (consolidation-ops-check + worker)</name>
  <read_first>
    - .planning/runbooks/row-04-consolidation-role-pipeline.md
    - .planning/ENV-AND-SECRETS.md
    - .planning/ROLLBACK.md
  </read_first>
  <what-built>Runbook row-04 authored; harness + dry-run proven. Frozen gates: consolidation-ops-check + worker-run + worker-ops-check + projection-recompute-once + gate-suite-check.</what-built>
  <action>EXECUTOR (autonomous): confirm row-04 runbook exists + dry-run proven, then PAUSE — do NOT attempt the supervised-worker capture (D-13). OPERATOR (supervised Postgres worker + model providers): perform the row-4 capture per how-to-verify. The executor never marks row 4 Done.</action>
  <how-to-verify>Operator only, against a supervised Postgres worker + model-backed extractor/summarizer/resolver: follow row-04 runbook to capture supervised worker + provider + projection evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for the row-4 commands with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-04 runbook exists; dry-run proven.
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for consolidation-ops-check + worker sub-checks, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-4 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 5: Signed provenance (provenance-ops-check + provenance-trust-check)</name>
  <read_first>
    - .planning/runbooks/row-05-signed-provenance.md
    - .planning/ENV-AND-SECRETS.md
  </read_first>
  <what-built>Runbook row-05 authored; harness + dry-run proven (C2PA verify passed locally). Frozen gates: provenance-ops-check + provenance-trust-check.</what-built>
  <action>EXECUTOR (autonomous): confirm row-05 runbook exists + dry-run proven, then PAUSE — do NOT attempt the production C2PA capture (D-13). OPERATOR (real c2patool + trusted issuer/root): perform the row-5 capture per how-to-verify. The executor never marks row 5 Done.</action>
  <how-to-verify>Operator only, against real c2patool + trusted issuer/root deployment: follow row-05 runbook to capture verifier + trust-root rotation + quarantine evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for the row-5 commands with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-05 runbook exists; dry-run proven.
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for provenance-ops-check + provenance-trust-check, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-5 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 6: Multimodal retrieval (multimodal-ops-check) [FR-20]</name>
  <read_first>
    - .planning/runbooks/row-06-multimodal-retrieval.md
    - .planning/ENV-AND-SECRETS.md
  </read_first>
  <what-built>Runbook row-06 authored; FR-20 image/audio/video breadth code-complete + locally validated (plan 06-05). Frozen gate: multimodal-ops-check.</what-built>
  <action>EXECUTOR (autonomous): confirm row-06 runbook exists + FR-20 breadth code landed + locally validated (plan 06-05) + dry-run proven, then PAUSE — do NOT attempt the production extractor/embedder capture (D-13). OPERATOR (non-local extractor + media-embedding + object store): perform the row-6 capture per how-to-verify. The executor never marks row 6 Done.</action>
  <how-to-verify>Operator only, against a non-local extractor + media-embedding + encrypted object store: follow row-06 runbook to capture extraction + media-vector retrieval evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for multimodal-ops-check with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-06 runbook exists; FR-20 breadth code landed + locally validated (plan 06-05); dry-run proven.
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes with multimodal-ops-check output present, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-6 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 7: Privacy & erasure (privacy-ops-check + policy/forgetting)</name>
  <read_first>
    - .planning/runbooks/row-07-privacy-and-erasure.md
    - .planning/ENV-AND-SECRETS.md
    - .planning/ROLLBACK.md
  </read_first>
  <what-built>Runbook row-07 authored; harness + dry-run proven. Frozen gates: privacy-ops-check + policy-ops-check + forgetting-policy-check.</what-built>
  <action>EXECUTOR (autonomous): confirm row-07 runbook exists + dry-run proven, then PAUSE — do NOT attempt the production KMS/residency capture (D-13). OPERATOR (real KMS/Vault + residency ops): perform the row-7 capture per how-to-verify. The executor never marks row 7 Done.</action>
  <how-to-verify>Operator only, against real KMS (Vault) + residency policy ops: follow row-07 runbook to capture KMS lifecycle/shred + residency + tombstone/hard-delete evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for the row-7 commands with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-07 runbook exists; dry-run proven.
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for privacy-ops-check + sub-checks, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-7 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 8: Observability dashboards (ops-dashboard-check + ops-report)</name>
  <read_first>
    - .planning/runbooks/row-08-observability-dashboards.md
    - .planning/ENV-AND-SECRETS.md
  </read_first>
  <what-built>Runbook row-08 authored; harness + dry-run proven (local dashboard package builds). Frozen gates: ops-dashboard-check + ops-report.</what-built>
  <action>EXECUTOR (autonomous): confirm row-08 runbook exists + dry-run proven, then PAUSE — do NOT attempt the hosted-dashboard capture (D-13). OPERATOR (hosted production dashboard URL, or pre-existing external package input evidence only): perform the row-8 capture per how-to-verify. The executor never marks row 8 Done.</action>
  <how-to-verify>Operator only, against a hosted production dashboard URL or pre-existing external package input evidence: follow row-08 runbook to capture hosted-dashboard ops evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for ops-dashboard-check with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-08 runbook exists; dry-run proven.
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes with ops-dashboard-check output present, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-8 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 9: Parametric tier (parametric-trainer-check) [FR-21]</name>
  <read_first>
    - .planning/runbooks/row-09-parametric-tier.md
    - .planning/ROLLBACK.md
    - .planning/ENV-AND-SECRETS.md
  </read_first>
  <what-built>Runbook row-09 authored; FR-21 LoRA/TTT trainer + protected-suite + rollback code-complete + isolated local validation (plan 06-06); parametric-trainer-check rollback drill documented (plan 06-03). Frozen gates: parametric-trainer-check + hosted-llm-check + calibration-tune.</what-built>
  <action>EXECUTOR (autonomous): confirm row-09 runbook exists + FR-21 code landed + locally validated (plan 06-06) + rollback drill documented (plan 06-03) + dry-run proven, then PAUSE — do NOT attempt the GPU-trainer capture (D-13). OPERATOR (deployed LoRA/TTT trainer + rollback orchestration): perform the row-9 capture per how-to-verify. The executor never marks row 9 Done.</action>
  <how-to-verify>Operator only, against a deployed LoRA/TTT trainer (GPU) + rollback orchestration: follow row-09 runbook to capture trainer deploy + protected-suite + rollback-drill evidence, then capture-production-evidence.sh so release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for the row-9 commands with findings empty. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-09 runbook exists; FR-21 code landed + locally validated (plan 06-06); rollback drill documented (plan 06-03); dry-run proven.
    - Operator-side: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes for parametric-trainer-check + sub-checks, findings empty.
  </acceptance_criteria>
  <resume-signal>Operator types "row-9 evidence captured" or "skip operator gates".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Operator Gate Row 10: Live parity suite + final v1.0 sign-off</name>
  <read_first>
    - .planning/runbooks/row-10-live-parity-suite.md
    - .planning/STRICT-BLUEPRINT-PARITY-AUDIT.md
    - .planning/runbooks/LOCAL-STAGING-DRY-RUN.md
  </read_first>
  <what-built>Runbook row-10 authored; Lane G sweep green with belief-revision-check (plan 06-07). Frozen acceptance: full compose-Postgres suite + belief-revision-check + production adapters enabled.</what-built>
  <action>EXECUTOR (autonomous): confirm row-10 runbook exists + Lane G sweep green with belief-revision-check (plan 06-07) + dry-run proven, then PAUSE — do NOT attempt the production-adapter run or the v1.0 attestation (D-13). OPERATOR (production adapters enabled, after rows 1-9 Done): perform the row-10 run + final DoD sign-off per how-to-verify. The executor never flips rows or writes the attestation.</action>
  <how-to-verify>Operator only, with optional production adapters enabled: follow row-10 runbook to run the full suite with production adapters + belief-revision-check, confirm every engine/runtime method green, then (after rows 1-9 are all Done) run the final DoD gate: release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local passes on a deployment-soak bundle where EVERY required command + provider sub-check carries real-infra operator evidence. Then re-run .planning/STRICT-BLUEPRINT-PARITY-AUDIT.md, flip all 10 rows Partial->Done, and supersede with a v1.0 release attestation. Executor cannot do this; it stops here.</how-to-verify>
  <acceptance_criteria>
    - Executor-side: row-10 runbook exists; Lane G sweep green with belief-revision-check (plan 06-07); dry-run proven.
    - Operator-side: full DoD release-audit passes with all rows' evidence; 10 rows flip Partial->Done; v1.0 attestation written.
  </acceptance_criteria>
  <resume-signal>Operator types "v1.0 signed off" after all 10 rows Done + attestation, or "skip operator gates".</resume-signal>
</task>

</tasks>

<verification>
- Executor side: all 10 row runbooks exist (plan 06-01), env/secrets + rollback authored (06-02/06-03), Lane G sweep green (06-07), and the global local-staging harness proof is recorded with production_validated=false (06-08). This is the entire executor responsibility for this plan.
- Operator side (NOT executor): each row flips Partial->Done only via release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local against real infra (D-13).
- No src/infra files modified by this plan (files_modified: []).
</verification>

<success_criteria>
The path to literal 100% is fully explicit: 10 named, runbook-backed, harness-verified operator gates. The executor proves readiness for every one; the operator (with real cloud infra) runs the production capture to flip the rows and sign off v1.0.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-09-SUMMARY.md` when done (recording readiness state per row; operator-evidence results recorded as the operator completes each gate).
</output>
