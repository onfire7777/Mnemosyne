---
phase: 06-exact-blueprint-runtime-parity
plan: 07
type: execute
wave: 2
depends_on: ["06-04", "06-05", "06-06"]
files_modified:
  - tests/test_postgres_engine_live.py
  - tests/test_runtime_parity_extensions.py
  - tests/completion/portability/test_cross_engine_parity.py
autonomous: true
requirements: [FR-9, FR-12, FR-20, FR-21]
must_haves:
  truths:
    - "The full compose-Postgres suite runs green with MNEMOSYNE_POSTGRES_DSN set: 0 failures, 0 errors."
    - "Every engine/runtime method is covered by the live sweep, including the new A12/FR-20/FR-21 code from plans 06-04/05/06."
    - "belief-revision-check is green."
    - "A11 hosted-MCP readiness is complete: StreamableHTTP/SSE transports are locally validated and only the operator hosted-endpoint evidence (row 3) remains."
  artifacts:
    - path: "tests/completion/portability/test_cross_engine_parity.py"
      provides: "Cross-engine parity coverage extended to the new code"
    - path: "tests/test_postgres_engine_live.py"
      provides: "Live Postgres method-coverage for cached-PPR + multimodal breadth + parametric"
  key_links:
    - from: "tests/*"
      to: "belief-revision-check"
      via: "the live sweep asserts belief-revision-check green"
      pattern: "belief-revision|belief_revision"
---

<objective>
Run Lane G (D-08): the full compose-Postgres test suite with `MNEMOSYNE_POSTGRES_DSN` set, confirming every engine/runtime method is covered (including the new A12/FR-20/FR-21 code), `belief-revision-check` is green, and there are 0 failures/errors. Fix only test-determinism or method-coverage gaps — SANCTIONED code only, no engine behavior change (D-02/D-08). Also complete A11 hosted-MCP readiness (D-09): the official SDK StreamableHTTP + legacy SSE transports are already locally validated; verify and close any remaining local readiness so ONLY operator hosted-endpoint evidence (`mcp-ops-check`, row 3) remains — the row-3 runbook is authored in plan 06-01. Add no new transport code unless a concrete gap is found.

Purpose: Lane G is the only sanctioned parity code lane (D-02). It proves the new optional code (04/05/06) holds under the production-equivalent compose-Postgres path, which is the final autonomous gate before the operator-evidence pass.
Output: green compose-Postgres sweep, belief-revision-check green, A11 readiness confirmed.

HISTORICAL COORDINATION HAZARD (D-14): at planning time an autonomous session owned `src/`. This plan changed TEST files only (determinism + coverage). If a concrete A11 transport gap is found, COMPLETE it additively and coordinate; do not rewrite the transport code. NEVER `git add -A` — stage only the named test files (plus, only if a real A11 gap forces it, the specific transport source file). Verify the tree is not mid-edit before committing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@.planning/STRICT-BLUEPRINT-PARITY-AUDIT.md
@.planning/phases/06-exact-blueprint-runtime-parity/06-04-SUMMARY.md
@.planning/phases/06-exact-blueprint-runtime-parity/06-05-SUMMARY.md
@.planning/phases/06-exact-blueprint-runtime-parity/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full compose-Postgres sweep + belief-revision-check; close determinism/coverage gaps</name>
  <files>tests/test_postgres_engine_live.py, tests/test_runtime_parity_extensions.py, tests/completion/portability/test_cross_engine_parity.py</files>
  <read_first>
    - .planning/STRICT-BLUEPRINT-PARITY-AUDIT.md (Live parity suite row — the engine/runtime methods the sweep must cover; "Remaining work: expand to every remaining engine/runtime method")
    - tests/completion/portability/test_cross_engine_parity.py (the cross-engine parity surface — extend to the new A12/FR-20/FR-21 methods)
    - tests/test_postgres_engine_live.py (live Postgres coverage — add cached-PPR + multimodal breadth + parametric live cases)
    - the SUMMARY files from plans 06-04/06-05/06-06 (what new methods/contracts landed and need live coverage)
  </read_first>
  <action>
    Run the full compose-Postgres suite with `MNEMOSYNE_POSTGRES_DSN` set (compose Postgres must be up — see infra/scripts/up.sh). Confirm 0 failures/0 errors and that `belief-revision-check` passes. Identify any engine/runtime method introduced by plans 06-04 (cached-PPR), 06-05 (image/audio/video breadth), and 06-06 (LoRA/TTT) that is NOT exercised under the live DSN, and add live coverage in the named test files. Fix any test-determinism gaps (ordering, seeds, time) — TEST changes only, no engine behavior change. If a method-coverage gap reveals a real engine bug, STOP and surface it (do not patch engine behavior under this sanctioned-test lane).
  </action>
  <verify>
    <automated>MNEMOSYNE_POSTGRES_DSN="${MNEMOSYNE_POSTGRES_DSN:?compose Postgres DSN must be set for Lane G}" .venv/bin/python -m pytest -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `MNEMOSYNE_POSTGRES_DSN=... .venv/bin/python -m pytest -q` reports 0 failures and 0 errors.
    - `belief-revision-check` is exercised and green within the sweep.
    - New cached-PPR, image/audio/video, and parametric methods are each covered under the live DSN (new test cases reference them).
    - No engine/src behavior change was made under this lane (only tests, plus an A11 transport gap if Task 2 surfaces one).
  </acceptance_criteria>
  <done>The full compose-Postgres sweep is green with every engine/runtime method (including new code) covered and belief-revision-check green.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm A11 hosted-MCP local readiness; only operator endpoint evidence remains</name>
  <files>tests/test_runtime_parity_extensions.py</files>
  <read_first>
    - .planning/STRICT-BLUEPRINT-PARITY-AUDIT.md (CLI/MCP runtime coverage row — StreamableHTTP/SSE local validation status; `mcp-http-soak`, `mcp-streamable-http-soak`, `mcp-sse-soak` already validated)
    - docs/ROADMAP-TO-100.md (A11 row: local JSON-RPC/TLS/self-test done; StreamableHTTP/SSE — evidence in Tier B; "Implement/validate official transport (evidence in Tier B)")
    - .planning/runbooks/row-03-cli-mcp-runtime-coverage.md (the row-3 runbook authored in plan 06-01 — the operator-evidence side)
  </read_first>
  <action>
    Verify A11 hosted-MCP transport local readiness is complete: run the local soak validators (`mcp-http-soak`, `mcp-streamable-http-soak`, `mcp-sse-soak`) and confirm they pass. Confirm the only remaining gap for row 3 is operator hosted-endpoint evidence (`mcp-ops-check`), which the row-3 runbook (plan 06-01) captures. If a CONCRETE local transport readiness gap is found (a transport that does not initialize/list/read-only-call locally), complete it additively and add a local test; otherwise add a coverage assertion in `tests/test_runtime_parity_extensions.py` that the three transports validate locally. Record in the SUMMARY that A11 is local-readiness-complete and only operator evidence remains. No new transport code unless a concrete gap is found.
  </action>
  <verify>
    <automated>.venv/bin/python -m mnemosyne.cli mcp-streamable-http-soak --help >/dev/null 2>&1 && .venv/bin/python -m pytest -q tests/test_runtime_parity_extensions.py -k "mcp or streamable or sse or transport or soak" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The three local MCP soak validators pass (or, if a concrete gap existed, it is closed additively with a passing local test).
    - A coverage assertion confirms StreamableHTTP + SSE + HTTP transports validate locally.
    - The SUMMARY states A11 is local-readiness-complete and only operator hosted-endpoint evidence (row 3) remains; no transport code was added absent a concrete gap.
  </acceptance_criteria>
  <done>A11 hosted-MCP readiness is confirmed complete locally; the row-3 runbook (plan 06-01) is the operator-evidence path.</done>
</task>

</tasks>

<verification>
- Full compose-Postgres sweep green (0 failures/errors) with MNEMOSYNE_POSTGRES_DSN set.
- belief-revision-check green; new code from 04/05/06 covered live.
- A11 local readiness complete; only operator endpoint evidence remains (D-09).
- Sanctioned test-only changes; no engine behavior change; no `git add -A` (D-02/D-14).
</verification>

<success_criteria>
The whole engine/runtime surface — including the new optional code — is proven green under the production-equivalent compose-Postgres path with belief-revision-check green, and A11 needs only operator evidence.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-07-SUMMARY.md` when done.
</output>
