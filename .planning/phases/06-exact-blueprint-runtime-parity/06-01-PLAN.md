---
phase: 06-exact-blueprint-runtime-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/runbooks/README.md
  - .planning/runbooks/row-01-production-postgres-retrieval.md
  - .planning/runbooks/row-02-tenant-isolation-and-auth.md
  - .planning/runbooks/row-03-cli-mcp-runtime-coverage.md
  - .planning/runbooks/row-04-consolidation-role-pipeline.md
  - .planning/runbooks/row-05-signed-provenance.md
  - .planning/runbooks/row-06-multimodal-retrieval.md
  - .planning/runbooks/row-07-privacy-and-erasure.md
  - .planning/runbooks/row-08-observability-dashboards.md
  - .planning/runbooks/row-09-parametric-tier.md
  - .planning/runbooks/row-10-live-parity-suite.md
autonomous: true
requirements: [FR-3, FR-7, FR-9, FR-12, FR-18, FR-19, FR-20, FR-21]
must_haves:
  truths:
    - "Each of the 10 parity rows has a dedicated runbook under .planning/runbooks/ that an operator can follow to capture production evidence."
    - "Every runbook cites its exact frozen gate command(s) verbatim from OPS-HANDOFF section 3, in run order."
    - "Every runbook states the real-infra dependency, the redaction requirement, and the universal acceptance pattern (evidence -> deployment-soak --evidence-dir -> release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local)."
    - "No runbook instructs the operator to write code or add a new *-ops-check gate."
  artifacts:
    - path: ".planning/runbooks/README.md"
      provides: "Index of the 10 runbooks + the universal acceptance pattern + the do-not-rederive rule"
      contains: "release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local"
    - path: ".planning/runbooks/row-03-cli-mcp-runtime-coverage.md"
      provides: "Row 3 (A11 hosted-MCP) operator runbook"
      contains: "mcp-ops-check"
    - path: ".planning/runbooks/row-10-live-parity-suite.md"
      provides: "Row 10 (Lane G) operator runbook"
      contains: "belief-revision-check"
  key_links:
    - from: ".planning/runbooks/*.md"
      to: ".planning/OPS-HANDOFF-AND-OWNERSHIP.md (section 3)"
      via: "verbatim gate command list per row"
      pattern: "(retrieval|auth|mcp|consolidation|provenance|multimodal|privacy|ops-dashboard|parametric)-ops-check|belief-revision-check"
    - from: ".planning/runbooks/*.md"
      to: "infra/scripts/capture-production-evidence.sh"
      via: "named reference to the sanctioned capture path"
      pattern: "capture-production-evidence.sh"
---

<objective>
Author Lane C: one operator runbook per parity row (10 total) under `.planning/runbooks/`, plus an index README. Each runbook documents HOW to run the already-frozen gate command(s) for that row against real infrastructure, in what order, with what redaction, ending in the universal acceptance pattern. This is documentation only — it sources the per-row command list verbatim from OPS-HANDOFF section 3 and never adds code or new gates (D-01, D-03).

Purpose: The runbooks are the executable instructions the operator follows later to flip each Partial row to Done. They are the load-bearing deliverable that makes the operator-evidence pass (Wave 3, plan 06-09) runnable without re-deriving scope.
Output: `.planning/runbooks/` containing README.md + 10 row runbooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@.planning/OPS-HANDOFF-AND-OWNERSHIP.md
@.planning/STRICT-BLUEPRINT-PARITY-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runbooks/ directory, README index, and rows 1-5</name>
  <files>.planning/runbooks/README.md, .planning/runbooks/row-01-production-postgres-retrieval.md, .planning/runbooks/row-02-tenant-isolation-and-auth.md, .planning/runbooks/row-03-cli-mcp-runtime-coverage.md, .planning/runbooks/row-04-consolidation-role-pipeline.md, .planning/runbooks/row-05-signed-provenance.md</files>
  <read_first>
    - .planning/OPS-HANDOFF-AND-OWNERSHIP.md (section 3 table — the per-row gate command list is the verbatim source; section 0 do-not-rederive rule; section 5 DoD)
    - .planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md (D-03 runbook contract; the 10-row list under "Specific Ideas")
    - infra/scripts/capture-production-evidence.sh (the sanctioned capture path each runbook references)
    - infra/PRODUCTION-EVIDENCE.md and infra/README.md (existing operator-facing evidence docs to cross-reference, not restate)
  </read_first>
  <action>
    Create `.planning/runbooks/README.md` as the index: list all 10 rows with one-line objective each; state the universal acceptance pattern once (operator runs gate against real infra -> redact evidence -> `deployment-soak --evidence-dir` with `validation_scope.production_validated=true, target_environment=production, operator_asserted=true` -> `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local` passes with that command's output shape present and `findings` empty); state the DO-NOT-REDERIVE rule (Partial = ops, never code; do not add `*-ops-check` gates) per D-01; point to `infra/scripts/capture-production-evidence.sh` as the wrapper that runs the soak+audit path.
    Then author rows 1-5, each with these sections: Objective; Real-infra dependency (from OPS-HANDOFF section 3 column "Real-infra dependency"); Gate command(s) in run order (copied VERBATIM from OPS-HANDOFF section 3 column "Gate command(s)" for that row — row1: `retrieval-ops-check` + `provider-check` retrieval_backends; row2: `auth-ops-check` + `idp-jwks-live-check` + `idp-authz-policy-rollout-check` + `tls-cert-check` + `tls-rotation-plan-check` + `tls-lifecycle-ops-check`; row3: `mcp-ops-check` + `mcp-http-soak` + `mcp-streamable-http-soak`; row4: `consolidation-ops-check` + `worker-run` + `worker-ops-check` + `projection-recompute-once` + `gate-suite-check`; row5: `provenance-ops-check` + `provenance-trust-check`); Redaction requirement (no raw tokens/keys/queries/documents/credentials in evidence); Acceptance (the universal pattern + "Done when" text from OPS-HANDOFF section 3). For row 3 explicitly note it carries the A11 hosted-MCP transport scope (D-09) and that the StreamableHTTP/SSE transports are already locally validated — only the hosted-endpoint operator evidence remains. Reference (do not restate) infra assets: `infra/keycloak/`, `infra/vault/`, `infra/c2pa/`. Do NOT include implementation code or instruct adding gates.
  </action>
  <verify>
    <automated>test -f .planning/runbooks/README.md && for r in 01-production-postgres-retrieval 02-tenant-isolation-and-auth 03-cli-mcp-runtime-coverage 04-consolidation-role-pipeline 05-signed-provenance; do test -f .planning/runbooks/row-$r.md || { echo "MISSING row-$r"; exit 1; }; done && grep -q "release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local" .planning/runbooks/README.md && grep -q "retrieval-ops-check" .planning/runbooks/row-01-production-postgres-retrieval.md && grep -q "auth-ops-check" .planning/runbooks/row-02-tenant-isolation-and-auth.md && grep -q "mcp-ops-check" .planning/runbooks/row-03-cli-mcp-runtime-coverage.md && grep -q "consolidation-ops-check" .planning/runbooks/row-04-consolidation-role-pipeline.md && grep -q "provenance-ops-check" .planning/runbooks/row-05-signed-provenance.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/runbooks/README.md` exists and contains the literal string `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local` and a statement of the do-not-rederive rule (Partial = ops).
    - `.planning/runbooks/row-01-production-postgres-retrieval.md` contains the literal gate command `retrieval-ops-check` and the literal `provider-check`.
    - `.planning/runbooks/row-02-tenant-isolation-and-auth.md` contains the literal gate commands `auth-ops-check`, `idp-jwks-live-check`, `tls-lifecycle-ops-check`.
    - `.planning/runbooks/row-03-cli-mcp-runtime-coverage.md` contains the literal `mcp-ops-check`, `mcp-streamable-http-soak`, and a note that it carries A11 (D-09) scope.
    - `.planning/runbooks/row-04-consolidation-role-pipeline.md` contains the literal `consolidation-ops-check` and `worker-ops-check`.
    - `.planning/runbooks/row-05-signed-provenance.md` contains the literal `provenance-ops-check` and `provenance-trust-check`.
    - No runbook contains a fenced code block defining a new gate or instructs writing source code.
  </acceptance_criteria>
  <done>README index + rows 1-5 exist, each cites its exact frozen gate command(s) verbatim from OPS-HANDOFF section 3, the universal acceptance pattern, real-infra dependency, and redaction requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Author rows 6-10</name>
  <files>.planning/runbooks/row-06-multimodal-retrieval.md, .planning/runbooks/row-07-privacy-and-erasure.md, .planning/runbooks/row-08-observability-dashboards.md, .planning/runbooks/row-09-parametric-tier.md, .planning/runbooks/row-10-live-parity-suite.md</files>
  <read_first>
    - .planning/OPS-HANDOFF-AND-OWNERSHIP.md (section 3 rows 6-10 — verbatim gate command source)
    - .planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md (D-03; D-11 multimodal row 6; D-12 parametric row 9; D-08 live parity row 10)
    - .planning/runbooks/README.md (the index + universal acceptance pattern created in Task 1 — keep section structure identical)
  </read_first>
  <action>
    Author rows 6-10 using the same section structure established in Task 1. Gate commands copied VERBATIM from OPS-HANDOFF section 3: row6 `multimodal-ops-check`; row7 `privacy-ops-check` + `policy-ops-check` + `forgetting-policy-check`; row8 `ops-dashboard-check` + `ops-report`; row9 `parametric-trainer-check` + `hosted-llm-check` + `calibration-tune`; row10 full compose-Postgres suite + `belief-revision-check`. For row 6, note it is the operator-evidence side of FR-20 multimodal breadth (D-11) — the breadth CODE is authored locally in plan 06-05, production extractor/embedder/object-store evidence stays operator-run here. For row 9, note it is the operator-evidence side of FR-21 LoRA/TTT (D-12) — the trainer CODE + local isolated validation is authored in plan 06-06, deployed GPU trainer + production rollback evidence stays operator-run here, and include the `parametric-trainer-check` rollback-drill reference (cross-links to ROLLBACK.md, plan 06-03). For row 10, document the Lane G acceptance: run the full suite with `MNEMOSYNE_POSTGRES_DSN` set, every engine/runtime method covered, `belief-revision-check` green, 0 failures/errors — and note that the executable Lane G sweep work is plan 06-07, while production-adapter-enabled final sign-off is operator-gated. Each row ends in the universal acceptance pattern and the row's "Done when" text from OPS-HANDOFF section 3.
  </action>
  <verify>
    <automated>for r in 06-multimodal-retrieval 07-privacy-and-erasure 08-observability-dashboards 09-parametric-tier 10-live-parity-suite; do test -f .planning/runbooks/row-$r.md || { echo "MISSING row-$r"; exit 1; }; done && grep -q "multimodal-ops-check" .planning/runbooks/row-06-multimodal-retrieval.md && grep -q "privacy-ops-check" .planning/runbooks/row-07-privacy-and-erasure.md && grep -q "ops-dashboard-check" .planning/runbooks/row-08-observability-dashboards.md && grep -q "parametric-trainer-check" .planning/runbooks/row-09-parametric-tier.md && grep -q "belief-revision-check" .planning/runbooks/row-10-live-parity-suite.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/runbooks/row-06-multimodal-retrieval.md` contains `multimodal-ops-check` and a note linking it to FR-20 breadth code in plan 06-05 (D-11).
    - `.planning/runbooks/row-07-privacy-and-erasure.md` contains `privacy-ops-check`, `policy-ops-check`, `forgetting-policy-check`.
    - `.planning/runbooks/row-08-observability-dashboards.md` contains `ops-dashboard-check` and `ops-report`.
    - `.planning/runbooks/row-09-parametric-tier.md` contains `parametric-trainer-check`, `hosted-llm-check`, `calibration-tune`, a note linking it to FR-21 code in plan 06-06 (D-12), and a `parametric-trainer-check` rollback-drill reference to ROLLBACK.md.
    - `.planning/runbooks/row-10-live-parity-suite.md` contains `belief-revision-check`, the `MNEMOSYNE_POSTGRES_DSN` instruction, and a pointer to plan 06-07 for the executable Lane G sweep.
  </acceptance_criteria>
  <done>All 10 row runbooks exist with verbatim frozen gate commands, real-infra dependency, redaction, the universal acceptance pattern, and cross-links to the code-bearing plans (06-05/06-06/06-07) and ROLLBACK.md.</done>
</task>

</tasks>

<verification>
- All 10 row runbooks + README index exist under `.planning/runbooks/`.
- Every row's gate command list matches OPS-HANDOFF section 3 verbatim (no re-derivation).
- No runbook adds code or new gates (D-01).
- The universal acceptance pattern and `infra/scripts/capture-production-evidence.sh` are referenced.
</verification>

<success_criteria>
The operator can pick any of the 10 runbooks and follow it end-to-end to capture that row's production evidence using only already-frozen gate commands.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-01-SUMMARY.md` when done.
</output>
