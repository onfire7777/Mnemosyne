---
phase: 06-exact-blueprint-runtime-parity
plan: 08
type: execute
wave: 2
depends_on: ["06-01", "06-07"]
files_modified:
  - .planning/runbooks/LOCAL-STAGING-DRY-RUN.md
autonomous: true
requirements: [FR-3, FR-7, FR-9, FR-19]
must_haves:
  truths:
    - "infra/scripts/capture-production-evidence.sh is verified to run the sanctioned path: operator evidence -> deployment-soak --evidence-dir -> release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local."
    - "The full LOCAL-staging dry-run runs end-to-end: setup-all.sh + validate-all.sh pass; deployment-soak --evidence-dir over local real services + provider metadata; release-audit --allow-provider-local passes for the available checks."
    - "The captured dry-run fingerprint is recorded, and the artifact explicitly states production_validated=false (staging proof, NOT production validation)."
  artifacts:
    - path: ".planning/runbooks/LOCAL-STAGING-DRY-RUN.md"
      provides: "Recorded local-staging dry-run proof: commands run, fingerprint, production_validated=false statement"
      contains: "production_validated=false"
  key_links:
    - from: ".planning/runbooks/LOCAL-STAGING-DRY-RUN.md"
      to: "infra/scripts/capture-production-evidence.sh + infra/scripts/setup-all.sh + infra/validate/validate-all.sh"
      via: "documents the verified sanctioned path + the dry-run that exercised it"
      pattern: "capture-production-evidence.sh|setup-all.sh|validate-all.sh"
---

<objective>
CURRENT STATUS refreshed 2026-06-29 through `bda5ccc`: complete locally.
`06-08-SUMMARY.md`, `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md`,
`docs/ROADMAP-TO-100.md`, and `.planning/STATE.md` record the sanctioned
harness verification and local staging dry-run with `production_validated=false`.
Do not reopen this as a production proof; only Tier B operator-captured
production evidence can flip strict-audit rows from Partial to Done.

Historical objective: Verify the evidence harness (D-06) and prove the full local-staging dry-run end-to-end (D-07). First, VERIFY (and only complete if incomplete) that `infra/scripts/capture-production-evidence.sh` runs the sanctioned path: operator evidence -> `deployment-soak --evidence-dir` -> `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local`. Then run the full LOCAL-staging dry-run: `infra/scripts/setup-all.sh` + `infra/validate/validate-all.sh` pass; a scoped `deployment-soak --evidence-dir` over local real services + provider metadata; `release-audit --allow-provider-local` passes for the available checks. Capture the fingerprint and record it in `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md`, which MUST state `production_validated=false` — this is staging proof, NOT production validation (D-07).

Purpose: This proves the official evidence path works end-to-end against real local services before any operator does the production pass, and produces the recorded fingerprint that anchors the phase DoD (D-15). It is the highest-value autonomous-no-cloud deliverable.
Output: verified harness + `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md` with the captured fingerprint and the production_validated=false statement.

HISTORICAL COORDINATION HAZARD (D-14): at planning time the active session owned `infra/` and had just touched `capture-production-evidence.sh`. VERIFY its current behavior by reading + dry-running it; only complete a genuine gap, never rewrite. The only file this plan WRITES is `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md`. If `capture-production-evidence.sh` needs a fix, surface it and coordinate before editing `infra/`. NEVER `git add -A` — stage only the new `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md` (and the infra script only if a coordinated fix was required).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@.planning/OPS-HANDOFF-AND-OWNERSHIP.md
@docs/ROADMAP-TO-100.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the production-evidence harness runs the sanctioned path</name>
  <files>.planning/runbooks/LOCAL-STAGING-DRY-RUN.md</files>
  <read_first>
    - infra/scripts/capture-production-evidence.sh (the harness — confirm it validates production-scope manifest, runs `deployment-soak --soak-manifest --evidence-dir`, then `release-audit --evidence-manifest --require-production-validated --require-provider-forbid-local`, and rejects secret-bearing manifest args)
    - infra/templates/production-soak-manifest.template.json (the operator manifest shape the harness consumes — validation_scope.production_validated/target_environment/operator_asserted + checks[])
    - src/mnemosyne/cli.py lines 134-209 (PRODUCTION_RELEASE_REQUIRED_COMMANDS + RELEASE_AUDIT_REQUIRED_OUTPUT_KEYS — the harness asserts the manifest covers these commands)
    - .planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md (D-06 harness contract)
  </read_first>
  <action>
    Read `infra/scripts/capture-production-evidence.sh` and confirm it implements the sanctioned path exactly: (a) validates `validation_scope.production_validated=true, target_environment=production, operator_asserted=true`; (b) rejects secret-bearing options in manifest args; (c) verifies the manifest covers all `PRODUCTION_RELEASE_REQUIRED_COMMANDS`; (d) runs `deployment-soak --soak-manifest <m> --evidence-dir <dir>`; (e) runs `release-audit --evidence-manifest <dir>/manifest.json --require-production-validated --require-provider-forbid-local`. Run `infra/scripts/capture-production-evidence.sh --help` and confirm usage. If a genuine gap exists (a step missing or mis-ordered), surface it and coordinate with the active session before any `infra/` edit. Record the verification result (verified-as-is, or coordinated-fix) as a section in `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md`.
  </action>
  <verify>
    <automated>infra/scripts/capture-production-evidence.sh --help 2>&1 | grep -q "deployment-soak" && grep -q "require-production-validated" infra/scripts/capture-production-evidence.sh && grep -q "require-provider-forbid-local" infra/scripts/capture-production-evidence.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The harness is confirmed to run the sanctioned path (deployment-soak --evidence-dir -> release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local) and to validate production-scoped + secret-free manifests.
    - `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md` records the harness-verification result.
    - Any harness gap was coordinated, not silently rewritten (D-14).
  </acceptance_criteria>
  <done>The capture-production-evidence.sh harness is verified to run the sanctioned operator-evidence path, with the result recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the full local-staging dry-run end-to-end and capture the fingerprint</name>
  <files>.planning/runbooks/LOCAL-STAGING-DRY-RUN.md</files>
  <read_first>
    - infra/scripts/setup-all.sh (seeds Keycloak realm + Vault transit + C2PA trust root + signed asset)
    - infra/validate/validate-all.sh (runs validate-keycloak + validate-vault + validate-c2pa)
    - infra/README.md and infra/PRODUCTION-EVIDENCE.md (the documented local-staging evidence flow + prior captured fingerprint reference)
    - docs/ROADMAP-TO-100.md (Tier B checkpoint: prior scoped local dry-run fingerprint and the explicit "this is not production validation" framing)
    - src/mnemosyne/cli.py deployment-soak + release-audit argparse (the `--evidence-dir`, `--allow-provider-local` flags)
  </read_first>
  <action>
    Run the full LOCAL-staging dry-run end-to-end (no cloud, no paid infra): (1) `infra/scripts/setup-all.sh` then `infra/validate/validate-all.sh` — both must pass. (2) Build/locate a LOCAL-scoped soak manifest (allow-provider-local; NOT production-scoped) and run `deployment-soak --soak-manifest <local-m> --evidence-dir <dir>` over the local real services + provider metadata. (3) Run `release-audit --evidence-manifest <dir>/manifest.json --allow-provider-local` and confirm it passes for the available checks. Capture the release-audit fingerprint. Write `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md` recording: the exact commands run, the services exercised, the captured fingerprint, the release-audit findings (must be empty for the available checks), and an EXPLICIT statement that this is staging proof with `production_validated=false` — NOT production validation, and does not prove live ParadeDB/AGE/pgvector or any operator-gated row. Cross-link the operator pass (plan 06-09) as the production-evidence path. Redact any tokens/secrets from recorded output.
  </action>
  <verify>
    <automated>test -f .planning/runbooks/LOCAL-STAGING-DRY-RUN.md && grep -q "production_validated=false" .planning/runbooks/LOCAL-STAGING-DRY-RUN.md && grep -Eiq "fingerprint" .planning/runbooks/LOCAL-STAGING-DRY-RUN.md && grep -q "setup-all.sh" .planning/runbooks/LOCAL-STAGING-DRY-RUN.md && grep -q "validate-all.sh" .planning/runbooks/LOCAL-STAGING-DRY-RUN.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `infra/scripts/setup-all.sh` and `infra/validate/validate-all.sh` both pass (recorded in the artifact).
    - `deployment-soak --evidence-dir` over local real services produced an evidence bundle; `release-audit --allow-provider-local` passed for the available checks with empty findings.
    - `.planning/runbooks/LOCAL-STAGING-DRY-RUN.md` records the exact commands, the captured fingerprint, and the literal `production_validated=false` staging-only statement.
    - No secrets/tokens appear in the recorded output.
  </acceptance_criteria>
  <done>The full local-staging dry-run is proven end-to-end with a captured fingerprint, recorded as explicit staging proof (production_validated=false).</done>
</task>

</tasks>

<verification>
- Harness verified to run the sanctioned path (D-06).
- Local-staging dry-run proven end-to-end with captured fingerprint; artifact states production_validated=false (D-07).
- infra/ verified/coordinated, not rewritten; only the new runbook file staged (D-14).
</verification>

<success_criteria>
The official evidence path is proven to work end-to-end against real local services with a captured fingerprint, and the recorded artifact is unambiguous that it is staging proof, not production validation — leaving only the operator production pass (plan 06-09) for literal 100%.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-08-SUMMARY.md` when done.
</output>
