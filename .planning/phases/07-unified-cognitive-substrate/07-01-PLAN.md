---
phase: 07-unified-cognitive-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mnemosyne/standing.py
  - src/mnemosyne/engine.py
  - src/mnemosyne/postgres_engine.py
  - src/mnemosyne/retrieval.py
  - eval/g0/standing_parity.py
  - eval/g0/runner.py
  - eval/g0/preregistrations/g5-standing-bytestable-parity.json
  - tests/test_standing_parity.py
autonomous: true
requirements: [FR-6, FR-7, FR-17, OQ4, "spec:§3", "spec:§9-P1", "spec:§13-H7", "spec:§13-H10"]
must_haves:
  truths:
    - "Standing is a DERIVED, recomputable value — never a source of truth; it can never write or overwrite the evidence ledger."
    - "P1 is BYTE-STABLE: with Standing introduced as a hi/lo mirror, every retrieval/consolidation/abstention decision is identical to the pre-Standing boolean path over the full differential corpus (zero divergence)."
    - "Every existing guardrail stays green: ECE, abstention precision/recall, confabulation, poison-block, recall/nDCG, fast-path P95, the 7 §31 rails, the 14 indicator scores."
  artifacts:
    - "src/mnemosyne/standing.py — deterministic, versioned Standing function + explain schema."
    - "eval/g0/preregistrations/g5-standing-bytestable-parity.json — preregistered zero-divergence gate."
  key_links:
    - from: "retrieval / consolidation eligibility / abstention gate"
      to: "src/mnemosyne/standing.py:standing(...)"
      via: "decisions read Standing instead of the boolean shadow flags"
      pattern: "standing\\(|standing_fn_version|reads? Standing"
---

<objective>
Land the keystone with ZERO behavior change. Introduce `Standing` as a single, derived, deterministic, versioned value computed from signals that already exist (provenance, corroboration, calibrated confidence, reality-class, contradiction pressure, trust tier, ACT-R activation), and re-point the retrieval-weight / consolidation-eligibility / abstention decisions to READ Standing instead of the boolean `shadow_only` / `ungrounded_only` flags. In P1, Standing is deliberately a hi/lo MIRROR that reproduces today's decisions exactly. This replaces the switch with a number that currently encodes the same decision — nothing more.

CRITICAL: This plan ships ONLY if a differential parallel-run proves zero divergence from the pre-Standing path (D-10/H10). It must not change any retrieval result, any abstention decision, or any guardrail metric. The booleans are NOT removed in P1 (that is P5); they are computed alongside Standing and asserted equal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-unified-cognitive-substrate/07-CONTEXT.md
@docs/superpowers/specs/2026-06-27-unified-cognitive-substrate-design.md
@docs/blueprint/cognitive-architecture/06-CONSCIOUSNESS-AND-CONTINUOUS-WORKSPACE.md
@eval/g0/runner.py
@eval/g0/gate.py
</context>

<tasks>

<task type="execute">
  <name>Define the deterministic, versioned Standing function</name>
  <read_first>
    - src/mnemosyne/calibration.py (reality_monitor_confidence, conformal scoring)
    - src/mnemosyne/consciousness.py (RealityMonitor, reality classes)
    - src/mnemosyne/policy.py (trust tiers, ACT-R actr_decay)
  </read_first>
  <what>Create `src/mnemosyne/standing.py` with `standing(unit_signals) -> Standing` where Standing carries a single scalar in P1 plus a `standing_fn_version` and an `explain` dict cashing out every input. `f` is deterministic (fixed precision, reproducible to the bit), monotonic in each grounding signal, and pure (no I/O, no engine state). Inputs are the already-stored signals listed in spec §3.1. Fail-closed default: missing/unknown signal ⇒ low Standing (H7).</what>
  <how-to-verify>Unit tests: same inputs ⇒ identical bytes across runs; monotonicity (more corroboration never lowers Standing; more contradiction never raises it); unknown inputs ⇒ low Standing; `explain` reproduces inputs. No engine import in standing.py.</how-to-verify>
  <acceptance_criteria>
    - `standing.py` is pure/deterministic/versioned; `tests/test_standing_parity.py` covers determinism + monotonicity + fail-closed default.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Re-point read sites to Standing (mirror mode), keep booleans alongside</name>
  <read_first>
    - src/mnemosyne/retrieval.py (ranking weight, abstention gate, ungrounded_only)
    - src/mnemosyne/engine.py + src/mnemosyne/postgres_engine.py (consolidation eligibility, shadow_only handling)
  </read_first>
  <what>At each decision site (retrieval weight, consolidation eligibility, the abstention gate that consumes `ungrounded_only`), compute Standing and derive the SAME decision the boolean produced today, choosing P1 thresholds so the mirror is exact. Keep the boolean computed in parallel and assert equality in debug/parallel-run mode. Emit Standing into the existing `explain` trace under a `standing` key (value + `standing_fn_version` + inputs).</what>
  <how-to-verify>Local + compose-Postgres suites green with `MNEMOSYNE_POSTGRES_DSN`; `explain.standing` present on hits; the parallel-run assertion (Standing-derived decision == boolean decision) holds across the test corpus.</how-to-verify>
  <acceptance_criteria>
    - Both engines route decisions through Standing in mirror mode; booleans retained; explain carries Standing.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Differential parallel-run harness + preregistered zero-divergence gate (H10)</name>
  <read_first>
    - eval/g0/runner.py, eval/g0/gate.py, eval/g0/baselines/baseline-0.json
    - eval/g0/preregistrations/ (existing prereg shape)
  </read_first>
  <what>Add `eval/g0/standing_parity.py`: over a large corpus, compute every decision both ways (boolean vs Standing-derived) and report `standing_decision_divergence` (must be 0.0) plus the standard guardrail set. Register it in `runner.py`. Preregister `eval/g0/preregistrations/g5-standing-bytestable-parity.json` with target `standing_decision_divergence` (0.0) and guardrails-not-down for ECE, abstention, confabulation, poison-block, recall/nDCG, fast-path P95, the 7 rails, and the 14 indicators.</what>
  <how-to-verify>`python -m eval.g0.gate --baseline eval/g0/baselines/baseline-0.json --candidate <candidate> --prereg eval/g0/preregistrations/g5-standing-bytestable-parity.json` passes with `standing_decision_divergence == 0.0` and no guardrail decrease. Decision appended to `eval/g0/decision-log.jsonl`.</how-to-verify>
  <acceptance_criteria>
    - Gate passes at zero divergence; G0 report regenerated; no guardrail regression.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `standing.py` deterministic/versioned/pure; recomputable; never writes evidence.
- Differential parallel-run: zero divergence between Standing-derived and boolean decisions over the full corpus (H10).
- Full local + compose-Postgres suites green; all G0 guardrails non-decreasing; the g5 byte-stable gate passes.
- No `infra/` or production-evidence files touched; src changes additive and default-safe (D-13/D-14).
</verification>

<success_criteria>
Standing exists as the single derived decision signal, in exact mirror of today's behavior, proven by a zero-divergence differential gate. The switch has been replaced by a number with identical semantics — the foundation for P2's continuity with no reliability cost.
</success_criteria>

<output>
Create `.planning/phases/07-unified-cognitive-substrate/07-01-SUMMARY.md` when done (record the differential-divergence result, the `standing_fn_version`, and the g5 gate decision).
</output>
