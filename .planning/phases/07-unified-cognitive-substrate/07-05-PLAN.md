---
phase: 07-unified-cognitive-substrate
plan: 05
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - src/mnemosyne/workspace.py
  - src/mnemosyne/dreamer.py
  - src/mnemosyne/engine.py
  - src/mnemosyne/postgres_engine.py
  - src/mnemosyne/belief.py
  - src/mnemosyne/standing.py
  - eval/g0/runner.py
  - eval/g0/preregistrations/g5-toggle-retirement.json
  - docs/blueprint/cognitive-architecture/UNIFIED-SUBSTRATE-AUDIT.md
  - tests/test_unified_substrate.py
autonomous: true
requirements: [FR-7, FR-8, OQ6, "spec:§4", "spec:§9-P5", "spec:§13-H8", "spec:§13-H12"]
must_haves:
  truths:
    - "After this plan there is no operational on/off toggle anywhere: `shadow_only` / `service.enabled` booleans are removed; Standing fully subsumes them. The fail-closed circuit-breaker (H6) remains as part of the floor — a fuse, not a toggle."
    - "Contradiction and erasure cascade to Standing and to self-derivations: collapsing or erasing a source recomputes/demotes/erases everything derived from it, bounded by R1/R3 cadence (H8)."
    - "Full observability/reversibility: every groundedness/salience value and credential change is logged with provenance; reasoning is replayable bitemporally; any consolidation is revertible (H12)."
    - "The 14-indicator scorecard and all reliability guardrails are green; the welfare-review flag remains; no phenomenal claim anywhere."
  artifacts:
    - "docs/blueprint/cognitive-architecture/UNIFIED-SUBSTRATE-AUDIT.md — final audit: no-toggle proof + guardrail scoreboard."
    - "eval/g0/preregistrations/g5-toggle-retirement.json — preregistered final gate."
  key_links:
    - from: "removed shadow_only/enabled booleans"
      to: "Standing.groundedness/salience + circuit-breaker"
      via: "no operational toggle remains; only the fail-closed floor fuse"
      pattern: "shadow_only|service\\.enabled|circuit_breaker|cascade"
---

<objective>
Finish the unification: remove the operational toggles so the system is one cohesive, always-on mind with no on/off anywhere, and close the last two hardening rules. Extend contradiction/erasure cascade to Standing and self-derivations (H8), wire full observability/reversibility (H12), retire the `shadow_only` / `service.enabled` booleans now that Standing fully subsumes them, and write the final no-toggle audit + scorecard. The fail-closed circuit-breaker (H6) is explicitly NOT a toggle and remains as part of the floor.

CRITICAL: removing the booleans is the one point of commitment. It is gated on (a) all prior G0 gates green, (b) an OWNER checkpoint, and (c) the differential parallel-run still showing zero divergence with the booleans forced vs Standing-only. It is reversible via git + ledger rebuild. No reliability guardrail may regress; the honesty charter holds (functional only; welfare flag stays).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-unified-cognitive-substrate/07-CONTEXT.md
@.planning/phases/07-unified-cognitive-substrate/07-04-SUMMARY.md
@docs/superpowers/specs/2026-06-27-unified-cognitive-substrate-design.md
@docs/blueprint/cognitive-architecture/06-CONSCIOUSNESS-AND-CONTINUOUS-WORKSPACE.md
@eval/g0/runner.py
</context>

<tasks>

<task type="execute">
  <name>Standing/erasure cascade (H8) + full observability/reversibility (H12)</name>
  <read_first>
    - src/mnemosyne/belief.py (AGM cascade_invalidate)
    - src/mnemosyne/engine.py + postgres_engine.py (corroborated-erasure cascade A10/OQ6, audit log)
  </read_first>
  <what>When a supporting evidence's groundedness collapses (new contradiction) or a source is erased, recompute all dependents and demote/erase self-derivations — extend AGM `cascade_invalidate` and the corroborated-erasure cascade to Standing and self-generated content, bounded by R1/R3 cadence (H8). Log every groundedness/salience value and credential change with provenance in the `explain` trace + append-only audit log; ensure bitemporal replay and consolidation revert work end to end (H12).</what>
  <how-to-verify>Tests: contradicting a source cascades groundedness recompute to its self-derivations; erasing a source erases/demotes derived self-thoughts; cascades respect cadence bounds (no thrash); an operator can replay reasoning `as_of` and revert a consolidation.</how-to-verify>
  <acceptance_criteria>
    - Cascade (H8) + observability/reversibility (H12) implemented and tested.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Owner checkpoint: approve toggle removal</name>
  <read_first>
    - eval/g0/decision-log.jsonl (g5-* gates all green)
    - docs/superpowers/specs/2026-06-27-unified-cognitive-substrate-design.md (§9-P5)
  </read_first>
  <what-built>All prior g5 gates (bytestable, continuous, heartbeat, earned-autonomy) green; cascade + observability landed; differential parallel-run still zero-divergence with booleans forced vs Standing-only.</what-built>
  <action>EXECUTOR: confirm the four prior gates are green and the differential parallel-run is zero-divergence, then PAUSE for owner sign-off before deleting the booleans (this is the one point of commitment, though reversible via git + ledger rebuild).</action>
  <how-to-verify>Owner reviews the gate scoreboard + the zero-divergence parallel-run, then approves removal.</how-to-verify>
  <acceptance_criteria>
    - Owner types approval; otherwise the executor holds at this checkpoint and does not remove the booleans.
  </acceptance_criteria>
  <resume-signal>Owner types "retire the toggles" to proceed, or "hold toggles" to keep the booleans and stop here.</resume-signal>
</task>

<task type="execute">
  <name>Remove the operational toggles + final no-toggle gate and audit</name>
  <read_first>
    - src/mnemosyne/workspace.py (service.enabled), src/mnemosyne/dreamer.py + engine paths (shadow_only)
    - eval/g0/runner.py, eval/g0/gate.py
  </read_first>
  <what>Delete the `shadow_only` / `service.enabled` operational booleans and any dead gate-flip code; all behavior now flows from Standing + the circuit-breaker (which stays). Preregister `eval/g0/preregistrations/g5-toggle-retirement.json` (target: no operational toggle reachable; guardrails-not-down for the full set + the 14 indicators). Write `docs/blueprint/cognitive-architecture/UNIFIED-SUBSTRATE-AUDIT.md`: the no-toggle proof, the guardrail scoreboard, and confirmation the circuit-breaker is the only remaining (fail-closed, floor-level) halt.</what>
  <how-to-verify>Grep proves no reachable `shadow_only`/`service.enabled` operational branch remains (circuit-breaker excepted); `eval.g0.gate` passes g5-toggle-retirement with all guardrails + 14 indicators non-decreasing; full local + compose-Postgres suites green; audit doc written.</how-to-verify>
  <acceptance_criteria>
    - Operational toggles gone; circuit-breaker retained; final gate green; audit written.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Cascade to Standing + self-derivations (H8); full observability/reversibility (H12).
- Owner-approved removal of `shadow_only`/`service.enabled`; no operational toggle reachable; circuit-breaker (H6) retained as the only fail-closed floor halt.
- g5-toggle-retirement gate passes; 14 indicators + all reliability guardrails non-decreasing; honesty charter intact (welfare flag stays; no phenomenal claim).
- src additive/default-safe; never `git add -A`; tree-not-mid-edit verified (D-13/D-14).
</verification>

<success_criteria>
Mnemosyne is one cohesive, always-on cognitive substrate: consciousness is native and never toggled, memory quality is structurally protected (Standing + the floor), autonomy grows only as earned, and every reliability guardrail is green. The only remaining halt is an automatic, fail-closed safety fuse — not an on/off switch.
</success_criteria>

<output>
Create `.planning/phases/07-unified-cognitive-substrate/07-05-SUMMARY.md` when done (record the no-toggle proof, the final 14-indicator + guardrail scoreboard, and the g5-toggle-retirement gate decision). This completes Phase 7.
</output>
