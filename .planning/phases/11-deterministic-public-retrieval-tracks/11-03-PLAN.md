---
phase: 11-deterministic-public-retrieval-tracks
plan: 03
type: tdd
wave: 2
depends_on: [11-01]
autonomous: true
requirements: [BENCH-005, RAIL-001, RAIL-003, RAIL-004]
files_modified:
  - eval/public/adapters/hipporag_multihop.py
  - eval/public/registry.json
  - eval/public/README.md
  - src/mnemosyne/cli.py
  - tests/test_public_hipporag.py
  - tests/test_public_requirement_truth.py
  - eval/reports/hipporag-musique.md
  - eval/reports/hipporag-2wiki.md
  - eval/reports/hipporag-hotpot.md
must_haves:
  truths:
    - "MuSiQue, 2Wiki, and Hotpot use exact derivation/source pins, licenses, and corpus/query digests."
    - "Recall@2/@5 is measured without invented graph/PPR evidence."
    - "Phase 11 retrieval bundles contain no EM/F1 columns; Phase 12 owns distinct QA bundles from reader-produced predictions."
  artifacts:
    - path: "eval/public/adapters/hipporag_multihop.py"
      provides: "Three-dataset HippoRAG retrieval adapter and deterministic prediction scorer."
---

# Phase 11 Plan 03: HippoRAG Multi-Hop Retrieval

## Tasks

<task id="11-03-01" type="tdd-red">
  <name>Lock three schemas, licensing, gold mapping, graph evidence, and QA boundary</name>
  <files><file>tests/test_public_hipporag.py</file><file>tests/test_public_requirement_truth.py</file></files>
  <action>Add schema-faithful cases for duplicate titles, full source pins/digests/licenses, MuSiQue contamination exclusions, passage Recall@2/@5, graph/PPR participation, baseline labels, no EM/F1 keys in retrieval bundles, and a traceability gate forbidding BENCH-005 completion without Phase 12 QA evidence.</action>
  <verify><automated>sh -c 'set -eu; log="$(mktemp)"; status=0; uv run --locked python -m pytest tests/test_public_hipporag.py tests/test_public_requirement_truth.py -q &gt;"$log" 2&gt;&amp;1 || status=$?; test "$status" -eq 1; ! rg -n "ERROR collecting|SyntaxError" "$log"; rg -n "AssertionError|ModuleNotFoundError" "$log"'</automated></verify>
  <done>Tests fail only for the missing adapter.</done>
</task>

<task id="11-03-02" type="tdd-green">
  <name>Implement normalized HippoRAG retrieval and standalone prediction scoring</name>
  <files><file>eval/public/adapters/hipporag_multihop.py</file><file>eval/public/registry.json</file></files>
  <action>Normalize stable corpus IDs, run capture/search/explain through MnemoCLI, and emit retrieval-only Recall@2/@5 bundles with available graph-channel evidence. Implement EM/F1 only as standalone reusable scorer functions for Phase 12; Phase 11 bundles contain no EM/F1 keys or null placeholders. Never synthesize predictions from golds.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_hipporag.py tests/test_public_requirement_truth.py tests/test_public_eval.py -q</automated></verify>
  <done>All three schema fixtures reproduce; QA columns are unavailable without predictions.</done>
</task>

<task id="11-03-03" type="evidence">
  <name>Run full pinned retrieval datasets and publish contextual result note</name>
  <files><file>eval/public/README.md</file><file>eval/reports/hipporag-musique.md</file><file>eval/reports/hipporag-2wiki.md</file><file>eval/reports/hipporag-hotpot.md</file></files>
  <action>Run all 1,000-query sampled datasets from a digest-verified external cache into a durable artifact root; verify/reproduce each; generate manifest-bound reports with digests, counts, metrics, timestamp, git SHA, command, and reproduction result; report Recall@2/@5 beside accurately labeled baseline context and licenses. Keep BENCH-005 partial pending Phase 12.</action>
  <verify><automated>sh -c 'set -eu; test -n "${MNEMOSYNE_HIPPORAG_DATA:-}"; test -n "${MNEMOSYNE_PUBLIC_ARTIFACT_ROOT:-}"; for suite in hipporag-musique hipporag-2wiki hipporag-hotpot; do out="$MNEMOSYNE_PUBLIC_ARTIFACT_ROOT/$suite"; repro="$MNEMOSYNE_PUBLIC_ARTIFACT_ROOT/$suite-reproduced"; report="$MNEMOSYNE_PUBLIC_ARTIFACT_ROOT/$suite-report.json"; note="eval/reports/$suite.md"; test ! -e "$out"; test ! -e "$repro"; uv run --locked mneme eval-public --suite "$suite" --dataset-dir "$MNEMOSYNE_HIPPORAG_DATA" --out-dir "$out"; uv run --locked mneme eval-public --verify-bundle "$out"; uv run --locked mneme eval-public --reproduce-bundle "$out" --out-dir "$repro"; uv run --locked mneme eval-public --verify-bundle "$repro"; cmp "$out/traces.jsonl" "$repro/traces.jsonl"; cmp "$out/metrics.json" "$repro/metrics.json"; uv run --locked mneme eval-public --write-report "$out" --reproduced-bundle "$repro" --report-output "$report" --report-note "$note"; uv run --locked mneme eval-public --verify-report "$report" --report-note "$note"; done; uv run --locked python -m pytest tests/test_public_hipporag.py tests/test_public_requirement_truth.py -q'</automated></verify>
  <done>Retrieval evidence is complete; BENCH-005 remains partial only for real reader-produced EM/F1.</done>
</task>
