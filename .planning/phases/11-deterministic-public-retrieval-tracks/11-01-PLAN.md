---
phase: 11-deterministic-public-retrieval-tracks
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [BENCH-004, BENCH-005, RAIL-001, RAIL-002, RAIL-003, RAIL-004]
files_modified:
  - eval/public/assets.py
  - eval/public/scoring.py
  - eval/public/bundle.py
  - eval/public/runner.py
  - eval/harness/cli_driver.py
  - src/mnemosyne/cli.py
  - tests/test_public_eval_assets.py
  - tests/test_public_eval_scoring.py
  - tests/test_public_eval_cli.py
must_haves:
  truths:
    - "External benchmark assets are immutable, licensed, attributed, and digest verified."
    - "Every metric is recomputed from benchmark-owned labels through an explicit scoring profile."
    - "Fractional retrieval and QA metrics remain separate and use the correct interval family."
  artifacts:
    - path: "eval/public/assets.py"
      provides: "Pinned external asset acquisition and normalization custody."
    - path: "eval/public/scoring.py"
      provides: "Versioned deterministic public scoring profiles."
---

# Phase 11 Plan 01: Generalize Public Asset and Scoring Custody

## Tasks

<task id="11-01-01" type="tdd-red">
  <name>Lock immutable asset and multi-metric scoring contracts red first</name>
  <files><file>tests/test_public_eval_assets.py</file><file>tests/test_public_eval_scoring.py</file></files>
  <action>Add expected-failure tests for mutable refs, digest/license/citation/split/contamination gaps, path/link attacks, schema ambiguity, Recall@2/@5, nDCG, EM, token F1, Wilson/bootstrap selection, family blending, and benchmark-owned recomputation.</action>
  <verify><automated>sh -c 'set -eu; log="$(mktemp)"; status=0; uv run --locked python -m pytest tests/test_public_eval_assets.py tests/test_public_eval_scoring.py -q &gt;"$log" 2&gt;&amp;1 || status=$?; test "$status" -eq 1; ! rg -n "ERROR collecting|SyntaxError" "$log"; rg -n "AssertionError|ModuleNotFoundError" "$log"'</automated></verify>
  <done>Only missing asset/scoring behavior fails.</done>
</task>

<task id="11-01-02" type="tdd-green">
  <name>Implement pinned asset custody and versioned deterministic scorers</name>
  <files><file>eval/public/assets.py</file><file>eval/public/scoring.py</file></files>
  <action>Use stdlib only. Acquire or accept external assets at exact revisions, verify SHA-256 before parse, normalize schema explicitly, and implement hand-checkable scoring profiles with fixed-seed intervals.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_eval_assets.py tests/test_public_eval_scoring.py -q</automated></verify>
  <done>All asset and scoring unit/adversarial cases pass without a core dependency.</done>
</task>

<task id="11-01-03" type="integration">
  <name>Generalize bundle verification without weakening Phase 10 custody</name>
  <files><file>eval/public/bundle.py</file><file>eval/public/runner.py</file><file>eval/harness/cli_driver.py</file><file>src/mnemosyne/cli.py</file><file>tests/test_public_eval_cli.py</file></files>
  <action>Dispatch an allowlisted scoring profile from anchored registry metadata; bind assets, labels, traces, metrics, licenses, and publication flags. Add tested `--dataset-dir`, `--write-report`, `--reproduced-bundle`, `--report-output`, `--report-note`, and `--verify-report` contracts. Add bounded `capture-batch --input-jsonl` plus `MnemoCLI.capture_batch` so full corpora use one public CLI process while retaining MemoryTools mediation per row and full prevalidation before writes. Report writing occurs only after source and reproduced bundles verify and match; the deterministic Markdown projection embeds the external report digest, bundle manifest digest, and git SHA. Dataset selection is suite-specific; cache use is offline and digest-gated, with no implicit mutable download. Preserve every Phase 10 custody check.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_eval.py tests/test_public_eval_assets.py tests/test_public_eval_scoring.py tests/test_public_eval_cli.py -q</automated></verify>
  <done>Smoke and generalized profiles pass; CLI asset/report contracts are executable; no verifier check is removed.</done>
</task>
