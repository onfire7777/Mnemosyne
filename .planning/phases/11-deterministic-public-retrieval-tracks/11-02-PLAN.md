---
phase: 11-deterministic-public-retrieval-tracks
plan: 02
type: tdd
wave: 2
depends_on: [11-01]
autonomous: true
requirements: [BENCH-004, RAIL-001, RAIL-003, RAIL-004]
files_modified:
  - eval/public/adapters/longmemeval.py
  - eval/public/registry.json
  - eval/public/README.md
  - src/mnemosyne/cli.py
  - tests/test_public_longmemeval.py
  - eval/reports/m1.2-longmemeval-retrieval.md
must_haves:
  truths:
    - "LongMemEval retrieval uses only cleaned upstream support labels and no LLM scorer."
    - "Every question preserves session/turn golds and ranked retrieval traces."
    - "Full results remain non-headline until independent reproduction."
  artifacts:
    - path: "eval/public/adapters/longmemeval.py"
      provides: "Cleaned LongMemEval retrieval adapter."
---

# Phase 11 Plan 02: LongMemEval Retrieval-Recall

## Tasks

<task id="11-02-01" type="tdd-red">
  <name>Lock cleaned schema, isolation, gold mapping, and scoring red first</name>
  <files><file>tests/test_public_longmemeval.py</file></files>
  <action>Add schema-faithful fixtures and expected failures for wrong revision/digest, unsupported rows, cross-question contamination, missing session/turn golds, LLM scoring, and incomplete traces.</action>
  <verify><automated>sh -c 'set -eu; log="$(mktemp)"; status=0; uv run --locked python -m pytest tests/test_public_longmemeval.py -q &gt;"$log" 2&gt;&amp;1 || status=$?; test "$status" -eq 1; ! rg -n "ERROR collecting|SyntaxError" "$log"; rg -n "AssertionError|ModuleNotFoundError" "$log"'</automated></verify>
  <done>Tests fail only for the missing adapter.</done>
</task>

<task id="11-02-02" type="tdd-green">
  <name>Implement the isolated LongMemEval public CLI adapter</name>
  <files><file>eval/public/adapters/longmemeval.py</file><file>eval/public/registry.json</file></files>
  <action>Pin official code and cleaned dataset revisions plus exact file digests; normalize session/turn labels, isolate each question history, capture/search through MnemoCLI, and emit Recall@5/nDCG traces with no reader or judge.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_longmemeval.py tests/test_public_eval.py -q</automated></verify>
  <done>Schema-faithful development data runs create/verify/reproduce/verify.</done>
</task>

<task id="11-02-03" type="evidence">
  <name>Run the full pinned cleaned dataset and write the result note</name>
  <files><file>eval/public/README.md</file><file>eval/reports/m1.2-longmemeval-retrieval.md</file></files>
  <action>Acquire the exact named cleaned and oracle files into an external cache, verify full digests, run all eligible questions, verify and reproduce into a durable external artifact root, and generate a report bound to the bundle manifest, asset digests, counts, metrics, timestamp, git SHA, command, and reproduction result. Keep all headline flags false.</action>
  <verify><automated>sh -c 'set -eu; test -n "${MNEMOSYNE_LONGMEMEVAL_DATA:-}"; test -n "${MNEMOSYNE_PUBLIC_ARTIFACT_ROOT:-}"; out="$MNEMOSYNE_PUBLIC_ARTIFACT_ROOT/longmemeval-retrieval"; repro="$MNEMOSYNE_PUBLIC_ARTIFACT_ROOT/longmemeval-retrieval-reproduced"; report="$MNEMOSYNE_PUBLIC_ARTIFACT_ROOT/m1.2-longmemeval-retrieval.json"; note="eval/reports/m1.2-longmemeval-retrieval.md"; test ! -e "$out"; test ! -e "$repro"; uv run --locked mneme eval-public --suite longmemeval-retrieval --dataset-dir "$MNEMOSYNE_LONGMEMEVAL_DATA" --out-dir "$out"; uv run --locked mneme eval-public --verify-bundle "$out"; uv run --locked mneme eval-public --reproduce-bundle "$out" --out-dir "$repro"; uv run --locked mneme eval-public --verify-bundle "$repro"; cmp "$out/traces.jsonl" "$repro/traces.jsonl"; cmp "$out/metrics.json" "$repro/metrics.json"; uv run --locked mneme eval-public --write-report "$out" --reproduced-bundle "$repro" --report-output "$report" --report-note "$note"; uv run --locked mneme eval-public --verify-report "$report" --report-note "$note"; uv run --locked python -m pytest tests/test_public_longmemeval.py -q'</automated></verify>
  <done>BENCH-004 has a full pinned non-headline bundle and result note.</done>
</task>
