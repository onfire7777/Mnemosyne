---
phase: 09-performance-and-refactoring-continuation
plan: 02
wave: 2
status: complete-local-gated
created: 2026-07-08
completed: 2026-07-08
target_metric: cache_drift_native_gil_release
target_value: documented_landed_caches_and_detached_mmr_ppr
gate: focused-local-plus-native-parity
---

# 09-02 Plan: Cache Drift Closure and Native GIL Detach

## Goal

Close stale performance-blueprint/cache planning claims and land the smallest
remaining native GIL-release slice without changing scoring, fusion, ordering,
or cache-visible behavior.

## Scope

- Register the HTTP embedding provider cache knobs in the config drift ledger.
- Update blueprint and Phase 09 planning docs so process-local HTTP embedding
  cache, retrieval result cache, and Postgres positive calibration lookup cache
  are treated as landed work.
- Add provider cache regression tests for mixed cache hits/misses, model
  revision cache busting, and `cache_size=0`.
- Wrap Rust-owned MMR and PPR native compute loops in `Python::detach`.
- Keep larger durable provider cache, prepared statements, provider conformance,
  vector index coverage, MCP warm reuse, and operator runtime-flip evidence as
  separate measured slices.

## Acceptance

- Provider cache tests pass.
- Postgres perf-lane tests still pass.
- Native Rust unit tests pass.
- Rebuilt PyO3 extension passes native parity/dispatch and benchmark guards.
- GSD Graphify artifacts are current and scratch output is ignored.
- CBM and gbrain are refreshed after commit.
