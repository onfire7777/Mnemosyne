---
phase: 09-performance-and-refactoring-continuation
plan: 05
wave: 5
status: complete-local-gated
created: 2026-07-09
completed: 2026-07-09
target_metric: postgres_vector_coverage_source_slice
target_value: none_partition_covered_without_hnsw_and_tuning_profile_versioned
gate: focused-schema-and-postgres-perf-lane-tests
---

# 09-05 Plan: Postgres Partition Coverage and Tuning Profile

## Goal

Close the source-owned part of Phase 09 Task 6 without claiming live tuning
evidence: make `embedding_partition='none'` explicit as a no-vector partition,
keep embeddable null fallback bounded and indexed, and version the Postgres
tuning candidate for a later operator run.

## Scope

- Add canonical schema indexes for:
  - `public` / `private` HNSW vector partitions as before.
  - `none` btree lifecycle/audit coverage.
  - embeddable-null fallback coverage excluding `none`.
- Update `PostgresEngine._ensure_evidence_vector_schema` to reconcile the same
  indexes privilege-tolerantly.
- Add `AND embedding_partition <> 'none'` to the null-embedding fallback query.
- Add a versioned `infra/postgres/postgresql-perf.conf` candidate profile with
  explicit before/after evidence instructions.
- Record the partition/tuning strategy in ADR-003 and update the blueprint,
  config drift docs, and Phase 09 planning state.

## Out Of Scope

- No third HNSW index for `none` rows.
- No halfvec, pgvectorscale, DiskANN, or model/default changes.
- No live production tuning application or performance-win claim.
- No operator-gated runtime flip.

## Acceptance

- Focused schema and Postgres perf-lane tests pass.
- The fallback query excludes `none` before Python embedding.
- Runtime schema ensure creates btree indexes for `none` and embeddable-null
  fallback, not HNSW indexes.
- Docs state that tuning values require retained before/after evidence before
  application.
