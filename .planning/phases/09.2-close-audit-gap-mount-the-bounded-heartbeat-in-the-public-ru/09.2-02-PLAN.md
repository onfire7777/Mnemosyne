---
phase: 09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru
plan: 02
type: tdd
wave: 2
depends_on:
  - 09.2-01
gap_closure: true
files_modified:
  - src/mnemosyne/cli.py
  - tests/test_cli_runtime_tools.py
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-02-SUMMARY.md
  - .planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-VERIFICATION.md
  - .planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-SECURITY.md
  - .planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-REVIEW.md
autonomous: true
requirements:
  - REQ-016
  - REQ-018
  - NFR-005
must_haves:
  truths:
    - "Release evidence rejects every cycle or final projection that resumes, advances, or reports healthy after the first terminal hard-stop, unhealthy, or frozen heartbeat tick."
    - "The first terminal heartbeat state remains terminal through all later cycle projections and the final projection, with no later tick or attempt increment and no healthy final report."
    - "Release evidence bounds attempted_ticks by the worker summary cycle count, worker max_cycles, and canonical heartbeat max_cycles, and relates attempts monotonically to tick_count and one-attempt-per-live-cycle semantics."
    - "Every cycle and final projection agrees on cycle, attempted_ticks, tick_count, lifecycle, failure state, and canonical heartbeat_safety counters; malformed or contradictory evidence fails closed."
    - "Valid productive, idle, hard-stop, ordinary-exception, safe-later-persistence, fail-on-dead, SystemExit/KeyboardInterrupt, fixed-metric, and production release evidence remains accepted or behaves exactly as Phase 09.2-01 established."
    - "The repair is confined to the shared validator and its focused tests; worker runtime behavior, queue outcomes, additive schema, privacy/authority rails, topology, dependencies, and Phase 7 safety remain unchanged."
    - "Independent verification, security review, code review, full regression, CBM, gbrain, exact-SHA CI, Git parity, and clean-state gates all pass before Phase 09.2 is closed."
  artifacts:
    - path: "src/mnemosyne/cli.py"
      provides: "One fail-closed shared validator for bounded monotonic heartbeat sequences and terminal-state preservation."
      contains: "_release_worker_run_evidence_findings"
    - path: "tests/test_cli_runtime_tools.py"
      provides: "Independent red-first regressions for terminal restart contradictions and unbounded/inconsistent attempt counters plus the complete Phase 09.2 closure matrix."
      contains: "workspace_heartbeat"
    - path: ".planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-02-SUMMARY.md"
      provides: "Gap-closure commits, exact commands, source scope, CI, CBM/gbrain, deviations, and clean-state evidence."
    - path: ".planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-VERIFICATION.md"
      provides: "Independent proof that both HIGH findings and all closure conditions are resolved."
  key_links:
    - from: "tests/test_cli_runtime_tools.py"
      to: "src/mnemosyne/cli.py"
      via: "Two independent adversarial fixtures call the production shared release validator and prove the pre-fix false accepts fail with exact pytest rc 1."
      pattern: "_release_worker_run_evidence_findings|attempted_ticks|hard_stop"
    - from: "src/mnemosyne/cli.py"
      to: "src/mnemosyne/workspace.py"
      via: "Validator bounds and sequence rules reuse the service/controller max_cycles and canonical heartbeat_safety counters already projected by the worker."
      pattern: "max_cycles|tick_count|attempted_ticks"
    - from: "src/mnemosyne/cli.py"
      to: "tests/test_cli_runtime_tools.py"
      via: "The same validator accepts canonical live worker evidence and rejects the complete release mutation table without fixture-only policy."
      pattern: "workspace_heartbeat.*findings|release_worker_run"
    - from: ".planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-VERIFICATION.md"
      to: ".planning/ROADMAP.md"
      via: "Phase completion is recorded only after independent verification passes at the exact pushed SHA."
      pattern: "status: passed|09.2-02"
---

# Phase 09.2 Plan 02: Fail Closed on Contradictory Heartbeat Attestation

<objective>
Close the two independently reproduced HIGH Phase 09.2 gaps by making the
shared worker release-evidence validator reject terminal-state resurrection and
unbounded or internally inconsistent attempt counters. Preserve the shipped
runtime mount, queue behavior, additive report schema, privacy and authority
rails, and production topology exactly; this plan changes attestation
validation, not heartbeat execution.
</objective>

<execution_context>
Execute autonomously as one Wave 2 gap-closure TDD plan after `09.2-01`.
Follow Ponytail and systematic debugging: reproduce each false accept with an
independent exact-node red test, repair the shared sequence validator once, and
avoid fixture-only checks or runtime redesign. Use CBM impact analysis before
the source edit. No new dependency, schema field, service, topology, workspace
component, queue behavior, or public toggle is permitted.
</execution_context>

<threat_model id="T-09.2-02" block_on="high">
  <boundary>Retained worker JSON crosses the deployment-soak and release-audit boundary and is treated as evidence that a bounded, terminal, non-authoritative workspace heartbeat actually ran.</boundary>
  <assets>Release-attestation integrity, operator trust, terminal safety semantics, bounded CPU and memory claims, queue correctness, tenant privacy, and promotion decisions.</assets>
  <risks>
    <risk severity="high">A forged sequence reports a terminal hard stop or unhealthy tick, then resumes attempts/ticks or ends healthy, producing a misleading release attestation despite the no-in-process-restart contract.</risk>
    <risk severity="high">Unbounded attempted_ticks or mutually inconsistent cycles, attempts, ticks, and controller limits are accepted, allowing a bounded-compute claim to pass without bounded evidence.</risk>
    <risk severity="high">A test-fixture-only patch rejects two samples while equivalent contradictory sequences still fail open in the production validator.</risk>
    <risk severity="high">Over-broad validation changes reject canonical worker evidence or cause source/runtime, queue, privacy, authority, or supervisor behavior to drift.</risk>
  </risks>
  <controls>
    <control>Validate the ordered cycle sequence and final projection in `_release_worker_run_evidence_findings`; do not encode the policy only in fixtures or production manifest rendering.</control>
    <control>Treat the first `hard_stop=true`, `ok=false`, `lifecycle=unhealthy`, or terminal failure code as an absorbing state: later projections cannot increase attempts/ticks, become healthy/running/stopped-success, or erase the terminal failure.</control>
    <control>Require attempted_ticks to be nondecreasing before terminal freeze, increase by at most one per worker cycle, never exceed the current worker cycle, summary cycles, worker max_cycles, canonical safety max_cycles, or its corresponding tick evidence, and remain unchanged after freeze.</control>
    <control>Require final cycle/attempt/tick/lifecycle/failure/safety evidence to match the terminal or last live cycle consistently; reject boolean-as-integer and malformed counters through existing shape validation.</control>
    <control>Retain fixed error messages/codes, field allowlists, shadow/non-authority rails, and existing valid release fixture shape; do not inspect raw payloads or add identity-derived labels.</control>
    <control>Run independent mutation tests plus the full Phase 09.2, Phase 7, G0, queue/fail-on-dead, exception-propagation, metrics, production-routing, and release-audit regression slices.</control>
  </controls>
  <verification>Block closure on either false accept, any misleading terminal/boundedness attestation, any canonical-valid evidence rejection, any runtime/queue/privacy/authority drift, or any open high-severity security or code-review finding.</verification>
</threat_model>

<must_haves>
  <truths>
    <truth>The first terminal hard-stop, unhealthy, frozen, or failure projection is absorbing for all later cycle and final evidence.</truth>
    <truth>Attempted ticks are bounded by actual worker cycles and canonical controller limits and are consistent with tick counts before and after terminal freeze.</truth>
    <truth>The two authoritative verifier mutations independently fail before the fix and are accepted as findings after the fix.</truth>
    <truth>The validator remains the single production policy owner; no fixture-only rejection or duplicate validator is introduced.</truth>
    <truth>All valid Phase 09.2-01 runtime, safety, queue, privacy, authority, metrics, and topology behavior remains green.</truth>
  </truths>
  <artifacts>
    <artifact path="src/mnemosyne/cli.py">Shared ordered-sequence and bounded-counter release validation.</artifact>
    <artifact path="tests/test_cli_runtime_tools.py">Exact red reproductions and full release mutation/valid-evidence regression coverage.</artifact>
    <artifact path=".planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-02-SUMMARY.md">Execution and exact-state closure record.</artifact>
    <artifact path=".planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-VERIFICATION.md">Independent closure proof for REQ-016, REQ-018, NFR-005, and T-09.2-02.</artifact>
  </artifacts>
  <key_links>
    <link from="tests/test_cli_runtime_tools.py" to="src/mnemosyne/cli.py">Independent hostile sequences exercise `_release_worker_run_evidence_findings` directly.</link>
    <link from="src/mnemosyne/cli.py" to="src/mnemosyne/workspace.py">Projected `heartbeat_safety.max_cycles` and tick counters remain the canonical service/controller bounds.</link>
    <link from="src/mnemosyne/cli.py" to="tests/test_cli_runtime_tools.py">Canonical valid worker evidence and every terminal/boundedness mutation share one validator path.</link>
    <link from="09.2-VERIFICATION.md" to=".planning/ROADMAP.md">Only passed independent verification permits the plan and phase to become complete.</link>
  </key_links>
</must_haves>

<tasks>

<task id="09.2-02-01" type="tdd-red" requirements="REQ-016,REQ-018,NFR-005" threat_ref="T-09.2-02">
  <name>Reproduce both fail-open attestation gaps independently</name>
  <read_first>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-VERIFICATION.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-01-PLAN.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-01-SUMMARY.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-CONTEXT.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-VALIDATION.md</file>
    <file>src/mnemosyne/cli.py</file>
    <file>src/mnemosyne/workspace.py</file>
    <file>tests/test_cli_runtime_tools.py</file>
    <file>tests/test_always_on_heartbeat.py</file>
    <file>tests/test_workspace_stream.py</file>
  </read_first>
  <files>
    <file>tests/test_cli_runtime_tools.py</file>
  </files>
  <action>
Use CBM graph/snippet/impact queries for `_workspace_heartbeat_findings`,
`_release_worker_run_evidence_findings`, `cmd_worker_run`, and
`ShadowWorkspaceService` controller/report fields before editing. Add two
independent, specifically named tests against the real
`_release_worker_run_evidence_findings` entry point using the canonical
`production_release_stdout("worker-run", ...)` fixture.

The first test must reproduce the verifier's terminal-restart contradiction:
make cycle 1 `ok=false`, `lifecycle=unhealthy`,
`failure_code=workspace_heartbeat_hard_stop`, and
`heartbeat_safety.hard_stop=true`, while leaving a later cycle and final report
healthy with advanced `attempted_ticks`/`tick_count`. Assert a release finding
for terminal-state continuation or resurrection. Expand the same table enough
to cover later healthy lifecycle, later attempt/tick increment, erased failure,
and healthy final evidence after both hard-stop and ordinary unhealthy/failure
terminal states.

The second test must independently set cycle and final `attempted_ticks` to
`999999` and assert a boundedness finding. Expand its mutation table to cover
attempts greater than the worker cycle, summary cycles, worker max_cycles,
canonical `heartbeat_safety.max_cycles`, attempts/ticks disagreement,
non-monotonic counters, more than one attempt increment per live cycle, final
counter mismatch, and post-terminal counter increments. Retain at least one
canonical valid productive+idle fixture assertion returning no findings so the
tests distinguish fail-closed enforcement from blanket rejection.

Run each of the two exact pytest nodes separately after collection. Each node
must return pytest rc exactly `1` on the unmodified production validator and
fail on its intended missing finding—not import, fixture, collection,
environment, or unrelated assertions. Do not edit production source or fixture
generation logic in this task.
  </action>
  <verify>
    <automated>sh -c 'set -eu; a="tests/test_cli_runtime_tools.py::test_release_worker_heartbeat_rejects_terminal_state_resurrection"; b="tests/test_cli_runtime_tools.py::test_release_worker_heartbeat_rejects_unbounded_and_inconsistent_attempts"; uv run --locked python -m pytest --collect-only -q "$a" "$b" | grep -F "$a"; uv run --locked python -m pytest --collect-only -q "$a" "$b" | grep -F "$b"; for node in "$a" "$b"; do set +e; uv run --locked python -m pytest -q "$node"; rc=$?; set -e; test "$rc" -eq 1; done; test -z "$(git diff --name-only -- src/mnemosyne)"'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Each named test independently collects and exits pytest rc exactly 1 against the pre-fix validator because its expected release finding is absent.</criterion>
    <criterion>The terminal test covers a hard-stop/unhealthy cycle followed by later healthy, incremented, and healthy-final evidence, plus ordinary failure terminality.</criterion>
    <criterion>The counter test covers the verifier's `999999` mutation and cross-checks attempts against cycles, ticks, worker/controller bounds, monotonicity, one-attempt-per-live-cycle, final consistency, and frozen-state stability.</criterion>
    <criterion>A canonical valid productive-plus-idle sequence still expects no findings.</criterion>
    <criterion>No production file, fixture generator, dependency, schema, topology, or planning artifact beyond this plan is changed by the RED task.</criterion>
  </acceptance_criteria>
  <done>Both exact adversarial test nodes independently return rc 1 solely for the two documented fail-open validator gaps, the mutation tables cover equivalent contradictions, canonical evidence remains represented, and the production diff is empty.</done>
</task>

<task id="09.2-02-02" type="tdd-green" requirements="REQ-016,REQ-018,NFR-005" threat_ref="T-09.2-02">
  <name>Repair the shared validator and independently close Phase 09.2</name>
  <read_first>
    <file>src/mnemosyne/cli.py</file>
    <file>src/mnemosyne/workspace.py</file>
    <file>tests/test_cli_runtime_tools.py</file>
    <file>tests/test_always_on_heartbeat.py</file>
    <file>tests/test_workspace_stream.py</file>
    <file>tests/test_g0_harness.py</file>
    <file>tests/test_runtime_surfaces.py</file>
    <file>tests/test_production_manifest_renderer.py</file>
    <file>tests/test_prod_compose_policy.py</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-SECURITY.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-REVIEW.md</file>
  </read_first>
  <files>
    <file>src/mnemosyne/cli.py</file>
    <file>tests/test_cli_runtime_tools.py</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-02-SUMMARY.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-VERIFICATION.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-SECURITY.md</file>
    <file>.planning/phases/09.2-close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru/09.2-REVIEW.md</file>
  </files>
  <action>
Make the minimum root-cause fix in the shared validator in
`src/mnemosyne/cli.py`. Keep `_workspace_heartbeat_findings` responsible for a
single projection's shape and intrinsic rails; extend
`_release_worker_run_evidence_findings` (or one private helper called only by
it) to validate the ordered cycle sequence plus final projection. Do not put
the policy in test fixtures.

For each cycle, require the heartbeat cycle to equal the worker cycle; require
`attempted_ticks` and `tick_count` to be monotonic while live, bounded by the
current cycle, summary cycle count, worker `max_cycles`, and canonical safety
`max_cycles`; require attempts to advance by at most one per non-frozen cycle;
require ticks not to exceed attempts; and require projection tick count to
match canonical safety tick count. Treat the first hard stop, unhealthy/false
health, or terminal failure code as an absorbing state. From that point onward,
reject any attempt/tick increment, healthy `ok`, running or successful stopped
lifecycle, erased/changed terminal failure, cleared hard-stop evidence, or
healthy final projection. Require final cycle, attempts, ticks, lifecycle,
failure, hard-stop, and safety counters to preserve the last/terminal cycle
consistently. Use stable redacted finding text and reject malformed evidence;
do not read payloads, results, errors, tenant IDs, or other high-cardinality
fields.

Run the two new nodes first, then the entire release mutation table. Ensure
dedicated coverage exists for the authoritative closure matrix: terminal hard
stop, ordinary heartbeat exception, no in-process restart, safe later queue
persistence, composed `--fail-on-dead`, `SystemExit` and `KeyboardInterrupt`
propagation, fixed low-cardinality metrics, valid productive/idle evidence, and
all terminal/counter mutations. Reuse existing tests where they already prove
an item; add only missing assertions in `test_cli_runtime_tools.py`. Then run
the complete Phase 09.2 public/runtime/routing slice, Phase 7 heartbeat and
workspace-stream suites, G0, full locked pytest, full Ruff, diff hygiene, and
GSD consistency. Confirm the production diff from the plan base changes only
`src/mnemosyne/cli.py`, with tests only in
`tests/test_cli_runtime_tools.py`; no `workspace.py`, dependency, schema,
topology, manifest, entrypoint, queue, or public-contract change is allowed.

Perform independent code review and security review for T-09.2-02 and block on
all high findings. Commit implementation/tests intentionally and push `main`.
Wait for the exact implementation SHA's required GitHub CI jobs to succeed;
older or unrelated green runs are not evidence. Refresh the full CBM index and
verify ready/current graph state; sync gbrain `mnemosyne-code`, verify zero lag,
100% embedding/empty queue, and `gbrain doctor` healthy. Write/update the
summary, verification, security, review, ROADMAP, and STATE only after exact-SHA
evidence exists. The independent `09.2-VERIFICATION.md` must set
`status: passed` only after rerunning both former adversarial mutations and all
four closure conditions with zero findings/gaps. Update ROADMAP to mark both
plans complete only then. Secret-sweep and stage only declared closure
artifacts, commit without hook bypass, push `main`, wait for the closure SHA's
exact CI, and verify local HEAD equals `origin/main` with a clean worktree.
  </action>
  <verify>
    <automated>sh -c 'set -eu; uv run --locked python -m pytest -q tests/test_cli_runtime_tools.py::test_release_worker_heartbeat_rejects_terminal_state_resurrection tests/test_cli_runtime_tools.py::test_release_worker_heartbeat_rejects_unbounded_and_inconsistent_attempts; uv run --locked python -m pytest tests/test_cli_runtime_tools.py -k "worker_run or workspace_heartbeat or release_audit_rejects or fail_on_dead or system_exit or keyboard_interrupt" -q; uv run --locked python -m pytest tests/test_cli_runtime_tools.py tests/test_runtime_surfaces.py tests/test_production_manifest_renderer.py tests/test_prod_compose_policy.py tests/test_always_on_heartbeat.py tests/test_workspace_stream.py tests/test_g0_harness.py -q; uv run --locked python -m pytest; uv run --locked ruff check .; git diff --check; gsd-sdk validate consistency'</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Both formerly red exact nodes pass and `_release_worker_run_evidence_findings` returns at least one stable fail-closed finding for every terminal-resurrection and bounded-counter mutation.</criterion>
    <criterion>Canonical valid productive/idle/final evidence returns no findings, and the worker source/runtime report schema and queue outcomes are unchanged.</criterion>
    <criterion>The closure matrix explicitly covers hard stop, ordinary exception, no restart, safe later persistence, composed fail-on-dead, SystemExit/KeyboardInterrupt propagation, fixed metrics, and the complete release mutation table.</criterion>
    <criterion>Focused Phase 09.2, Phase 7/workspace-stream, G0, full pytest, full Ruff, diff hygiene, and GSD consistency commands exit 0.</criterion>
    <criterion>The production diff is confined to `src/mnemosyne/cli.py`; tests are confined to `tests/test_cli_runtime_tools.py`; no dependency, schema, topology, manifest, entrypoint, queue, workspace service, or public toggle changes.</criterion>
    <criterion>Code review and security review have no open high finding, independent verification is `status: passed`, and REQ-016/REQ-018/NFR-005 plus T-09.2-02 have explicit evidence.</criterion>
    <criterion>CBM is current/ready, gbrain is synchronized and healthy, both exact pushed SHAs have successful required CI, local HEAD equals `origin/main`, and the worktree is clean.</criterion>
  </acceptance_criteria>
  <done>The shared production validator rejects both HIGH gap classes and every equivalent mutation, canonical evidence and all Phase 7/09.2 runtime rails remain green, independent verification passes, durable indexes are current, exact-SHA CI succeeds for implementation and closure commits, and main is pushed, synchronized, and clean.</done>
</task>

</tasks>

<verification>
1. Each of the two new exact pytest nodes independently proves pre-fix rc 1 and post-fix rc 0.
2. Terminal evidence is absorbing across cycle and final projections; counter evidence is bounded, monotonic, and controller-consistent.
3. Canonical valid worker evidence and the entire Phase 09.2/Phase 7/G0 safety and runtime matrix remain green.
4. Full pytest, full Ruff, diff hygiene, GSD consistency, code review, security review, and independent verifier pass.
5. Source scope is one production file and one test file; no runtime/schema/topology/dependency drift occurs.
6. CBM, gbrain, exact-SHA GitHub CI, local/remote parity, and clean worktree prove durable closure.
</verification>

<success_criteria>
- Both HIGH findings in `09.2-VERIFICATION.md` are independently reproduced red and resolved green through the shared validator.
- No evidence after the first terminal projection can resume, advance, or claim healthy completion.
- `attempted_ticks`, `tick_count`, cycles, final evidence, and controller bounds agree under all valid and hostile sequences.
- REQ-016, REQ-018, NFR-005, and T-09.2-02 pass independent verification with no runtime, queue, privacy, authority, schema, topology, or dependency regression.
- Phase 09.2 closes only with exact-SHA CI success, current CBM/gbrain state, pushed `main`, local/remote parity, and a clean worktree.
</success_criteria>

<source_coverage_audit>

| Source | ID / area | Plan coverage | Status | Notes |
|---|---|---|---|---|
| GOAL | Bounded public worker heartbeat with truthful safety/unhealthy evidence and no authority | 09.2-01 + 09.2-02 | COVERED | Plan 01 mounts the runtime; Plan 02 closes the two attestation-integrity gaps without reopening the mount. |
| REQ | REQ-016 warm-loop consolidation/runtime ownership | 09.2-01 + 09.2-02 Task 02 | COVERED | Runtime remains unchanged; valid mounted evidence is re-verified. |
| REQ | REQ-018 shadow optimization and invariant rails | 09.2-02 Tasks 01-02 | COVERED | Terminal and boundedness claims fail closed while shadow/non-authority rails remain green. |
| REQ | NFR-005 observability and operator evidence | 09.2-02 Tasks 01-02 | COVERED | Misleading terminal and counter evidence is rejected and fixed metrics remain stable. |
| VERIFICATION | HIGH terminal restart contradiction | 09.2-02 Tasks 01-02 | COVERED | Independent exact-node RED reproduction, absorbing terminal-state validator, and mutation table. |
| VERIFICATION | HIGH unbounded/inconsistent attempt counts | 09.2-02 Tasks 01-02 | COVERED | Independent `999999` RED reproduction plus cycle/tick/controller/final consistency. |
| VERIFICATION | Closure condition 3 regression matrix | 09.2-02 Task 02 | COVERED | Hard stop, exception, no restart, persistence, fail-on-dead, base exceptions, metrics, and release mutations. |
| VERIFICATION | Closure condition 4 full durable gates | 09.2-02 Task 02 | COVERED | Focused/full tests, Ruff, GSD, reviews, exact-SHA CI, CBM, gbrain, push/parity/clean state. |
| CONTEXT | D-01-D-03 host/topology | 09.2-01; 09.2-02 no-change gates | COVERED | No new host, service, executable, dependency, or topology. |
| CONTEXT | D-04-D-07 bounded lifecycle | 09.2-02 Tasks 01-02 | COVERED | Attempt bounds and terminal no-restart evidence become fail-closed. |
| CONTEXT | D-08-D-10 privacy/authority | 09.2-01; 09.2-02 regression gates | COVERED | No runtime input or authority path changes. |
| CONTEXT | D-11-D-14 public evidence | 09.2-02 Tasks 01-02 | COVERED | Shared validator now enforces complete, bounded, internally consistent evidence. |
| CONTEXT | D-15-D-17 failure semantics | 09.2-02 Tasks 01-02 | COVERED | Terminal failure remains absorbing and final evidence cannot claim health. |
| CONTEXT | D-18-D-19 verification | 09.2-02 Tasks 01-02 | COVERED | Red-first tests and complete closure gates are explicit. |
| RESEARCH | Shared validator, controller bounds, one-attempt cadence, final agreement | 09.2-02 Tasks 01-02 | COVERED | Root-cause fix stays in the established validator and consumes canonical projected bounds. |
| RESEARCH | Deferred MCP/API mount, daemon, authority promotion, live rollout | None | DEFERRED | Explicitly outside Phase 09.2 and unchanged by gap closure. |

</source_coverage_audit>

<output>
Create `09.2-02-SUMMARY.md`; update `09.2-VERIFICATION.md`,
`09.2-SECURITY.md`, `09.2-REVIEW.md`, ROADMAP, and STATE with exact closure
evidence. Do not create another production module, validator, schema version,
service, dependency, or topology surface.
</output>
