---
phase: 10-public-harness-and-neutral-governance-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [BENCH-001, BENCH-002, BENCH-003, RAIL-001, RAIL-002, RAIL-003, RAIL-004]
files_modified:
  - eval/public/__init__.py
  - eval/public/registry.json
  - eval/public/runner.py
  - eval/public/bundle.py
  - eval/public/adapters/__init__.py
  - eval/public/adapters/smoke.py
  - eval/public/fixtures/smoke.json
  - eval/public/README.md
  - src/mnemosyne/cli.py
  - tests/test_public_eval.py
  - eval/reports/m1.1-public-harness.md
must_haves:
  truths:
    - "The public harness drives Mnemosyne only through the public CLI subprocess seam."
    - "Every bundle is pinned, complete, mutation-detecting, secret-free, and reproducible from locked instructions."
    - "Metric families remain separate and the smoke suite is permanently non-publishable."
  artifacts:
    - path: "eval/public/runner.py"
      provides: "Isolated registry-driven public runner."
    - path: "eval/public/bundle.py"
      provides: "Canonical bundle writer, verifier, and reproducer."
    - path: "tests/test_public_eval.py"
      provides: "Fail-closed public harness regression coverage."
  key_links:
    - from: "eval/public/runner.py"
      to: "eval/harness/cli_driver.py"
      via: "All system actions use MnemoCLI subprocess calls."
      pattern: "MnemoCLI"
---

# Phase 10 Plan 01: Public Harness and Reproducibility Bundle Scaffold

## Objective

Ship `mneme eval-public --suite smoke --out-dir ...` through public CLI-only
system calls and produce a mutation-detecting, one-command-reproducible PBPP
bundle that is explicitly non-publishable.

## Tasks

<task id="10-01-01" type="tdd-red">
  <name>Lock public CLI, registry, custody, contamination, and reproduction contracts red first</name>
  <files><file>tests/test_public_eval.py</file></files>
  <action>Add red tests for command/e2e traces, only-MnemoCLI interaction, exact pins/digests/licenses/split roles, private-suite isolation, family separation, traversal/link/secret rejection, atomic new output, inventory/fingerprint mutation detection, trace/metric count binding, dirty-tree non-publishability, and bundle-only reproduction.</action>
  <verify><automated>sh -c 'set -eu; log="$(mktemp)"; status=0; uv run --locked python -m pytest tests/test_public_eval.py -q &gt;"$log" 2&gt;&amp;1 || status=$?; test "$status" -eq 1; ! rg -n "ERROR collecting|SyntaxError" "$log"; rg -n "ModuleNotFoundError.*eval\.public|AssertionError" "$log"'</automated></verify>
  <done>Tests collect and fail only because the public harness is absent.</done>
</task>

<task id="10-01-02" type="tdd-green">
  <name>Implement the minimal isolated runner, smoke adapter, and CLI command</name>
  <files><file>eval/public/__init__.py</file><file>eval/public/registry.json</file><file>eval/public/runner.py</file><file>eval/public/adapters/__init__.py</file><file>eval/public/adapters/smoke.py</file><file>eval/public/fixtures/smoke.json</file><file>src/mnemosyne/cli.py</file></files>
  <action>Reuse MnemoCLI and existing metrics. Add exact registry validation and a redistributable development fixture. Lazily wire `eval-public`; add no dependency or new entry point. Use an allowlisted child environment and never import private suite data or engine internals. Require family-specific metric metadata: deterministic retrieval declares Wilson intervals; QA declares its reader/judge and bootstrap interval. Reject missing/wrong interval metadata and any cross-family aggregation.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_eval.py -q</automated></verify>
  <done>The smoke suite drives capture/search only through public subprocesses and emits per-question stored/retrieved/answer traces.</done>
</task>

<task id="10-01-03" type="custody">
  <name>Implement canonical bundle writing, verification, and reproduction</name>
  <files><file>eval/public/bundle.py</file><file>eval/public/README.md</file><file>eval/reports/m1.1-public-harness.md</file></files>
  <action>Write canonical JSON/JSONL atomically; bind build/config/benchmark/judge/metrics/traces; reject links, escapes, secrets, mutations, duplicate IDs, non-finite metrics, count drift, wrong interval-family metadata, and family blending. Generate `reproduce.sh` inside each bundle and support `--reproduce-bundle SOURCE --out-dir DEST` using only bundle custody plus the pinned installed CLI/environment contract. Emit fixed locked reproduction instructions and explicit false publication flags.</action>
  <verify><automated>sh -c 'set -eu; root="$(mktemp -d)"; out="$root/bundle"; reproduced="$root/reproduced"; uv run --locked mneme eval-public --suite smoke --out-dir "$out"; uv run --locked mneme eval-public --verify-bundle "$out"; uv run --locked mneme eval-public --reproduce-bundle "$out" --out-dir "$reproduced"; uv run --locked mneme eval-public --verify-bundle "$reproduced"; cmp "$out/traces.jsonl" "$reproduced/traces.jsonl"; cmp "$out/metrics.json" "$reproduced/metrics.json"; uv run --locked python -m pytest tests/test_public_eval.py -q'</automated></verify>
  <done>A fresh run and verification reproduce identical canonical trace/metric digests; M3 remains false.</done>
</task>

## Success Criteria

- M1.1 and the reusable M2 bundle substrate are source-complete.
- No public number, headline eligibility, or external reproduction is claimed.
- Focused/full/Ruff/GSD/CBM/gbrain/exact-SHA CI pass.
