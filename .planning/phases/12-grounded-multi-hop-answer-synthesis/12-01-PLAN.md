---
phase: 12-grounded-multi-hop-answer-synthesis
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [CAP-003, BENCH-005, RAIL-002, RAIL-003, RAIL-004]
files_modified:
  - eval/public/registry.json
  - eval/public/runner.py
  - eval/public/bundle.py
  - eval/public/scoring.py
  - eval/public/README.md
  - tests/test_public_eval.py
  - tests/test_public_eval_scoring.py
  - tests/test_public_longmemeval.py
must_haves:
  truths:
    - "The reader protocol and all frozen retrieval baselines are preregistered before held-out QA execution."
    - "QA bundles disclose reader/config/prompt custody and remain separate from retrieval bundles."
    - "Benchmark-owned labels recompute EM/F1; reader outputs never self-score."
---

# Phase 12 Plan 01: Freeze Protocol and Complete Public QA Custody

## Tasks

<task id="12-01-01" type="tdd-red">
  <name>Lock QA bundle and preregistration failures red first</name>
  <files><file>tests/test_public_eval.py</file><file>tests/test_public_eval_scoring.py</file><file>tests/test_public_longmemeval.py</file></files>
  <action>Add expected-failure cases for missing/ambiguous reader model revision, prompt digest, decoding config, evidence budget, abstention rule, split declaration, answer labels, claim citations, QA profile registration, and mixed retrieval/QA columns.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_eval.py tests/test_public_eval_scoring.py tests/test_public_longmemeval.py -q</automated></verify>
  <done>Only absent QA custody behavior fails.</done>
</task>

<task id="12-01-02" type="tdd-green">
  <name>Register disclosed QA scoring and bundle contracts</name>
  <files><file>eval/public/registry.json</file><file>eval/public/runner.py</file><file>eval/public/bundle.py</file><file>eval/public/scoring.py</file></files>
  <action>Integrate the existing `qa-em-f1-v1` scorer into runner/verifier profile registries; accept explicit reader/judge/config metadata; verify resolved model content digest, prompt-template/serializer digest, all decoding options, canonical empty-answer abstention, and benchmark-owned answer-label recomputation; preserve retrieval bundle bytes/contracts and null-reader rules.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_eval.py tests/test_public_eval_scoring.py tests/test_public_eval_cli.py -q</automated></verify>
  <done>Valid QA bundles verify and all retrieval custody regressions stay green.</done>
</task>

<task id="12-01-03" type="protocol">
  <name>Freeze protocol and no-recall-regression contract</name>
  <files><file>eval/public/registry.json</file><file>eval/public/README.md</file></files>
  <action>Record the static model/prompt/decoding/evidence/hop/abstention contract, split roles, Phase 11 report and manifest digests, exact point baselines, no-overwrite external post-commit candidate-manifest schema, preregistered retry count, and single-pass held-out policy. Add a verifier gate rejecting baseline weakening or held-out-as-development declarations. Do not place a self-referential git SHA in committed registry data.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_requirement_truth.py tests/test_public_eval.py -q</automated></verify>
  <done>The candidate protocol and external post-commit freeze procedure are immutable and machine-checked before reader implementation.</done>
</task>
