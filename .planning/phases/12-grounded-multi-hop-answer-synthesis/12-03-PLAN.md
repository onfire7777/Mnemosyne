---
phase: 12-grounded-multi-hop-answer-synthesis
plan: 03
type: tdd
wave: 3
depends_on: [12-02]
autonomous: true
requirements: [CAP-001, CAP-002, RAIL-001, RAIL-002, RAIL-003]
files_modified:
  - infra/providers/role-llm.py
  - infra/providers/role-ladder.py
  - infra/profiles/self-hosted.env
  - infra/templates/production-operator-env.inventory.md
  - src/mnemosyne/providers/grounded_reader.py
  - src/mnemosyne/cli.py
  - eval/harness/cli_driver.py
  - tests/test_phase3_provider_profiles.py
  - tests/test_cli_runtime_tools.py
  - tests/test_public_eval_cli.py
must_haves:
  truths:
    - "The grounded reader returns structured claims and citations through the existing Ollama command boundary."
    - "Only external validation over authorized assembled evidence can approve a non-abstained answer."
    - "The public answer CLI is bounded, read-only, fail-closed, and benchmark-usable."
---

# Phase 12 Plan 03: Grounded Reader and Public Answer Surface

## Tasks

<task id="12-03-01" type="tdd-red">
  <name>Lock reader/provider/CLI failures red first</name>
  <files><file>tests/test_phase3_provider_profiles.py</file><file>tests/test_cli_runtime_tools.py</file><file>tests/test_public_eval_cli.py</file></files>
  <action>Add malformed JSON, timeout, unknown CID, empty/nonempty abstention mismatch, uncited atomic claim, duplicate citation, injection, retrieval-abstained, unauthorized context, mutation, unresolved model content digest, incomplete prompt/decoding digest, and nondisclosed-model cases.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_phase3_provider_profiles.py tests/test_cli_runtime_tools.py tests/test_public_eval_cli.py -q</automated></verify>
  <done>Only absent grounded-reader and public-answer behavior fails.</done>
</task>

<task id="12-03-02" type="tdd-green">
  <name>Add decomposer and grounded reader to the existing command transport</name>
  <files><file>infra/providers/role-llm.py</file><file>infra/providers/role-ladder.py</file><file>infra/profiles/self-hosted.env</file><file>infra/templates/production-operator-env.inventory.md</file><file>src/mnemosyne/providers/grounded_reader.py</file></files>
  <action>Extend the installed shell-free role command ABI with `query_decomposer` and `grounded_reader`. The decomposer returns only bounded subquery strings. The reader returns strict `{claims:[{text,evidence_cids}], unresolved}` JSON from quoted evidence data; the application derives the answer from validated ordered claims. Add a minimal command adapter with argv-array execution, timeout/output limits, schema validation, profile/env selection, resolved Ollama model content-digest preflight, prompt-template/serializer digest, and complete decoding-options digest. Fail closed on provider/schema/preflight failure; never hardwire a repo-relative script path or let the provider validate its own citations.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_phase3_provider_profiles.py -q</automated></verify>
  <done>Provider contract and adversarial prompt-boundary tests pass.</done>
</task>

<task id="12-03-03" type="integration">
  <name>Expose read-only answer and batch-answer CLI contracts</name>
  <files><file>src/mnemosyne/cli.py</file><file>eval/harness/cli_driver.py</file></files>
  <action>Wire orchestration and configured command provider through bounded public CLI commands; require evaluation read-only mode for batch execution; prevalidate all rows; preserve order; emit only permitted decomposition/hop/channel/retrieved-CID/claim/abstention fields plus disclosed reader config. Hash the store and every runtime sidecar across success, timeout, partial response, and aborted batch tests; no mutation is allowed and denial reason codes are never public.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_cli_runtime_tools.py tests/test_public_eval_cli.py tests/test_runtime_surfaces.py -q</automated></verify>
  <done>Public CLI answer flows are immutable, fail-closed, and trace-complete.</done>
</task>
