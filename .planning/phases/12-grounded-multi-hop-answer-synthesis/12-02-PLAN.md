---
phase: 12-grounded-multi-hop-answer-synthesis
plan: 02
type: tdd
wave: 2
depends_on: [12-01]
autonomous: true
requirements: [CAP-001, CAP-002, RAIL-001, RAIL-002]
files_modified:
  - src/mnemosyne/answering.py
  - src/mnemosyne/models.py
  - src/mnemosyne/cli.py
  - eval/harness/cli_driver.py
  - tests/test_grounded_answering.py
  - tests/test_public_eval_cli.py
  - tests/test_shared_engine_contract.py
  - tests/completion/rails/test_untrusted_to_system_prompt.py
must_haves:
  truths:
    - "Every hop preserves the complete original read context and can only narrow access."
    - "Episode-aware assembly is a bounded projection over existing evidence metadata, not a new storage system."
    - "Unsupported, filtered, erased, quarantined, expired, or fabricated evidence produces existence-silent abstention."
---

# Phase 12 Plan 02: Bounded Multi-Hop and Episode-Aware Orchestration

## Tasks

<task id="12-02-01" type="tdd-red">
  <name>Lock orchestration, authorization, and episode rails red first</name>
  <files><file>tests/test_grounded_answering.py</file><file>tests/test_shared_engine_contract.py</file><file>tests/completion/rails/test_untrusted_to_system_prompt.py</file></files>
  <action>Add failures comparing every immutable `AnswerReadContext` field on every hop; cover hop/query/evidence/token overflow, cycles, nondeterministic ordering, decomposer filter/command injection, cross-tenant/session episode joins, oracle leakage, relation-ID citation, protected-existence leakage, concurrent validity/access drift, and answer persistence.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_grounded_answering.py tests/completion/rails/test_untrusted_to_system_prompt.py -q</automated></verify>
  <done>Only missing orchestration behavior fails.</done>
</task>

<task id="12-02-02" type="tdd-green">
  <name>Implement minimal deterministic answer orchestration</name>
  <files><file>src/mnemosyne/answering.py</file></files>
  <action>Add typed bounded request/trace/evidence/answer records and an injectable decomposer protocol. Accept only schema-validated query strings from the decomposer, with no policy/filter/command fields; dedupe/cap them and reapply the unchanged immutable read context. Reuse shared deep retrieval and engine PPR; dedupe by active evidence CID; group adjacent same-session/source turns from source identity plus non-oracle turn-index metadata.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_grounded_answering.py tests/test_shared_engine_contract.py -q</automated></verify>
  <done>Two-hop, graph-positive, episode-contiguous, abstention, and parity fixtures pass without ranking or schema forks.</done>
</task>

<task id="12-02-03" type="security">
  <name>Validate provenance closure and existence-silent failure</name>
  <files><file>src/mnemosyne/answering.py</file><file>tests/test_grounded_answering.py</file></files>
  <action>Build an immutable `authorized_cids -> sanitized evidence row` map only from hop results authorized under the original read context. Hydrate transitive relation/projection sources through that same context before adding them. The provider returns references only; validate strict map membership, then repeat the authorized retrieval and compare an active-evidence fingerprint before emission to fail closed on TOCTOU drift. Never treat raw `get_evidence` as authorization. Collapse unknown/filtered/drift failures to the same external abstention; internal reason codes stay out of the public CLI trace.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_grounded_answering.py tests/test_access_policy_enforcement.py tests/test_shared_engine_contract.py -q</automated></verify>
  <done>Unsupported-citation and fabricated-CID rates are structurally zero in adversarial tests.</done>
</task>

<task id="12-02-04" type="episode-metadata">
  <name>Preserve safe episode adjacency through public capture</name>
  <files><file>src/mnemosyne/cli.py</file><file>eval/harness/cli_driver.py</file><file>tests/test_public_eval_cli.py</file><file>tests/test_grounded_answering.py</file></files>
  <action>Extend validated capture/batch rows only with non-oracle session/source and turn-index metadata needed for adjacency; reject unknown metadata shapes; prove oracle answer-session/turn fields never enter stored evidence, retrieval queries, decomposer prompts, reader prompts, or traces.</action>
  <verify><automated>uv run --locked python -m pytest tests/test_public_eval_cli.py tests/test_grounded_answering.py -q</automated></verify>
  <done>Episode-aware assembly has real safe metadata without scoring-label leakage.</done>
</task>
