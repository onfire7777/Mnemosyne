---
phase: 09.2
slug: close-audit-gap-mount-the-bounded-heartbeat-in-the-public-ru
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-10
---

# Phase 09.2 — Validation Strategy

> Per-task feedback contract for mounting the bounded heartbeat in the supported public worker runtime.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest via locked `uv` environment; Ruff for static checks |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `uv run --locked python -m pytest tests/test_cli_runtime_tools.py -k 'worker_run or release_audit_rejects' -q` |
| **Safety run command** | `uv run --locked python -m pytest tests/test_always_on_heartbeat.py tests/test_workspace_stream.py tests/test_g0_harness.py -q` |
| **Full suite command** | `uv run --locked python -m pytest && uv run --locked ruff check . && git diff --check && gsd-sdk validate consistency` |
| **Estimated runtime** | Task feedback under 240 seconds; full gate depends on optional backends |

---

## Sampling Rate

- **After every task commit:** Run the smallest new nodes plus the quick worker/release suite.
- **After the implementation task:** Run the complete worker, Phase 7 heartbeat/stream, G0 guardrail, and production-routing slices.
- **Before `$gsd-verify-work`:** Full suite, Ruff, schema/GSD hygiene, clean review, security audit, CBM/gbrain freshness, and exact-SHA CI must pass.
- **Max feedback latency:** 240 seconds for task-level checks.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 09.2-01-01 | 01 | 1 | REQ-016, REQ-018, NFR-005 | T-09.2-01 | Public contract rejects missing/weak heartbeat proof; hostile payloads never enter public heartbeat output; red failures are exact assertions, not collection errors. | red-first contract | `uv run --locked python -m pytest tests/test_cli_runtime_tools.py -k 'worker_run or release_audit_rejects' -q` | ✅ | ⬜ pending |
| 09.2-01-02 | 01 | 1 | REQ-016, REQ-018, NFR-005 | T-09.2-01 | One service starts per worker invocation, ticks once per productive/idle cycle, never restarts after hard stop, cannot steer queue outcomes, and fails visibly after safe persistence/reporting. | lifecycle integration | `uv run --locked python -m pytest tests/test_cli_runtime_tools.py tests/test_always_on_heartbeat.py tests/test_workspace_stream.py -q` | ✅ | ⬜ pending |
| 09.2-01-03 | 01 | 1 | REQ-016, REQ-018, NFR-005 | T-09.2-01 | Release evidence and production routing prove a bounded, shadow-only, non-critical, non-mutating heartbeat on the existing consolidator path. | release + regression | `uv run --locked python -m pytest tests/test_cli_runtime_tools.py tests/test_runtime_surfaces.py tests/test_production_manifest_renderer.py tests/test_prod_compose_policy.py tests/test_g0_harness.py -q && uv run --locked python -m pytest && uv run --locked ruff check . && git diff --check && gsd-sdk validate consistency` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing CLI subprocess, worker/release evidence, Phase 7 service, G0, production manifest, and compose-policy infrastructure covers every phase requirement. No dependency, schema, daemon, service, or test framework is needed.

---

## Manual-Only Verifications

All source-owned phase behaviors are automated. Live production rollout/capture is explicitly outside this phase; the existing production command path must still be source-runnable and test-proven.

---

## Validation Sign-Off

- [x] Every task has a closest-layer automated check.
- [x] No three consecutive actions can occur without automated feedback.
- [x] Existing infrastructure covers all test references.
- [x] No watch-mode flags.
- [x] Feedback latency target is under 240 seconds.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** approved 2026-07-10 under the user's no-compromise autonomous authorization.
