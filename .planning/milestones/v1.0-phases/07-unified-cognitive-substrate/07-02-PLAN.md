---
phase: 07-unified-cognitive-substrate
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/mnemosyne/standing.py
  - src/mnemosyne/retrieval.py
  - src/mnemosyne/engine.py
  - src/mnemosyne/postgres_engine.py
  - src/mnemosyne/calibration.py
  - eval/g0/standing_calibration.py
  - eval/g0/runner.py
  - eval/g0/preregistrations/g5-standing-continuous.json
  - tests/test_standing_continuous.py
autonomous: true
requirements: [FR-6, FR-7, FR-8, FR-11, OQ6, "spec:§3", "spec:§9-P2", "spec:§13-H1", "spec:§13-H2", "spec:§13-H3", "spec:§13-H7"]
must_haves:
  truths:
    - "Standing is a 2-tuple (groundedness ⟂ salience). Answer-authority (assert vs flag vs abstain) depends on GROUNDEDNESS ONLY — never on salience (H2)."
    - "Only provenance-INDEPENDENT external evidence raises groundedness; self-generated content can never corroborate other self-generated content (H1)."
    - "A permanent evidence-dominance gap holds: self-generated groundedness can never reach the external-evidence band (H3)."
    - "groundedness is conformally calibrated (predicts corroboration with coverage guarantees); fail-closed default abstains (H7). No existing guardrail regresses."
  artifacts:
    - "eval/g0/standing_calibration.py — Standing reliability diagram + coverage metric."
    - "eval/g0/preregistrations/g5-standing-continuous.json — preregistered gate."
  key_links:
    - from: "answer authority / abstention"
      to: "Standing.groundedness"
      via: "assertion gated on groundedness only; salience never raises authority"
      pattern: "groundedness|salience|independent_corrob"
---

<objective>
Turn the P1 mirror into a genuine continuous trust gradient, and split it into two orthogonal axes so popularity can never masquerade as truth. Standing becomes `(groundedness, salience)`: groundedness from provenance + INDEPENDENT external corroboration + contradiction pressure (smoothly, with decay); salience from ACT-R activation / recency / usage. Retrieval weight and consolidation eligibility become smooth functions of the tuple, but **the right to be asserted depends on groundedness ONLY** (H2). Add the independent-corroboration rule (H1), the permanent evidence-dominance gap (H3), and conformal calibration of groundedness (H7).

CRITICAL: salience may change WHAT surfaces in retrieval; it may NEVER change WHAT can be asserted as true. Self-generated content may never raise another self-generated unit's groundedness. No existing guardrail may regress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-unified-cognitive-substrate/07-CONTEXT.md
@.planning/phases/07-unified-cognitive-substrate/07-01-SUMMARY.md
@docs/superpowers/specs/2026-06-27-unified-cognitive-substrate-design.md
@eval/g0/runner.py
@eval/g0/gate.py
</context>

<tasks>

<task type="execute">
  <name>Split Standing into (groundedness ⟂ salience) and make it continuous</name>
  <read_first>
    - src/mnemosyne/standing.py (P1 scalar)
    - src/mnemosyne/lifecycle.py (ACT-R activation, decay)
  </read_first>
  <what>Extend `standing.py` to return a 2-tuple: `groundedness` (provenance grade + independent corroboration count + negative contradiction pressure, smoothly combined, with time decay) and `salience` (ACT-R base-level / recency / usage). Bump `standing_fn_version`. Wire retrieval weight = monotone function of both; consolidation eligibility = function of groundedness primarily; **assertion/abstention authority = groundedness ONLY** (salience excluded from the authority path by construction).</what>
  <how-to-verify>Tests: raising salience alone never changes an assertion/abstention decision (H2); raising independent corroboration raises groundedness and can change authority; decay lowers stale uncorroborated groundedness. `explain.standing` shows both axes.</how-to-verify>
  <acceptance_criteria>
    - 2-tuple implemented; authority provably independent of salience; continuity + decay covered by tests.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Independent-corroboration rule (H1) + evidence-dominance gap (H3)</name>
  <read_first>
    - src/mnemosyne/engine.py + postgres_engine.py (provenance CIDs, source_evidence_cids, trust tiers)
    - src/mnemosyne/consciousness.py (reality classes / self_generated)
  </read_first>
  <what>Corroboration counts toward groundedness ONLY from evidence whose provenance chain is independent of the unit and does NOT share a self-generated ancestor; weight scales with the corroborating source's trust tier and counts only after poison-block/sanitization. Enforce a hard ceiling so self-generated groundedness can never enter the external-evidence band (H3), at birth or after any future autonomy uplift.</what>
  <how-to-verify>Tests: two self-generated units cannot raise each other's groundedness; a shared-self-ancestor "corroboration" is rejected; low-trust/poisoned source contributes ~0; max self-generated groundedness < min external-evidence groundedness across the corpus.</how-to-verify>
  <acceptance_criteria>
    - Echo-chamber corroboration is structurally impossible; the evidence-dominance gap holds for all units.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Conformal Standing-calibration + preregistered gate (H7)</name>
  <read_first>
    - src/mnemosyne/calibration.py (conformal machinery, ECE)
    - eval/g0/runner.py, eval/g0/gate.py
  </read_first>
  <what>Calibrate groundedness with the existing conformal machinery so a groundedness value corresponds to a measured corroboration likelihood with coverage guarantees. Add `eval/g0/standing_calibration.py` reporting a Standing reliability diagram, `standing_calibration_error`, and coverage. Preregister `eval/g0/preregistrations/g5-standing-continuous.json`: target `standing_calibration_error` (≤ threshold), guardrails-not-down for the full set.</what>
  <how-to-verify>`eval.g0.gate` passes the g5-standing-continuous prereg; `standing_calibration_error` within target; ECE/abstention/confabulation/poison-block/recall/nDCG/P95/rails/indicators all non-decreasing; compose-Postgres suite green.</how-to-verify>
  <acceptance_criteria>
    - Standing is calibrated and gate-proven with no guardrail regression.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Standing is `(groundedness ⟂ salience)`; authority depends on groundedness only (H2) — proven by the salience-invariance test.
- Independent corroboration only (H1) and evidence-dominance gap (H3) enforced and tested.
- groundedness conformally calibrated (H7); g5-standing-continuous gate passes; no guardrail regression.
- src additive/default-safe; never `git add -A`; tree-not-mid-edit verified (D-13/D-14).
</verification>

<success_criteria>
Standing is a genuine, calibrated, two-axis trust gradient. Popularity cannot become truth, the mind cannot corroborate itself, and self-generated content can never equal external evidence — all proven by gates, with every reliability guardrail held.
</success_criteria>

<output>
Create `.planning/phases/07-unified-cognitive-substrate/07-02-SUMMARY.md` when done (record the calibration result, the salience-invariance proof, and the g5-continuous gate decision).
</output>
