---
phase: 06-exact-blueprint-runtime-parity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mnemosyne/ingestion.py
  - src/mnemosyne/storage.py
  - src/mnemosyne/retrieval.py
  - tests/test_runtime_parity_extensions.py
autonomous: true
requirements: [FR-20]
must_haves:
  truths:
    - "The extractor / media-embedding / object-store / job / retrieval contracts cover image, audio, AND video as authorable code with local validation."
    - "Media-derived text and media vectors index and retrieve for all three modalities through the existing single associative substrate."
    - "Local validation (no cloud) proves the breadth; production extractor/embedder evidence remains operator-run under row 6."
  artifacts:
    - path: "src/mnemosyne/ingestion.py"
      provides: "Image+audio+video extraction/embedding/job contract breadth"
      contains: "video"
    - path: "src/mnemosyne/retrieval.py"
      provides: "Multimodal media-vector + derived-text retrieval breadth"
    - path: "tests/test_runtime_parity_extensions.py"
      provides: "Local validation for image/audio/video breadth"
  key_links:
    - from: "src/mnemosyne/ingestion.py"
      to: "src/mnemosyne/storage.py object store"
      via: "externalized multimodal payloads with media vectors"
      pattern: "media|image|audio|video"
    - from: "src/mnemosyne/ingestion.py"
      to: "src/mnemosyne/retrieval.py"
      via: "media-derived-text + media vector indexed for retrieval"
      pattern: "media|derived_text"
---

<objective>
CURRENT STATUS refreshed 2026-06-29 through `bda5ccc`: complete locally.
`06-05-SUMMARY.md`, `.planning/STATE.md`, and
`tests/test_runtime_parity_extensions.py` show the image/audio/video ingestion,
media embedding, object-store, media-extract job, derived-text lineage, and
retrieval contracts are covered with local validation. Do not reopen this as
new source work; only Tier B operator evidence remains for deployed
extractor/embedder/object-store proof.

Historical objective: Implement FR-20 multimodal breadth (D-11): extend the extractor / media-embedding / object-store / job / retrieval contracts so image, audio, AND video are covered as authorable code with LOCAL validation (no cloud). The existing ingestion path already carries media wiring (image/audio/video, media-derived text, media vectors); this plan completes the breadth and adds local validation. Production extractor/embedder/object-store evidence stays operator-run under parity row 6 (plan 06-09).

Purpose: FR-20 is one of the optional items the operator opted into this phase (D-02/D-11). Completing the breadth + local validation makes the multimodal capability code-complete so only operator evidence remains.
Output: image/audio/video breadth across the multimodal contracts + local tests.

HISTORICAL COORDINATION HAZARD (D-14): at planning time an autonomous session owned `src/`. VERIFY the current multimodal wiring in `ingestion.py`/`storage.py`/`retrieval.py` first, COMPLETE only the missing modality breadth additively (do not rewrite existing media-extract/embedding paths), and NEVER `git add -A` — stage only the four named files. Verify the tree is not mid-edit before committing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-exact-blueprint-runtime-parity/06-CONTEXT.md
@docs/ROADMAP-TO-100.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete extractor + media-embedding + object-store breadth for image/audio/video</name>
  <files>src/mnemosyne/ingestion.py, src/mnemosyne/storage.py, tests/test_runtime_parity_extensions.py</files>
  <read_first>
    - src/mnemosyne/ingestion.py (existing media-extract job, command extractor boundary, media-derived-text relation, command media-embedding attach path — identify which of image/audio/video are fully covered vs partial)
    - src/mnemosyne/storage.py (clear/encrypted externalized object storage — confirm all three modalities externalize identically)
    - tests/test_runtime_parity_extensions.py (the runtime-parity test surface to extend with local image/audio/video cases)
    - .planning/STRICT-BLUEPRINT-PARITY-AUDIT.md (Multimodal retrieval row — current coverage: OCR/transcript/caption/alt/description derived text, command extractor, command media embedding, dense retrieval before derived text)
  </read_first>
  <behavior>
    - Test (image): an externalized image evidence row gets a media vector + derived text and is retrievable both by media vector (before derived text) and by derived text.
    - Test (audio): same contract for an audio asset (transcript-derived text + media vector).
    - Test (video): same contract for a video asset, completing the third modality.
    - Test (object store): all three modalities externalize raw bytes to the object store without changing content-addressed identity.
  </behavior>
  <action>
    Complete the extractor / media-embedding / object-store contracts in `ingestion.py` and `storage.py` so image, audio, and video are each fully covered: externalized payload, media vector attach, derived-text extraction, and the `media-derived-text` lineage relation. Where a modality is already wired, VERIFY parity and only fill gaps; do not rewrite the working paths. Add local (no-cloud) tests in `tests/test_runtime_parity_extensions.py` for all three modalities.
  </action>
  <verify>
    <automated>grep -niE "video" src/mnemosyne/ingestion.py | head -1 && .venv/bin/python -m pytest -q tests/test_runtime_parity_extensions.py -k "media or image or audio or video or multimodal" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/mnemosyne/ingestion.py` covers image, audio, AND video extraction + media-embedding + job + object-store externalization (grep `video` matches a real handling path).
    - Local tests in `tests/test_runtime_parity_extensions.py` pass for all three modalities with no cloud dependency.
    - Existing media-extract/embedding paths are not rewritten — only breadth gaps filled.
  </acceptance_criteria>
  <done>Image/audio/video are each code-complete across extractor, media-embedding, object-store, and job contracts, validated locally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete multimodal retrieval breadth + local retrieval validation</name>
  <files>src/mnemosyne/retrieval.py, tests/test_runtime_parity_extensions.py</files>
  <read_first>
    - src/mnemosyne/retrieval.py (media-vector dense retrieval + derived-text retrieval paths — confirm all three modalities rank through the same fused channels with provenance and sanitized retrieved_text)
    - src/mnemosyne/ingestion.py (the breadth completed in Task 1 — retrieval must consume image/audio/video media vectors + derived text uniformly)
    - tests/test_runtime_parity_extensions.py (extend with retrieval-side image/audio/video cases)
  </read_first>
  <behavior>
    - Test (retrieval breadth): a query retrieves image, audio, and video evidence through the fused channels with provenance, before and after derived-text extraction.
    - Test (substrate parity): all three modalities go through the same associative substrate (no modality-special retrieval branch that breaks fusion/budget/trust filtering).
  </behavior>
  <action>
    Complete `retrieval.py` so image/audio/video media vectors and derived text retrieve uniformly through the existing fused lexical/dense/graph channels with provenance, MMR, budget, and trust filtering — no modality-special fork that bypasses the rails. VERIFY existing media retrieval first; fill only breadth gaps. Add the local retrieval-breadth tests to `tests/test_runtime_parity_extensions.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_runtime_parity_extensions.py -k "media or image or audio or video or multimodal or retriev" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/mnemosyne/retrieval.py` retrieves all three modalities through the fused channels with provenance and trust filtering.
    - Local retrieval-breadth tests pass for image, audio, and video.
    - No modality-special retrieval branch bypasses fusion/budget/trust rails.
  </acceptance_criteria>
  <done>Multimodal retrieval breadth is code-complete for image/audio/video through the single associative substrate, locally validated.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest -q tests/test_runtime_parity_extensions.py` passes locally for image/audio/video.
- Compose-Postgres breadth coverage runs under plan 06-07 Lane G sweep.
- Breadth is additive; existing media paths not rewritten (D-14).
- Only the four named files staged; no `git add -A`.
</verification>

<success_criteria>
Image, audio, and video are each code-complete across the multimodal contracts with local validation, leaving only operator production extractor/embedder evidence (row 6) for full FR-20 parity.
</success_criteria>

<output>
Create `.planning/phases/06-exact-blueprint-runtime-parity/06-05-SUMMARY.md` when done.
</output>
