---
phase: 09-performance-and-refactoring-continuation
plan: 03
wave: 3
status: complete-local-gated
created: 2026-07-08
completed: 2026-07-08
target_metric: postgres_hot_query_plan_reuse
target_value: psycopg_prepare_true_for_hot_retrieval_selects
gate: focused-postgres-perf-lane-plus-ci
---

# 09-03 Plan: Postgres Hot Query Preparation

## Goal

Land the smallest parity-safe Postgres prepared-statement slice: immediate
Psycopg server-side preparation for repeated FTS and pgvector retrieval SELECTs
on existing pooled connections.

## Scope

- Add an operator kill switch, `MNEMOSYNE_PG_PREPARE_HOT_QUERIES=0`.
- Keep tenant and HNSW session GUC statements as explicit
  transaction-local `set_config(..., true)` calls.
- Prepare only hot retrieval SELECTs in `lexical_search` and `vector_search`.
- Update the drift ledger and performance blueprint status.
- Add unit perf-lane tests for default preparation and rollback.

## Acceptance

- Postgres perf-lane tests pass.
- Ruff passes for touched source/test files.
- Existing local and live-DSN behavior remains byte-compatible; DSN-backed smoke
  runs when `MNEMOSYNE_POSTGRES_DSN` is available.
