---
phase: 09.1
status: passed
verified: 2026-07-10T19:27:50Z
verifier: independent-gsd-phase-verifier
score: 7/7
implementation_sha: 3731b1901f3deaa3d0b257fb7b20edd08b110944
verified_head_sha: 0a7c12dd25543bcb254fb8682411f65d6fefadbb
ci_run_id: 29117413619
requirements: [REQ-003, REQ-004, REQ-007, NFR-004]
threat_model: T-09.1-01
---

# Phase 09.1 Verification

## Result

**PASS — 7/7 must-haves verified.** The phase goal is true in the current
codebase at `0a7c12dd25543bcb254fb8682411f65d6fefadbb`. Search, deep search,
and explain preserve one canonical fail-closed caller context across the direct
facade, CLI, generated MCP schema, JSON-RPC, and official SDK. No required human
verification or unresolved goal gap remains.

## Must-Have Score

| # | Truth | Evidence | Result |
|---|---|---|---|
| 1 | All three reads expose the same nine optional context fields and default omitted role to `reader`. | `MemoryTools.search`, `deep_search`, and `explain` signatures plus exact generated-schema assertions. | PASS |
| 2 | Legacy positional meaning is preserved. | Direct regression exercises positional search `max_sensitivity=0` before `role="agent"` and deep-search fourth-position role; new fields are keyword inputs. | PASS |
| 3 | One canonical authorization path is used. | Each facade calls `MemoryTools._read_context()` and passes the result as engine `filt`; CLI dispatch uses `_read_context_kwargs`; no post-filter exists. | PASS |
| 4 | Fail-closed policy semantics and search trust precedence hold. | Three-operation policy matrix covers principal, role, capability, purpose, sensitivity, residency/region, lawful basis, and two-sided break-glass; exact filter test proves max trust wins over min trust. | PASS |
| 5 | Generated MCP, CLI tools, JSON-RPC, and official SDK have contract parity. | Signature-derived schema and CLI-tools parity tests pass; JSON-RPC and in-process official SDK behavioral tests pass. | PASS |
| 6 | Denials remain existence-silent. | Direct, CLI, JSON-RPC, and SDK assertions reject protected content, CID, provenance/support IDs, hidden counts, denial reasons, and policy metadata. | PASS |
| 7 | No prohibited architectural drift occurred. | Production diff is limited to `src/mnemosyne/mcp_tools.py` and `src/mnemosyne/cli.py`; engines, access policy, MCP server, response models, dependencies, and lockfile are unchanged. | PASS |

## Artifact and Key-Link Verification

- `src/mnemosyne/mcp_tools.py` contains the canonical `_read_context` calls for
  all three reads and retains `max_sensitivity=0`, omits absent optionals, and
  includes `break_glass` only when true.
- `src/mnemosyne/cli.py` wires search, deep-search, and explain through the
  existing `_add_read_context_args` and `_read_context_kwargs` helpers.
- `tests/test_access_policy_enforcement.py` proves exact filter shape,
  positional compatibility, policy behavior, and non-leakage.
- `tests/test_cli_runtime_tools.py` proves all shared flags, reader-default
  denial, and serialized CLI non-leakage.
- `tests/test_parity_mcp.py` proves exact types, defaults, optionality, and
  generated schema parity for all nine fields.
- `tests/test_runtime_surfaces.py` proves JSON-RPC and official SDK behavior,
  session-required enforcement, tenant/user mismatch rejection, and
  server-bound role authority.
- The existing engine `filt` path continues into the shared `may_read_item`
  enforcement point. No alternate authorization or post-retrieval filter was
  introduced.

## Requirement Evidence

| Requirement | Verified evidence | Result |
|---|---|---|
| REQ-003 | Tenant, principal, role, source trust, and policy constraints survive every public read surface and signed-session binding rejects conflicting identity claims. | PASS |
| REQ-004 | Capture/search contract stability is preserved: all three reads expose the same additive caller context through signature-derived MCP and matching CLI tools schemas. | PASS |
| REQ-007 | Explain uses the same authorized deep-retrieval path and cannot expose inaccessible fact, evidence, provenance, or support identifiers. | PASS |
| NFR-004 | Sensitivity, principal, purpose, capability, residency/region, lawful basis, and two-sided operator break-glass are enforced fail closed. | PASS |

## Signed-Session and Non-Leakage Findings

`prepare_tool_arguments()` verifies the signed session before dispatch.
`_bind_session_identity()` rejects conflicting tenant/user claims and replaces
caller-supplied role with the signed role whenever the method exposes `role`.
The JSON-RPC and official SDK adapters both invoke this same preparation path.
Focused transport tests confirmed unsigned/mismatched calls fail and a reader
cannot self-assert agent/operator authority to disclose the protected record.

Denied responses were inspected as complete serialized objects. They contained
no protected payload, CID, hit/provenance/support identifier, hidden-result
count, existence signal, denial reason, or policy-sensitive metadata.

## Independent Automated Evidence

- Focused direct/CLI/schema/JSON-RPC/SDK/session suite: `9 passed`.
- Generated schema plus CLI-tools parity nodes: `2 passed`.
- Fresh post-review full local suite at verified HEAD: `1853 passed, 139 skipped,
  191 deselected in 187.86s`.
- Full source/test Ruff scope: PASS.
- Config/schema drift suite: `32 passed`; drift false.
- `gsd-sdk validate consistency`: passed with zero warnings.
- `git diff --check`: PASS before this verification-only update.
- CBM project `Users-admin-Mnemosyne`: ready, 16,041 nodes / 61,519 edges;
  `detect_changes` reports `changed_count=0` for the verified code state.

The requested reference duration was `178.86s`; the fresh independent rerun
produced the same counts in **187.86s**. This report records the observed result
rather than silently substituting the reference timing.

## Review and Security

- `09.1-REVIEW.md`: clean, critical 0, warning 0, info 0.
- `09.1-REVIEW-FIX.md`: the one earlier test-coverage warning was fixed; no
  production behavior changed.
- `09.1-SECURITY.md`: `status: verified`, `threats_open: 0`, no accepted risks.
- Independent inspection found no remaining high-severity or required goal gap.

## Git and Exact-SHA CI

- Implementation commit: `3731b1901f3deaa3d0b257fb7b20edd08b110944`;
  implementation CI run `29113912017` succeeded.
- Current verified HEAD and `origin/main`:
  `0a7c12dd25543bcb254fb8682411f65d6fefadbb`.
- Exact-HEAD CI run
  [29117413619](https://github.com/onfire7777/Mnemosyne/actions/runs/29117413619)
  completed successfully. Lint, Unit + drift, Provider conformance, Postgres
  integration, macOS native wheels, and Ubuntu native wheels all succeeded;
  the documented nightly chaos job was non-gating and skipped.

## Conclusion

Phase 09.1 satisfies REQ-003, REQ-004, REQ-007, and NFR-004 at the verified
HEAD. The phase is ready to remain marked complete and Phase 09.2 may proceed.
