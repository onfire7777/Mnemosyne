# Strict Blueprint Parity Audit

**Date:** 2026-06-21
**Blueprint:** `/Users/admin/Desktop/Mnemosyne/Mnemosyne-v2-Build-Blueprint.md`  
**Status:** gaps remain; exact 1:1 parity is not complete.

## Current Verified Coverage

- Local deterministic engine covers evidence deduplication, byte recall, bitemporal assertions, as-of queries, branch/merge/discard, hybrid local retrieval, provenance-bearing hits, correction, export, forget propagation, capability checks, consolidation gate, learning loop, lifecycle demotion, and shadow self-optimization with contextual bandit policy selection.
- Runtime surfaces added after strict audit:
  - `src/mnemosyne/mcp_server.py` for stdio JSON-RPC `initialize`, `tools/list`, and `tools/call`.
  - `src/mnemosyne/postgres_engine.py` for core PostgreSQL append/upsert/search/as-of/branch operations against `sql/schema.sql`, including SQL FTS, pgvector assertion search, stored pgvector evidence search with deterministic null-embedding fallback, and recursive graph/PPR.
  - `src/mnemosyne/retrieval.py` for embedding and reranker adapter boundaries plus local semantic entropy.
  - `src/mnemosyne/storage.py`, `src/mnemosyne/provenance.py`, `src/mnemosyne/ingestion.py`, and `src/mnemosyne/queue.py` for clear/encrypted object storage, signed-provenance decisions, multimodal ingestion, and local queue semantics.
  - `src/mnemosyne/prefetch.py` and `src/mnemosyne/parametric.py` for FR-18 and FR-21 local and command-provider boundaries.
- Verification currently passes with `.venv/bin/python -m pytest -q` collecting 342 tests and returning 291 passing tests plus 51 skipped live-DB tests when `MNEMOSYNE_POSTGRES_DSN` is unset.
- Compose Docker/Postgres verification passes with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne .venv/bin/python -m pytest -q` returning all 342 tests passing against the pgvector database and verifying tenant RLS, SQL FTS, pgvector assertion search, stored pgvector evidence search, deterministic null-embedding evidence fallback, provider-backed CLI/Postgres evidence pgvector storage, HTTP reranker final-hit ordering, fail-closed malformed provider output, command-backed raw-media embedding storage and vector retrieval for externalized media, Postgres MCP runtime profile/learning state persistence, recursive graph/PPR with active-only relation validity and historical `as_of` traversal, explain channels/rails/provenance/activation/read marks/calibration/semantic entropy/gist-only/statistical-trace/confabulation-risk abstention, retired-summary filtering, tenant-scoped branch names, branch/discard including TMS justification/contradiction pruning for abandoned branch assertions and dependency-only orphan cleanup, branch merge retrieval and exported merge logs, bitemporal supersession, trust-tier-precedence source truth, tenant isolation, tombstone and hard-delete forget modes, immutable evidence CID semantics, tombstone replay prevention, command-backed object key management, lossless evidence-envelope preservation including external session identity, export including calibration sets and entity registry rows, shared local/Postgres evidence/retrieval/explain/direct lexical/vector/graph primitives/justification/contradiction/export-all JSON snapshots/branch/as-of/relation/preference/correction/forget-propagation with entity registry trim/removal and summary-derived gist invalidation, retrieval-filter/deep-graph-isolation/hard-delete-audit/access-telemetry/calibrated-abstention/entity-registry contracts, source/tier audit metadata on shared write paths, Markdown/git source-sync provenance and override behavior, priority replay by `importance*novelty*surprise*reward`, projection-recompute summary refresh scheduling, materialized one-level gist summaries with source-to-summary relations, confabulation-risk metadata, and refresh retirement metadata, latent user-model refresh from consolidation, consolidation evidence embedding persistence, consolidation lifecycle demotion metadata, HTTP-configurable retrieval adapter wiring with strict provider response validation and fail-closed CLI provider health checks, provider-manifest lexical/graph backend gates, explicit-empty Postgres DSN failure, CLI `--backend postgres`, stateless MCP ingestion over tenant-scoped durable Postgres queues, tenant-scoped durable Postgres queue leasing/drain with result persistence, CLI file ingestion through the C2PA verifier adapter with asset binding, metadata-derived text retrieval for externalized payloads, async media extraction over object-store bytes, gated consolidation promotion on Postgres including command-backed extractor/summarizer/resolver adapters with registry update, Postgres residency-transfer deny/allow policy persistence, and Postgres local fallback sensitivity filtering.
- Code-review blockers partially remediated after `REVIEW.md`: local graph tenant/branch leak fixed, MCP tool-result envelope added, Postgres public string-ID mapping added, Postgres high-level runtime methods added, and Postgres evidence conflict handling now restores durable fields.
- CLI-first runtime tool coverage added for profile context, graph neighbors, prefetch, proposal/confirmation, trajectory logging, failure attribution, lesson induction/promotion, procedure induction/validation, residency-policy inspection, Markdown/git `source-sync`, and parametric proposal/evaluation/rollback. MCP dispatch mirrors the same operations through a TOOL_SPEC allowlist, rejects non-object JSON-RPC tool arguments, validates generated input schemas for JSON-RPC and official SDK tool calls, preserves explicit optional-null schema fields, emits MCP-style `inputSchema` entries from CLI `tools`, requires mediated role/trust authority before lesson promotion or procedure validation/promotion/rollback mutate runtime learning state, persists parametric artifacts for MCP rollback, exposes object-store encryption/key-provider/residency/runtime-residency/queue configuration for MCP ingestion, has local and Postgres stateless modes that rebuild engine/tools per call from durable state, supports signed HMAC session binding through `--session-secret`/`--require-session`, supports hosted HTTP TLS and client-certificate enforcement, and can optionally run through the official Python MCP SDK via `mneme-mcp --sdk` with SDK-level tool listing/call/session-binding tests.
- Command-backed parametric adapter providers now expose a shell-free CLI/MCP boundary for LoRA/test-time-training proposal, evaluation, and rollback, require operator-grade write authorization before trainer calls, strictly require JSON object responses, persist provider metrics/payloads, and enforce local mutation-rate bounds, external-only reward metadata, monotonic-trust/sink restrictions, exact artifact/gate matching, protected-case pass requirements, and margin-over-noise before artifacts are accepted.
- Direct shared Local/Postgres `MemoryTools` parametric facade coverage now verifies persisted protected-suite use through `RuntimeState` and `PostgresRuntimeState`, activated lesson/procedure source IDs, promotion decision shape, rollback references, and operator authorization for proposal/evaluation/rollback without relying on JSON-RPC transport coverage.
- Direct shared Local/Postgres `MemoryTools.propose`/`MemoryTools.confirm` facade coverage now verifies proposal branch creation, pre-confirm main isolation, operator-authorized merge metadata, and source-evidence preservation across both engines.
- `ops-dashboard-check` now rejects packaged dashboard snapshots with incomplete metric taxonomy: package validation requires the embedded `ops-report.json` report to retain counts, queue, metrics, learning, and tripwire sections, including audit/deletion counts, retrieval/calibration buckets, lesson diversity, proxy-vs-true gap, and gate promotion/rollback counters.
- `privacy-ops-check` now requires an operator hard-delete corroboration case with hashed request/approval/subject/audit/delete-receipt evidence, `min_corroboration_for_delete >= 2`, enough distinct supporting source hashes, proof that uncorroborated delete would be refused, and post-delete read-probe failure.
- CLI backend selection added with `--backend local|postgres`, `--postgres-dsn`, and `MNEMOSYNE_POSTGRES_DSN`; explicit empty `--postgres-dsn ""` now fails closed instead of falling back to the environment; the live smoke verifies capture/assert/relation/preference/search/deep-search/export/branch/discard through the Postgres CLI path.
- Fail-closed capability enforcement added in `MemoryTools` for preference writes, hard-instruction profile writes, destructive forget operations, fact assertions, relations, proposal branches, supersessions, corrections, branch creation, confirmation, merge, discard, lesson promotion, and procedure validation/promotion/rollback. CLI and facade tests verify denied low-trust writes and allowed trusted writes; CLI `assert`, `relation`, `branch`, `merge`, `discard`, and learning activation commands now route through the facade instead of bypassing authorization. Signed HMAC-SHA256 CLI session tokens now bind tenant/user identity plus role/source-trust claims before command execution, fail closed on malformed/tampered/expired/out-of-range tokens, and can supply branch/confirm/merge/discard/learning-activation authorization context without redundant CLI role/trust flags. CLI and MCP session verification now support keyed HMAC keyrings with signed `kid` claims, command-backed session-secret providers for signing/verification, plus fail-closed key-id and session-id revocation lists. OIDC/JWKS exchange now validates RS256/ES256 IdP tokens for required issuer, audience, tenant, user, and expiry claims; can either require raw Mnemosyne role/trust claims or map verified IdP client/tenant/claim rules through a fail-closed authz policy; rejects unsafe JWKS metadata; supports bounded file/URL JWKS loading with cache TTL refresh and refresh-on-unknown-`kid` rotation; denies malformed policies, unknown fields, invalid roles, missing client allowlists, rule misses, ambiguous matches, and raw-claim fallback when policy mode is configured; exposes `idp-authz-policy-check` for bounded redacted policy validation/audit summaries with stable fingerprints; exposes `idp-authz-policy-rollout-check` for fingerprint-acknowledged current/candidate diffs and fail-closed claim-simulation change gates; and mints bounded Mnemosyne signed-session tokens through CLI `session-exchange` and hosted HTTP `/session/exchange`. MCP JSON-RPC and official SDK transports can require the same signed session contract, reject tenant/user mismatches, and override self-asserted role/trust arguments before authorization.
- Tenant RLS added to `sql/schema.sql` for tenant-owned tables. `PostgresEngine` sets `mnemosyne.tenant_id` before tenant-scoped SQL and requires tenant-scoped branch/merge/discard operations.
- Explicit erasure modes added to local/Postgres forget paths and CLI: `tombstone_recompute` preserves the audit/deletion path while clearing evidence, and `hard_delete_legal` removes the evidence row while propagating derived assertion retraction/trimming. Encrypted local object storage now supports AES-GCM per-object keys, JSON-backed local key management, command-backed KMS/HSM/Vault-style key management, and key shredding after legal hard-delete. CLI and MCP ingestion now enforce configured data residency labels, can require explicit runtime processing residency, fail closed on cross-region processing unless a `source->target` transfer is explicitly allowlisted, and record residency/runtime-residency transfer decisions in evidence access policy. Forget now transitively erases media-derived evidence and retracts/strips assertions, preferences, and relations that depended on source or derived evidence.
- HTTP-compatible embedding and reranker adapters added, with CLI/env wiring for Postgres retrieval (`--embedding-provider`, `--embedding-url`, `--reranker-provider`, `--reranker-url`, model, API-key, dimension, and timeout controls), strict response validation, bearer-token coverage, and nonzero CLI `provider-check` failures. Command media embedding, command object-key/KMS, command media extractor, command parametric providers, and residency policy are now included in `provider-check`, including fail-closed malformed key-provider coverage. `provider-check --provider-manifest` can now hydrate deployment provider settings from JSON, resolve secret values from environment variables, require named checks, report lexical/graph retrieval backend names, and fail closed when a manifest forbids local embedding, reranker, lexical, or graph retrieval adapters. Local deterministic providers remain the default for offline verification.
- CLI-first signed-provenance ingestion added with `ingest --file`, modality/media-type controls, signed-provenance JSON/file inputs, `--c2pa-tool`, trusted issuer/root mapping, `--provenance-trust-policy`, fail-closed verifier errors, report-to-asset binding checks, scoped trusted-signer and certificate-root enforcement for valid-but-untrusted manifests, provenance validity/trust/binding capability tags, and local/Postgres tests. Quarantined evidence is now excluded from default retrieval unless the caller explicitly opts into `include_quarantined`.
- Trust-tier semantics now match the blueprint direction: tier `0` is direct-user/highest trust and tier `5` is untrusted external. Retrieval/write gates, confidence weighting, provenance deltas, model defaults, SQL defaults, CLI defaults, and local/Postgres tests were migrated to that scale. Assertion conflicts now honor trust tier before recency so tier-0 human source truth overrides lower-trust machine memory without breaking bitemporal `as_of` cutoffs.
- Ingest classification now identifies actor/source trust, applies capability/taint tags, detects email/phone/SSN-style PII, raises sensitivity/access policy metadata, marks untrusted imperative content as data-only without instruction authority, and enqueues an ordered persisted local consolidation job after new evidence is appended. The worker now records structured pass results, extracts simple direct-user fact candidates, promotes them only through the gate, and refuses data-only untrusted fact-shaped evidence.
- Consolidation now resolves deterministic entity keys and executes summarizer, lesson_distiller, and skill_inducer role outputs for local fact candidates; distilled lessons and checklist procedures persist through runtime learning state and are verified through direct worker and CLI `ops-report` tests.
- The local queue now supports drain semantics and typed runtime handlers for consolidation, calibration, lifecycle sweep, eval suite, and observability snapshot jobs, with CLI `queue-enqueue`, `queue-drain`, `queue-snapshot`, and `consolidate-once` surfaces.
- Protected regression cases can now be persisted with CLI `gate-case-add/list`; consolidation handlers load the saved suite and block promotion when a protected case fails.
- Promotion gates now route branch, merge, and discard through tenant-aware engine calls when available, with live Postgres coverage for gated direct-user fact promotion.
- Externalized multimodal evidence now indexes metadata-derived `derived_text`, OCR text, transcripts, captions, alt text, and descriptions while keeping raw bytes in object storage. When derived text is absent, ingest queues an async `media_extract` job; the worker reads object-store bytes through an operator-configured command extractor, appends derived evidence, and queues consolidation for the derived row. A shell-free command media embedding provider boundary accepts image/audio/video bytes through a temp file plus JSON stdin, requires strict JSON embedding output, normalizes vector dimensions, and is covered by direct adapter and CLI `provider-check` tests. Ingestion can now attach command media embeddings to raw externalized media evidence, LocalMemoryEngine and PostgresEngine use those stored vectors for dense retrieval before extraction, and local plus live CLI/Postgres tests verify raw-media retrieval without derived text.
- CLI `ops-report` now emits a tenant dashboard snapshot with memory counts, audit/deletion counts, queue state including running jobs, learning lesson diversity, proxy-vs-true gap, and tripwire status; `--dashboard-html` writes the snapshot as a static HTML dashboard artifact with retrieval, calibration, learning, and gate/eval sections; and `--dashboard-package-dir` writes a deployable local bundle with HTML, JSON snapshot, and manifest files.
- CLI `tls-lifecycle-ops-check` now validates fingerprinted production TLS lifecycle evidence for non-local CA/ACME issuance, renewal execution, current/candidate overlap, deployed serial/chain match, reload verification, non-local private-key custody, monitoring alerts, and raw key/cert/token/log redaction; release-audit requires this output shape in the production command profile.
- Production `release-audit` now rejects empty or malformed `worker-run` runtime evidence: worker-run output must carry non-empty worker, summary, queue, cycles, jobs, and metrics sections, prove at least one cycle and processed job, include a processed-job cycle, and include job kind/status evidence. This prevents placeholder worker output from being mistaken for concrete deployment evidence; operator-run worker deployment proof is still required.

## Remaining Exact-Parity Gaps

| Area | Status | Required For 1:1 Blueprint Parity |
|---|---|---|
| Production Postgres retrieval | Partial | SQL FTS, pgvector assertion retrieval, stored pgvector evidence retrieval with deterministic null-embedding fallback, recursive graph/PPR with active-only relation validity and historical `as_of` traversal, activation-scored final ranking, assertion read-time strengthening telemetry, conformal calibration-backed abstention, semantic-entropy explain output, HTTP-compatible embedding/reranker adapters, strict provider response validation, fail-closed CLI provider health checks, live CLI/Postgres HTTP provider smoke that persists 1024-dim pgvector evidence embeddings, reports adapter metadata, applies HTTP reranking, and fails closed on malformed vectors, live DB smoke, and shared local/Postgres evidence/retrieval/explain/direct lexical/vector/graph primitives/justification/contradiction/export-all JSON snapshots/branch/as-of/relation/preference/correction/forget-propagation/retrieval-filter/deep-graph-isolation/access-telemetry/calibrated-abstention contracts are implemented. Command-backed lexical and graph retrieval adapters now let Postgres deployments route BM25 and specialist graph calls through shell-free JSON contracts, including provider-manifest configuration, provider-check probes, and PostgresEngine delegation while preserving native FTS/recursive-PPR defaults. `retrieval-ops-check` validates fingerprinted production evidence bundles for forbid-local provider manifests, non-local embedding/reranker providers, non-local lexical and graph backends, hashed lexical/vector/graph/reranker adapter probes with command/source/query/tenant/result/top-id fingerprints and bounded latency, Postgres production validation, hashed tenant/query cases proving lexical/vector/graph/reranked/calibrated retrieval paths, calibration dataset fingerprints and metrics, raw-field redaction, and release-audit profile coverage. Remaining work: capture operator-run evidence from concrete deployed ParadeDB BM25, AGE, pgvector, reranker, or other specialist retrieval adapters and continue broadening shared parity coverage across production adapters. |
| Tenant isolation and auth | Partial | Capability checks now protect preference, hard-instruction profile, destructive forget, fact, relation, proposal, supersession, correction, branch, merge, confirm, and discard writes; signed CLI and MCP session tokens bind tenant/user/role/source-trust claims before privileged operations; keyed HMAC keyrings with `kid` claims plus revoked key/session-id lists are covered; command-backed session-secret providers can supply CLI/MCP signing and verification material without inline/env secret custody; provider-check validates command-backed session-secret custody with redacted signed-token round trips and fail-closed active-key rotation checks; OIDC/JWKS provider-check preflight validates JWKS reachability/shape and redacted authz policy summaries; OIDC/JWKS exchange can mint bounded signed Mnemosyne sessions from issuer/audience-validated IdP tokens and fails closed on malformed claims, unsafe JWKS metadata, bad signatures, tampering, refresh failure, oversized JWKS, malformed authz policies, unknown policy fields, invalid roles, missing client allowlists, rule misses, ambiguous matches, and raw-claim fallback when policy mode is configured; file/URL JWKS sources support cache TTL refresh plus refresh-on-unknown-`kid` rotation; authz policy mode maps verified IdP client/tenant/claim rules to Mnemosyne role/trust claims for CLI and hosted HTTP exchange; CLI `idp-authz-policy-check` validates policies and emits secret-free audit summaries with stable fingerprints; CLI `idp-authz-policy-rollout-check` requires acknowledged current/candidate fingerprints, reports secret-free diffs, and fails closed on simulation authorization changes unless explicitly allowed; hosted HTTP can enforce TLS and client certificates; Postgres tenant RLS is implemented and live-smoked; Postgres runtime side-state tenant isolation now verifies two tenants cannot cross-load user model, learning, queue, metrics, gate cases, or mirrored runtime tables; MCP `tools/call` has opt-in shared-token enforcement plus required signed-session mode for JSON-RPC and SDK transports. Trust-tier direction now matches the blueprint scale. `auth-ops-check` now validates fingerprinted production evidence bundles for live IdP/JWKS validation and rotation, authz rollout fingerprints/simulations, external command-backed session-secret custody and rotation, TLS certificate and rotation controls, tenant RLS/signed-session binding cases, raw token/claim/secret/private-key redaction, and release-audit profile coverage. Remaining work: capture operator-run evidence from concrete production IdP, secret-manager, certificate, and tenant-isolation deployments and broaden deployment validation as those surfaces evolve. |
| CLI/MCP runtime coverage | Partial | CLI-first coverage now exists for profile, graph, trajectory, lesson, procedure, prefetch, parametric flows, local/Postgres backend selection, backend-independent `eval` seed-suite JSON output, blueprint-facing ABI aliases, and OIDC/JWKS `session-exchange`; MCP tools expose signature-derived JSON schemas, runtime JSON-RPC tests, TOOL_SPEC-allowlisted dispatch, auth-token gating, signed-session binding, direct session argument binding coverage for JSON-RPC/SDK shared preparation, object-store/object-key/residency runtime env-default parsing coverage, non-object argument rejection, generated schema validation for JSON-RPC and SDK tool calls, explicit optional-null schema support, CLI `tools` MCP inputSchema parity, mediated lesson/procedure activation authorization, parametric rollback persistence, local stateless mode over durable JSON/runtime state, stateless Postgres MCP ingestion plus profile/learning runtime state over tenant-scoped durable queues and runtime tables, hosted HTTP TLS/client-certificate enforcement, authz policy rollout checks, direct MCP server `main()` dispatch coverage for default stdio, official SDK stdio, hosted HTTP, official SDK StreamableHTTP, and serving-mode mutual exclusion, and optional official Python MCP SDK stdio mode with SDK handler smoke/session coverage. `mneme-mcp --self-test` now provides a local deployment preflight for initialize/list/schema/auth/session/read-only-call behavior with secret-redacted JSON output and SDK `sdk_build` readiness success/failure recording. `mneme-mcp --http` now hosts the same JSON-RPC facade over HTTP with `/healthz`, `/mcp`, bearer/session-header binding, optional TLS and client-certificate enforcement, optional OIDC-backed `/session/exchange`, body-size limits, malformed/non-object/unsupported-method rejection, auth/session/schema tests, locked facade access, and stateless restart durability coverage. `mneme-mcp --sdk-streamable-http` has runtime and CLI soak coverage for the official SDK StreamableHTTP initialize/list/read-only-call loop, and entrypoint dispatch now separates transport `transport_stateless` from facade `stateless` config. CLI `deployment-soak` can orchestrate allowlisted hosted JSON-RPC HTTP, SDK StreamableHTTP, legacy SSE, provider, worker, ops dashboard, IdP authz rollout, TLS endpoint, and TLS rotation-plan preflights with narrow non-secret global runtime placement args, and `--evidence-dir` writes durable report/check/manifest evidence bundles. `mcp-ops-check` now validates fingerprinted production evidence bundles for hosted JSON-RPC HTTP, official StreamableHTTP, optional legacy SSE, stateless loop/read-only-call/tool-list/initialize controls, bearer and signed-session binding, latency thresholds, TLS/client-certificate controls, raw token/request/response redaction, and release-audit profile coverage. Remaining work is capturing operator-run evidence from concrete deployed MCP endpoints and continuing broader production stateless soak expansion as transports evolve. |
| Consolidation role pipeline | Partial | Ingest now queues ordered persisted local consolidation pass payloads; the CLI can inspect/enqueue/drain local jobs; `--queue-backend postgres` and `mneme-mcp --queue-backend postgres` provide tenant-scoped durable row leasing, retry/dead/complete lifecycle, failure error persistence, and result persistence; the worker records structured pass results, prioritizes replay by `importance*novelty*surprise*reward`, promotes candidates only through the gate, refuses data-only untrusted facts, resolves deterministic entity keys by default, can invoke command-backed production candidate extractor, evidence summarizer, and entity resolver providers with strict JSON contracts, updates a durable tenant-scoped entity registry after successful promotion, executes deterministic summarizer with materialized RAPTOR-style gist evidence, leaf/root summary hierarchy metadata, source-to-summary and summary-to-parent relations, level-aware summary retirement, lesson_distiller, skill_inducer, embedder, forgetter lifecycle demotion plus persisted must-keep rehearsal scheduling, and latent user_model_updater outputs persisted in runtime learning/user-model/evidence state, loads persisted protected regression gate cases before promotion, has `gate-suite-check` with protected count/fingerprint/change-control gates, and has an explicit `projection_recompute` runtime job/CLI preflight that walks only changed evidence plus derived provenance edges and queues consolidation only for surviving affected evidence. Local handlers exist for calibration, lifecycle, eval, and observability jobs, including rehearsal counts. Live Postgres queue coverage now verifies failing workers persist retry then dead status, attempts, last_error, snapshots, and tenant-isolated lease/list behavior. `consolidation-ops-check` now validates fingerprinted production evidence bundles for production validation scope, Postgres-backed supervised worker cycles, forbid-local provider and hosted role checks, production embedding evidence, projection recompute with consolidation re-enqueue, protected-suite tier coverage, model-backed consolidator write authority, calibration/lifecycle/ops tripwire health, deployment supervision/alert/fingerprint/count binding with bounded latency, raw prompt/request/response/evidence/embedding/CID/credential/tenant/user redaction, and release-audit profile coverage. Production `release-audit` now rejects placeholder or malformed `worker-run` runtime evidence that lacks non-empty worker, summary, queue, cycle, job, metrics, processed-cycle, or job kind/status proof. Remaining work: capture operator-run evidence from concrete production worker, provider, embedding, protected-suite, projection, calibration, lifecycle, ops-report, and deployment-control surfaces. |
| Signed provenance | Partial | CLI and ingestion can invoke a `c2patool`-style verifier, map trusted issuers and certificate-root SHA-256 fingerprints, load scoped JSON trust policies, fail closed on verifier errors or policy signer/root/scope rejection, require verifier reports to bind back to the exact asset by SHA-256/path, propagate provenance validity/trust/binding capability tags, and hide quarantined evidence from default retrieval. `provenance-trust-check` validates fingerprinted C2PA verifier suites with asset-bound manifests, configured trusted issuers, certificate-root expectations, redacted verifier evidence, required case IDs, and release-audit profile coverage. `provenance-ops-check` now validates fingerprinted production C2PA operations bundles for production validation scope, non-local verifier identity, trusted issuer/root policy fingerprints, trust-root rotation and stale-root rejection, asset-bound trusted plus quarantined cases, quarantine exclusion from default retrieval, Postgres ingestion evidence with hashed tenant/evidence IDs and provenance capability tags, supervised verifier/trust-root/quarantine/ingestion deployment evidence, alert routing, deployment policy/ingestion fingerprint binding, raw asset/manifest/verifier/certificate/credential redaction, and release-audit profile coverage. Remaining work: capture operator-run evidence from concrete production C2PA verifier, trust-root, quarantine, and ingestion deployments. |
| Multimodal retrieval | Partial | Metadata-derived OCR/transcript/caption/alt/description text is indexed for externalized object payloads and verified locally, through the CLI, and against live Postgres. Async `media_extract` can invoke an operator-configured extractor command over object-store bytes and append derived evidence without mutating the source; CLI `provider-check` validates the configured command extractor contract. A command-backed media embedding provider boundary now covers image/audio/video bytes behind strict JSON vector output, provider-check validation, raw externalized media evidence embeddings, and Local/Postgres dense retrieval before derived text exists. `multimodal-ops-check` now validates fingerprinted production evidence bundles for production validation scope, forbid-local media extractor/embedder checks, encrypted external object storage, image/audio/video extraction cases with asset and derived-CID hashes, raw media embedding indexing, Postgres production retrieval over stored media vectors and derived text, Postgres-backed `media_extract` jobs, supervised extraction/embedding/object-store/job/retrieval deployment evidence, alert routing, execution fingerprints, deployment count binding, raw media/extractor/embedding/document/credential redaction, and release-audit profile coverage. Remaining work: capture operator-run evidence from concrete production extractor, media-embedding, object-store, retrieval, and media-job deployments and continue broadening production multimodal provider operations. |
| Privacy and erasure | Partial | Tombstone recompute and legal hard-delete modes now exist locally and in Postgres with live CLI coverage. Local encrypted object storage now supports AES-GCM per-object keys, a JSON-backed key manager, a command-backed KMS/HSM/Vault-style key manager, provider-check key-cycle validation, post-shred `has_key`/`get_key` verification, and key shredding after legal hard-delete. CLI and MCP ingestion enforce configured residency labels, strict runtime processing residency when enabled, and explicit cross-region `source->target` transfer allowlists, then record the residency decision in evidence access policies. CLI/MCP can now inspect the active residency policy, and `provider-check` reports it alongside provider contracts. Forget transitively erases media-derived evidence and retracts/strips dependent assertions, preferences, relations, and entity registry rows; shared Local/Postgres tests now prevent tombstone replay and duplicate-CID mutation. `privacy-ops-check` now validates fingerprinted production evidence bundles for non-local KMS lifecycle/rotation/shred, strict residency allow/deny enforcement, tombstone recompute safety, legal hard-delete safety, operator hard-delete corroboration, required case IDs, explicit key/object/subject/KMS-response redaction flags, recursive raw-field rejection, and release-audit profile coverage. Remaining work: operator-run validation against real production KMS deployments and production residency-policy operations. |
| Observability dashboards | Partial | CLI `ops-report` exports tenant counts, audit/deletion counts, queue state, learning diversity, proxy-vs-true gap, tripwire status, durable retrieval request/channel/abstention counters, rolling p95 latency metrics, calibration job/abstention/threshold metrics, gate promotion/rollback counters, open-contradiction backlog tripwires, a top-level `ok` deployment gate, a static `--dashboard-html` artifact, and a `--dashboard-package-dir` bundle with HTML, JSON snapshot, and manifest files from runtime state/exported memory. Renderer, packaged-CLI, and deployment-soak smoke tests cover memory, queue, retrieval, calibration, learning, gate/eval, dashboard artifact/package creation, and bounded JSON dashboard sections. `ops-dashboard-check` now validates fingerprinted dashboard packages or hosted dashboard URLs plus optional production operations evidence for manifest shape, snapshot health, required embedded metric taxonomy, tripwire pass state, dashboard HTML markers, tenant binding, HTTPS-by-default hosted access, refresh freshness, source snapshot fingerprinting, access controls, alert delivery, and raw HTML/JSON/snapshot/token/user-data redaction, and it is required by the production release-audit profile. Remaining work: operator-run hosted dashboard deployment evidence against a real production dashboard URL or package. |
| Parametric tier | Partial | Local isolated shadow artifacts are persisted to a sidecar JSON store, exposed through CLI/MCP proposal/evaluation/rollback surfaces, and carry rollback metadata. A command-backed LoRA/test-time-training provider boundary now invokes external trainer/rollback adapters without a shell, requires operator-grade authorization, persists provider metrics/payloads, validates the same persisted artifact URI before promotion, and enforces local mutation-rate/reward/sink/gate rails. Parametric evaluation and rollback now prefer persisted protected regression cases from runtime state, report stable protected-suite snapshots, persist rollback suite metadata, pass protected-suite evidence to command-backed rollback providers through CLI/MCP, and validate that rollback contract in `provider-check`. Direct shared Local/Postgres `MemoryTools` facade coverage now proves proposal/evaluation/rollback use persisted `RuntimeState`/`PostgresRuntimeState` protected cases, activated lesson/procedure source IDs, promotion decision shape, rollback refs, and operator authorization across both engines. `parametric-trainer-check` now validates fingerprinted production evidence bundles with explicit `trainer`, `protected_suite`, `gate`, `rollback`, `deployment`, `rail_report`, `metrics`, and `redaction` sections; it requires non-local trainer providers, immutable artifact URI/hash proof, canonical protected-suite IDs/counts/non-synthetic source/tier coverage, gate candidate/artifact matching, passed protected cases, margin floors, no promoted rollback branch, HTTPS supervised deployment/canary/alert evidence, protected-suite/artifact/rollback fingerprint binding, rollback drill verification, bounded deployment latency, runtime rail metadata for external reward/monotonic trust/non-negative trust delta/no system-prompt sink/no eval-source overlap, authorized rollback, mutation/reward/sink thresholds, raw-field redaction checks, and release-audit profile coverage. Remaining work: capture operator-run evidence against real trainer deployments and validate against deployed trainer infrastructure. |
| Live parity suite | Partial | Engine and CLI live Postgres smokes now pass in Docker; the live suite now covers bitemporal supersession, tenant isolation, tenant-scoped branch names and branch metadata export shape, branch merge retrieval plus exported merge-log parity, active-only graph/PPR relation validity plus historical `as_of` graph traversal, shared local/Postgres evidence/retrieval/direct lexical/vector/graph primitives/justification/contradiction/export-all JSON snapshots/branch/as-of/relation/preference/correction/forget-propagation/retrieval-filter/deep-graph-isolation/hard-delete-audit/access-telemetry/calibrated-abstention/entity-registry contracts, branch discard TMS pruning for abandoned justifications/contradictions including dependency-only orphan cleanup, lossless evidence-envelope preservation including external session identity, local export-all calibration/entity/tenant snapshot parity with erased evidence filtered, asset-bound C2PA ingest, externalized payload derived-text retrieval, raw-media embedding-before-extraction ingestion and dense retrieval, async media extraction, transitive projection erasure across assertions/preferences/relations/entity registry rows, CLI backend use, CLI eval seed-suite JSON serialization, gated consolidation, fallback sensitivity filtering, Postgres-persisted cross-region residency transfer policy, standalone local `RuntimeState` public-helper round trips plus direct `RuntimeState`/`PostgresRuntimeState` parity for user-model entries, latent profiles, learning trajectories/attributions/lessons/procedures, queue payloads, metrics, and protected gate cases, two-tenant Postgres runtime-state isolation across side-state loads and mirror tables, direct public-helper coverage for idempotent Postgres tenant/branch setup, queue schema/listing, tenant-scoped queue isolation, and runtime-state sidecar path derivation, shared Local/Postgres evidence-embedding update coverage for success, missing-CID fail-closed behavior, export parity, and audit provenance, direct `RuntimeJobHandlers` coverage for media extraction skip handling, calibration, lifecycle sweep, eval suite, and observability snapshot methods in addition to worker dispatch, direct `MemoryTools` facade coverage for source-sync, profile, graph, branch lifecycle, learning, procedure, and outcome wrapper methods in addition to JSON-RPC transport coverage, and live Postgres queue failure-path parity for retry/dead lifecycle persistence. Remaining work: expand to every remaining engine/runtime method with optional production adapters enabled. |

## 2026-06-21 Audit Delta

- Live Postgres command-backed consolidation now covers candidate extraction, entity resolution, and evidence summarization together, including resolver pass metadata and resolver-supplied entity canonical persistence.
- Provider deployment manifests now emit `retrieval_backends` and fail closed on local lexical/graph backends when `forbid_local` is set; production-style `paradedb-bm25` and `apache-age` labels are covered by focused CLI tests.
- CLI Postgres DSN handling now fails closed on explicit empty `--postgres-dsn ""` even when `MNEMOSYNE_POSTGRES_DSN` is set; backend, runtime-state, and queue loading share the parser-selected DSN behavior.
- Full compose verification now passes all 348 collected tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`; the no-DSN local run passes 296 tests and skips the 52 live-DB cases.
- Local and Postgres audit logs now record source/tier provenance on write paths. Evidence appends, assertion upserts, preference writes, learning-store writes, and forget/delete audit rows expose `actor`, `source`, `trust_tier`, `capability_tags`, and structured `diff` metadata; Postgres writes the existing tier/tag columns and normalizes source during export. Focused shared-contract tests pass locally and against the compose DSN, and `tests/test_postgres_engine_live.py` remains green.
- Consolidation replayer now performs priority replay by `importance*novelty*surprise*reward` using evidence metadata or payload replay-score overrides, orders the evidence stream before downstream passes, and reports per-CID factors/scores. Focused consolidation coverage verifies that priority order differs from input order.
- Consolidation `user_model_updater` now refreshes the latent user profile from prioritized evidence/candidates using the existing deterministic embedding and user-model persistence path; CLI worker persistence saves user-model state alongside learning state.
- Consolidation `embedder` now persists deterministic embeddings for evidence missing vectors through local/Postgres `set_evidence_embedding`, audits the update, reports provider/dimensions/embedded CIDs, and respects the Postgres 1024-dimensional evidence vector schema.
- Consolidation `forgetter` now persists lifecycle demotion metadata through local/Postgres `update_evidence_metadata`, audits the update, reports evaluated/demoted/failed CIDs, and no longer emits `forgetter_not_implemented` for default consolidation runs.
- Markdown/git `source-sync` now compiles committed human source assertions into `markdown_git` evidence with Git SHA/path/line provenance and `human-source-truth` capability tags. Assertion conflict handling now applies monotonic trust-tier precedence before recency in both local and Postgres engines, so committed tier-0 source truth overrides newer lower-trust machine assertions while preserving valid-time `as_of` behavior.
- Consolidation summarizer now materializes one-level gist evidence instead of returning only pass metadata. Derived `consolidation-summary` rows carry `abstractive_gist` metadata, source CID lists, derived capability tags, and `summary-derived-gist` relation edges; focused local, CLI, and live Postgres tests verify exportable provenance.
- Retrieval now preserves summary/lifecycle metadata on local and Postgres evidence hits and forces abstention when every budgeted support item is gist-tier. Results expose `explain.gist_support` plus a source-inspection uncertainty note, satisfying the sole-gist support safety rail.
- One-level gist refresh now records source fingerprints and active/retired summary status. Rerunning summarization over the same source set retires the previous gist with supersession metadata, and local/Postgres retrieval filter retired summary evidence before ranking.
- Forget now discovers derived evidence transitively through `source_evidence_cid`, top-level `source_evidence_cids`, and `summary.source_evidence_cids`, so raw-source erasure invalidates derived gist evidence and expires summary relations in the shared Local/Postgres contract.
- Projection recompute now expands changed evidence through summary source metadata and `summary-derived-gist` relations, reports affected summary evidence/relations, and queues bounded consolidation refresh jobs for surviving non-summary source evidence.
- Retrieval abstention now treats sole `statistical_trace` lifecycle support and explicit `confabulation_risk` summary support as insufficient evidence; generated gist summaries carry `confabulation_risk=true`, and the shared Local/Postgres contract covers both cases.
- Consolidation summarization now builds a deterministic RAPTOR-style hierarchy for multi-cluster source sets. Shared Local/Postgres contract coverage verifies two leaf summaries plus a root summary, raw transitive `source_evidence_cids`, root `source_summary_cids`/`child_summary_cids`, raw-to-leaf and leaf-to-root `summary-derived-gist` relations, and root-summary retrieval abstention.
- CLI `search` now has direct coverage for gist-only abstention propagation from the engine result through the command facade, including the uncertainty note, abstractive-gist hit metadata, and `explain.gist_support`.
- RAPTOR-aware projection recompute now has shared Local/Postgres contract coverage. Changing a raw source in a summary tree marks the raw source, its leaf summary, and root summary as affected, reports required summary lineage relations, and queues consolidation for surviving raw source evidence only.
- Branch parity now covers explicit non-main source-branch inheritance in the shared Local/Postgres contract, including source-branch evidence/assertion/relation inheritance, child `from_branch` export metadata, and isolation of source-only rows from `main`.
- MCP `search` now has direct JSON-RPC facade coverage for gist-only abstention propagation from engine retrieval, including the uncertainty note, abstractive-gist hit metadata, and `explain.gist_support`.
- Hosted HTTP JSON-RPC MCP `search` now has transport-level coverage for gist-only abstention across capture persistence, consolidation summary materialization, server restart, and `/mcp` `tools/call` search. CLI `tools` output now has exact contract-parity coverage against MCP `tools/list`.
- Deep retrieval now classifies `summary-derived-gist` graph/PPR relation hits as gist-tier support after Local/Postgres graph hits preserve relation source, predicate, target, confidence, and source evidence metadata. Shared Local/Postgres coverage verifies deep-search abstention on generated-summary graph support, and MCP `deep_search`/`explain` preserve the same abstention payload.
- Official MCP SDK StreamableHTTP local coverage now validates session initialize, tool listing, read-only policy calls, evidence capture, and search provenance through the official in-process client surface.
- Hosted HTTP JSON-RPC MCP `deep_search` and `explain` now validate relation-only `summary-derived-gist` graph abstention over `/mcp`, and Postgres forget propagation now expires relations with a timestamp guaranteed to satisfy `valid_to > valid_from` even when SQL transaction time predates app-generated relation `valid_from`.
- Shared Local/Postgres direct primitive coverage now verifies `lexical_search`, `vector_search`, and `graph_ppr` honor caller `k` result caps while preserving tenant/branch/channel metadata.
- CLI `deep-search` and `explain` now have direct coverage for relation-only `summary-derived-gist` graph abstention, closing the CLI runtime analogue of the MCP/hosted HTTP safety-rail tests.
- CLI `projection-recompute-enqueue` now has direct coverage for durable queued `projection_recompute` payload shape, including tenant/user/branch, multi-CID input, max attempts, and disabled consolidation enqueue semantics.
- CLI `profile-record-explicit` and `trajectory-attribute` now have direct runtime coverage for scoped explicit profile persistence and failed-trajectory attribution output, closing the focused profile/trajectory alias gaps from the static parity scan.
- CLI runtime coverage now mentions all 69 `cmd_*` handlers in `tests/test_cli_runtime_tools.py`; direct coverage was added for `prefetch`, `discard`, `trajectory-log`, `lesson-induce`, and `procedure-induce` so the static handler scan has 0 unmentioned command surfaces.
- Requirements status now reflects verified local/clean-DSN parity for conformal calibration, belief justification/cascade invalidation, and trajectory-driven learning, while preserving explicit production-validation gaps. Calibration coverage now includes oversized prediction-set abstention.
- MCP behavioral parity now includes direct in-process JSON-RPC `tools/call` coverage for source sync, scoped profile aliases, graph timeline/as-of, trajectory attribution, lesson/procedure induction/search, and outcome evaluation, rather than relying only on `tools/list` schema parity.
- Durable Postgres worker parity now covers maintenance handlers for calibration, lifecycle sweep, eval suite, observability snapshot, and projection recompute through `PostgresQueue` plus `QueueWorker`; the eval-suite handler now serializes dataclass outcomes correctly with `asdict`.
- Deployment-soak evidence bundles now explicitly mark local CLI-orchestrator scope, `production_validated=false`, and raw-command/stderr redaction metadata so local harness evidence cannot be mistaken for real production endpoint/provider validation.
- Release-audit production validation now fails closed unless the deployment-soak report also declares `target_environment=production` and `operator_asserted=true`; a forged `production_validated=true` boolean alone is no longer enough to satisfy the production handoff gate.
- Release-audit now validates required command output shapes for the production command profile, so broad operator gates such as auth, MCP, retrieval, consolidation, multimodal, privacy, provenance, trainer, worker, projection, dashboard, and ops-report checks cannot be replaced with placeholder `{ok: true}` stdout.
- Release-audit now also rejects placeholder worker cadence evidence: production `worker-run` output must prove supervised multi-cycle operation, idle-exit thresholding, nonzero poll interval, and fail-on-dead behavior.
- The six-category user model is now explicitly covered for identity, hard instruction, explicit preference, inferred preference, situational preference, and temporary state, with authority ordering and scoped-context behavior verified.
- Direct `MemoryTools` facade parity now covers previously schema/transport-light aliases and wrappers: `assert_fact`, `preference`, `branch`, `merge`, `discard`, `confirm`, `correct`, `prefetch`, `supersede`, `profile_context`, `trajectory_record`, `lesson_propose`, `procedure_propose`, `lesson_promote`, `lesson_search`, and `procedure_search`, including trusted fact/preference writes with low-trust denial checks, authorized branch lifecycle operations with fail-closed branch-write, non-operator promotion, and low-trust operator promotion/discard denial checks, shared Local/Postgres coverage for write/correction/prefetch/branch/profile/graph/learning facades, authorized proposal promotion, correction evidence linkage with low-trust denial, prefetch safety gating, authorized lesson promotion, and direct search visibility.
- Live Postgres MCP runtime parity now covers the optional command-backed parametric trainer adapter path: profile/learning state is persisted through the Postgres backend, reloaded through a fresh MCP facade, and then used for `parametric_propose`, `parametric_evaluate`, and `parametric_rollback` with command provider calls and protected-suite rollback evidence.
- Shared Local/Postgres runtime parity now directly covers `RuntimeJobHandlers` maintenance methods beyond projection recompute: the shared contract dispatches through the public handler map for media-extract skip handling, calibration, lifecycle sweep, eval suite, and observability snapshot, then verifies structured results, exact calibration persistence, identity-keyed lifecycle rehearsal output, eval-suite serialization, and counter side effects across both backends.
- Shared Local/Postgres multimodal parity now proves raw externalized media evidence receives a provider embedding before extraction, queues media extraction before consolidation when derived text is absent, preserves the raw bytes in object storage, and retrieves the empty-content media row through the stored vector on both backends.
- REQ-015 rehearsal scheduling now persists through lifecycle metadata rather than existing only as an interval helper. Due must-keep/protected memories are rehearsed before demotion, `successful_rehearsals` and `next_rehearsal_at` round-trip through Local/Postgres evidence metadata, and local plus durable Postgres lifecycle sweeps report rehearsal counts.
- REQ-018 now includes a contextual bandit learner for shadow policy variants. Logged policy outcomes are stored in the self-model store with rewards/context/metrics, UCB scoring selects future invariant-rail-safe variants, and the optimizer remains gate-bound before any policy promotion.
- REQ-011 expanded-suite controls now require more than a protected-case count. `gate-suite-check` can enforce minimum total cases and required smoke/core/archive tier coverage, reports missing tiers, and fails closed before accepting expected fingerprints.
- REQ-016 consolidation runs now emit a `role_pipeline` report that maps pass results to narrow society-of-roles names, provider strategies, provider types, and statuses. CLI and live Postgres command-provider tests verify model-backed extractor, resolver, and summarizer roles are auditable, and command-backed extractor/summarizer requests now include an explicit prompt-boundary contract separating trusted instructions from untrusted payload/evidence content.
- REQ-002 merge promotion audit is now exported per tenant. Local and Postgres `export_tenant` include `merge_log`, and the shared branch-merge contract verifies promoted projections plus exported merge counters.
- MCP session argument preparation now has direct coverage for metadata stripping, signed session-token discovery from transport params, tenant/user/role/trust binding, source-identity binding, and fail-closed missing-session or tenant-mismatch paths before JSON-RPC/SDK dispatch.
- CLI/MCP runtime environment defaults now have direct coverage for object-store root, object-store encryption, object-key store, command-provider inference, key-command timeout parsing, allowed residency labels, runtime residency, residency transfers, and strict runtime-residency flags.
- Shared Local/Postgres `MemoryTools` facade parity now covers the profile, graph, trajectory, lesson, procedure, and outcome wrapper group previously exercised only through Local runtime-surface tests.
- MCP server entrypoint dispatch now has direct runtime coverage for default stdio, official SDK stdio, hosted HTTP, official SDK StreamableHTTP, direct SDK StreamableHTTP app construction, and serving-mode mutual exclusion; SDK StreamableHTTP dispatch no longer collides transport and facade stateless arguments, and self-test SDK readiness records both build success and structured failure with token/DSN credential redaction.
- Shared Local/Postgres graph PPR parity now directly covers tokenized seed matching, proving a seed token can activate a multi-token relation source on both backends while preserving relation metadata and provenance.

## Supersession Note

The earlier `.planning/v1.0-MILESTONE-AUDIT.md` remains useful as evidence that the local deterministic scaffold passed its original milestone suite. It is not sufficient evidence for the user's current exact 1:1 blueprint parity objective.

## Next Required Implementation Slice

1. Keep the compose Docker/Postgres full-suite evidence current and expand it only when new engine/runtime surfaces are introduced.
2. Run provider checks against real production embedding, cross-encoder, image/audio embedding, extractor, summarizer, and entity-resolver deployments plus calibration datasets, including deployed prompt-boundary enforcement for model-backed consolidation wrappers.
3. Run hosted HTTP JSON-RPC, official StreamableHTTP/SSE, IdP/JWKS rotation, `tls-lifecycle-ops-check`, and stateless soak validation against real deployed endpoints.
4. Validate real secret-manager-backed session-secret command deployment/rotation before making any production multi-tenant claim.
5. Validate real isolated LoRA/test-time-training deployments behind the command-backed parametric provider boundary, including deployed protected-suite management and production rollback orchestration.
