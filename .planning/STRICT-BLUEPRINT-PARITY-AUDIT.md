# Strict Blueprint Parity Audit

**Date:** 2026-06-20
**Blueprint:** `/Users/admin/Desktop/Mnemosyne/Mnemosyne-v2-Build-Blueprint.md`  
**Status:** gaps remain; exact 1:1 parity is not complete.

## Current Verified Coverage

- Local deterministic engine covers evidence deduplication, byte recall, bitemporal assertions, as-of queries, branch/merge/discard, hybrid local retrieval, provenance-bearing hits, correction, export, forget propagation, capability checks, consolidation gate, learning loop, lifecycle demotion, and shadow self-optimization.
- Runtime surfaces added after strict audit:
  - `src/mnemosyne/mcp_server.py` for stdio JSON-RPC `initialize`, `tools/list`, and `tools/call`.
  - `src/mnemosyne/postgres_engine.py` for core PostgreSQL append/upsert/search/as-of/branch operations against `sql/schema.sql`, including SQL FTS, pgvector assertion search, deterministic evidence dense fallback, and recursive graph/PPR.
  - `src/mnemosyne/retrieval.py` for embedding and reranker adapter boundaries plus local semantic entropy.
  - `src/mnemosyne/storage.py`, `src/mnemosyne/provenance.py`, `src/mnemosyne/ingestion.py`, and `src/mnemosyne/queue.py` for clear/encrypted object storage, signed-provenance decisions, multimodal ingestion, and local queue semantics.
  - `src/mnemosyne/prefetch.py` and `src/mnemosyne/parametric.py` for FR-18 and FR-21 local and command-provider boundaries.
- Verification currently passes with `.venv/bin/python -m compileall -q src tests` and `.venv/bin/python -m pytest` collecting 211 tests and returning 178 passing tests plus 33 skipped live-DB tests when `MNEMOSYNE_POSTGRES_DSN` is unset.
- Fresh-schema Docker/Postgres verification passes with `MNEMOSYNE_POSTGRES_DSN=postgresql://... .venv/bin/python -m pytest -q tests/test_postgres_engine_live.py tests/test_shared_engine_contract.py` returning 54 passing tests against the compose database and verifying tenant RLS, SQL FTS, pgvector assertion search, deterministic evidence dense fallback, recursive graph/PPR, explain channels/rails/provenance/activation/read marks/calibration/semantic entropy, tenant-scoped branch names, branch/discard including TMS justification/contradiction pruning for abandoned branch assertions and dependency-only orphan cleanup, branch merge retrieval, bitemporal supersession, tenant isolation, tombstone and hard-delete forget modes, command-backed object key management, lossless evidence-envelope preservation including external session identity, export including calibration sets and entity registry rows, shared local/Postgres evidence/retrieval/explain/direct lexical/vector/graph primitives/justification/contradiction/export-all JSON snapshots/branch/as-of/relation/preference/correction/forget-propagation with entity registry trim/removal, retrieval-filter/deep-graph-isolation/hard-delete-audit/access-telemetry/calibrated-abstention/entity-registry contracts, HTTP-configurable retrieval adapter wiring with strict provider response validation and fail-closed CLI provider health checks, CLI `--backend postgres`, stateless MCP ingestion over tenant-scoped durable Postgres queues, tenant-scoped durable Postgres queue leasing/drain with result persistence, CLI file ingestion through the C2PA verifier adapter with asset binding, metadata-derived text retrieval for externalized payloads, async media extraction over object-store bytes, gated consolidation promotion on Postgres with registry update, Postgres residency-transfer deny/allow policy persistence, and Postgres local fallback sensitivity filtering.
- Code-review blockers partially remediated after `REVIEW.md`: local graph tenant/branch leak fixed, MCP tool-result envelope added, Postgres public string-ID mapping added, Postgres high-level runtime methods added, and Postgres evidence conflict handling now restores durable fields.
- CLI-first runtime tool coverage added for profile context, graph neighbors, prefetch, trajectory logging, failure attribution, lesson induction/promotion, procedure induction/validation, residency-policy inspection, and parametric proposal/evaluation/rollback. MCP dispatch mirrors the same operations through a TOOL_SPEC allowlist, rejects non-object JSON-RPC tool arguments, persists parametric artifacts for MCP rollback, exposes object-store encryption/key-provider/residency/runtime-residency/queue configuration for MCP ingestion, has local and Postgres stateless modes that rebuild engine/tools per call from durable state, supports signed HMAC session binding through `--session-secret`/`--require-session`, and can optionally run through the official Python MCP SDK via `mneme-mcp --sdk` with SDK-level tool listing/call/session-binding tests.
- Command-backed parametric adapter providers now expose a shell-free CLI/MCP boundary for LoRA/test-time-training proposal, evaluation, and rollback, require operator-grade write authorization before trainer calls, strictly require JSON object responses, persist provider metrics/payloads, and enforce local mutation-rate bounds, external-only reward metadata, monotonic-trust/sink restrictions, exact artifact/gate matching, protected-case pass requirements, and margin-over-noise before artifacts are accepted.
- CLI backend selection added with `--backend local|postgres`, `--postgres-dsn`, and `MNEMOSYNE_POSTGRES_DSN`; the live smoke verifies capture/assert/relation/preference/search/deep-search/export/branch/discard through the Postgres CLI path.
- Fail-closed capability enforcement added in `MemoryTools` for preference writes, hard-instruction profile writes, destructive forget operations, fact assertions, relations, proposal branches, supersessions, corrections, branch creation, confirmation, merge, and discard. CLI and facade tests verify denied low-trust writes and allowed trusted writes; CLI `assert`, `relation`, `branch`, `merge`, and `discard` now route through the facade instead of bypassing authorization. Signed HMAC-SHA256 CLI session tokens now bind tenant/user identity plus role/source-trust claims before command execution, fail closed on malformed/tampered/expired/out-of-range tokens, and can supply branch/confirm/merge/discard authorization context without redundant CLI role/trust flags. CLI and MCP session verification now support keyed HMAC keyrings with signed `kid` claims plus fail-closed key-id and session-id revocation lists. MCP JSON-RPC and official SDK transports can require the same signed session contract, reject tenant/user mismatches, and override self-asserted role/trust arguments before authorization.
- Tenant RLS added to `sql/schema.sql` for tenant-owned tables. `PostgresEngine` sets `mnemosyne.tenant_id` before tenant-scoped SQL and requires tenant-scoped branch/merge/discard operations.
- Explicit erasure modes added to local/Postgres forget paths and CLI: `tombstone_recompute` preserves the audit/deletion path while clearing evidence, and `hard_delete_legal` removes the evidence row while propagating derived assertion retraction/trimming. Encrypted local object storage now supports AES-GCM per-object keys, JSON-backed local key management, command-backed KMS/HSM/Vault-style key management, and key shredding after legal hard-delete. CLI and MCP ingestion now enforce configured data residency labels, can require explicit runtime processing residency, fail closed on cross-region processing unless a `source->target` transfer is explicitly allowlisted, and record residency/runtime-residency transfer decisions in evidence access policy. Forget now transitively erases media-derived evidence and retracts/strips assertions, preferences, and relations that depended on source or derived evidence.
- HTTP-compatible embedding and reranker adapters added, with CLI/env wiring for Postgres retrieval (`--embedding-provider`, `--embedding-url`, `--reranker-provider`, `--reranker-url`, model, API-key, dimension, and timeout controls), strict response validation, bearer-token coverage, and nonzero CLI `provider-check` failures. Command media embedding, command object-key/KMS, command media extractor, command parametric providers, and residency policy are now included in `provider-check`, including fail-closed malformed key-provider coverage. Local deterministic providers remain the default for offline verification.
- CLI-first signed-provenance ingestion added with `ingest --file`, modality/media-type controls, signed-provenance JSON/file inputs, `--c2pa-tool`, trusted issuer/root mapping, `--provenance-trust-policy`, fail-closed verifier errors, report-to-asset binding checks, scoped trusted-signer and certificate-root enforcement for valid-but-untrusted manifests, provenance validity/trust/binding capability tags, and local/Postgres tests. Quarantined evidence is now excluded from default retrieval unless the caller explicitly opts into `include_quarantined`.
- Trust-tier semantics now match the blueprint direction: tier `0` is direct-user/highest trust and tier `5` is untrusted external. Retrieval/write gates, confidence weighting, provenance deltas, model defaults, SQL defaults, CLI defaults, and local/Postgres tests were migrated to that scale.
- Ingest classification now identifies actor/source trust, applies capability/taint tags, detects email/phone/SSN-style PII, raises sensitivity/access policy metadata, marks untrusted imperative content as data-only without instruction authority, and enqueues an ordered persisted local consolidation job after new evidence is appended. The worker now records structured pass results, extracts simple direct-user fact candidates, promotes them only through the gate, and refuses data-only untrusted fact-shaped evidence.
- Consolidation now resolves deterministic entity keys and executes summarizer, lesson_distiller, and skill_inducer role outputs for local fact candidates; distilled lessons and checklist procedures persist through runtime learning state and are verified through direct worker and CLI `ops-report` tests.
- The local queue now supports drain semantics and typed runtime handlers for consolidation, calibration, lifecycle sweep, eval suite, and observability snapshot jobs, with CLI `queue-enqueue`, `queue-drain`, `queue-snapshot`, and `consolidate-once` surfaces.
- Protected regression cases can now be persisted with CLI `gate-case-add/list`; consolidation handlers load the saved suite and block promotion when a protected case fails.
- Promotion gates now route branch, merge, and discard through tenant-aware engine calls when available, with live Postgres coverage for gated direct-user fact promotion.
- Externalized multimodal evidence now indexes metadata-derived `derived_text`, OCR text, transcripts, captions, alt text, and descriptions while keeping raw bytes in object storage. When derived text is absent, ingest queues an async `media_extract` job; the worker reads object-store bytes through an operator-configured command extractor, appends derived evidence, and queues consolidation for the derived row. A shell-free command media embedding provider boundary accepts image/audio/video bytes through a temp file plus JSON stdin, requires strict JSON embedding output, normalizes vector dimensions, and is covered by direct adapter and CLI `provider-check` tests. Local, CLI, and live Postgres tests verify retrieval over derived media text.
- CLI `ops-report` now emits a tenant dashboard snapshot with memory counts, audit/deletion counts, queue state including running jobs, learning lesson diversity, proxy-vs-true gap, and tripwire status, and `--dashboard-html` writes the snapshot as a static HTML dashboard artifact with retrieval, calibration, learning, and gate/eval sections.

## Remaining Exact-Parity Gaps

| Area | Status | Required For 1:1 Blueprint Parity |
|---|---|---|
| Production Postgres retrieval | Partial | SQL FTS, pgvector assertion retrieval, deterministic evidence dense fallback, recursive graph/PPR, activation-scored final ranking, assertion read-time strengthening telemetry, conformal calibration-backed abstention, semantic-entropy explain output, HTTP-compatible embedding/reranker adapters, strict provider response validation, fail-closed CLI provider health checks, live DB smoke, and shared local/Postgres evidence/retrieval/explain/direct lexical/vector/graph primitives/justification/contradiction/export-all JSON snapshots/branch/as-of/relation/preference/correction/forget-propagation/retrieval-filter/deep-graph-isolation/access-telemetry/calibrated-abstention contracts are implemented. Remaining work: validate real production provider deployments and calibration datasets, add ParadeDB BM25 where needed, AGE/specialist graph adapters where needed, and broaden the shared parity suite across every remaining engine method and production adapter. |
| Tenant isolation and auth | Partial | Capability checks now protect preference, hard-instruction profile, destructive forget, fact, relation, proposal, supersession, correction, branch, merge, confirm, and discard writes; signed CLI and MCP session tokens bind tenant/user/role/source-trust claims before privileged operations; keyed HMAC keyrings with `kid` claims plus revoked key/session-id lists are covered; Postgres tenant RLS is implemented and live-smoked; MCP `tools/call` has opt-in shared-token enforcement plus required signed-session mode for JSON-RPC and SDK transports. Trust-tier direction now matches the blueprint scale. Remaining work: deployment-grade auth integration with production identity providers, hosted session issuance, TLS/client identity, and policy operations. |
| CLI/MCP runtime coverage | Partial | CLI-first coverage now exists for profile, graph, trajectory, lesson, procedure, prefetch, parametric flows, local/Postgres backend selection, and blueprint-facing ABI aliases; MCP tools expose signature-derived JSON schemas, runtime JSON-RPC tests, TOOL_SPEC-allowlisted dispatch, auth-token gating, signed-session binding, non-object argument rejection, parametric rollback persistence, local stateless mode over durable JSON/runtime state, stateless Postgres MCP ingestion over tenant-scoped durable queues, and optional official Python MCP SDK stdio mode with SDK handler smoke/session coverage. Remaining work is hosted MCP deployment validation and broader production stateless soak testing. |
| Consolidation role pipeline | Partial | Ingest now queues ordered persisted local consolidation pass payloads; the CLI can inspect/enqueue/drain local jobs; `--queue-backend postgres` and `mneme-mcp --queue-backend postgres` provide tenant-scoped durable row leasing, retry/dead/complete lifecycle, and result persistence; the worker records structured pass results, promotes simple direct-user fact candidates only through the gate, refuses data-only untrusted facts, resolves deterministic entity keys, updates a durable tenant-scoped entity registry after successful promotion, executes deterministic summarizer, lesson_distiller, and skill_inducer outputs persisted in runtime learning state, and loads persisted protected regression gate cases before promotion. Local handlers exist for calibration, lifecycle, eval, and observability jobs. Remaining work includes hosted worker supervision/deployment operations, richer production entity resolution beyond deterministic keys, incremental recompute, richer model-backed extraction/summarization, and deployment-grade protected-suite management. |
| Signed provenance | Partial | CLI and ingestion can invoke a `c2patool`-style verifier, map trusted issuers and certificate-root SHA-256 fingerprints, load scoped JSON trust policies, fail closed on verifier errors or policy signer/root/scope rejection, require verifier reports to bind back to the exact asset by SHA-256/path, propagate provenance validity/trust/binding capability tags, and hide quarantined evidence from default retrieval. Remaining work: validate against real production C2PA trust roots and broaden deployment trust-root operations. |
| Multimodal retrieval | Partial | Metadata-derived OCR/transcript/caption/alt/description text is indexed for externalized object payloads and verified locally, through the CLI, and against live Postgres. Async `media_extract` can invoke an operator-configured extractor command over object-store bytes and append derived evidence without mutating the source; CLI `provider-check` validates the configured command extractor contract. A command-backed media embedding provider boundary now covers image/audio/video bytes behind strict JSON vector output and provider-check validation. Remaining work: production extractor/embedder deployment validation and richer retrieval over raw externalized media objects. |
| Privacy and erasure | Partial | Tombstone recompute and legal hard-delete modes now exist locally and in Postgres with live CLI coverage. Local encrypted object storage now supports AES-GCM per-object keys, a JSON-backed key manager, a command-backed KMS/HSM/Vault-style key manager, provider-check key-cycle validation, and key shredding after legal hard-delete. CLI and MCP ingestion enforce configured residency labels, strict runtime processing residency when enabled, and explicit cross-region `source->target` transfer allowlists, then record the residency decision in evidence access policies. CLI/MCP can now inspect the active residency policy, and `provider-check` reports it alongside provider contracts. Forget transitively erases media-derived evidence and retracts/strips dependent assertions, preferences, relations, and entity registry rows. Remaining work: validate against real production KMS deployments and production residency-policy operations. |
| Observability dashboards | Partial | CLI `ops-report` exports tenant counts, audit/deletion counts, queue state, learning diversity, proxy-vs-true gap, tripwire status, durable retrieval request/channel/abstention counters, rolling p95 latency metrics, calibration job/abstention/threshold metrics, gate promotion/rollback counters, open-contradiction backlog tripwires, and a static `--dashboard-html` artifact from runtime state/exported memory. Renderer and packaged-CLI smoke tests cover memory, queue, retrieval, calibration, learning, gate/eval, and bounded JSON dashboard sections. Remaining work: deployment hosting/packaging and production dashboard integration. |
| Parametric tier | Partial | Local isolated shadow artifacts are persisted to a sidecar JSON store, exposed through CLI/MCP proposal/evaluation/rollback surfaces, and carry rollback metadata. A command-backed LoRA/test-time-training provider boundary now invokes external trainer/rollback adapters without a shell, requires operator-grade authorization, persists provider metrics/payloads, validates the same persisted artifact URI before promotion, and enforces local mutation-rate/reward/sink/gate rails. Remaining work: validate real trainer deployments, separate immutable-rail service/credentials in production, protected-suite deployment management, and production rollback orchestration. |
| Live parity suite | Partial | Engine and CLI live Postgres smokes now pass in Docker; the live suite now covers bitemporal supersession, tenant isolation, tenant-scoped branch names and branch metadata export shape, branch merge retrieval, shared local/Postgres evidence/retrieval/direct lexical/vector/graph primitives/justification/contradiction/export-all JSON snapshots/branch/as-of/relation/preference/correction/forget-propagation/retrieval-filter/deep-graph-isolation/hard-delete-audit/access-telemetry/calibrated-abstention/entity-registry contracts, branch discard TMS pruning for abandoned justifications/contradictions including dependency-only orphan cleanup, lossless evidence-envelope preservation including external session identity, asset-bound C2PA ingest, externalized payload derived-text retrieval, async media extraction, transitive projection erasure across assertions/preferences/relations/entity registry rows, CLI backend use, gated consolidation, fallback sensitivity filtering, and Postgres-persisted cross-region residency transfer policy. Remaining work: expand to every remaining engine/runtime method with optional production adapters enabled. |

## Supersession Note

The earlier `.planning/v1.0-MILESTONE-AUDIT.md` remains useful as evidence that the local deterministic scaffold passed its original milestone suite. It is not sufficient evidence for the user's current exact 1:1 blueprint parity objective.

## Next Required Implementation Slice

1. Continue expanding the live Docker/Postgres smoke into the full shared contract suite.
2. Wire production embedding, cross-encoder, and image/audio embedding providers behind the existing adapter boundaries; run provider checks against real deployments.
3. Add hosted MCP deployment validation and broader production stateless soak tests.
4. Replace local HMAC session-secret issuance with deployment-grade authorization integration before making any production multi-tenant claim.
5. Validate real isolated LoRA/test-time-training deployments behind the command-backed parametric provider boundary, including protected-suite operations and production rollback orchestration.
