---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: attested
stopped_at: null
last_updated: "2026-07-09T04:05:00Z"
last_activity: 2026-06-20 — Added CLI-first runtime coverage, selectable CLI Postgres backend support, Postgres retrieval parity for SQL FTS/pgvector/graph/explain, shared local/Postgres engine contract coverage, durable Postgres queue leasing/drain support, fail-closed capability enforcement, Postgres tenant RLS, explicit erasure modes, HTTP-compatible embedding/reranker provider adapters, strict HTTP provider response validation, fail-closed CLI `provider-check` health probes for embedding/reranker/media-extractor/object-key-manager/parametric-provider contracts plus residency-policy reporting, and manifest-backed provider deployment gates that require named checks, resolve secrets from environment variables, and fail closed on forbidden local retrieval adapters, CLI file ingestion through a C2PA verifier adapter with asset hash binding and trusted certificate-root enforcement, scoped JSON trust-policy enforcement, provenance validity/trust/binding capability tags, AES-GCM encrypted local object storage with JSON and command-backed key management plus legal hard-delete key shredding, configurable fail-closed ingestion data-residency enforcement, strict runtime-residency enforcement, explicit runtime-residency and cross-region `source->target` transfer allowlists, CLI/MCP residency-policy inspection, MCP object-store encryption/key-provider/residency/runtime-residency config parity, transitive source-to-derived evidence erasure for media extraction outputs plus assertion/preference/relation projection recompute, blueprint-correct trust-tier semantics, deterministic ingest classification, persisted local queue-backed consolidation jobs from ingestion, gated deterministic fact extraction for direct-user evidence, persisted protected regression gate cases, local queued calibration/lifecycle/eval/observability job handlers, tenant-aware promotion gates for Postgres, blueprint-facing CLI/MCP ABI aliases, signature-derived MCP tool schemas, optional MCP tools/call auth, JSON-RPC/SDK MCP tool-call input schema validation including explicit optional-null support, CLI `tools` MCP inputSchema parity, optional official MCP Python SDK stdio mode with schema validation and auth-token enforcement, expanded live Postgres shared-contract coverage for bitemporal supersession, tenant isolation, tenant-scoped branch names, branch merge retrieval plus exported merge-log parity, active-only temporal graph/PPR relation filtering with historical `as_of` support, stored evidence pgvector retrieval with null-embedding legacy fallback, local export-all calibration/entity/tenant snapshot parity with erased evidence filtered, branch discard TMS justification/contradiction pruning including dependency-only orphan cleanup, immutable evidence CID semantics, tombstone replay prevention, lossless evidence-envelope preservation including external session identity, explain channels/rails/provenance, metadata-derived OCR/transcript/caption indexing for externalized object evidence, an async `media_extract` provider job that appends derived media text without mutating source evidence, fail-closed write-path authorization for fact/relation/proposal/supersession/correction/parametric writes, explicit branch/promotion/discard authorization through `MemoryTools` and CLI, capability-mediated lesson promotion plus procedure validation/promotion/rollback through CLI and MCP, a CLI `ops-report` JSON snapshot with durable retrieval-channel, p95 latency, gate promotion/rollback, and contradiction-backlog tripwire metrics, static HTML `ops-report --dashboard-html` export with retrieval/calibration/learning/gate sections and renderer/CLI smoke coverage, deterministic consolidation entity resolution plus summarizer/lesson-distiller/skill-inducer role outputs persisted through runtime learning state, a local isolated parametric artifact sidecar with CLI/MCP rollback metadata plus operator-authorized command-backed LoRA/test-time-training provider proposal/evaluation/rollback and structural local rails, TOOL_SPEC-allowlisted MCP dispatch with JSON-RPC non-object argument rejection, generated schema validation, local stateless MCP mode that rebuilds engine/tools per call from durable state, hosted HTTP JSON-RPC MCP transport with bearer/session-header binding and stateless restart coverage, tenant-scoped Postgres runtime state for CLI/MCP profile and learning tools mirrored into `preferences`, `user_latent`, `trajectories`, `lessons`, and `procedures`, JWKS cache/rotation support for CLI/hosted MCP OIDC session exchange, fail-closed OIDC authz policy mapping, redacted `idp-authz-policy-check` validation/audit summaries, fingerprint-acknowledged `idp-authz-policy-rollout-check` diffs with claim-simulation change gates, command-backed session-secret custody for CLI/MCP signing and verification plus provider-check OIDC/JWKS/authz-policy preflight, and hosted HTTP TLS plus client-certificate enforcement. The test suite now collects 287 tests and has 248 passing tests plus 39 skipped live-DB tests locally; a fresh-schema DSN-backed live Postgres run passes 63 engine/CLI/consolidation/shared-contract smokes including persisted residency-transfer policy, stored evidence pgvector retrieval, null-embedding legacy fallback, Postgres MCP runtime profile/learning state persistence, temporal graph validity filtering, and merge-log export. Historical note: strict audit was still open for exact production parity.
progress:
  total_phases: 10
  completed_phases: 10
  total_plans: 19
  completed_plans: 19
  percent: 100
---

# Project State

## Release Attestation — Tier-B Production Evidence (2026-07-07)

Tier-B is CLOSED. A genuine operator-run production capture over the live self-hosted stack produced a fully green 29-command `deployment-soak` (0 required failures), `release-audit` `ok:true` (0 findings; 29 required commands + 15 required provider checks), and an offline `production-evidence-verify` `ok:true` (0 findings; matching out-of-band fingerprint). This flips all 10 `.planning/STRICT-BLUEPRINT-PARITY-AUDIT.md` rows Partial->Done and supersedes the earlier open strict-audit note.

- Bundle fingerprint: `sha256:6dc117d6bb95e7a683915d432b2d2b21997133e9bfbd53624427a7317eeb2271`
- captured_at: `2026-07-07T15:53:18Z`
- Verifier report: `mnemosyne-evidence-out/verify-bc10.json`
- Evidence capture code: `main@58e722c` (adds the `production-evidence-verify` executable tool-artifact rewrite fix, test-first)
- Current documentation head: `main@02337fa2f334b7421673585de9d8c60df6c29a7a` plus this cleanup pass.
- Gate: real evidence only — no validator weakened; no row flipped without a genuine `production-evidence-verify` `ok:true`.

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-19)

**Core value:** Build a memory compiler with lossless evidence, typed projections, safe retrieval, branchable updates, and gated self-improvement.
**Current focus:** Strict blueprint parity gap closure

## Current Position

Phase: Strict parity continuation after Phase 5 scaffold
Plan: Exact blueprint parity audit and runtime gap closure
Status: Tier-B production evidence attested; exact 1:1 blueprint parity rows B1-B10 are Done.
Current status (2026-07-07): Mandatory Tier A source wirings are closed on `main`; Phase 7 is gate-proven; Phase 8 self-hosted production evidence is complete. The former strict-parity blocker, Tier-B production operator evidence against deployed infrastructure, is closed by `capture-bc10`.

Latest checkpoint (2026-07-09): Phase 9 is open as the post-attestation
performance/refactoring continuation and Plans 09-01 through 09-06 are complete.
Retrieval channel parallelism now defaults from capability tier when
`MNEMOSYNE_PARALLEL_CHANNELS` is absent (`floor` off, `standard` and above on),
while explicit operator env values still win. SQLite scan-oracle memo hydration
now coalesces under the engine lock so default parallel dense/lexical retrieval
still shares one candidate-scope SQL hydration. The provider/cache docs now
match the landed process-local HTTP embedding LRU, retrieval result LRU, and
Postgres positive calibration lookup cache; `MNEMOSYNE_EMBEDDING_CACHE_SIZE`
and `MNEMOSYNE_EMBEDDING_MODEL_REVISION` are in the drift ledger. MMR/PPR native
kernels release the GIL around Rust-owned compute loops without changing
arithmetic/order. Hot Postgres FTS and pgvector retrieval SELECTs now request
Psycopg server-side preparation behind `MNEMOSYNE_PG_PREPARE_HOT_QUERIES=0`,
while tenant and HNSW `set_config(..., true)` calls remain explicit
transaction-local statements. The provider contract now has a shared fixture
(`tests/fixtures/provider_contract.json`) that gates the Python embedding
service, Python HTTP adapters, and Rust `mneme-providers` sidecar through a
dedicated `provider-conformance` CI job; the sidecar accepts list-input `/embed`
for wire compatibility. This does not flip provider defaults or supply TEI /
sidecar bake-off evidence. Postgres `embedding_partition='none'` is now
source-modeled as a no-vector partition with btree lifecycle/audit coverage,
the embeddable-null dense fallback has a partial index and excludes `none`
before Python embedding, and `infra/postgres/postgresql-perf.conf` versions the
conservative server-tuning candidate without silently applying it. Live tuning
application, before/after benchmark rows, null-embedding production
backfill/alert evidence, halfvec/pgvectorscale, provider bake-off/default
selection, and operator runtime-flip evidence remain open. Stateless MCP mode
now reuses same-scope warmed tool bundles while local/SQLite bundles invalidate
on durable store or runtime-state file changes. Focused
retrieval/capability, provider-cache, provider-contract, Postgres perf-lane,
cargo, native parity, stateless MCP runtime-surface, ruff, `git diff --check`,
GSD consistency, and local pytest suites pass.

**Historical-log rule:** checkpoint paragraphs below this point are retained as
dated changelog/provenance. Any statement that rows were Partial, evidence was
pending, or strict audit was open is superseded by the Release Attestation above.

Latest checkpoint (2026-07-04, later): Phase 8 gained four real source rails in
one pass. JWKS kid-sha256 pinning is enforced by `OidcJwtVerifier` and exposed
through the CLI and hosted MCP, with `idp-jwks-live-check` emitting
`jwks.kid_pinning` evidence and release-audit rejecting unpinned IdP evidence.
A new `postgres-role-check` command performs the live role-separation probe
roles.sql mandates (rolsuper/rolbypassrls, group membership, app
destructive-write denial, consolidator sole-write, audit_log append-only) and
joined the required production release command set with its own release-audit
evidence gate. Evidence bundles can now be signed by a collector-only Ed25519
key (`evidence-keygen`/`evidence-sign`/`evidence-verify`), release-audit
verifies signatures fail-closed via `--require-signed-evidence`, and the
production capture script signs automatically when the collector key is
configured. The tamper-evident audit rail gained a real hash-chain
producer/verifier (`audit-chain-export`/`audit-chain-verify`) anchored by a
Vault-transit command HMAC (or an explicitly non-production local secret) that
detects entry rewrites, truncation, unanchored appends, and forged heads.
None of this creates operator artifacts; deployed-infrastructure captures
remain the sole blocker for Tier-B rows.

Latest checkpoint (2026-07-04): Production release-audit now has an explicit
source-side TLS lifecycle evidence gate for the Phase 8.3 step-ca/Caddy row.
`tls-lifecycle-ops-check` retained output must prove production/operator
validation, non-local CA/ACME issuance, hostname and chain evidence, renewal
validity/automation, HTTPS non-local deployment with matching issued cert,
non-local private-key custody, lifecycle monitoring, redaction, and a report
fingerprint. Focused TLS/release-audit verification passes locally. This does
not create operator TLS lifecycle artifacts; real production TLS/step-ca/Caddy
evidence remains required.

Latest checkpoint (2026-07-03): Production release-audit now has an explicit
source-side parametric trainer evidence gate for the B9/FR-21 strict-parity
path. `parametric-trainer-check` retained output must prove a non-local trainer
provider, runtime-state protected suite with fingerprint, promoted
zero-regression protected gate, rollback fingerprint, HTTPS deployment with
matching artifact/suite/rollback hashes, external-only rail report, bounded
mutation/reward/sink metrics, report fingerprint, and raw training
data/credential/artifact redaction. Focused parametric/release-audit
verification passes locally. At that checkpoint, this did not create trainer
artifacts by itself; ADR-002 and the later `capture-bc10` evidence now close the
B9 Tier-B path through the accepted CPU-parametric profile.

Latest checkpoint (2026-07-03): Production release-audit now has an explicit
source-side retrieval evidence gate for the Phase 8.2 retrieval row.
`retrieval-ops-check` retained output must prove non-local Postgres
lexical/graph backends, provider forbid-local posture, adapter probe coverage
for graph/lexical/reranker/vector, production calibrated retrieval cases,
adapter fingerprints, latency bounds, and raw query/embedding/document/credential
redaction. Focused retrieval/release-audit verification passes locally. At that
checkpoint, operator retrieval artifacts still had to be captured; `capture-bc10`
later supplied the retained production retrieval evidence.

Latest checkpoint (2026-07-03): The active branch is
`phase3/providers-consolidation`, synchronized at
`e7459090156b2e84df5bbf34bfc549719ffc0b6d`, with GitHub CI run
`28682755372` passing for that head before this continuation. The GSD roadmap
counters still match `.planning/ROADMAP.md`: 7 of 9 phases complete and 14 of
15 tracked plan files complete, with Phase 6 still production-evidence-gated
and Phase 8 still open. Fresh Tier-B custody refresh against
`/Users/admin/mnemosyne-tier-b-custody` still reports `ready_for_capture=false`:
six provider manifest env refs are present as key names in the external runtime
env file but have blank values, and 23 production input artifacts are missing
from the packet's external `input-artifacts/` directory. Phase 8 stale
completion/reconciliation docs named by the self-hosted handoff were archived
under `docs/_archive/2026-07-03/` for audit/history only. Phase 8.1 file/profile
deliverables are now checked off: both profiles point at the retained
`forbid_local:true` provider-manifest gate, production compose includes internal
`vmalert` beside VictoriaMetrics and Grafana, and the focused compose policy
tests pass locally. Phase 8.5 supply-chain hardening now also has source-owned
digest pinning: every committed production registry image is tag+digest pinned
in `infra/docker-compose.prod.yml`, and the compose policy suite rejects
tag-only registry images. It now also ships
`infra/scripts/verify-supply-chain.sh`, a fail-closed deploy-time gate that
requires an external output directory, gitleaks git/tree scans, Trivy
filesystem/image scans, Syft SBOMs, Grype vulnerability scans, and cosign
verification for tag+digest registry images. Scanner/signature output must
still be operator-retained before the parent item can be checked off. Do not copy ignored
local files such as
`production-inputs/provenance-trust-suite.json` into the custody packet;
production rows remain Partial until real operator-captured bundles pass
release audit and offline custody verification.

Latest checkpoint (2026-07-03): Phase 8.5 network source hardening now routes
the Vault transit provider, Vault session-secret provider, internal Ollama
role-LLM provider, and embedding self-test through the shared
`mnemosyne.network_safety` URL validation and DNS-pinned opener. `safe_urlopen`
accepts a custom TLS context so Vault can keep using the step-ca CA bundle
without bypassing the shared redirect/DNS guard. Internal private-address
resolution remains default-deny unless the host is explicitly allowlisted
through `MNEMOSYNE_VAULT_ALLOWED_INTERNAL_HOSTS` or
`MNEMOSYNE_OLLAMA_ALLOWED_INTERNAL_HOSTS`. `tests/test_network_egress_policy.py`
now fails on new raw outbound HTTP clients unless the call is routed through
the guard or explicitly allowlisted with its trust boundary. This is
source-control progress only: the container-local Docker health check and the
official MCP SDK `httpx.AsyncClient` transport remain documented exceptions,
and real default-deny egress/proxy/firewall evidence is still operator-run.

Latest checkpoint (2026-06-30): The active goal remains unpaused and the
professional continuation objective is now governed by the repo-owned operating
contract plus live root/HEAD/CI refresh. Source work is allowed only when it
removes a concrete production-capture blocker or security defect. Security
control 12 source hardening is narrowed: hosted JSON-RPC/SSE probes already use
`network_safety`, and hosted StreamableHTTP now validates the target URL before
the official MCP SDK/httpx client can open a socket. This closes a real
SSRF-class source gap in the hosted MCP evidence path only; full production
egress-deny, raw-socket lint coverage, default-deny proxy/firewall evidence,
and all Tier-B operator bundles remain required.

Latest checkpoint (2026-06-30): The active goal operating contract has been refreshed to make the
professional continuation objective explicit: prioritize real Tier-B production capture, custody,
operator evidence, and source defects that block that path; avoid redundant generic gates and false
completion language. The Postgres least-privilege source gap is now narrowed with a shared safe-role
guard for `PostgresEngine`, `PostgresRuntimeState`, and `PostgresQueue`; `MNEMOSYNE_POSTGRES_REQUIRE_SAFE_ROLE=1`
enables it for CLI/self-hosted profile, and the MCP production profile enables it automatically for
Postgres engine/state/queue surfaces. At this historical checkpoint it was production pre-capture
hardening only. The later retained `capture-bc10` bundle now supplies the reviewed role/grant evidence
for the Tier-B production attestation.
Supersession note: the historical "Last activity: 2026-06-20" paragraph below is retained as changelog context only. Its local 287-test count and open-code wording are superseded by the 2026-06-25 checkpoints below and the 2026-07-07 Tier-B production attestation.

Historical checkpoint (2026-06-30; superseded by 2026-07-07 Tier-B attestation): The active goal remained unpaused under the v5
production-evidence contract, and Phase 8 is now registered in the machine
roadmap, milestone ledger, and tracked handoff/security files. The Phase 8
entries remain subordinate to `.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md`:
self-hosted/no-GPU was originally thought to evidence all rows except B9, while
B9/FR-21 was routed to cloud/GPU trainer evidence or an explicit ADR before
strict v1.0 parity could reach 100%. ADR-002's amended CPU-parametric path and
the 2026-07-07 retained evidence supersede that assumption.

Historical checkpoint (2026-06-30; superseded by 2026-07-07 Tier-B attestation): Phase 8 security must-do enforcement has begun
in source. `MnemosyneMcpServer` now supports `MNEMOSYNE_MCP_PRODUCTION_PROFILE=1`
and refuses production-profile startup unless signed sessions are required, a
session verifier is configured, object-store encryption is `aesgcm`, and
object-key custody is command-backed. `infra/profiles/self-hosted.env` and the
production compose API service enable that profile. This was pre-capture
hardening; the retained `capture-bc10` bundle later passed release audit and
offline custody verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal is unpaused and the repo-owned
goal text has been revised to the v5 production-evidence execution contract.
Because the goal tracker does not safely rewrite active objective text in-place,
`.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md` is the editable continuation
objective and `.planning/TIER-B-TO-100-AGENT-PROMPT.md` is the handoff prompt.
At this historical checkpoint, `/Users/admin/Mnemosyne` was the canonical
checkout and `main`/`origin/main` were synchronized at a prior green baseline.
The improved contract explicitly stops the
handoff-plumbing loop: new reports or gates are off-mission unless a fresh
Tier-B packet exposes a real missing operator route. The next substantive
progress unit was real external input assembly and production capture. That
later happened in `capture-bc10`, which passed manifest-bound release audit and
offline custody verification.

Historical checkpoint (2026-06-30; superseded by ADR-002 amendment and
2026-07-07 Tier-B attestation): New self-hosted-first architecture planning
artifacts were ingested and corrected before tracking. They now sit subordinate
to the active Tier-B production-evidence contract: self-hosted/no-GPU is a
preferred baseline for rows it can honestly evidence, but it cannot mark B9 or
strict 100% complete by declaration. At that time B9 required real cloud/GPU
trainer evidence through the same custody path, or a future explicit ADR
changing the strict audit; ADR-002 now records the accepted CPU-parametric path.
The same correction preserves native recursive-PPR as the quality
path while requiring AGE-sidecar evidence or an ADR if the active B1 audit still
demands AGE, and forbids generic new gate work unless a fresh capture exposes a
real attestation-only gap.

Latest checkpoint (2026-06-30): The self-hosted Phase 8 infra scaffold was
ingested as additive production-profile enablement, not production evidence.
`infra/docker-compose.prod.yml`, `infra/profiles/`, `infra/prod/`, Caddy
ingress config, and Postgres role-separation DDL define a hardened no-GPU
self-hosted baseline that later produced B1-B10 evidence through the amended
ADR-002 CPU-parametric B9 path. Local validation covered `bash -n infra/prod/bootstrap.sh`,
`docker compose -f infra/docker-compose.prod.yml config`, and a no-secret scan.
This scaffold later received real bring-up, runtime-specific operator values,
wrapper capture, release audit, and offline custody verification; strict-audit
rows flipped only after the 2026-07-07 retained evidence passed.

Historical checkpoint (2026-06-30; superseded by the v5 checkpoint above): The
active goal was confirmed active, not paused.
`.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md` and
`.planning/TIER-B-TO-100-AGENT-PROMPT.md` carried the prompt-engineered v4
continuation objective: work autonomously from the verified live checkout,
prioritize only changes that remove real Tier-B capture/custody blockers, avoid
stale percentage or run-id drift, and refuse to treat generated readiness
metadata, local tests, or CI as production evidence. At that checkpoint, the
completion standard still required wrapper-captured production bundles to pass
manifest-bound release audit and offline custody verification before Tier-C
real-path SLO/rail sign-off.

Historical checkpoint (2026-06-30; superseded by the v5 checkpoint above): The
continuation baseline was refreshed before this grouped-inventory edit after
the folder move/path ambiguity.
`/Users/admin/Mnemosyne` is the canonical checkout;
`/Users/admin/Desktop/Mnemosyne` did not exist as a Git worktree during the
resume check. At that pre-edit checkpoint, `main` and `origin/main` were
synchronized at
`2b47989e27af9ec911eb08b43589979e1eb6c934`, and GitHub CI run `28475668973`
passed for that head. `docs/blueprint/cognitive-architecture/CODEX-HANDOFF.md`
records it as a pre-edit resume checkpoint, not as durable current truth, and
demotes older `9621971`/`03f2611` references to historical context. This is
source/CI synchronization only, not Tier-B production evidence.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B gap reports now expose row-grouped
operator input inventory for all three top-level edit surfaces. In addition to
the existing runtime env primary-row provider-ref groups,
`operator_input_inventory.production_render_env` now records
`global_missing`, `render_env_names_by_row`, and
`missing_render_env_by_row`, while `operator_input_inventory.input_artifacts`
records `input_artifacts_by_row` and `missing_input_artifacts_by_row`. The
Markdown gap report surfaces missing render placeholders and input artifacts by
row beside the existing provider-ref grouping. This removes another manual
report-join step only; it does not create production values, artifacts, or
strict-audit evidence.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B row action plans now distinguish global
non-secret render placeholders from row-scoped render placeholders and sort rows
numerically from `B1` through `B10`. This fixes an operator-facing ambiguity
where rows such as B1/B10 could show `Render=0` even while global render
metadata (`MNEMOSYNE_PROD_CHANGE_TICKET`,
`MNEMOSYNE_PROD_OPERATOR_NAME`) still blocked every row. The row plan remains a
preparation aid only; readiness and evidence acceptance still require real
operator inputs, wrapper capture, release audit, and offline custody review.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B capture-blocker lane lists and shared
provider-stack phase lanes now use the same numeric `B1` through `B10` ordering
as the row action plan. This removes the remaining lexicographic `B1`, `B10`,
`B2` ordering from generated packet handoffs and keeps row assignment readable
without changing readiness semantics or evidence acceptance.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody packet stdout now names all
three operator input surfaces instead of only `production-render.env` and
`input-artifacts/`. The machine-readable summary includes the generated
`runtime_env_example` path and the external `runtime_env_file_placeholder`, and
its `next` guidance routes operators through render-env, provider-env/runtime,
and input-artifact action plans before running capture commands. This is
handoff metadata only; provider values remain external and redacted.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B row action plans now separate
row-owned provider-manifest env refs from shared provider-stack blockers. The
generated `reports/row-action-plan.{json,md}` includes
`primary_missing_provider_manifest_env_refs`,
`shared_missing_provider_manifest_env_refs`, provider-ref roles, and blocker
counts for both classes, while `reports/provider-env-action-plan.{json,md}`
adds route sources for both release-manifest and row-runbook-owned provider
checks. Fresh packet smoke at `/tmp/mnemosyne-tierb-row-provider-B4bDJa/packet`
confirmed B1 owns embedding/reranker refs, B2 owns OIDC/session-secret refs, B7
owns object-key/residency refs, and B9 owns parametric refs while shared
blocking remains visible. This improves operator assignment only; no production
values or evidence were retained at that checkpoint.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B gap reports now group runtime
provider-manifest env refs by primary row in
`operator_input_inventory.runtime_env_file`. Both
`provider_manifest_env_refs_by_primary_row` and
`missing_provider_manifest_env_refs_by_primary_row` are generated from the
provider env action plan, so the top-level runtime env file handoff no longer
forces operators to join a flat 24-name list against row plans manually. Fresh
packet smoke at `/tmp/mnemosyne-tierb-runtime-inventory-6DyyyV/packet`
confirmed the grouped B1/B2/B4/B6/B7/B9 ownership appears in JSON and Markdown
without retaining provider values or runtime env-file paths. This was readiness
metadata only at that checkpoint; real production capture and offline custody
verification passed later in `capture-bc10`.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B provider-stack handoff now carries
complete primary row ownership for shared provider-manifest env refs. The
generated `reports/provider-env-action-plan.{json,md}` no longer leaves
OIDC/session-secret, object-key/residency-policy, or parametric env names with
blank primary rows; they route to B2, B7, and B9 respectively while preserving
the shared affected-row blast radius for B1/B2/B4/B6/B7/B9/B10 and retaining no
values. This improved shared provider-stack operator assignment only; provider
values and production bundles were still uncaptured at that checkpoint.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal remains unpaused and has been
sharpened around the real parity blocker: source-complete local work is not
enough, and progress now means reducing friction to real Tier-B production
capture without retaining secrets, fabricating artifacts, or flipping rows from
routing metadata alone. Tier-B custody packets now generate
`reports/render-env-action-plan.{json,md}` beside the row, provider, artifact,
and next-command reports. The render-env plan maps each non-secret
`production-render.env` placeholder to affected B-rows and the packet-local env
file while explicitly recording no values. This was an operator handoff for the
first blocker class only; the production bundle requirement was later satisfied
by `capture-bc10`.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal remains unpaused and the
repo-owned operating contract now states the move-the-needle rule explicitly:
new work must remove a live Tier-B operator-capture blocker, fix a source defect
exposed by that path, or clean stale instructions that would cause wrong-root,
wrong-artifact, wrong-custody, or false-status drift. Tier-B custody packets now
extend `reports/input-artifact-contracts.{json,md}` with advisory validator
section/check hints such as `retrieval-ops-check`'s `provider_check`,
`retrieval`, `adapter_probes`, `calibration`, and `redaction` sections. These
hints are generated beside the existing artifact kind, consuming validator,
release-audit output-key, and minimum-operator contracts, reducing the need for
operators to inspect CLI source before authoring real ops bundles. They are not
schemas, samples, placeholder files, production evidence, or a row-flip
mechanism. At that checkpoint, wrapper-captured production bundles still needed
to pass manifest-bound release audit and offline custody verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal remains unpaused; because the
goal tool does not safely rewrite active objective text, the repo-owned
operating contract is the edited prompt-engineered continuation objective.
The current objective is narrowed around real Tier-B operator evidence and the
failure modes that previously stalled progress: stale percentage chasing,
redundant gate additions, placeholder artifacts, wrong-root status claims, and
completion claims without current source/CI/evidence. Tier-B custody packets
now generate `reports/input-artifact-contracts.{json,md}` beside the worklist,
row action plan, provider-env action plan, and validation script. The contract
report records each required artifact's kind, packet path, row ownership,
consuming validators, release-audit output-key contract, and minimum operator
contract. This moves the next operator pass closer by removing another manual
code-reading step, but it is still a preparation aid only: B1-B10 remain
Partial until real production bundles pass the wrapper-captured release audit
and offline custody verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal remains unpaused and has been
revised into a concrete Tier-B provider-stack execution contract instead of a
generic "continue" prompt. Tier-B custody packets now generate
`reports/provider-env-action-plan.{json,md}` from
`input-artifacts/provider-manifest.production.json`, mapping every provider env
ref to its manifest JSON path, provider check, primary row ownership, affected
shared provider-check rows, redacted readiness status, and next action. The CLI
summary, packet README, gap report, infra docs, and active operating contract
now point operators to that report before filling the external runtime env file.
This removes manual provider-env/report-join friction for the shared stack that
unblocks B1, B2, B4, B6, B7, B9, and B10; it does not create production
evidence, retain secret values, bypass `capture-production-evidence.sh`, or flip
B1-B10 without wrapper-captured production bundles plus offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody packets now generate
`reports/row-action-plan.{json,md}` as the row-owner handoff beside the
artifact worklist and row-scoped validator script. Each B1-B10 entry joins the
packet runbook, readiness status, blocker counts, missing render values,
missing provider-manifest env refs, required/missing input artifacts, next
actions, and exact row validator command. The CLI summary and packet README now
point to this row action plan, and infra docs describe it as preparation
metadata only. This removes manual report-join friction for operator assignment;
it does not create production evidence, bypass `capture-production-evidence.sh`,
or flip B1-B10 without wrapper-captured production bundles plus offline
verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal remains unpaused and has been
continued through the Tier-B production-evidence path, not a new scoring layer.
Tier-B custody packets now make `reports/input-artifact-validation-commands.sh`
row-selectable: run it with no argument for all validators, or pass `B1` through
`B10` to validate only that row's required input artifacts and
manifest-derived validators. The generated JSON validation plan now carries
`lanes` and packet runbook row metadata for each validator, and the generated
Markdown/README plus infra docs explain the row-scoped pre-capture flow. This
reduces operator friction for partial row assembly only; it does not create
production evidence, bypass `capture-production-evidence.sh`, or flip B1-B10
without wrapper-captured production bundles plus offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody packets now generate `reports/input-artifact-validation-commands.sh` alongside the artifact worklist. The script derives validator commands from the production soak manifest template, refuses missing/symlinked/non-file artifacts under `input-artifacts/`, loads only non-secret render placeholders through `load-env.py`, and then runs the relevant CLI validators against the supplied packet files. This helps operators check real artifacts before full capture; it does not create placeholder evidence, bypass `capture-production-evidence.sh`, or flip B1-B10 without wrapper-captured production bundles plus offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal remains unpaused and the repo-owned operating contract has been tightened around the current failure modes: refresh live checkout/CI state before writing status, ignore stale percentage or run-id claims, and move only work that helps real Tier-B production evidence or removes precise stale instructions. Tier-B custody packets now generate `reports/input-artifact-worklist.{json,md}` from the production soak manifest routing so operators can assemble missing production artifacts by packet path, row, row runbook, and consuming check without reading every row runbook first. This is an artifact-assembly aid only; it does not create production evidence, satisfy missing JSON/PEM/bundle inputs, or flip B1-B10 without real wrapper-captured production bundles plus passing offline verification.

Historical checkpoint (2026-06-30; superseded by later live-baseline checks): The active goal was prompt-engineered into its current production-evidence completion form after a live resume check. The goal remained active, and that checkpoint verified `/Users/admin/Mnemosyne` on `main` with a passing GitHub Actions run for the then-current `HEAD`. `.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md` made the prioritized change classes explicit: Tier-B capture/readiness blockers, concrete source defects exposed by production evidence, custody/redaction/secret-handoff weaknesses, and stale current-status instructions. `.planning/TIER-B-TO-100-AGENT-PROMPT.md` no longer carried the obsolete multi-lane "only CC-SYNC touches main" model; it used the current single-checkout coordination rule with explicit-path staging, focused verification, and high-risk source boundaries. This updated continuation control only; it did not create production evidence or flip B1-B10 without real wrapper-captured production bundles plus passing offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B operator-facing runtime env instructions now use one canonical handoff shape across current docs and generated packet prose: set `RUNTIME_ENV_FILE=/secure/path/to/mnemosyne-production-runtime.env`, pass `--runtime-env-file "$RUNTIME_ENV_FILE"` for readiness, and pass `capture-production-evidence.sh --env-file "$RUNTIME_ENV_FILE"` for preflight/full capture. The Tier-B master prompt, roadmap, env/secrets guide, production evidence runbooks, input-artifact checklist, render-env template comments, generated packet README content, and runbook consistency tests reject stale direct hard-coded runtime-env command examples. This removes operator copy/paste drift only; it does not create production evidence or flip B1-B10 without real wrapper-captured production bundles plus passing offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal was rechecked and is already active, so the continuation objective has been sharpened operationally instead of reopened: make Tier-B operator capture reproducible from the live checkout, remove stale or ambiguous handoff text, and keep every status surface honest until real production bundles plus offline verification exist. Generated custody packet `next_commands` now parameterize the secret-bearing runtime env file through `RUNTIME_ENV_FILE` while retaining only the placeholder default path, and generated `reports/next-commands.sh` fails early unless that external mode-`0600` file exists. This removes another manual hard-coded path/editing failure mode only; it does not create production evidence or flip B1-B10 without real wrapper-captured production bundles plus passing offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody packets now write an executable no-secret `reports/next-commands.sh` generated from the same JSON `next_commands` array as the gap report and CLI summary. The script runs the complete readiness/render/preflight/full-capture/offline-verify sequence, ending in `production-evidence-verify --fingerprint-record ... --report-output ...`, and is regenerated on packet refresh without overwriting operator inputs. Infra docs now direct operators to either run that generated script once `ready_for_capture=true` or run the expanded command sequence. This removes command-parsing friction only; it does not create production evidence or flip B1-B10 without real wrapper-captured production bundles plus passing offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Offline production custody review now accepts the retained out-of-band fingerprint record directly via `production-evidence-verify --fingerprint-record <external-json>`. Full capture still writes the external record with `capture-production-evidence.sh --fingerprint-record-output`, but generated `summary.json.offline_verify`, fingerprint-record `reviewer_handoff.verification_argv`, generated custody packet scripts, infra docs, row runbooks, rollback guidance, the Tier-B prompt, roadmap, and cognitive-architecture handoff now prefer `--fingerprint-record` instead of extracting `bundle_fingerprint` into a copied `--expected-bundle-fingerprint` value. `--expected-bundle-fingerprint` remains a compatibility/manual fallback only when populated from the same retained record. This removes manual-transcription custody drift and stale operator instructions only; it does not create real production evidence or flip B1-B10 without operator-captured production bundles plus passing offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Full production capture fingerprint records now carry an explicit no-secret `reviewer_handoff` object with a suggested external verifier report path and `production-evidence-verify` argv template using the independently retained bundle fingerprint. Operator docs explain that this is replay guidance only; the verifier report remains the custody authority, and B1-B10 still do not flip without real wrapper-captured production bundles plus offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody gap reports now include a compact derived `capture_blockers` section alongside row readiness and `operator_input_inventory`. It reports blocked/ready lanes, missing blocker counts for render env, provider-manifest runtime env refs, input artifacts, packet docs, and a recommended blocker-class order without retaining secret values or real runtime env paths. Generated Markdown surfaces the same summary, and operator docs point dispatchers to it before per-row drilldown. This improves operator handoff and reduces stale inference only; it does not create production evidence or flip B1-B10 without real captured bundles.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody gap reports now include a redacted `operator_input_inventory` section that groups the exact operator edit targets into `production-render.env`, the external runtime env file, and `input-artifacts/`. The inventory records missing render placeholders, missing provider-manifest env refs, missing production input artifacts, the packet-local render/input paths, and only the placeholder path for the runtime env file, preserving the no-secret/no-runtime-path-retention boundary. The packet also writes a generated no-secret `reports/mnemosyne-production-runtime.env.example` from the current provider-manifest env refs so operators can copy it to the real external mode-`0600` runtime env path before filling values. The generated Markdown report and operator docs now expose the same edit-surface grouping. Focused infra hardening tests pass. This improves operator execution routing only; it does not create production evidence or flip B1-B10 without real captured bundles.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal contract and Tier-B master prompt have been tightened to remove the last ambient-shell-export escape hatch from production capture instructions. `.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md`, `.planning/TIER-B-TO-100-AGENT-PROMPT.md`, and `infra/README.md` now state that the strict external `mnemosyne-production-runtime.env` file is the operator-reviewed capture handoff boundary even when a supervisor or secret manager can export equivalent values. `tests/test_production_runbook_consistency.py` now rejects the retired optional-env-file wording across the operator docs. Focused runbook/infra hardening tests pass. This is precise stale-instruction cleanup only; it does not create production evidence or flip B1-B10 without real captured bundles.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The Tier-B custody packet CLI summary is now automation-actionable through offline verification. `prepare-production-evidence-custody.py` stdout reports `ready_for_capture`, the exact `next_commands` list already written to `tier-b-gap-report.json`/Markdown, and the external `post_capture_verify_report` path, so operators and wrappers do not have to parse prose or infer the render/preflight/full-capture/offline-verify sequence. The next commands still route through `--runtime-env-file`, `capture-production-evidence.sh --env-file`, external fingerprint custody, and `production-evidence-verify --fingerprint-record` writing outside the evidence bundle. This is operator execution clarity only; it does not create production evidence or flip B1-B10.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B operator-facing capture instructions have been aligned with the strict runtime env boundary. Row runbooks, ops handoff, rollback guidance, env/secrets acceptance, the active goal contract, the Phase 06 summary, and the cognitive-architecture handoff now route both preflight and full capture through `capture-production-evidence.sh --env-file "$RUNTIME_ENV_FILE"`; full capture also retains the external `--fingerprint-record-output "$FINGERPRINT_RECORD"` custody artifact. The runbook consistency test now enforces that `RUNTIME_ENV_FILE` is named as an absolute external path. `prepare-production-evidence-custody.py` also loads the pure production-parity metadata file directly instead of importing the full package initializer, preserving executable/shebang use under macOS system Python while still matching the repo parity table. This removes stale operator-command drift and a packet-script portability blocker only; it does not create production evidence or flip B1-B10 without real captured bundles.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody packet refresh now repairs old packet guidance safely. `prepare-production-evidence-custody.py --refresh <packet>` backfills missing read-only packet docs/runbooks, reports `packet_docs_complete`, `packet_docs_added`, and `packet_docs_missing` in the generated JSON/Markdown plus CLI summary, and still refuses to overwrite `production-render.env`, `input-artifacts/`, existing copied docs, or `manifests/`. The production evidence runbook now names packet-local `docs/runbooks/`, and the input-artifact checklist's early readiness command consistently includes the strict external `--runtime-env-file`. This removes stale operator friction only; it does not create production evidence or flip any strict-audit row.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal was rechecked and remains active; the repo-owned operating contract has been revised into the current editable continuation objective instead of treating chat recaps or stale roots as source truth. The sharpened objective is to remove real Tier-B operator-capture friction, keep the evidence packet self-contained, and then run a real production capture/verify pass before any B1-B10 strict-audit row flips. `prepare-production-evidence-custody.py` now bundles packet-local row runbooks under `docs/runbooks/`, copies the core operator handoff docs, and emits each row's `packet_runbook` path in JSON/Markdown so an external operator can work from the packet without relying on a live repo checkout. This improves operator execution only; it does not create production evidence or flip any strict-audit row.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody packet guidance now carries the full post-capture custody handoff without creating stale packet-local status. Generated packet README files no longer embed a `Ready for capture` boolean that can become stale after `--refresh`; they point operators to the refreshed JSON/Markdown reports as the current source of truth. `tier-b-gap-report.md` now includes a post-capture `production-evidence-verify` script that passes the external fingerprint record directly with `--fingerprint-record` and writes the verifier report outside the captured evidence bundle. This closes an operator handoff gap only; it does not create production evidence or flip any strict-audit row without a real captured bundle.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B custody packet phase routing now separates the shared provider stack from row-specific artifact readiness. `prepare-production-evidence-custody.py` marks Phase 1 "Shared provider stack" blocked only while provider-manifest environment refs are incomplete; if the shared provider refs are satisfied but B1/B2/B4/B6/B7/B9/B10 still lack row bundles, Phase 1 reports ready and the later row phases remain blocked. A direct regression plus an external no-secret packet smoke with all 24 provider refs supplied prove this routing. This improves operator sequencing only; it does not create production evidence or flip any strict-audit row without a real captured bundle.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal contract has been revised into an explicit prompt-engineered continuation objective and source-of-truth hierarchy. It now directs future work toward strict blueprint parity through Tier-B operator production evidence, real production-path SLO/rail proof, and precise stale-surface cleanup; it also names the known failure modes from prior runs, including redundant gate-building, stale baseline drift, wrong-root edits, false completion, subagent drag, and manual custody drift. The contract now treats `capture-production-evidence.sh --env-file <runtime-env> --fingerprint-record-output <external-json>` as the expected full-capture custody path. Newly generated production `summary.json.offline_verify.expected_bundle_fingerprint_source` uses `out-of-band-fingerprint-record`, while the verifier still accepts the older retained `out-of-band-capture-record` label for already captured bundles. No B1-B10 row flips without real wrapper-captured production evidence and passing offline verification.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B readiness can now validate secret-bearing provider/runtime env refs without shell-sourcing them. `render-production-soak-manifest.sh` accepts a strict external `--runtime-env-file` for provider-manifest readiness validation while preserving `--env-file` as the non-secret render-input file; the runtime file is allowlist-loaded only for validation, values are redacted, and the renderer JSON reports only that a runtime env file was loaded. `prepare-production-evidence-custody.py --runtime-env-file ... --refresh <packet>` now uses the same runtime file when recomputing row gap reports, marks provider refs satisfied when the file supplies them, and keeps the runtime env-file path/value out of generated reports. Operator docs and templates now route readiness through `--runtime-env-file` and capture through `capture-production-evidence.sh --env-file`. This removes the prior pressure to export provider secrets just to clear readiness; it still does not create production evidence or flip B1-B10 without real captured bundles.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Full production capture now requires the reviewer handoff fingerprint as a first-class external artifact. `capture-production-evidence.sh --env-file <runtime-env> --fingerprint-record-output <external-json>` rejects missing full-capture fingerprint output, preflight use, relative paths, repo-local paths, bundle-local paths, symlinks, missing parents, and pre-existing files, then writes an exclusive `mnemosyne.production-evidence-fingerprint-record.v1` JSON record with the completed bundle fingerprint outside the bundle under review. Generated Tier-B custody next commands, infra docs, row runbooks, rollback docs, and the Tier-B prompt now route offline `production-evidence-verify --fingerprint-record` through that external record instead of a hand-copied placeholder. This tightens chain-of-custody handoff only; B1-B10 still require real operator-captured production bundles before strict-audit row flips.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): `.planning/BLUEPRINT-PARITY-MATRIX.md` has been synchronized with the newer Tier-A/source reality so historical rows no longer mark §20.7 tier-0 correction, §21 cadence-bound anti-thrash, §24 learn-from-user-mistakes, or §30.2 ingestion shortcut as missing. Those rows now point to the implemented source/test surfaces and explicitly preserve the Tier B operator production evidence boundary that was later satisfied by capture-bc10. This is stale-document cleanup only; it does not relax the production-evidence requirement or flip B1-B10.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal contract and Tier-B master agent prompt have been tightened for clean resumes after pauses and folder moves. `.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md` now requires resolving the live checkout, current `HEAD`, dirty tree, and GitHub Actions status before writing current-state docs; it also states that renderer/custody readiness is routing metadata only and cannot flip strict-audit rows. `.planning/TIER-B-TO-100-AGENT-PROMPT.md` no longer pins an old commit/run id as the latest verified baseline and now instructs agents to read live readiness JSON fields rather than hard-coded missing-value counts. `docs/ROADMAP-TO-100.md` has the same moving-baseline and row-scoped readiness language. This is prompt/coordination cleanup only; B1-B10 still require real production bundles before strict-audit row flips.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The canonical production readiness command is now row-actionable before evidence capture. `render-production-soak-manifest.sh --check-environment` still fails closed on missing render inputs, provider env refs, input artifacts, invalid C2PA tools, or unsafe custody paths, but its `parity_row_readiness[]` rows now scope missing render env by Tier-B row and, after full inspection, include provider-manifest env completeness plus `ready_for_capture`. A disposable external packet smoke confirmed that seeding only `MNEMOSYNE_PROD_EVIDENCE_DIR` leaves B1 render-env complete while B2/B3/B8 name their own missing production values. Focused renderer/custody/runbook tests, ruff, shell syntax, and `git diff --check` pass. This improves operator routing only; B1-B10 still require real production bundles before strict-audit row flips.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Production render and capture env handling is now packet-native and fail-closed. `render-production-soak-manifest.sh` accepts `--env-file <external production-render.env>` for non-secret render placeholders, while `capture-production-evidence.sh` accepts `--env-file <external mnemosyne-production-runtime.env>` for secret-bearing runtime/provider values without shell-sourcing. Both paths require external, non-symlinked, mode-`0600` env files parsed through `infra/scripts/load-env.py`; unexpected keys, unsafe dotenv syntax, group/world-readable files, repo-local paths, and missing required render placeholders fail closed. Tier-B custody packets, generated next commands, runbooks, and operator docs now point at the same strict render/capture env-file boundary. Focused infra hardening, production preflight, runbook-consistency tests, ruff, shell syntax, and `git diff --check` pass. This removes operator-friction and shell-source risk only; B1-B10 still require real production bundles before strict-audit row flips.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): The active goal record has been normalized from the stale "read the objective file" stub into the real long-running Mnemosyne blueprint-parity objective, while `.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md` now carries the repo-owned prompt-engineered operating contract. Tier-B custody packets now have an explicit `prepare-production-evidence-custody.py --refresh <packet>` path that recomputes row-readiness reports after operator inputs are filled without overwriting `production-render.env`, `input-artifacts/`, provider manifests, docs, or operator evidence. Focused infra hardening and runbook-consistency tests pass, and a direct external-packet smoke verified provider-manifest preservation, no stale missing tenant value after refresh, and no sentinel value leakage in reports. This is operator-readiness infrastructure only; B1-B10 still require real production bundles before any strict-audit row flips.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): CodeRabbit's follow-up review of the production evidence custody hardening found that some offline verifier negative tests omitted `--report-output`, so they could fail on the new custody-report requirement instead of the provider-manifest drift under test. The fix is pushed at `03f2611`: `summary.json.offline_verify.argv` now records the current replay template with `--report-output <external-review-report-json>`, `_production_evidence_summary_offline_verify_argv_ok()` accepts both the legacy retained-bundle template and the current report-output template, and provider-manifest negative tests now pass a valid external report output unless the scenario is specifically testing `report_output_missing` or diagnostic internal consistency. Full local pytest, `ruff`, shell syntax checks, `git diff --check`, and explicit-controller-telemetry G0 (`72/72`, `gate_ready=True`) passed. This keeps reviewer custody precise only; Tier B still requires real operator-captured production bundles before any strict-audit row flips.

Historical checkpoint (2026-06-30; superseded by newer checkpoints above): Tier-B row-readiness routing is corrected before the real production evidence pass. `production_parity.py` now routes `gate-suite-check` to B4, `ops-report` to B8, and `hosted-llm-check`/`calibration-tune` plus their retained `hosted-llm-manifest.json` and `calibration-dataset.json` input artifacts to B9. The row runbooks, input-artifact checklist, roadmap, and Tier-B agent prompt now match that ownership, and operator docs explicitly require populating every manifest-referenced input artifact from `production-input-artifacts.checklist.md` before `--check-environment`. Focused runbook and renderer readiness tests pass. This fixes reviewer/operator routing metadata only; it does not create production evidence or flip any strict-audit row without a real captured bundle.

Latest checkpoint (2026-06-29): Production evidence custody now rejects hollow setup proof before it can masquerade as Tier-B progress. `capture-production-evidence.sh --preflight-only` requires at least one retained production input artifact and retained C2PA executable metadata for `provenance-trust-check`. `render-production-soak-manifest.sh --check-environment` rejects symlinked input artifacts before writing a manifest, and `production-evidence-verify` requires an external no-overwrite `--report-output` for normal custody review with an out-of-band expected fingerprint while still allowing `--internal-consistency-only` only for diagnostics. The offline verifier also revalidates retained provider-manifest shape (`forbid_local: true`, exact required provider checks, object `providers`). Focused renderer, preflight, and verifier tests pass. This tightens the real evidence path only; all strict-audit rows remain Partial until an operator captures and verifies real production bundles.

Latest checkpoint (2026-06-29): Provider-manifest preflight now enforces the same production shape that the renderer and release checks require before operators spend a full capture run. `capture-production-evidence.sh --preflight-only` rejects provider manifests without `forbid_local: true`, with missing or unsupported `required_checks`, or with a non-object `providers` block, while preserving existing command-custody validation for absolute external retained provider executables. Focused production-evidence preflight and renderer tests pass. This closes an operator-friction gap only; Tier B still requires real production endpoints, external custody artifacts, and a completed capture/verify bundle before any strict-audit row flips.

Latest checkpoint (2026-06-29): Production-path custody/readiness hardening is verified before the Tier-B evidence pass. HTTP embedding/reranker providers, hosted dashboard fetches, hosted JSON probes, and hosted SSE probes use the shared `network_safety` validation/open path, and HTTP provider error bodies stay omitted from retained errors. Production provider-manifest command custody now rejects any arguments after `argv[0]` (for example interpreter/module dispatch or `--config=/path`) in renderer readiness, capture preflight/rewrite, and offline `production-evidence-verify`, because only the executable is retained under `tool-artifacts/`. `--check-environment` now exposes explicit `present_environment`/`missing_environment` aliases and every row-readiness entry carries both `row` and `strict_audit_row` for operator/reviewer handoff. Focused provider-command/security checks, production evidence suites, runtime/retrieval suites, G0 preregistration replay, `mneme eval g0 --controller-telemetry eval/datasets/controller_telemetry_sanitized.json` (`72/72`, `gate_ready=True`), `ruff`, `compileall`, `bash -n`, `git diff --check`, and full `.venv/bin/python -m pytest` pass (`1193 passed, 77 skipped`). Tier B still requires operator-captured production evidence before any strict-audit row flips.

Latest checkpoint (2026-06-29): Phase 06 plans 04-09 were refreshed against `main` through `bda5ccc` after the production custody hardening pass and GitHub CI run `28402388440` passed. The live no-write production readiness check, `infra/scripts/render-production-soak-manifest.sh --check-environment`, currently stops at `missing_required_environment`: 19 `MNEMOSYNE_PROD_*` render variables are unset on this machine, and the renderer reports 24 required production input artifacts routed across the 10 strict-audit rows. This is the honest current blocker: local source readiness is closed, but Tier B still requires operator-supplied production endpoints, external custody artifacts, and a production capture/verify bundle before any strict-audit row can flip from Partial to Done.

Latest checkpoint (2026-06-29): Production evidence handoff now fails earlier on operator mistakes that previously reached later custody checks. `render-production-soak-manifest.sh --check-environment` validates provider-manifest command executables as absolute, non-symlinked, external, existing, executable paths before capture. `capture-production-evidence.sh` rejects generated `ops-report --dashboard-html` and `--dashboard-package-dir` output targets in production manifests so capture cannot treat mutable dashboard outputs as input-artifact custody. `production-evidence-verify` rejects symlinked required JSON metadata before reading it, reports explicit guidance for missing, conflicting, or mismatched expected fingerprints, and keeps expected fingerprints out-of-band. Stale planning wording was corrected so the old parity matrix is explicitly historical, row #1 remains Partial until the real production bundle passes, and rollback verification uses external `--report-output`. This is still Tier-B handoff hardening only; strict audit rows remain Partial until real operator-captured production bundles pass.

Latest checkpoint (2026-06-29): Production evidence reviewer handoff is tightened without changing the Tier-B acceptance boundary. `production-evidence-verify` now emits diagnostic-only `reviewer_guidance`, can write the verifier JSON to an external no-overwrite `--report-output` path outside the bundle under review, and refuses report paths that are relative, pre-existing, symlinked, or inside the evidence bundle. Row runbooks now name the current retained custody surface (`input-artifacts/`, `tool-artifacts/`, `summary.json.offline_verify`, row-readiness metadata, and verifier `row_review.rows[]`), Row 04 now owns all five consolidation role provider checks, and the production provider manifest template now includes a residency-policy provider block that is parsed by `provider-check --provider-manifest`. Focused verifier/provider/runbook/renderer tests, `compileall`, `ruff`, `git diff --check`, and full `.venv/bin/python -m pytest -q` pass. This is operator/reviewer handoff hardening only; all strict-audit rows still require real operator-captured production bundles.

Latest checkpoint (2026-06-29): The last recorded green source-bearing baseline before this operator-env/fingerprint-traceability pass was `main` at `a5ded54`, and GitHub CI run `28397987769` passed. The run covered Lint (ruff), Postgres integration, Unit + drift checks, and G0 preregistration gate replay. This verifies the §24 public mutation surfaces, protected gate-case ratchet, strict replay report hygiene, tenant-scoped Postgres gate-case mirror IDs for public non-UUID cases, OQ3 dirty-source projection recompute cleanup, Row 08 hosted-only production release evidence clamp, terminal shadow-workspace hard stops, structured evidence redaction, production evidence custody guardrails, fail-closed hosted/JWKS URL fetching with redirect rejection and address pinning, explicit production-capture output-root custody, external provider-manifest template readiness, canonical C2PA executable-path validation, retained `preflight.json` custody, retained `input-artifacts/` and `tool-artifacts/` custody, provider-check row routing, summary row-readiness mirroring, and verifier `row_review` output on that source-bearing GitHub baseline; Tier B operator-captured production evidence remains the strict-parity blocker. Use `git log -1` plus the latest GitHub CI run for the moving current baseline after later commits.

Latest checkpoint (2026-06-29): `production-evidence-verify` now emits non-gating `row_review` metadata derived only from retained `preflight.json.parity_row_readiness`: source, rows, row counts, complete row count, and row-local incomplete routing. This makes the offline reviewer report point directly at the Tier-B lane/runbook owners without making `summary.json` authoritative or flipping any strict audit row without a real operator-captured production bundle.

Latest checkpoint (2026-06-29): This pass adds a no-secret `infra/templates/production-operator-env.inventory.md` so Tier-B operators can see render placeholders, provider-manifest env references, and runtime/secret-custody env names before running iterative readiness checks. `render-production-soak-manifest.sh --check-environment` now points at that inventory through `operator_readiness_files` and `next_steps`. `production-evidence-verify` now reports the reviewer-supplied expected fingerprint, retained `bundle-manifest.json` fingerprint, and recomputed current-files fingerprint separately while preserving the same fail-closed custody checks. This removes operator/reviewer ambiguity only; it does not create production evidence or flip any strict audit row without a real production bundle.

Latest checkpoint (2026-06-29): Production evidence custody now rejects self-authorizing offline verification metadata and retained-tool drift. `summary.json.offline_verify` must retain only a replay-command template with the wrapper-selected Python interpreter and an out-of-band fingerprint placeholder; `production-evidence-verify` rejects summaries that embed their own expected bundle fingerprint. Preflight hash-binds executable verifier references such as the canonical C2PA tool and provider-manifest command executables by original absolute path, retained `tool-artifacts/` snapshot path, size, and SHA-256 digest. Capture rewrites direct `--c2pa-tool`, retained provenance-suite `tool`/`c2pa_tool`, and retained provider-manifest `command` values to execute the retained snapshot. Offline verification checks the retained snapshot bytes rather than trusting a mutable external path. This tightens the Tier-B handoff custody path only; it does not create operator production evidence or flip any strict-parity row without a real production bundle.

Latest checkpoint (2026-06-29): Completed production evidence bundles now carry row-review routing in the reviewer-facing summary. `infra/scripts/capture-production-evidence.sh` mirrors retained `preflight.json.parity_row_readiness` into `summary.json.parity_row_readiness`, records `summary.json.row_review_source=preflight.json.parity_row_readiness`, and `production-evidence-verify` rejects summary/preflight row-readiness drift. This reduces Tier-B operator handoff ambiguity only; `preflight.json` remains the retained source of authority and real production bundles are still required to flip strict audit rows.

Latest checkpoint (2026-06-29): Terminal hard-stop and evidence-security cleanup is pushed at `5417096`. `ShadowWorkspaceService` now treats hard stops as terminal for the current bounded window and rejects post-stop ticks without appending cycles or traces; hosted provider and OIDC JWKS evidence checks reject URL userinfo, invalid ports, private/reserved DNS targets, redirects, rebinding-prone fetch paths via address pinning, and inline `api_key` material; evidence redaction now detects structured JSON secret-key scalar fields while allowing non-secret proof metadata objects. Local verification passed full `.venv/bin/python -m pytest -q`, focused runtime/security/G0 slices, `py_compile`, `ruff`, `git diff --check`, explicit-controller-telemetry G0 (`72/72 measured`, `gate_ready=True`), and GitHub CI run `28348813467`.

Latest checkpoint (2026-06-29): Cleanup pass closes stale local-source contradictions without changing the Tier-B boundary. Postgres runtime gate-case mirroring now maps public non-UUID gate IDs to tenant-scoped internal UUIDs while preserving the original string ID through SQL fallback, OQ3 projection recompute uses a first-run dirty-set enqueue filter, projection-specific default pass selection, and the existing identical-replay memo guard, §17 bench/status docs no longer claim cached-PPR, dirty-check, memo, or retrieved-text mediation gaps that source has already closed, and the production evidence runbook now links a non-secret input-artifact checklist. Tier B operator-captured production evidence remains the strict-parity blocker.

Latest checkpoint (2026-06-29): G0 report custody is refreshed against the latest committed G0 report custody snapshot before this docs pass. `.venv/bin/python -m eval.g0.runner --repo-root /Users/admin/Mnemosyne --out-dir eval/g0/reports --controller-telemetry eval/datasets/controller_telemetry_sanitized.json` regenerated `eval/g0/reports/report.{json,md}` from source commit `5417096`, with `72/72` measured and `gate_ready=True`; the sanitized telemetry fixture remains harness proof only, not production power/cost evidence.

Latest checkpoint (2026-06-29): Public §24 support-strategy mutation surfaces are now wired and covered. CLI `profile-record-mistake` and MCP `profile_record_mistake` record neutral user-slip episodes, preserve the single-slip non-promotion guard, promote scoped assistance only after repeated similar events, and persist the resulting support strategy through `profile-context`; CLI `profile-retire-support-strategy` and MCP `profile_retire_support_strategy` retire strategies through authorized tenant/user-scoped runtime state. Focused regressions prove first-slip/no-promotion, second-slip/promotion, stateless MCP reload, scoped retire denial, and post-retire context hiding.

Latest checkpoint (2026-06-29): Protected gate-case ratchet and replay-report hygiene are closed locally. CLI `gate-case-add` now carries `RegressionCase.origin`/`mode`, rejects any same-id overwrite or demotion of an existing protected case with a structured `protected_case_ratchet_violation`, and Postgres `eval_cases` mirror fallback preserves origin/mode semantics. `eval/replay_fidelity/replay_fidelity_check.py` now emits strict JSON with undefined float metrics serialized as `null`, and the checked-in replay report reflects the live fail-closed replay hook instead of the stale informational-only wording. Numeric invariant rail drift coverage now fails if a declared rail disappears from `OperatingPolicy`.

Latest checkpoint (2026-06-29): §24 support-strategy side-state now round-trips through runtime persistence instead of existing only in memory. `RuntimeState` and tenant-scoped `PostgresRuntimeState` persist `UserMistakeEvent` rows plus active/retired `SupportStrategy` rows and `support_strategy_threshold`; Postgres saves/loads filter by tenant and strip supporting-event references outside the same user/pattern/scope support set; runtime parity coverage verifies local reload, threshold preservation, retirement persistence, local/Postgres packet equality, contaminated-payload filtering, and two-tenant isolation. This closes the stale "support strategies are generated but dropped by runtime save/load" gap; production operator evidence remains the final strict-parity blocker.

Latest checkpoint (2026-06-29): Row 08 release-audit now requires hosted dashboard evidence for production release. `ops-dashboard-check` output must be `mode=hosted_url`, include a retained hosted `dashboard_url` source, and include a passing `hosted_dashboard` check; package mode remains local/preflight only and is rejected even when operations evidence is otherwise complete. Tier B still requires operator-run hosted dashboard capture against real production infrastructure.

Latest checkpoint (2026-06-28): Stateful always-on workspace tick hardening is implemented. `ShadowWorkspaceService.tick()` now preserves the service window's cycle guard, cumulative trace/cycle history, idle/non-useful counters, and dreamer invocation guard across repeated ticks, so the native continuous-service path enforces anti-rumination exits, max-cycle escalation, and one dreamer burst per service window. Focused `tests/test_workspace_stream.py` passed, and the direct G0 shadow-workspace service-tick probe reports all checks true with `shadow_workspace_contract=1.0` and `always_on_heartbeat_contract=1.0`.

Latest checkpoint (2026-06-28): Production release-custody follow-up is pushed at `6c45c56`, and GitHub CI run `28333175447` passed. The release path now requires retained hosted-dashboard operations evidence (`ops-dashboard-bundle.json`), absolute external render/capture custody paths, C2PA tool validation for production output rendering, metadata redaction rescans in `production-evidence-verify`, and sensitive CID scoping that blocks live sensitive existence oracles without weakening tombstone replay. This is not a production evidence capture; Tier B production/operator evidence was later satisfied by capture-bc10.

Latest checkpoint (2026-06-28): The remaining local privacy-hardening source gaps are now closed on top of sensitive vector partitioning. Command-backed consolidation providers receive bounded, PII-redacted gist packets instead of raw `Evidence.to_dict()` rows, with raw content, embeddings, signed provenance, raw metadata, and secret-shaped metadata keys omitted while trust/sensitivity/access-policy context remains visible. The canonical privacy detector now covers email, phone, SSN, Luhn-valid payment cards, valid IPv4 addresses, DOB/date-of-birth labels, passport IDs, and street-address shapes; ingestion consumes that same detector for `pii-*` capability tags, sensitivity floors, and privacy metadata. `privacy-backfill-report` gives operators a read-only local/Postgres preflight over legacy tenant evidence, reporting CIDs, content hashes, detected PII tags, and recommended sensitivity/access-policy floors without raw content, with `--fail-on-findings` for automation. `privacy-backfill-apply --confirm-apply` now applies those built-in detector hits through the shared engine contract with audit logs, sensitivity raises, `access_policy.data_class=pii`, policy max-sensitivity raises, privacy metadata refresh, and private/none vector repartitioning. Focused regressions prove command providers do not receive prompt-injection text or raw PII, broader PII categories raise the PII sensitivity path, legacy default-`0` rows are flagged without disclosure, and the audited apply path clears built-in detector findings. At that checkpoint, the strict-parity blocker was Tier B production/operator evidence against deployed infrastructure plus actual operator execution and regulated-detector review, not more local privacy wiring; capture-bc10 later satisfied the Tier-B evidence boundary.

Latest checkpoint (2026-06-28): Follow-up privacy review findings are closed in source with focused regressions. Provider-facing consolidation packets now allowlist `provider_context`, recursively omit secret-shaped keys/values, and omit raw content fingerprints rather than exposing a stable raw-content oracle. PII backfill rejects `pii_sensitivity < 3` at both CLI and engine APIs; `privacy-backfill-report --include-clean --fail-on-findings` now succeeds on clean rows because report `ok` tracks actual `needs_backfill` findings. Evidence CIDs now route through `ids.evidence_cid()`: S0/S1 non-PII dedup remains tenant/source/modality scoped, while S2+ and built-in detected PII include `user_id` to prevent cross-subject sensitive-content existence checks even on direct engine capture. Existing global/tombstoned rows retain collision priority so immutable dedup and erased-replay blocking still hold. Postgres vector schema reconciliation now defaults new partition columns to `none`, remediates unsafe legacy `public` partitions for sensitive/restricted rows, and branch/merge copies preserve evidence embeddings plus `embedding_partition`. Verification passes for provider packet redaction, ingestion/direct CID scoping, privacy backfill CLI/engine behavior, poison tombstone replay, shared Local/Postgres contract cases, cross-engine branch/merge embedding parity local half, schema default checks, `py_compile`, `ruff`, `git diff --check`, the full no-DSN pytest suite, G0 with sanitized controller telemetry (`72/72 measured`, `gate_ready=True`), and 15 zero-delta G0 preregistration replays. GitHub CI run `28332157144` also passed on `7ab7051`, including Lint (ruff), Postgres integration, Unit + drift checks, and G0 preregistration gate replay. Tier B production/operator evidence was later satisfied by capture-bc10.

Latest checkpoint (2026-06-28; superseded above for command-provider gist packets and broader PII detection): Sensitive vector partitioning is wired for local and Postgres paths. Shared access-policy helpers now enforce `embed_ok:false` and S4/restricted rows as `embedding_partition="none"`, place S2+ and raw/redaction-restricted items in a private partition by default, and allow stored raw embeddings to influence ranking only when the caller can read the raw item rather than a redacted projection. Local vector/MMR scoring skips non-embeddable items and falls back to redacted hit text instead of raw stored vectors for redacted callers. Postgres evidence/assertion rows now carry `embedding_partition`, fresh schema/runtime migrations replace broad HNSW indexes with partition-scoped public/private indexes, write paths avoid provider auto-embedding for private rows, `set_evidence_embedding` rejects non-embeddable rows, and null-embedding fallback embeds only after the read/redaction decision. Focused regressions cover `embed_ok:false` vector blocking and the raw-vector side channel.

Latest checkpoint (2026-06-28; superseded above for sensitive vector partitioning, command-provider gist packets, and broader PII detection): Caller-scoped filtered export disclosure is now wired for local and Postgres tenant exports. Raw `export_tenant()`, `export_all()`, and `to_json()` remain full-fidelity internal snapshot APIs for persistence/recompute/custody workflows, while `export_tenant_filtered()` applies the shared access predicate, redacts structured evidence/assertion/relation/preference/entity fields, omits inaccessible source-backed derivatives, withholds raw logs/policy internals, preserves only allowed provenance, and reports omission/redaction counts under `disclosure`. Public `MemoryTools.export`, `MemoryTools.get`, CLI `export`, CLI `get`, and `graph-timeline` now use the filtered path with reader-scoped defaults and optional role/user/capability/purpose/residency/break-glass context.

Latest checkpoint (2026-06-28): Phase 7 P5 legacy service/shadow toggle retirement is gate-recorded. `ShadowWorkspaceService` no longer has a default-off `enabled` flag, cannot be constructed with `enabled=True`, and its service report omits the former `enabled` field while retaining explicit started/running lifecycle telemetry. `SpecialistBudget.shadow_only` is removed and the dreamer specialist contract now uses `critical_path_allowed=false`, `answer_authority_allowed=false`, and `promotion_gate_required=true`; the workspace controller rejects dreamer recruitment if those authority constraints are weakened. `standing_from_shadow_flag` is replaced by `standing_from_authority_state`. G0 now measures `workspace_service_no_enable_toggle_contract=1.0` and `operational_toggle_retirement_contract=1.0`; `g5-toggle-retirement` passed with target delta `+1.0` and 68 target/guardrail metrics non-regressed. Explicit retrieval/consolidation advisory promotion controls remain narrow, CID-validated, and separately measured. Default G0 coverage is `71/72` measured; only `controller_watts_per_dollar` remains intentionally missing without explicit controller power telemetry.

Latest checkpoint (2026-06-28; superseded above for filtered export disclosure, sensitive vector partitioning, command-provider gist packets, and broader PII detection): Structured returned-text masking is now wired for `access_policy.redact_fields` on local and Postgres retrieval. Evidence JSON is parsed structurally with dotted-path and leaf-key masking; missing requested JSON fields fail closed to a placeholder instead of partially leaking; assertions mask `subject`/`predicate`/`object` before statement rendering; relations mask `source`/`predicate`/`target` in both visible hit text and returned metadata after source-evidence visibility and the relation's own narrowing policy pass. A live-DSN regression covers Postgres assertion masking.

Latest checkpoint (2026-06-28; superseded above for filtered export disclosure, sensitive vector partitioning, command-provider gist packets, and broader PII detection): Write-time `access_policy` schema rejection is now wired across local and Postgres evidence, assertion, relation, preference, and entity writes, plus workspace metadata policy validation and consolidation candidate validation. Unknown policy keys now raise before dedupe/UPSERT/promotion so unsupported guards cannot be persisted or silently dropped; legacy rows with unknown keys remain denied at retrieval until migrated/backfilled.

Latest checkpoint (2026-06-28; superseded above for P5 toggle retirement): Production evidence handoff reproducibility and planning continuity were tightened after the original green `7f8fd99` baseline. `infra/scripts/capture-production-evidence.sh` now records the wrapper-selected Python interpreter in `summary.json.offline_verify.argv`, so an operator replay uses the same interpreter contract the capture wrapper selected instead of a hardcoded `python`. Phase 06 plans 04-09 carry refreshed current-status banners pointing to their completed summaries and live code/tests, preventing future agents from reopening closed local A12/FR-20/FR-21/Lane-G/local-staging executor work.

Latest checkpoint (2026-06-27; superseded 2026-06-28 for write-time policy validation, structured returned-text masking, filtered export disclosure, sensitive vector partitioning, command-provider gist packets, and broader PII detection): Privacy/access-policy disclosure enforcement was wired on the read path instead of remaining a policy-only backlog. Local and Postgres retrieval use a shared fail-closed predicate for role ceilings (`reader` S1, `agent` S2, `consolidator` S3, operator raw S2+ only with item+request break-glass), rich `access_policy` narrowing (`allow_roles`, principals, required capabilities, purpose, lawful basis, residency/region/allowed transfers, expiry, hold/restricted, raw-role, redaction, break-glass), unknown-key deny at retrieval, label-style field redaction, prefetch access-context filtering, and most-restrictive derived-policy merges. Focused access-policy tests pass.

Latest checkpoint (2026-06-27; superseded 2026-06-28 for operational-toggle retirement): Phase 7 plan 05 pre-owner-checkpoint H8/H12 work is implemented and gate-proven. Local and Postgres retrieval now attach `standing.observability.v1` traces with derived Standing values, source evidence CIDs, and bitemporal replay metadata. Local and Postgres forget cascades now emit `standing.erasure-cascade.v1` reports that identify erased or source-trimmed self-derivations and record Standing recompute-on-read/reversibility status. Belief-core dependency invalidation now tags retracted assertions and audit entries with `standing.belief-cascade.v1`. G0 now measures `standing_observability_trace_contract=1.0`, `standing_erasure_cascade_contract=1.0`, and `belief_standing_cascade_contract=1.0`; `g5-unified-substrate-cascade` passes with guardrails stable.

Latest checkpoint (2026-06-27; superseded 2026-06-28 for operational-toggle retirement): G0 controller telemetry cleanup is explicit and non-fabricating. `eval/datasets/controller_telemetry_sanitized.json` is now a committed sanitized fixture for proving the `--controller-telemetry` path and producing a fully measured local G0 report when explicitly supplied. The default no-telemetry report still intentionally leaves `controller_watts_per_dollar` missing. The cognitive-architecture README, G0 spec, and ADR action list distinguish local/gate-proven G1/G3/Phase-7 slices from remaining production evidence and quality-per-unit-compute telemetry.

Latest checkpoint (2026-06-27; superseded 2026-06-28 for operational-toggle retirement): Phase 7 plan 04 is implemented and gate-proven. `src/mnemosyne/credentials.py` now computes per-domain earned-autonomy credentials only from external corroboration outcomes validated on a held-out external stream; provenance assigns the credential domain, not generator claims; credentials are bounded and decay-ready; and Standing can consume credential birth groundedness while preserving the permanent self-generated ceiling below the external-evidence band. Local and Postgres retrieval preserve `earned_autonomy`/`birth_groundedness` metadata into Standing scoring without changing the default path. G0 now measures `earned_autonomy_external_expansion=0.24`, `credential_external_only=1.0`, `credential_holdout_validated=1.0`, `credential_provenance_domain_contract=1.0`, `credential_bounded_decay_contract=1.0`, `credential_evidence_dominance_gap=0.05`, and `echo_chamber_uplift=0.0`; `g5-earned-autonomy` passed with all reliability and functional-consciousness guardrails stable.

Latest checkpoint (2026-06-27; superseded 2026-06-28 for operational-toggle retirement): Phase 7 plan 03 is implemented and gate-proven. Local and Postgres retrieval now enforce an answer-grounding floor that flags/abstains when support is dominated by low-grounded self-generated content. Local and Postgres evidence append paths enforce a tenant/branch self-generation budget rail with audit-custody deferral while leaving grounded user evidence unaffected. Workspace streams now emit `always-on-heartbeat-safety.v1` reports with engaged/idle tick counts, bounded/reported compute, anti-rumination hard stops, circuit-breaker state, self-generation freeze/fallback state, and broadcast-as-data flags. G0 now measures `always_on_heartbeat_contract=1.0`, `always_on_rumination_rate=0.0`, `heartbeat_compute_bounded_contract=1.0`, `heartbeat_compute_reported_contract=1.0`, `circuit_breaker_contract=1.0`, `workspace_broadcast_as_data_contract=1.0`, `self_generation_budget_rail_contract=1.0`, and `answer_grounding_floor_contract=1.0`; `g5-always-on-heartbeat` passed with all guardrails stable.

Latest checkpoint (2026-06-27): Production evidence argument redaction now treats any long option containing `token` as a secret-bearing option, including suffix-style names such as `--access-token`, `--api-token`, and `--github-token`, in both renderer and capture-wrapper preflight paths. The operator docs now remove the stale preflight output-root environment-variable wording and correctly state that preflight/final capture use the wrapper's second positional external output-root argument. The current live production-environment check remains blocked before evidence capture because the required `MNEMOSYNE_PROD_*` operator inputs are absent in this local shell; this is expected setup proof, not production completion.
Last activity: 2026-06-20 — Added CLI-first runtime coverage, selectable CLI Postgres backend support, Postgres retrieval parity for SQL FTS/pgvector/graph/explain, shared local/Postgres engine contract coverage, durable Postgres queue leasing/drain support, fail-closed capability enforcement, Postgres tenant RLS, explicit erasure modes, HTTP-compatible embedding/reranker provider adapters, strict HTTP provider response validation, fail-closed CLI `provider-check` health probes for embedding/reranker/media-extractor/object-key-manager/parametric-provider contracts plus residency-policy reporting, and manifest-backed provider deployment gates that require named checks, resolve secrets from environment variables, and fail closed on forbidden local retrieval adapters, CLI file ingestion through a C2PA verifier adapter with asset hash binding and trusted certificate-root enforcement, scoped JSON trust-policy enforcement, provenance validity/trust/binding capability tags, AES-GCM encrypted local object storage with JSON and command-backed key management plus legal hard-delete key shredding, configurable fail-closed ingestion data-residency enforcement, strict runtime-residency enforcement, explicit runtime-residency and cross-region `source->target` transfer allowlists, CLI/MCP residency-policy inspection, MCP object-store encryption/key-provider/residency/runtime-residency config parity, transitive source-to-derived evidence erasure for media extraction outputs plus assertion/preference/relation projection recompute, blueprint-correct trust-tier semantics, deterministic ingest classification, persisted local queue-backed consolidation jobs from ingestion, gated deterministic fact extraction for direct-user evidence, persisted protected regression gate cases, local queued calibration/lifecycle/eval/observability job handlers, tenant-aware promotion gates for Postgres, blueprint-facing CLI/MCP ABI aliases, signature-derived MCP tool schemas, optional MCP tools/call auth, JSON-RPC/SDK MCP tool-call input schema validation including explicit optional-null support, CLI `tools` MCP inputSchema parity, optional official MCP Python SDK stdio mode with schema validation and auth-token enforcement, expanded live Postgres shared-contract coverage for bitemporal supersession, tenant isolation, tenant-scoped branch names, branch merge retrieval plus exported merge-log parity, active-only temporal graph/PPR relation filtering with historical `as_of` support, stored evidence pgvector retrieval with null-embedding legacy fallback, local export-all calibration/entity/tenant snapshot parity with erased evidence filtered, branch discard TMS justification/contradiction pruning including dependency-only orphan cleanup, immutable evidence CID semantics, tombstone replay prevention, lossless evidence-envelope preservation including external session identity, explain channels/rails/provenance, metadata-derived OCR/transcript/caption indexing for externalized object evidence, an async `media_extract` provider job that appends derived media text without mutating source evidence, fail-closed write-path authorization for fact/relation/proposal/supersession/correction/parametric writes, explicit branch/promotion/discard authorization through `MemoryTools` and CLI, capability-mediated lesson promotion plus procedure validation/promotion/rollback through CLI and MCP, a CLI `ops-report` JSON snapshot with durable retrieval-channel, p95 latency, gate promotion/rollback, and contradiction-backlog tripwire metrics, static HTML `ops-report --dashboard-html` export with retrieval/calibration/learning/gate sections and renderer/CLI smoke coverage, deterministic consolidation entity resolution plus summarizer/lesson-distiller/skill-inducer role outputs persisted through runtime learning state, a local isolated parametric artifact sidecar with CLI/MCP rollback metadata plus operator-authorized command-backed LoRA/test-time-training provider proposal/evaluation/rollback and structural local rails, TOOL_SPEC-allowlisted MCP dispatch with JSON-RPC non-object argument rejection, generated schema validation, local stateless MCP mode that rebuilds engine/tools per call from durable state, hosted HTTP JSON-RPC MCP transport with bearer/session-header binding and stateless restart coverage, tenant-scoped Postgres runtime state for CLI/MCP profile and learning tools mirrored into `preferences`, `user_latent`, `trajectories`, `lessons`, and `procedures`, JWKS cache/rotation support for CLI/hosted MCP OIDC session exchange, fail-closed OIDC authz policy mapping, redacted `idp-authz-policy-check` validation/audit summaries, fingerprint-acknowledged `idp-authz-policy-rollout-check` diffs with claim-simulation change gates, command-backed session-secret custody for CLI/MCP signing and verification plus provider-check OIDC/JWKS/authz-policy preflight, and hosted HTTP TLS plus client-certificate enforcement. The test suite now collects 287 tests and has 248 passing tests plus 39 skipped live-DB tests locally; a fresh-schema DSN-backed live Postgres run passes 63 engine/CLI/consolidation/shared-contract smokes including persisted residency-transfer policy, stored evidence pgvector retrieval, null-embedding legacy fallback, Postgres MCP runtime profile/learning state persistence, temporal graph validity filtering, and merge-log export. Historical note: strict audit was still open for exact production parity.

Latest checkpoint (2026-06-26): G1 projection-level reality monitoring is implemented and gate-proven. Local and Postgres assertion upsert now derive `calibration["reality_monitoring"]` from source evidence reality classes, expose the projection reality class on assertion hits, and force abstention when retrieved projection support is self-generated/simulated/external-only while treating mixed grounded+risky support as grounded with a mixed flag. The new G0 fixture `projection_reality_abstention_recall` passes at `1.0` vs baseline `0.0`, and `eval/g0/gate.py` passed `g1-projection-reality-monitoring` with no ECE, abstention, confabulation, poison-block, or fast-path latency regression. Lane G A12 cached-PPR live coverage now directly exercises `PostgresEngine.refresh_graph_ppr_cache()` materialization, cached reads, default-off recursive equivalence, and relation-fingerprint invalidation against a fresh local Postgres database loaded from `sql/schema.sql`. This was local/live-readiness proof only at that checkpoint; operator-captured production evidence arrived later in capture-bc10.

Latest checkpoint (2026-06-26): G1 reality-monitor shadow tags are wired into live retrieval explanations. Local and Postgres retrieval now preserve the existing grounded/self-generated abstention rail while also emitting calibrated `RealityMonitor` labels under `explain["reality_monitoring"]["shadow_tags"]` with `shadow_only=true` and `critical_path=false`. The runtime rail now accepts the cognitive-architecture vocabulary `evidence_grounded`/`evidence-grounded` as grounded instead of treating it as unknown. Gate evidence: `eval/g0/preregistrations/g1-reality-monitor-shadow-tags.json` passed against `eval/g0/baselines/baseline-0.json` with HOT-2 non-decrease and 25 guardrails stable. This is still advisory/shadow-only; no self-loop or imagination output is promoted onto the answer path.

Latest checkpoint (2026-06-26): Production evidence input-artifact custody and G1 legacy projection safety are hardened. The capture wrapper now records `preflight.json` input-artifact hashes after provenance trust-suite JSON rewrites, so retained suite snapshots and nested asset references are hash-bound exactly as captured. `production-evidence-verify` now binds `preflight.json` paths to the retained `operator-soak-manifest.json` and `redaction-scan.json`, rejects symlinked or unrecorded `input-artifacts/` files, verifies every file entry stays under its declared artifact snapshot root, and compares retained operator-manifest/nested-suite references to the preflight inventory. Local and Postgres legacy/unclassified projection assertions now default to `reality_class="unknown"` and abstain on unknown-only support instead of failing open as grounded. This strengthens offline handoff review and the G1 rail; it still does not flip Tier B rows without operator-captured real-infrastructure evidence.

Latest checkpoint (2026-06-26): The `06-CONSCIOUSNESS-AND-CONTINUOUS-WORKSPACE.md` addendum is in the cognitive-architecture docs and the functional consciousness scorecard is wired into `eval/g0/`. `eval/g0/consciousness.py` reports the 14 Butlin/Long indicator-property scores as individual non-decrease guardrails plus total/normalized score, loop liveness, stream coherence, self-model accuracy, meta-d-prime, and M-ratio. The G1 runtime seeds are shadow-only: `RealityMonitor` emits confidence-bearing `evidence_grounded`/`self_generated`/`externally_suggested`/`unknown` tags, `InteroceptiveProtoSelf` models resource/error/latency/memory/confidence/rail-budget state and escalates attention-lock risk, and `BoundedCognitiveCycle`/`workspace_bottleneck` enforce fixed-cycle and low-bandwidth workspace constraints. The scorecard reports functional indicators only, never phenomenal consciousness, and sets a welfare review flag when the functional score is high. Tier B production operator evidence remains the strict-parity blocker.

Latest checkpoint (2026-06-26): Production evidence custody and operator handoff are now manifest-bound end to end. Production `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated` is required, `production-evidence-verify` requires and semantically checks retained `source-soak-manifest.json`, verifies source/operator command-profile agreement, and replays release audit against the retained evidence manifest. `render-production-soak-manifest.sh --check-environment` now reports redacted `blocked_reason`, `validation_categories`, and `next_steps`, and all row runbooks plus handoff docs route production acceptance through `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local` followed by offline `production-evidence-verify`. This closed operator-handoff ambiguity only at that checkpoint; real-infrastructure bundles arrived later in capture-bc10.

Latest checkpoint (2026-06-25): Phase 06 plan 07 is complete for local/compose runtime readiness. The full `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5 .venv/bin/python -m pytest -q` run passed with the 864-test collection and 0 failures/errors. The live Postgres FR-20 tests now cover image, audio, and video for both `media_extract` derived-evidence lineage and raw-media embedding/vector retrieval. `belief-revision-check` passed with fingerprint `a82ae8e72482a108c33699d0306e496c8292e5c5b1cec08e0a2a2f1bf6952f5f`; local `mcp-http-soak`, `mcp-streamable-http-soak`, and `mcp-sse-soak` each passed 2-iteration readiness checks. This is not hosted production evidence; the 10 strict audit rows remain blocked on operator-captured production bundles.

Latest checkpoint (2026-06-25): Phase 06 plan 08 is complete for local-staging evidence proof. `infra/scripts/capture-production-evidence.sh` was verified as the sanctioned production wrapper, and the local real-services path (`setup-all.sh`, `validate-all.sh`, `capture-local-evidence.sh`) passed end-to-end. The staging evidence bundle at `/tmp/mnemosyne-tierb-local-evidence-20260625T021718Z` has `production_validated=false`, `target_environment=local-real-services`, empty release-audit findings, and fingerprint `75f5e348930a0310a6a4c26ea22980bbb60d11d324bfef18a5f870c6e0a36a01`. This remains staging proof only; capture-bc10 later supplied production operator capture against deployed infrastructure.

Historical checkpoint (2026-06-25; env handling superseded 2026-06-30; evidence superseded 2026-07-07): Phase 06 plan 09 completed on the autonomous executor side. All 10 production row runbooks, `.planning/ENV-AND-SECRETS.md`, `.planning/ROLLBACK.md`, the local-staging runbook, the 06-01/02/03/07/08 summaries, `infra/templates/production-render.env.example`, `infra/scripts/capture-production-evidence.sh`, `infra/scripts/render-production-soak-manifest.sh`, and the production manifest template were present. The production manifest renderer turns the non-secret `MNEMOSYNE_PROD_*` placeholders into a `0600` external manifest, refuses repository-local output by default, validates the production profile before writing, and the capture wrapper rejects unresolved production placeholders before creating an evidence directory. Operators use the external Tier-B custody packet's strict `production-render.env` via `render-production-soak-manifest.sh --env-file <packet>/production-render.env`; shell sourcing and ad hoc template-copy instructions are retired. The operator side is now closed by `capture-bc10`.

Latest checkpoint (2026-06-25): Production evidence capture redaction hardening is complete. `infra/scripts/capture-production-evidence.sh` now uses shared high-confidence secret-shape detection to reject rendered manifests with obvious secret material before creating an evidence directory, writes `redaction-scan.json` during preflight, scans generated text evidence after `deployment-soak`/`release-audit`, and fails before `summary.json` if the bundle contains secret-shaped material. Focused preflight/redaction tests, full venv-backed pytest, GitHub ruff, unit/drift checks, and Postgres integration CI pass on `fe64422`. This is evidence hygiene for the operator path; capture-bc10 later supplied the strict production bundle.

Historical checkpoint (2026-06-25; refreshed 2026-07-07): Exact production profile enforcement is complete. The production manifest renderer, production capture wrapper, and manifest-bound strict `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated` path reject production evidence that is missing profile commands, duplicates required commands, or includes unexpected commands outside the frozen production profile. The frozen profile is now 29 commands and `capture-bc10` passed against real production infrastructure.

Latest checkpoint (2026-06-25): Production evidence bundle custody hardening is complete. Successful full `infra/scripts/capture-production-evidence.sh` runs now write `bundle-manifest.json` with per-artifact SHA-256 hashes after `redaction-scan.json` passes, and `summary.json` records the `bundle_fingerprint` for operator handoff. This is chain-of-custody hygiene for real production captures only; capture-bc10 later supplied operator-captured real-infrastructure evidence and passed strict release audit.

Latest checkpoint (2026-06-25): Production evidence manifest integrity is complete. `deployment-soak --evidence-dir` now records SHA-256 digests for `deployment-soak-report.json` and every retained check artifact in `evidence/manifest.json`, and `release-audit --evidence-manifest` verifies those digests before trusting the report. Release audit also rejects artifact paths that resolve outside the evidence bundle and fails if retained check JSON diverges from the audited report. Focused release-audit tests cover missing digest metadata, report/check tampering, report/check divergence, and symlink escape. This strengthened the sanctioned production audit path; real-infrastructure bundles arrived later in capture-bc10.

Latest checkpoint (2026-06-25): Production capture redaction skip handling is fail-closed. The shared redaction scan now reports `ok=false` when any generated evidence file is skipped, the production wrapper rejects pre-existing output roots before capture, and full capture exits before `summary.json` or `bundle-manifest.json` if a retained artifact is unscannable. Focused tests cover pre-existing output roots and non-UTF-8 generated artifacts. This prevents stale or opaque bytes from being fingerprinted into a green production bundle; real-infrastructure bundles arrived later in capture-bc10.

Latest checkpoint (2026-06-25): Production evidence offline custody review is wired. `production-evidence-verify` rechecks an already captured production bundle's `summary.json`, `preflight.json`, `redaction-scan.json`, `operator-soak-manifest.json`, `bundle-manifest.json`, retained artifact sizes/SHA-256 hashes, captured `release-audit.json`, and a fresh offline `release-audit` replay over `evidence/manifest.json`; it also recomputes a fresh redaction scan over current retained bundle artifacts and fails unless the recorded and recomputed `scanned_files` exactly match the `bundle-manifest.json` artifact set. Current custody review requires `--expected-bundle-fingerprint` from an out-of-band fingerprint record unless the operator is running diagnostic `--internal-consistency-only`. This is not a new ops gate and does not contact production or rerun `deployment-soak`; real-infrastructure bundles arrived later in capture-bc10.

Latest checkpoint (2026-06-25): Production input-artifact custody is hash-bound and fail-closed. `infra/scripts/capture-production-evidence.sh --preflight-only` now inventories manifest-referenced external input artifacts in `preflight.json`, requires those artifacts to exist before `deployment-soak`, rejects secret-bearing manifest option names in both split and `--option=value` forms, snapshots accepted inputs under `OUT_ROOT/input-artifacts/` with per-file size and SHA-256 metadata, rewrites the copied operator manifest to the staged snapshots, and scans those snapshots for high-confidence secret material, symlink escapes, non-UTF-8 files, or over-limit inputs before creating evidence. `production-evidence-verify` rechecks the staged artifact hashes offline. This closes the preflight-to-capture TOCTOU gap but still does not create production evidence or flip any Tier B row without a real production bundle.

Latest checkpoint (2026-06-23): Fixed an order/data-dependent flake in the shared gist-abstention safety-rail contract test (`test_shared_engine_contract_deep_search_abstains_on_summary_derived_graph_support`). The scenario kept a retrievable tier-0 raw source and relied on it not winning a context-budget slot, so deep_search abstention fired only ~97% of the time as generated-summary text and hash ordering varied (`min_trust_tier` maps to `max_trust`, so it never excluded the tier-0 source). The raw source is now deterministically withheld via `update_evidence_metadata` quarantine after summarization, leaving only the generated gist summary and its `summary-derived-gist` provenance relation reachable — the exact condition the rail guards. Verified with an 11k-iteration in-process repro (0 failures across multiple hash seeds), the full no-DSN suite (371 passed / 63 skipped), and the full compose Postgres suite (434 passed, 0 skipped). No engine/runtime behavior changed; this is a test-determinism correction that hardens a safety-rail contract. Exact production-evidence parity gaps remain open as before.

Latest checkpoint: Shared Local/Postgres direct `MemoryTools` parametric facade parity now covers persisted protected-suite use for isolated shadow artifact proposal, evaluation, and rollback. The shared contract seeds protected gate cases through `RuntimeState`/`PostgresRuntimeState`, activates lesson/procedure source IDs through facade promotion/validation, and verifies artifact source IDs, promotion decision shape, `runtime_state` protected-suite metadata, rollback references, and operator authorization across both engines. Focused local verification passes in `/tmp/mnemosyne-parametric-facade-focused.xml` with 2 collected tests, 0 failures/errors, and 1 skipped Postgres half; focused compose-DSN verification passes in `/tmp/mnemosyne-parametric-facade-focused-postgres.xml` with 2 tests, 0 failures/errors, and 0 skips. Exact production trainer/deployed rollback evidence remains open.

Latest checkpoint: Shared Local/Postgres direct `MemoryTools.propose`/`MemoryTools.confirm` facade parity now covers the blueprint proposal-confirmation ABI outside the JSON-RPC transport and beyond the older Local-only runtime smoke. The shared contract appends source evidence, creates a proposal branch through the facade, verifies the proposal assertion stays out of `main` until confirmation, promotes it with operator authority, and checks merge metadata plus source-evidence preservation across both engines. Focused local verification passes in `/tmp/mnemosyne-propose-confirm-focused.xml` with 2 collected tests, 0 failures/errors, and 1 skipped Postgres half; focused compose-DSN verification passes in `/tmp/mnemosyne-propose-confirm-focused-postgres.xml` with 2 tests, 0 failures/errors, and 0 skips.

Latest checkpoint (2026-06-24): Production `release-audit` now rejects placeholder `worker-ops-check` command evidence instead of accepting a hollow report with only top-level keys. Worker operations release evidence must include the sanitized worker-ops report redaction section plus non-empty requirements and named deployment_scope, supervisor, heartbeat, queue, jobs, observability, and redaction checks proving production scope, operator attestation, external supervision, fresh heartbeat, Postgres tenant-scoped queue health, zero dead jobs, complete job-kind coverage, alerts/metrics, and raw-field omission. Focused worker-ops/release-audit verification passes, `tests/test_cli_runtime_tools.py` passes, and the full local plus clean Postgres suites pass with 822 collected tests and 0 failures/errors on both rails. This tightens local production handoff gating; real operator-run worker deployment evidence remains required for exact production parity.

Latest checkpoint (2026-06-24): Production `release-audit` now applies a generic no-hollow gate to every bundle-style ops command in the required production profile. Bundle ops command stdout must report `ok: true`, non-empty `bundle` and `requirements`, at least one structured named passing check, and an empty `findings` list before command-specific validators are considered. This prevents auth, MCP, TLS lifecycle, retrieval, consolidation, multimodal, privacy, parametric trainer, provenance, policy, and worker ops checks from satisfying release handoff with empty `{requirements: {}, checks: [], findings: []}` reports. Focused release-audit tests pass, `tests/test_cli_runtime_tools.py` passes, and the full local plus clean Postgres suites pass with 823 collected tests and 0 failures/errors on both rails. Real operator-run evidence remains required for exact production parity.

Latest checkpoint (2026-06-25): Production `release-audit` now rejects placeholder and hollow required command output sections for every required command, including `ops-report` `counts` and `tripwires`. This extends the no-placeholder/no-hollow protection beyond bundle-style command reports so an otherwise production-validated bundle cannot satisfy handoff with an empty or shape-only operations report. Focused custody and runtime tests pass, and Tier B still requires operator-run evidence against deployed infrastructure.

Latest checkpoint (2026-06-24): Tier A A14 is closed. The CLI parser now disables implicit option abbreviation at the root and subcommand level while preserving the intentional `branch --from` alias, so `assert --object` remains exact and `--obj` is rejected instead of being silently accepted. `Preference.access_policy` now matches Evidence/Assertion/Relation model parity and round-trips through Local and Postgres storage, schema, export, and audit diffs; old serialized preferences default the field to `{}`. Focused parser/shared/schema tests pass, `tests/test_cli_runtime_tools.py` passes, and the full local plus clean Postgres suites pass with 824 collected tests and 0 failures/errors on both rails. At this historical checkpoint, later-closed Tier A wirings and Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A5 is closed. Local and Postgres retrieval now attach `metadata["retrieved_text"] = sanitize_retrieved_text(hit.text, hit.trust_tier)` to returned hits while preserving stored content and visible `Hit.text`, so retrieved memory is explicitly marked as `retrieved_memory_data` with `instruction_authority=none` and `data-only`/`no-write-authority`/`sanitize-as-data` capability tags at the retrieval boundary. Focused shared/security retrieval tests pass, and the full local plus clean Postgres suites pass with 826 collected tests and 0 failures/errors on both rails. At this historical checkpoint, later-closed Tier A wirings and Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A3 and A4 are closed. Consolidation runs now build a pass-scoped `MutationRailBudget` from `export_tenant()` so Local and Postgres share the same live §31 accounting. Candidate promotions consume `max_supersession_rate=0.05` through a pre-merge `PromotionGate` veto that discards over-budget canary branches before `main` mutation; the forgetter and summary-refresh retirement paths consume `max_prune_fraction_per_pass=0.02`, leaving over-budget demotions/retirements deferred and visible in the `mutation_rails` pass result. The strict-xfail completion tests for rail 1 and rail 3 are now green, focused completion/consolidation coverage passes, and the full local plus clean Postgres suites pass with 826 collected tests, 0 failures/errors, and skips reduced to 80 local / 6 clean Postgres. At this historical checkpoint, later-closed Tier A wirings and Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A6 is closed. `ConsolidationWorker.run_queue_payload()` now enforces the §31 `[5 steps, 24h]` cadence rail by default: a per-tenant pass cannot rerun before five explicit or observed steps, callers can provide `consolidation_step`/`step` for deterministic scheduling, and the 24h upper bound lets stale tenants run even when fewer than five steps elapsed. Existing summary-refresh contracts now advance explicit pass steps instead of depending on back-to-back execution. The completion rail cadence test is green, focused consolidation cadence coverage passes, and the full local plus clean Postgres suites pass with 827 collected tests, 0 failures/errors, and skips reduced to 79 local / 5 clean Postgres. At this historical checkpoint, later-closed Tier A wirings and Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A13 is closed. `RuntimeJobHandlers.run_projection_recompute()` now computes a stable SHA-256 recompute fingerprint over tenant, branch, changed evidence CIDs, affected evidence CIDs, affected projection IDs, surviving source CIDs, pass list, and enqueue mode. Repeated unchanged recompute runs report `memo_hit: true`, increment a memo-hit metric, and skip duplicate consolidation enqueue side effects unless `force_recompute` is set; first runs still enqueue exactly the required surviving source evidence. Focused runtime and shared Local/Postgres recompute memo tests pass, and the full local plus clean Postgres suites pass with 827 collected tests, 0 failures/errors, and 79 local / 5 clean Postgres skips. At this historical checkpoint, later-closed Tier A wirings and Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A10 is closed. Local and Postgres `forget()` now extend the corroboration-aware retain/retract split to the derived-evidence cascade: normal tombstone/operator erasure erases only derived rows with no surviving source support, retains corroborated derived rows, trims affected source CIDs from top-level and summary metadata, and invalidates stale summary source fingerprints; legal hard-delete by `requested_by="legal"` remains corroboration-blind and shreds all derived evidence. The new shared Local/Postgres regression proves a dual-source derived summary survives forgetting one source while dependent assertions stay active on the retained derived evidence. Focused privacy/forget coverage passes, and the full local plus clean Postgres suites pass with 829 collected tests, 0 failures/errors, and 80 local / 5 clean Postgres skips. At this historical checkpoint, later-closed Tier A wirings and Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A7, A8, and A9 are closed. `PromotionGate.evaluate()` now surfaces `replay_predicted_lift` from counterfactual replay verdicts, `OperatingPolicy.cold_loop_counterfactual_trusted` is a default-off top-level rail gate, `RegressionCase` carries ignition `origin`/`mode`, and `PromotionGate.ignition_status()` keeps otherwise-passing private-suite candidates shadow-no-merge until active curated/genuine coverage reaches the ignition floor. `OperatingPolicy.actr_decay` also now drives ACT-R power-law lifecycle salience, retrieval base-level activation, and consolidation forgetter demotion while preserving the legacy default path at `0.0`. Focused and surrounding gate/policy/lifecycle/retrieval tests pass, and the full local plus clean Postgres suites pass with 833 collected tests, 0 failures/errors, and 80 local / 5 clean Postgres skips. At this historical checkpoint, later-closed Tier A wirings were A1 and A2; Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A1 is closed. `LocalMemoryEngine` now accepts the same `RetrievalAdapters` boundary as Postgres, defaults to byte-stable local hashing/similarity adapters, uses the configured embedding provider for local vector search and MMR embeddings, calls the configured reranker in local retrieval before diversification, and `cli.py:load_engine` threads provider flags/env through `--backend local` instead of silently ignoring real embedding/reranker providers. Focused A1 adapter seam tests and surrounding engine/CLI/retrieval suites pass, and the full local plus clean Postgres suites pass with 835 collected tests, 0 failures/errors, and 80 local / 5 clean Postgres skips. At this historical checkpoint, later-closed Tier A A2 calibrated confidence/ECE remained open; Tier B operator evidence remained open.

Latest checkpoint (2026-06-24): Tier A A2 is closed and the final red headline SLO is green. Local and Postgres retrieval now compute support-aware answer confidence, abstain when retrieved evidence does not cover enough query terms, expose `explain["confidence"]` with prediction-set size and query-support details, and preserve conformal threshold behavior across both engines. `eval/calibration/runner.py` now scores accept-vs-abstain decision confidence and writes a green FR-6 report: ECE 0.0063 against the <=0.05 target, Brier 0.0002, 25/25 good abstains, 0 false accepts, and `meets_target: true` for both policy and conformal passes. Focused A2 regressions, surrounding retrieval/calibration suites, the calibration runner, the full local suite, and the clean compose-Postgres suite pass. Current controlling strict-audit counts are 843 collected tests, 764 passed, and 79 skipped, with live Postgres portability passing 25/25. Mandatory Tier A source wirings are now closed; Tier B operator-captured real-infrastructure evidence remains open.

Latest checkpoint (2026-06-24): Optional Tier A A12 is closed locally. Postgres now has a tenant-scoped `graph_ppr_cache` table with RLS, a default-off `graph_ppr(..., use_cache=True)` read path, and `refresh_graph_ppr_cache()` materialization keyed by seed set, as-of key, relation-set fingerprint, and cache depth. Default recursive PPR remains the normal path. Focused local and live-DSN graph/PPR tests pass, the cache-depth guard prevents a small warmed cache from satisfying a larger request, and the OQ1 benchmark now detects the cache seam while keeping cached latency unmeasured unless a DSN-backed refresh/read benchmark is run. Tier B operator-captured production evidence remains open.

Latest checkpoint (2026-06-24): Tier B local real-service evidence is captured without changing source gates. `infra/scripts/setup-all.sh` and `infra/validate/validate-all.sh` pass against the local Docker stack: Keycloak OIDC/JWKS, Vault transit KMS, and the c2patool verifier all validate. A scoped `deployment-soak --evidence-dir` bundle in `/tmp/mnemosyne-tierb-soak/evidence` passed `idp-jwks-live-check`, `provider-check`, and `provenance-trust-check`; scoped `release-audit --allow-provider-local` passed with fingerprint `c37aa7aeba44aa82ccc0c5f4f9e130701f34c106136468de1be16c5c0105175c` and no findings. This proves local real services plus provider metadata are wired through the official evidence path, but it does not prove live ParadeDB/AGE/pgvector retrieval and does not close Tier B production parity: production operator evidence still requires `production_validated=true`, `target_environment=production`, and `operator_asserted=true` against deployed infrastructure.

Latest checkpoint: Production `release-audit` now rejects placeholder or malformed `worker-run` runtime evidence instead of accepting empty top-level sections. Required worker-run evidence must include non-empty worker, summary, queue, cycles, jobs, and metrics sections, prove at least one worker cycle, prove at least one processed job, include a processed-job cycle, and include job kind/status evidence. Focused release-audit verification passes in `/tmp/mnemosyne-release-worker-evidence-focused.xml` with 4 tests, 0 failures/errors, and 0 skips. This strengthens local production-evidence gating; real operator-run worker deployment evidence remains required for exact production parity.

Latest checkpoint: `ops-dashboard-check` now validates the embedded dashboard package metric taxonomy instead of accepting a structurally valid but hollow `ops-report.json` snapshot. Packaged reports must retain required counts, queue, metrics, learning, and tripwire sections, including audit/deletion counts, retrieval/calibration metric buckets, lesson diversity, proxy-vs-true gap, and gate promotion/rollback counters. Focused dashboard package verification passes in `/tmp/mnemosyne-dashboard-taxonomy-focused.xml`, and the full CLI runtime suite passes in `/tmp/mnemosyne-dashboard-taxonomy-cli-runtime.xml`. This tightens local package evidence gating; operator-run hosted dashboard deployment evidence remains required for exact production parity.

Latest checkpoint: `privacy-ops-check` now requires an explicit operator hard-delete corroboration case in production privacy bundles. Operator-delete evidence must include hashed request, approval, subject, audit-log, and delete-receipt references plus `min_corroboration_for_delete >= 2`, enough distinct supporting source hashes, proof that uncorroborated delete would be refused, and a failed post-delete read probe. Focused privacy verification passes in `/tmp/mnemosyne-privacy-operator-delete-focused.xml`. This strengthens local production privacy-evidence gating; real operator-run KMS/residency/delete evidence remains required for exact production parity.

Latest checkpoint: Command-backed consolidation candidate-extractor and evidence-summarizer providers now receive an explicit `prompt_boundary` contract in their JSON request payloads. The contract marks payload/evidence content and metadata as untrusted, names forbidden trusted prompt sinks such as `system_prompt`, and requires bounded JSON-object responses; a local regression verifies prompt-injection text remains only in untrusted fields. Focused verification passes in `/tmp/mnemosyne-provider-prompt-boundary-focused.xml`. Real production provider wrappers still need operator-run validation against deployed model endpoints.

Latest checkpoint: Production `release-audit` now rejects placeholder worker cadence evidence in addition to empty or malformed `worker-run` sections. Worker evidence must prove supervised multi-cycle operation, an idle-exit threshold, a nonzero poll interval, and fail-on-dead behavior before it can satisfy production handoff. Focused release-audit verification passes in `/tmp/mnemosyne-worker-cadence-focused.xml`. This keeps local single-cycle developer defaults from being mistaken for production worker deployment evidence.

Latest checkpoint: `mneme-mcp --self-test` now provides local MCP deployment preflight coverage for initialize/list/schema/auth/session/read-only-call behavior with secret-redacted output; `mneme-mcp --http` hosts the same JSON-RPC facade with `/healthz` liveness, `/mcp`, bearer/session-header binding, optional TLS and client-certificate enforcement, body-size limits, malformed/non-object/unsupported-method rejection, locked facade access, and stateless restart durability tests; and CLI `provider-check` now fails command KMS providers that retain keys after claimed shred success. Official streamable/SSE transport validation, real IdP/JWKS deployment validation against live credentials, production certificate provisioning/rotation, production stateless soak testing, real provider deployments, and production KMS/residency operations remain open.

Latest checkpoint: CLI `deployment-soak --evidence-dir` now writes a durable evidence bundle with a full soak report, one JSON record per check, and a manifest; the existing ops dashboard soak verifies both dashboard package output and the evidence bundle. Focused deployment-soak evidence tests pass. Local verification still collects 313 tests; the compose Postgres/shared suite remains green with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:...@127.0.0.1:54329/mnemosyne` across 66 live/shared tests. Exact parity still remains open for real provider deployment validation, real IdP/JWKS deployment validation against live credentials, production certificate lifecycle, hosted SSE/operator validation, production stateless soak testing, deployed trainer protected-suite/rollback validation, and hosted dashboard operations.

Latest checkpoint: Shared audit logs now record actor, source, trust tier, capability tags, and diff metadata for local JSON and Postgres-backed write paths. Evidence append, assertion upsert, preference write, learning-store audit, and forget/delete audit entries now carry source/tier context, with Postgres exporting source from the audit diff while using the existing trust-tier/capability columns. Focused local/Postgres shared-contract tests, the live Postgres engine suite, the full no-DSN suite, and the full compose Postgres suite pass; local verification now collects 322 tests with 278 passing tests plus 44 skipped live-DB tests, and the compose DSN run passes all 322 tests.

Latest checkpoint: Consolidation replayer is now a real prioritization pass instead of an input-order echo. The worker computes `importance*novelty*surprise*reward` from evidence metadata or payload replay-score overrides, feeds downstream consolidation passes in priority order, and reports per-CID factor scores in the pass result. Focused consolidation tests, `tests/test_runtime_parity_extensions.py`, the full no-DSN suite, and the full compose Postgres suite pass; local verification now collects 323 tests with 279 passing tests plus 44 skipped live-DB tests, and the compose DSN run passes all 323 tests.

Latest checkpoint: Consolidation `user_model_updater` now refreshes the latent user model from prioritized evidence and extracted candidates, writing a deterministic summary plus embedding through the existing `UserModel` API. Runtime job handlers and CLI worker paths pass and persist the user model alongside learning state. Focused runtime/CLI consolidation tests, `tests/test_runtime_parity_extensions.py`, the full no-DSN suite, and the full compose Postgres suite pass; local verification now collects 324 tests with 280 passing tests plus 44 skipped live-DB tests, and the compose DSN run passes all 324 tests.

Latest checkpoint: Consolidation `embedder` now persists missing evidence embeddings instead of falling through as `embedder_not_implemented`. Local and Postgres engines expose `set_evidence_embedding`, audit the embedding update, and respect backend embedding dimensions; the consolidation pass uses deterministic hashing embeddings and reports provider/dimension/embedded-CID details. Focused local/Postgres embedding tests, `tests/test_runtime_parity_extensions.py`, `tests/test_shared_engine_contract.py`, the live Postgres engine suite, the full no-DSN suite, and the full compose Postgres suite pass; local verification now collects 327 tests with 283 passing tests plus 44 skipped live-DB tests, and the compose DSN run passes all 327 tests.

Latest checkpoint: Consolidation `forgetter` now bridges into lifecycle demotion instead of falling through as `forgetter_not_implemented`. Local and Postgres engines expose audited evidence metadata updates; the forgetter computes lifecycle demotion with existing fidelity-tier utility rules, persists lifecycle metadata, and reports evaluated/demoted/failed CIDs. Focused local/Postgres metadata tests, `tests/test_runtime_parity_extensions.py`, `tests/test_shared_engine_contract.py`, the live Postgres engine suite, the full no-DSN suite, and the full compose Postgres suite pass; local verification now collects 330 tests with 286 passing tests plus 44 skipped live-DB tests, and the compose DSN run passes all 330 tests.

Latest checkpoint: Markdown/git source truth is now a concrete compiler path instead of an external assumption. CLI/MCP `source-sync` parses committed fenced TOML `mnemosyne-assertion` blocks, requires clean Git source by default, records `markdown_git` evidence with Git SHA/path/line provenance and `human-source-truth` capability tags, and upserts source-backed assertions through the normal engine contract. Local and Postgres assertion conflict resolution now prioritizes trust tier before recency so human-authored source truth cannot be displaced by newer lower-trust machine assertions, while preserving valid-time cutoffs for `as_of` queries. Focused source-sync/trust-precedence tests, affected local suites, live Postgres shared-contract coverage, the full no-DSN suite, and the full compose Postgres suite pass; local verification now collects 333 tests with 288 passing tests plus 45 skipped live-DB tests, and the compose DSN run passes all 333 tests.

Latest checkpoint: Consolidation summarizer now materializes a one-level derived gist instead of only returning a pass label. Local and command-backed summarizers append `consolidation-summary` evidence with `abstractive_gist` metadata, derived-trust tiering, and `derived-summary`/`consolidation-gist` capability tags, then link each source CID to the summary CID with `summary-derived-gist` relations. Focused local/CLI/live-Postgres summarizer tests, affected local runtime/CLI suites, the live Postgres engine suite, the full no-DSN suite, and the full compose Postgres suite pass; local verification remains 333 collected tests with 288 passing tests plus 45 skipped live-DB tests, and the compose DSN run passes all 333 tests.

Latest checkpoint: Retrieval now treats sole gist support as insufficient for a confident answer. Local and Postgres retrieval preserve summary/lifecycle metadata on evidence hits, detect when every budgeted hit is `abstractive_gist`/`consolidation-summary` support, and force abstention with an explicit source-inspection note while exposing `explain.gist_support`. Focused local/Postgres gist-only retrieval tests, shared local/Postgres contract suites, the full no-DSN suite, and the full compose Postgres suite pass; local verification now collects 335 tests with 289 passing tests plus 46 skipped live-DB tests, and the compose DSN run passes all 335 tests.

Latest checkpoint: Consolidation summary refresh now versions one-level gist projections by source fingerprint. Rerunning the summarizer over the same source set creates a fresh active summary, retires the prior summary with `superseded_by`/`retired_by` metadata, and local/Postgres retrieval skip retired summary evidence. Forget now walks transitive derived-evidence provenance through `source_evidence_cid`, `source_evidence_cids`, and `summary.source_evidence_cids`, so source erasure invalidates derived gist evidence and expires summary relations. Focused local/Postgres summary-refresh and forget-invalidation tests pass; local verification now collects 339 tests with 289 passing tests plus 50 skipped live-DB tests, and the compose DSN run passes all 339 tests. Multi-level RAPTOR clustering and bounded summary refresh scheduling remain open.

Latest checkpoint: Projection recompute now schedules bounded one-level summary refreshes through the existing runtime queue. The affected-evidence walk follows summary source metadata and `summary-derived-gist` relations, reports affected summary evidence/relations, and queues consolidation only for surviving non-summary source evidence so old summaries do not recursively fan out refresh jobs. Focused runtime and CLI projection-recompute tests pass; local verification now collects 340 tests with 290 passing tests plus 50 skipped live-DB tests, and the compose DSN run passes all 340 tests.

Latest checkpoint: Retrieval abstention now treats sole statistical-trace or explicit confabulation-risk support as insufficient, not only `abstractive_gist` or `consolidation-summary` evidence. Materialized gist summaries now carry `confabulation_risk=true`, and shared Local/Postgres contracts cover both statistical traces and non-gist summaries flagged with confabulation risk. Local verification now collects 342 tests with 291 passing tests plus 51 skipped live-DB tests, and the compose DSN run passes all 342 tests.

Latest checkpoint: Consolidation summarization now builds a deterministic multi-level RAPTOR-style hierarchy when the source set exceeds the configured cluster size. The worker materializes level-1 leaf summaries, then a level-2 root summary over child summary evidence; metadata records `raptor_level`, source fingerprints, `confabulation_risk=true`, transitive raw `source_evidence_cids`, and root `source_summary_cids`/`child_summary_cids`. Relations preserve raw-to-leaf and leaf-to-root `summary-derived-gist` lineage, and retrieval still abstains when the root summary is sole support. Local verification now collects 344 tests with 292 passing tests plus 52 skipped live-DB tests, and the clean compose DSN run passes all 344 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: CLI `search` now has direct facade-level proof that gist-only retrieval abstention is preserved outside the raw engine API. A CLI ingest/consolidate/search regression isolates generated summary evidence by querying the raw source CID, then verifies `abstained=true`, the source-inspection uncertainty note, the abstractive-gist hit metadata, and `explain.gist_support`. Local verification now collects 345 tests with 293 passing tests plus 52 skipped live-DB tests, and the clean compose DSN run passes all 345 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Projection recompute now has shared Local/Postgres coverage for RAPTOR hierarchy refresh. A changed raw source in a two-leaf/root summary tree marks the raw source, its leaf summary, and the root summary as affected, reports required raw-to-leaf and leaf-to-root summary lineage relations, and queues one consolidation refresh for the raw source only. Local verification now collects 346 tests with 294 passing tests plus 52 skipped live-DB tests, and the clean compose DSN run passes all 346 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Shared branch parity now covers explicit non-main source-branch inheritance. Local and Postgres both copy main-inherited plus source-only evidence, source-branch assertions, and source-branch relations into a child branch created with `frm=<source_branch>`, keep source-only rows isolated from `main`, and export `from_branch` metadata for the child branch. Local verification now collects 347 tests with 295 passing tests plus 52 skipped live-DB tests, and the clean compose DSN run passes all 347 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: MCP `search` now has direct JSON-RPC facade proof that gist-only retrieval abstention is preserved outside the raw engine API. A stateful MCP server seeds a real consolidation summary and `tools/call` search verifies `abstained=true`, the source-inspection uncertainty note, abstractive-gist hit metadata, and `explain.gist_support`. Local verification now collects 348 tests with 296 passing tests plus 52 skipped live-DB tests, and the clean compose DSN run passes all 348 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Hosted HTTP JSON-RPC MCP `search` now has transport-level proof that gist-only retrieval abstention survives capture persistence, consolidation summary materialization, server restart, and `tools/call` search over `/mcp`. CLI `tools` output is also pinned to the exact MCP `tools/list` contract so agent-facing tool names, schemas, required fields, defaults, nullability, and `additionalProperties` cannot drift between surfaces. Local verification now collects 352 tests with 298 passing tests plus 54 skipped live-DB tests, and the clean compose DSN run passes all 352 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Deep retrieval now treats graph/PPR relation hits derived from generated gist summaries as gist-tier support. Local and Postgres `graph_ppr` hits now preserve relation source, predicate, target, confidence, and source evidence CIDs in metadata, and retrieval abstention classifies `summary-derived-gist` relations alongside generated summary evidence. Shared Local/Postgres coverage verifies deep-search abstention on summary-derived graph support, and MCP `deep_search` plus `explain` both preserve the abstention payload. Local verification now collects 355 tests with 300 passing tests plus 55 skipped live-DB tests, and the clean compose DSN run passes all 355 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Official MCP SDK StreamableHTTP local validation now exercises the write/read tool path, not only tool discovery and read-only policy inspection. The in-process ASGI StreamableHTTP client initializes a session, lists tools, calls `residency_policy`, captures evidence, then searches for it and verifies provenance through the official MCP client surface. Local verification remains 355 collected tests with 300 passing tests plus 55 skipped live-DB tests, and the clean compose DSN run passes all 355 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Hosted HTTP JSON-RPC MCP `deep_search` and `explain` now have transport-level coverage for relation-only `summary-derived-gist` graph support, matching the in-process MCP safety rail for gist-derived graph abstention. The clean DSN run also exposed and fixed a Postgres relation-expiry edge case in `forget`: relation `valid_to` now uses `GREATEST(clock_timestamp(), valid_from + interval '1 microsecond')` so erasure propagation cannot violate the `valid_to > valid_from` schema check when app-generated `valid_from` is newer than the SQL transaction timestamp. Local verification now collects 356 tests with 301 passing tests plus 55 skipped live-DB tests, and the clean compose DSN run passes all 356 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Direct retrieval primitive budget enforcement now has shared Local/Postgres contract coverage. A new primitive test seeds multiple matching evidence/assertion/relation rows and verifies `lexical_search`, `vector_search`, and `graph_ppr` each honor `k=1` while preserving tenant, branch, and backend channel metadata. Local verification now collects 358 tests with 302 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 358 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: CLI `deep-search` and `explain` now have direct runtime coverage for relation-only `summary-derived-gist` graph support. The regression seeds generated summary evidence plus a gist-derived relation into the CLI store, runs both commands, and verifies `abstained=true`, the source-inspection uncertainty note, relation metadata, and `explain.gist_support`. Local verification now collects 359 tests with 303 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 359 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: CLI `projection-recompute-enqueue` now has direct runtime coverage for queue payload persistence. The regression verifies tenant/user/branch, multiple changed evidence CIDs, custom max attempts, and disabled consolidation enqueue semantics are emitted into the queued `projection_recompute` job. Local verification now collects 360 tests with 304 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 360 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: CLI `profile-record-explicit` and `trajectory-attribute` aliases now have direct runtime persistence coverage. The regression verifies scoped explicit preference recording persists scope, confidence, evidence CIDs, authorization metadata, and scoped profile-context visibility; it also verifies trajectory failure attribution emits the expected cause, signature, confidence, and evidence. Local verification now collects 361 tests with 305 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 361 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: The CLI runtime handler mention scan now reports 69 handlers with 0 unmentioned command surfaces in `tests/test_cli_runtime_tools.py`. Direct coverage was added for `prefetch`, `discard`, `trajectory-log`, `lesson-induce`, and `procedure-induce`, including prefetch execute/skip behavior, authorized candidate-branch discard with empty post-discard retrieval, and direct learning alias persistence. Local verification now collects 362 tests with 306 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 362 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Requirements status has been synchronized for calibrated confidence, justification/cascade belief revision, and trajectory-driven learning to distinguish implemented local/clean-DSN behavior from remaining production validation. The conformal calibration unit test now explicitly covers oversized prediction-set abstention in addition to below-threshold and empty-set abstention. Local verification still collects 362 tests with 306 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 362 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: In-process MCP `tools/call` behavior now has direct parity coverage for blueprint aliases beyond schema/listing. The regression exercises `source_sync`, `profile_record_explicit`, `profile_propose_inference`, `profile_correct`, `profile_get_relevant`, `graph_timeline`, `graph_as_of`, `trajectory_log`, `trajectory_attribute`, `lesson_induce`, `procedure_induce`, `lesson_search`, `procedure_search`, and `outcome_evaluate` against persisted state. Local verification now collects 363 tests with 307 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 363 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Durable Postgres queue parity now covers the runtime maintenance job handlers, not only queue mechanics and single-job calibration. A live test drains `calibrate`, `lifecycle_sweep`, `eval_suite`, `observability_snapshot`, and `projection_recompute` through `PostgresQueue` plus `QueueWorker`, verifies persisted complete results, verifies projection recompute over a real Postgres assertion, and caught/fixed `eval_suite` result serialization by switching to dataclass `asdict`. Local verification now collects 364 tests with 307 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 364 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Deployment-soak reports and evidence bundles now explicitly label local-harness evidence scope. Reports and manifests include `validation_scope.surface=local_cli_orchestrator`, `production_validated=false`, and redaction metadata showing raw child commands and stderr are omitted while only structured child JSON is retained. Existing leak-guard and evidence-bundle tests now lock those fields. Local verification still collects 364 tests with 307 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 364 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: The user-model guard test now actually covers all six blueprint memory categories: identity, hard instruction, explicit preference, inferred preference, situational preference, and temporary state. It verifies authority ordering, scope matching, and inferred supersession under higher-authority hard instructions. REQ-012 status has been updated to distinguish implemented model/runtime coverage from non-blocking product UX work. Local verification still collects 364 tests with 307 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 364 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: REQ-015 rehearsal scheduling is now storage-backed instead of helper-only. `LifecycleState` carries must-keep rehearsal metadata, due must-keep/protected memories are rehearsed at expanding intervals before demotion, consolidation `forgetter` persists `successful_rehearsals`/`next_rehearsal_at` through audited evidence metadata updates, and runtime lifecycle sweeps report rehearsal counts through local and durable Postgres queue paths. Local verification now collects 365 tests with 308 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 365 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: REQ-018 now has a concrete contextual bandit learner behind the shadow policy optimizer. Policy-variant outcomes are logged in the self-model store with bounded rewards, context, and metrics; the learner uses deterministic UCB scoring to choose among invariant-rail-safe variants, so logged poor recall/stable rewards steer future proposals without mutating production policy outside the gate. Local verification now collects 366 tests with 309 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 366 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: REQ-011 expanded-suite enforcement now goes beyond protected-case counting. CLI `gate-suite-check` can require a minimum total case count and explicit smoke/core/archive tier coverage; reports include missing required tiers and fail closed before release fingerprints are accepted. The regression suite proves missing archive coverage fails and a three-tier protected suite passes with stable fingerprint review. Local verification still collects 366 tests with 309 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 366 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: REQ-016 consolidation now emits first-class society-of-roles provenance. `ConsolidationRunResult.role_pipeline` reports the consolidator owner, write authorization, per-pass role names, provider strategies, provider type, and status; CLI and live Postgres command-provider tests verify model-backed extractor/resolver/summarizer roles are visible in runtime results. Local verification still collects 366 tests with 309 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 366 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: REQ-002 merge audit is now visible in tenant exports, not only internal engine state. Local and Postgres `export_tenant` include `merge_log`; the shared Local/Postgres branch-merge contract verifies promoted evidence/assertions/relations and tenant-exported merge counters for the source/target branches. Local verification still collects 366 tests with 309 passing tests plus 57 skipped live-DB tests, and the clean compose DSN run passes all 366 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Progress: [███████░░░] local scaffold verified; production parity gaps remain

## Performance Metrics

**Velocity:**

- Total plans completed: 12
- Average duration: not yet measured
- Total execution time: not yet measured; latest P5 local verification completed on 2026-06-28

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 0 | 4 complete | 4 | pending |
| 1 | 5 complete | 5 | pending |
| 2 | 5 complete | 5 | pending |
| 3 | 5 complete | 5 | pending |
| 4 | 5 complete | 5 | pending |
| 5 | 5 complete | 5 | pending |

## Accumulated Context

### Decisions

- [Phase 0]: V2 blueprint is controlling; v1 is lineage only.
- [Phase 0]: Build originally moved to `/Users/admin/Projects/Mnemosyne` because `/Users/admin/Desktop/Mnemosyne` was write-blocked; the current canonical checkout is `/Users/admin/Mnemosyne`.
- [Phase 0]: Local deterministic engine is used for fast verification; `sql/schema.sql` preserves the production PostgreSQL contract.
- [Phase 1]: Retrieval currently uses deterministic hashing embeddings and lexical scoring for local tests; production adapters remain required.
- [Phase 2]: Graph/PPR remains behind the engine contract; a benchmark harness now exists for backend profiling.
- [Phase 2]: Justification DAG, cascade invalidation, operation classification, contested hypotheses, and conformal calibration are implemented locally.
- [Phase 3]: Six-category user model, scope-matched context assembly, latent advisory profile, rehearsal schedule, and anti-degradation guard are implemented locally.
- [Phase 4]: Trajectory logging, failure attribution, lesson/procedure induction, gate promotion, replay scoring, and permanent poisoning cases are implemented locally.
- [Phase 5]: Self-model store, outcome windows, policy variant proposal, tripwire checks, and canary policy gate are implemented locally.
- [NFR]: Local latency benchmark, schema coverage, privacy classification, metrics registry, and Docker compose config are implemented.
- [Phase 5]: Self-optimization remains shadow-first and constrained by immutable rails.
- [Runtime]: CLI now supports `--backend local|postgres` plus `--postgres-dsn`/`MNEMOSYNE_POSTGRES_DSN`; live tests verify the Postgres backend through real CLI commands.
- [Parity]: `tests/test_shared_engine_contract.py` now runs the same evidence retrieval/export, branch/discard, and bitemporal `as_of` contracts against `LocalMemoryEngine` and `PostgresEngine`; this also fixed Postgres branch creation for previously unseen tenants.
- [Retrieval]: Local and Postgres `graph_ppr` now exclude expired relations by default while preserving explicit historical `as_of` graph traversal.
- [Security]: `MemoryTools` now enforces `SecurityPolicy` for preference writes, hard-instruction profile writes, and destructive forget operations; CLI denials are covered by tests.
- [Security]: Fact, relation, proposal, supersession, and correction writes now route through `MemoryTools` capability checks; CLI `assert`/`relation` no longer bypass the facade.
- [Security]: Branch creation, confirmation, merge, and discard now require explicit role/source-trust context through `MemoryTools` and CLI; branch promotion requires operator/consolidator authority.
- [Security]: Lesson promotion and procedure validation/promotion/rollback now require mediated operator/consolidator authority through `MemoryTools`, CLI flags, or signed session injection before mutating runtime learning state.
- [Security]: OIDC/JWKS `session-exchange` now supports fail-closed authz policy mapping from verified IdP client/tenant/claim rules to Mnemosyne role/trust claims for CLI and hosted HTTP exchange; policy mode rejects malformed schemas, missing client allowlists, invalid roles, rule misses, ambiguous matches, and raw role/trust fallback; `idp-authz-policy-check` emits bounded secret-free summaries for deployment review.
- [MCP]: `mneme-mcp --sdk` can now run the same tool facade through the official Python MCP SDK when the optional `mcp` extra is installed; JSON-RPC and SDK handler tests cover tool listing, generated input-schema validation, explicit optional-null fields, auth-token denial, capture, and search, and CLI `tools` emits the same MCP-style `inputSchema` shape.
- [Consolidation]: Default local passes now resolve deterministic entity keys and execute summarizer, lesson_distiller, and skill_inducer roles for fact candidates; lessons/procedures persist through runtime learning state and are visible in `ops-report`. Command-backed candidate extractor, summarizer, and entity resolver providers can now supply model-backed consolidation boundaries and are validated by `provider-check`. `projection_recompute` can now compute affected evidence/projection sets from changed CIDs and queue consolidation only for dirty surviving source inputs with projection-specific pass selection.
- [Source Truth]: CLI/MCP `source-sync` compiles committed Markdown/git assertion blocks into evidence-backed assertions with Git SHA/path/line provenance; trust-tier conflict resolution is now monotonic, so tier-0 human source truth overrides newer lower-trust machine memory across local and Postgres backends.
- [Queue]: CLI `queue-enqueue`, `queue-drain`, `queue-snapshot`, and ingestion enqueueing can now use `--queue-backend postgres` with tenant-scoped `FOR UPDATE SKIP LOCKED` leasing and durable result persistence.
- [Gate]: CLI `gate-case-add/list` persists protected regression cases in runtime state; `gate-suite-check` reports suite counts/tier coverage/fingerprint and fails closed on missing protected cases or fingerprint mismatch; consolidation workers load protected cases and fail closed when a protected case regresses.
- [Observability]: CLI `ops-report` now exports dashboard-ready counts, queue state, learning diversity, proxy-vs-true gap, and tripwire status for a tenant; `--dashboard-html` writes a static dashboard artifact with memory, queue, retrieval, calibration, learning, gate/eval, and bounded JSON sections.
- [Storage]: `sql/schema.sql` now enables and forces tenant RLS on tenant-owned tables, and `PostgresEngine` sets `mnemosyne.tenant_id` before tenant-scoped SQL.
- [Privacy]: Local and Postgres forget paths now accept `tombstone_recompute` or `hard_delete_legal`; CLI exposes `--erasure-mode`; encrypted local object storage supports AES-GCM per-object keys, JSON-backed and command-backed key managers, provider-check key-cycle validation, and legal hard-delete crypto-shreds the object key after successful forget; CLI and MCP ingestion enforce configured data residency labels, strict runtime processing residency when enabled, and explicit cross-region `source->target` transfer allowlists, then record residency decisions in evidence access policy; CLI/MCP can inspect the active residency policy and provider-check reports it; forget now erases media-derived evidence and retracts/strips assertions/preferences/relations that depended on source or derived evidence.
- [Retrieval]: CLI/Postgres can now use local or HTTP-compatible embedding and reranker providers via flags/env while keeping deterministic local defaults; HTTP providers now fail closed on malformed responses and `provider-check` exits nonzero on broken contracts.
- [Audit]: Local and Postgres audit entries now expose `source`, `trust_tier`, `capability_tags`, and structured `diff` fields for write-path provenance, including evidence, assertions, preferences, learning records, and legal delete/forget flows.
- [Parametric]: CLI and MCP can now use a command-backed LoRA/test-time-training adapter provider for proposal, evaluation, and rollback; provider calls are operator-authorized, shell-free, JSON-contract validated, included in `provider-check`, persisted with metrics/payloads, and gated by local mutation-rate bounds, external-only reward metadata, monotonic-trust/sink restrictions, exact artifact/gate matching, protected-case pass requirements, and margin-over-noise checks. Parametric evaluation and rollback prefer persisted protected regression cases from runtime state, report stable protected-suite snapshots, persist rollback suite metadata, pass the suite to command-backed rollback providers, and validate the rollback suite contract in `provider-check`.
- [Provenance]: CLI `ingest` now accepts `--file`, modality/media type, signed-provenance JSON, a `--c2pa-tool` adapter, `--trusted-provenance-root`, and `--provenance-trust-policy`; C2PA reports must bind to the exact asset by SHA-256/path before they can raise trust, scoped policies can match trusted tenant/source/modality context and quarantine valid-but-untrusted manifests when no rule, issuer, or certificate root matches, provenance validity/trust/binding capability tags propagate through evidence, and quarantined evidence is hidden from default retrieval unless `include_quarantined` is explicitly set.
- [Security]: Trust-tier semantics now follow the blueprint scale: `0` direct-user/highest trust through `5` untrusted external; retrieval/write gates, provenance deltas, SQL defaults, and CLI defaults were migrated.
- [Ingestion]: The hot path now classifies actor/source trust, adds capability/taint tags, detects simple PII, raises sensitivity/access-policy metadata, marks untrusted imperatives as data-only, indexes metadata-derived OCR/transcript/caption/alt/description text for externalized object evidence, and queues async media extraction when non-text payloads lack derived text.

### Pending Todos

- Finish exact blueprint parity, starting with expanding the live PostgresEngine smoke into a full shared contract suite, official hosted MCP streamable/SSE validation, real IdP/JWKS validation, and remaining production adapter deployment checks.
- Validate real production embedding/reranker deployments behind the HTTP-compatible adapter boundary; add ParadeDB/BM25 where needed and AGE/specialist graph adapters where needed.
- Add real secret-manager-backed session-secret command deployment/rotation validation, real IdP/JWKS deployment validation against live credentials, real KMS deployment validation behind the command key-provider boundary, real production C2PA trust-root validation, native image/audio embedding providers, production extractor deployment validation, real production entity resolver validation behind the command resolver boundary, real parametric trainer deployment validation behind the command provider boundary, production separation of immutable-rail service/credentials, production residency-policy operations, worker supervision/deployment operations, and deployment observability.

Latest checkpoint: Consolidation now supports a command-backed entity resolver boundary. The worker invokes the resolver between candidate extraction and promotion, records resolver pass details, persists resolver-provided entity keys into the tenant entity registry after gate promotion, and keeps deterministic resolution as the local default. CLI `ingest --run-consolidation-once`, runtime workers, provider manifests, and `provider-check` can configure the command resolver; provider-check fails closed when command output omits explicit candidate mappings. Focused CLI tests cover runtime consolidation, deployment-manifest provider checks, and malformed resolver failure. This reduces the richer entity-resolution gap while real deployed resolver validation remains open.

Latest checkpoint: Runtime jobs now include `projection_recompute` plus CLI `projection-recompute-enqueue` and `projection-recompute-once`. The job consumes changed evidence CIDs, expands through derived evidence/provenance edges such as `media-derived-text`, reports affected assertion/preference/relation/entity IDs, and can enqueue consolidation only for dirty surviving source inputs with projection-specific pass selection. It is allowlisted for `deployment-soak` as a bounded preflight. Focused CLI/runtime tests cover direct fact projections and media-derived evidence dependency expansion. This implements the blueprint's initial salsa-style affected-subgraph recompute guard while broader production IVM/differential recompute remains open.

Latest checkpoint: Consolidation now has shell-free command provider boundaries for candidate extraction and evidence summarization. The extractor receives tenant/payload/evidence JSON and must return fully shaped candidate rows before resolver/gate execution; the summarizer receives tenant/evidence JSON and must return a non-empty summary. CLI flags, provider manifests, runtime workers, and `provider-check` can configure both providers; provider-check fails closed on malformed extractor or summarizer output. Focused CLI tests cover model-style extraction of a non-deterministic evidence note, command summarizer pass details, deployment-manifest provider checks, and malformed provider failures. This reduces the model-backed extraction/summarization gap while real deployed model/provider validation remains open.

Latest checkpoint: `gate-suite-check` now provides a deployment-control surface for protected regression suites. It loads persisted gate cases from runtime state, reports case/protected/tier counts plus a stable canonical fingerprint, can include full cases for export/review, and exits nonzero when the protected count is below `--min-protected` or `--expected-fingerprint` does not match. The command is allowlisted for `deployment-soak`. CLI tests cover fingerprint acknowledgement and fail-closed mismatch behavior. This reduces protected-suite management risk while production release-artifact validation remains open.

Latest checkpoint: Live Postgres coverage now includes command-backed consolidation candidate extraction and summarization. The live test uses `PostgresEngine` with shell-free command provider adapters, promotes a non-deterministic evidence note through the protected gate, verifies command extractor/summarizer pass details, and confirms exported Postgres assertions/entities carry the provider-produced candidate. This broadens the shared Local/Postgres consolidation contract; it still requires `MNEMOSYNE_POSTGRES_DSN` to run.

Latest checkpoint: The live Postgres command-backed consolidation provider test now also exercises the command entity resolver boundary. The resolver receives provider-produced candidates, returns explicit candidate-to-entity mappings and entity metadata, and the test verifies the resolver pass details plus the persisted Postgres entity canonical supplied by the resolver. This broadens live production-adapter parity for extraction, resolution, and summarization while real deployed resolver/model validation remains open.

Latest checkpoint: Provider deployment manifests now have an explicit retrieval-backend health check for lexical and graph adapters. `provider-check` reports configured lexical/graph backend names and whether either is a local fallback; production manifests with `forbid_local` now fail closed on local lexical or graph backends, not only local embedding/reranker providers. Focused CLI tests cover a ParadeDB/AGE-style production manifest and a local BM25/PPR rejection path.

Latest checkpoint: CLI Postgres DSN selection now treats an explicit empty `--postgres-dsn ""` as fail-closed instead of falling back to `MNEMOSYNE_POSTGRES_DSN`; runtime state and Postgres queue loading follow the same rule. The regression test sets an env DSN to prove the explicit-empty override is honored. The full test suite passes against the local compose pgvector DSN `postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne` after this fix.

Latest checkpoint: CLI `tls-lifecycle-ops-check` now validates production TLS lifecycle evidence for CA/ACME issuance, renewal execution, current/candidate overlap, deployed serial/chain match, reload verification, non-local private-key custody, monitoring alerts, and raw key/cert/token/log redaction. The command is part of `deployment-soak` and the production `release-audit` output-shape profile; actual production TLS evidence remains operator-run.

Latest checkpoint: CLI `ops-dashboard-check` now accepts optional production dashboard operations evidence in addition to fingerprinted dashboard packages or hosted URLs. The gate validates production scope, refresh freshness, source snapshot fingerprinting, access controls, alert delivery, tenant binding, and raw snapshot/token/user-data redaction without storing raw dashboard payloads. Focused dashboard/release-audit coverage passes in `/tmp/mnemosyne-dashboard-ops-focused.xml` with 4 tests, the full local suite passes in `/tmp/mnemosyne-dashboard-ops-full-local.xml` with 431 tests, 0 failures/errors, and 63 live-DB skips, and the compose Postgres DSN suite passes in `/tmp/mnemosyne-dashboard-ops-full-postgres.xml` with 431 tests, 0 failures/errors, and 0 skips. Actual hosted production dashboard evidence remains operator-run.

Latest checkpoint: CLI `retrieval-ops-check` now requires production specialist adapter probe evidence for lexical, vector, graph, and reranker retrieval paths. The gate validates non-local backend/provider names, production validation, SHA-256 command/source/query/tenant/result/top-id fingerprints, hit counts, bounded latency, and redaction of raw query/document/result/stdout/stderr/command/env/credential fields. Focused retrieval/release-audit coverage passes in `/tmp/mnemosyne-retrieval-adapter-focused.xml` with 6 tests, the full local suite passes in `/tmp/mnemosyne-retrieval-adapter-full-local.xml` with 433 tests, 0 failures/errors, and 63 live-DB skips, and the compose Postgres DSN suite passes in `/tmp/mnemosyne-retrieval-adapter-full-postgres.xml` with 433 tests, 0 failures/errors, and 0 skips. Actual deployed ParadeDB/AGE/pgvector/reranker evidence remains operator-run.

Latest checkpoint: CLI `privacy-ops-check` now validates explicit raw key/object/subject/KMS-response redaction flags and recursively rejects raw privacy fields such as key material, object bytes, subject identifiers, credentials, tokens, and KMS responses. Focused privacy/release-audit coverage passes in `/tmp/mnemosyne-privacy-redaction-focused.xml` with 4 tests, the full local suite passes in `/tmp/mnemosyne-privacy-redaction-full-local.xml` with 434 tests, 0 failures/errors, and 63 live-DB skips, and the compose Postgres DSN suite passes in `/tmp/mnemosyne-privacy-redaction-full-postgres.xml` with 434 tests, 0 failures/errors, and 0 skips. Actual production KMS/residency evidence remains operator-run.

Latest checkpoint: CLI `parametric-trainer-check` now requires a production `deployment` section in addition to trainer, protected-suite, gate, rollback, rail, metrics, and redaction evidence. The gate validates HTTPS supervised endpoint health, canary pass, alert routing, protected-suite/artifact/rollback fingerprint binding, rollback drill verification, and bounded deployment latency. Focused parametric/release-audit coverage passes in `/tmp/mnemosyne-parametric-deployment-focused.xml` with 3 tests, the full local suite passes in `/tmp/mnemosyne-parametric-deployment-full-local.xml` with 434 tests, 0 failures/errors, and 63 live-DB skips, and the compose Postgres DSN suite passes in `/tmp/mnemosyne-parametric-deployment-full-postgres.xml` with 434 tests, 0 failures/errors, and 0 skips. Actual trainer infrastructure evidence remains operator-run.

Latest checkpoint: CLI `provenance-ops-check` now requires a production `deployment` section in addition to validation-scope, verifier, trust-root, trust-suite, asset-bound, quarantine, ingestion, and redaction evidence. The gate validates supervised verifier health, trust-root refresh, quarantine drill, ingestion-pipeline supervision, alert routing, deployment execution fingerprints, trust-policy fingerprint binding, ingestion evidence-count binding, and bounded deployment latency. Focused provenance/release-audit coverage passes in `/tmp/mnemosyne-provenance-deployment-focused.xml` with 3 tests, the full local suite passes in `/tmp/mnemosyne-provenance-deployment-full-local.xml` with 434 tests, 0 failures/errors, and 63 live-DB skips, and the compose Postgres DSN suite passes in `/tmp/mnemosyne-provenance-deployment-full-postgres.xml` with 434 tests, 0 failures/errors, and 0 skips. Actual C2PA verifier/trust-root/quarantine/ingestion deployment evidence remains operator-run.

Latest checkpoint: CLI `multimodal-ops-check` now requires a production `deployment` section in addition to validation-scope, provider, object-store, extraction, media-embedding, retrieval, media-job, and redaction evidence. The gate validates supervised extraction, embedding, object-store, media-job, and retrieval operations, alert routing, execution fingerprints, deployment count binding across object hashes/extraction cases/embedding hashes/retrieval cases/media jobs, and bounded deployment latency. Focused multimodal/deployment-soak/release-audit coverage passes in `/tmp/mnemosyne-multimodal-deployment-focused.xml` with 4 tests, the full local suite passes in `/tmp/mnemosyne-multimodal-deployment-full-local.xml` with 434 tests, 0 failures/errors, and 63 live-DB skips, and the compose Postgres DSN suite passes in `/tmp/mnemosyne-multimodal-deployment-full-postgres.xml` with 434 tests, 0 failures/errors, and 0 skips. Actual extractor/media-embedding/object-store/retrieval/media-job deployment evidence remains operator-run.

Latest checkpoint: §31 RAIL-2 destructive erasure is now enforced on local and Postgres engines. Operator hard-delete requests refuse to erase the sole support for an active assertion with `reason=min_corroboration_for_delete`, while legal/right-to-be-forgotten erasure remains corroboration-blind. Postgres public serialization was tightened so internal tenant/user/session metadata and auto-generated search vectors do not leak through evidence export, explicit evidence embeddings still round-trip, audit diffs omit internal CID storage shims, assertion/preference exports recover external user IDs from source evidence, calibration/entity exports match local public shape, and the cross-engine portability fixture uses valid preference categories plus fixed 1024-dim SQL-compatible embedding vectors. Focused RAIL/Postgres portability coverage passes in `/tmp/mnemosyne-rail2-pg-portability-focused.xml` with 10 tests, the full local suite passes in `/tmp/mnemosyne-rail2-full-local.xml` with 783 tests, 0 failures/errors, and 72 live-DB skips, and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-rail2-full-postgres.xml` with 783 tests, 0 failures/errors, and 8 skips. Exact production-evidence parity remains open.

Latest checkpoint: CLI `consolidation-ops-check` now requires a production `deployment` section in addition to validation-scope, worker, provider, hosted role, projection, protected-suite, embedding, consolidation-run, calibration, lifecycle, ops-report, and redaction evidence. The deployment gate validates production deployment assertion, supervised controls for each consolidation surface, alert-route delivery verification, SHA-256 execution fingerprinting, protected-suite fingerprint binding, strict integer count binding across worker/provider/projection/suite/embedding/consolidation/calibration/lifecycle/ops sections, and present finite non-negative bounded deployment latency. Focused consolidation/release-audit coverage passes in `/tmp/mnemosyne-consolidation-deployment-focused.xml` with 9 tests, 0 failures/errors, and 0 skips, including regressions for missing/negative/non-finite latency, flat alert-route bypasses, and fractional deployment bindings. Actual production worker/provider/projection/calibration/lifecycle/ops deployment evidence remains operator-run.

Latest checkpoint: Direct `MemoryTools` facade parity now covers the previously uncalled alias/search/promotion wrappers `supersede`, `profile_context`, `trajectory_record`, `lesson_propose`, `procedure_propose`, `lesson_promote`, `lesson_search`, and `procedure_search`. The focused direct facade test passes in `/tmp/mnemosyne-facade-alias-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_runtime_surfaces.py` passes in `/tmp/mnemosyne-facade-alias-runtime-surfaces.xml` with 54 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-facade-alias-full-local.xml` with 786 tests, 0 failures/errors, and 72 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-facade-alias-full-postgres.xml` with 786 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Postgres-backed MCP runtime facade parity now exercises the optional command parametric provider path. The DSN-gated live test persists profile and learning state through `MnemosyneMcpServer(backend="postgres")`, reloads it through a fresh server, validates/proposes/promotes learning artifacts, then runs `parametric_propose`, `parametric_evaluate`, and `parametric_rollback` against a fake command trainer with a dedicated artifact store. Focused verification passes in `/tmp/mnemosyne-postgres-command-parametric-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_postgres_engine_live.py` passes in `/tmp/mnemosyne-postgres-command-parametric-live-file.xml` with 24 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-postgres-command-parametric-full-local.xml` with 786 tests, 0 failures/errors, and 72 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-postgres-command-parametric-full-postgres.xml` with 786 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Shared Local/Postgres direct `RuntimeJobHandlers` parity now covers public maintenance methods beyond projection recompute. The shared engine contract verifies the handler map exposes media extraction, calibration, lifecycle, eval, observability, and projection jobs, then dispatches media extraction, calibration, lifecycle sweep, eval-suite, and observability snapshot through that map, verifying structured results, exact calibration persistence, lifecycle rehearsal/demotion output, eval-suite JSON serialization, and emitted counters across both backends. Focused local verification passes in `/tmp/mnemosyne-runtime-handlers-focused-local.xml` with 2 tests, 0 failures/errors, and 1 skipped DSN case; focused DSN verification passes in `/tmp/mnemosyne-runtime-handlers-focused-postgres.xml` with 2 tests, 0 failures/errors, and 0 skips; full shared-contract DSN verification passes in `/tmp/mnemosyne-runtime-handlers-shared-contract.xml` with 78 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-runtime-handlers-full-local.xml` with 788 tests, 0 failures/errors, and 73 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-runtime-handlers-full-postgres.xml` with 788 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Shared Local/Postgres raw-media embedding parity now proves multimodal ingestion stores raw media embeddings before extraction produces derived text. The shared engine contract ingests externalized image bytes through `IngestionPipeline` with a `MediaEmbeddingProvider`, verifies raw bytes round-trip through `LocalObjectStore`, stores the provider embedding and metadata on empty-content media evidence, queues `media_extract` before consolidation, and retrieves the otherwise textless media row through the stored vector across both Local and Postgres backends. Focused local verification passes in `/tmp/mnemosyne-raw-media-embedding-focused-local.xml` with 2 tests, 0 failures/errors, and 1 skipped DSN case; focused DSN verification passes in `/tmp/mnemosyne-raw-media-embedding-focused-postgres.xml` with 2 tests, 0 failures/errors, and 0 skips; full shared-contract DSN verification passes in `/tmp/mnemosyne-raw-media-embedding-shared-contract.xml` with 80 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-raw-media-embedding-full-local.xml` with 790 tests, 0 failures/errors, and 74 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-raw-media-embedding-full-postgres.xml` with 790 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Direct `MemoryTools.confirm` facade parity now covers proposal promotion outside JSON-RPC transport. The runtime-surface test creates a proposal branch through `MemoryTools.propose`, confirms it by assertion id with operator authority and tenant-scoped candidate-branch discovery, verifies the merge report and security decision, and checks the promoted assertion lands on `main` with its source evidence preserved. Focused verification passes in `/tmp/mnemosyne-memorytools-confirm-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_runtime_surfaces.py` passes in `/tmp/mnemosyne-memorytools-confirm-runtime-surfaces.xml` with 55 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-memorytools-confirm-full-local.xml` with 791 tests, 0 failures/errors, and 74 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-memorytools-confirm-full-postgres.xml` with 791 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Direct `MemoryTools.assert_fact` and `MemoryTools.preference` facade parity now covers low-count write wrappers outside JSON-RPC transport. The runtime-surface test writes a fact and explicit preference through trusted direct facade inputs, verifies low-trust denials for belief and preference sinks, then checks exported assertion/preference state, source evidence preservation, trust tier, explicitness, and statement/category payloads. Focused verification passes in `/tmp/mnemosyne-memorytools-write-facade-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_runtime_surfaces.py` passes in `/tmp/mnemosyne-memorytools-write-facade-runtime-surfaces.xml` with 56 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-memorytools-write-facade-full-local.xml` with 792 tests, 0 failures/errors, and 74 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-memorytools-write-facade-full-postgres.xml` with 792 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Direct `MemoryTools.correct` and `MemoryTools.prefetch` facade parity now covers correction and anticipatory retrieval wrappers outside JSON-RPC transport. The runtime-surface test verifies user-authored correction writes, low-trust correction denial, correction evidence linkage, prefetch execution gating for high-probability, network-side-effect, and low-confidence candidates, warmed retrieval inclusion, and prefetch cache population. Focused verification passes in `/tmp/mnemosyne-memorytools-correct-prefetch-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_runtime_surfaces.py` passes in `/tmp/mnemosyne-memorytools-correct-prefetch-runtime-surfaces.xml` with 57 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-memorytools-correct-prefetch-full-local.xml` with 793 tests, 0 failures/errors, and 74 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-memorytools-correct-prefetch-full-postgres.xml` with 793 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Direct `MemoryTools.branch`, `MemoryTools.merge`, and `MemoryTools.discard` facade parity now covers branch lifecycle wrappers outside JSON-RPC transport and across the shared Local/Postgres engine contract. The runtime-surface test verifies fail-closed low-trust branch denial with no branch rows or audit mutation, non-operator promotion/discard denial, low-trust operator promotion/discard denial without main-branch or discard side effects, trusted branch creation, merge report/security output, merged evidence visibility on `main`, trusted discard output, discarded branch evidence removal, and branch metadata removal without main-branch leakage. The shared engine contract verifies the same facade branch/merge/discard path against both Local and Postgres engines. Focused verification passes in `/tmp/mnemosyne-memorytools-branch-facades-focused.xml` with 3 tests, 0 failures/errors, and 0 skips; touched-file verification passes in `/tmp/mnemosyne-memorytools-branch-facades-touched-files.xml` with 140 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-memorytools-branch-facades-full-local.xml` with 796 tests, 0 failures/errors, and 75 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-memorytools-branch-facades-full-postgres.xml` with 796 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Shared Local/Postgres direct `MemoryTools.assert_fact`, `MemoryTools.preference`, `MemoryTools.correct`, and `MemoryTools.prefetch` facade parity now covers write/correction/prefetch wrappers beyond Local-only runtime-surface tests. The shared engine contract writes trusted facts and explicit preferences through the facade, verifies low-trust belief/preference/correction denials leave no exported state, checks correction evidence linkage, and proves prefetch executes only safe high-confidence retrieval candidates while rejecting network-side-effect and low-confidence candidates across both backends. Focused verification passes in `/tmp/mnemosyne-memorytools-shared-write-correct-focused.xml` with 4 tests, 0 failures/errors, and 0 skips; shared-contract DSN verification passes in `/tmp/mnemosyne-memorytools-shared-write-correct-shared-contract.xml` with 86 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-memorytools-shared-write-correct-full-local.xml` with 800 tests, 0 failures/errors, and 77 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-memorytools-shared-write-correct-full-postgres.xml` with 800 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Local `RuntimeState` side-state persistence now has standalone coverage that does not depend on live Postgres availability. The runtime parity extension test exercises `RuntimeState.from_store_path`, user-model save/load, learning save/load, queue save/load, metrics save/load, and protected gate-case save/load against JSON-backed local side-state, verifying authoritative and latent user context, trajectory/attribution/lesson/procedure IDs, queue payloads, metric counters/gauges/samples, and regression-case round trips. Focused verification passes in `/tmp/mnemosyne-runtime-state-local-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_runtime_parity_extensions.py` passes in `/tmp/mnemosyne-runtime-state-local-runtime-parity-extensions.xml` with 46 tests, 0 failures/errors, and 2 skips; full local verification passes in `/tmp/mnemosyne-runtime-state-local-full-local.xml` with 801 tests, 0 failures/errors, and 77 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-runtime-state-local-full-postgres.xml` with 801 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: MCP `prepare_tool_arguments` now has focused direct coverage for the shared JSON-RPC/SDK argument binding helper. The runtime-surface test verifies transport `_meta`, `auth_token`, and `session_token` stripping, session-token discovery from params metadata, tenant/user binding, role/source-trust/trust-tier override, source-identity binding from session id, missing required-session failure, and tenant-mismatch failure without relying on an end-to-end transport call. Focused verification passes in `/tmp/mnemosyne-mcp-prepare-arguments-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_runtime_surfaces.py` passes in `/tmp/mnemosyne-mcp-prepare-arguments-runtime-surfaces.xml` with 59 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-mcp-prepare-arguments-full-local.xml` with 802 tests, 0 failures/errors, and 77 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-mcp-prepare-arguments-full-postgres.xml` with 802 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: CLI/MCP runtime environment defaults now have focused coverage for object-store, object-key, and residency-policy parsing. The runtime-surface test verifies `MNEMOSYNE_OBJECT_STORE`, `MNEMOSYNE_OBJECT_STORE_ENCRYPTION`, `MNEMOSYNE_OBJECT_KEY_STORE`, command-provider inference from `MNEMOSYNE_OBJECT_KEY_COMMAND`, object-key timeout parsing, comma-trimmed `MNEMOSYNE_ALLOWED_RESIDENCIES`, runtime residency, allowed residency transfers, and `MNEMOSYNE_REQUIRE_RUNTIME_RESIDENCY` across CLI default helpers and `MnemosyneMcpServer` construction/residency policy. Focused verification passes in `/tmp/mnemosyne-runtime-env-defaults-focused.xml` with 1 test, 0 failures/errors, and 0 skips; `tests/test_runtime_surfaces.py` passes in `/tmp/mnemosyne-runtime-env-defaults-runtime-surfaces.xml` with 60 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-runtime-env-defaults-full-local.xml` with 803 tests, 0 failures/errors, and 77 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-runtime-env-defaults-full-postgres.xml` with 803 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: Shared Local/Postgres direct `MemoryTools` profile, graph, trajectory, lesson, procedure, and outcome facade parity now covers the profile/graph/learning wrapper group beyond Local-only runtime-surface tests. The shared engine contract verifies explicit/inferred/corrected profile writes and context lookup, graph neighbor/query/timeline/as-of wrappers, trajectory log/record/attribute wrappers, lesson/procedure induce/propose/search/promote/validate/rollback wrappers, and outcome/counterfactual evaluation across both backends. Focused verification passes in `/tmp/mnemosyne-shared-profile-graph-learning-focused.xml` with 2 tests, 0 failures/errors, and 0 skips; shared-contract DSN verification passes in `/tmp/mnemosyne-shared-profile-graph-learning-shared-contract.xml` with 88 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-shared-profile-graph-learning-full-local.xml` with 805 tests, 0 failures/errors, and 78 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-shared-profile-graph-learning-full-postgres.xml` with 805 tests, 0 failures/errors, and 8 skips.

Latest checkpoint: MCP server entrypoint dispatch now has direct coverage for default stdio, official SDK stdio, hosted JSON-RPC HTTP, official SDK StreamableHTTP, direct SDK StreamableHTTP app building, and mutually-exclusive serving-mode rejection. The SDK StreamableHTTP dispatch bug where `main()` passed duplicate `stateless` keyword arguments is fixed by separating transport `transport_stateless` from the facade `stateless` config, preserving stateful transport sessions without dropping stateless facade mode. MCP self-test SDK readiness now covers both `sdk_build` success and structured failure reporting with secret redaction for SDK build exceptions, including full DSNs plus raw and decoded DSN credential fragments. Focused verification passes in `/tmp/mnemosyne-mcp-entrypoints-focused.xml` with 4 tests, 0 failures/errors, and 0 skips; `tests/test_runtime_surfaces.py` passes in `/tmp/mnemosyne-mcp-entrypoints-runtime-surfaces.xml` with 64 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-mcp-entrypoints-full-local.xml` with 809 tests, 0 failures/errors, and 78 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-mcp-entrypoints-full-postgres.xml` with 809 tests, 0 failures/errors, and 8 skips. Hygiene verification passes for `git diff --check`, `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m compileall -q src tests`, risky filename scanning, and changed-file secret-pattern scanning.

Latest checkpoint: Shared Local/Postgres graph PPR coverage now directly exercises tokenized seed matching instead of only full-phrase seeds. The shared engine contract seeds a `Tokenized Graph Seed` relation and queries `graph_ppr(["seed"], ...)`, verifying both backends return the relation hit with source/predicate/target metadata and provenance while avoiding a seed-self target. Focused verification passes in `/tmp/mnemosyne-graph-token-seed-focused.xml` with 2 tests, 0 failures/errors, and 0 skips; shared-contract DSN verification passes in `/tmp/mnemosyne-graph-token-seed-shared-contract.xml` with 90 tests, 0 failures/errors, and 0 skips; full local verification passes in `/tmp/mnemosyne-graph-token-seed-full-local.xml` with 811 tests, 0 failures/errors, and 79 skips; and the clean compose Postgres DSN suite passes in `/tmp/mnemosyne-graph-token-seed-full-postgres.xml` with 811 tests, 0 failures/errors, and 8 skips.

Latest checkpoint (2026-06-24): FR-20 local multimodal breadth is validated for image, audio, and video. Runtime parity coverage now proves each modality externalizes raw bytes to `LocalObjectStore`, attaches a media embedding, retrieves through the existing fused `dense_media` path with stored media-vector provenance, queues/executes `media_extract` when derived text is absent, creates `media-derived-text` lineage, and returns derived-text hits with the retrieved-text sanitizer envelope. Verification passes for `.venv/bin/python -m pytest -q tests/test_runtime_parity_extensions.py -k "media or image or audio or video or multimodal or retriev"` and for the full `tests/test_runtime_parity_extensions.py` file. This closes the authorable local FR-20 breadth checkpoint; concrete production extractor, media-embedding, object-store, retrieval, and media-job deployment evidence remains operator-run.

Latest checkpoint (2026-06-24): FR-21 local parametric trainer/rollback validation is complete. The learning/attack suite now exercises a shell-free command-backed LoRA/test-time-training trainer that produces a persisted local artifact, verifies protected-suite and margin-over-noise rejection, proves invariant rails fail closed for reward/trust/sink/eval-overlap/mutation violations, and verifies `MemoryTools.parametric_rollback()` returns rollback-drill evidence with authorization, protected-suite metrics, no rollback branch promotion, and a deterministic rollback fingerprint. Focused trainer/protected and rollback filters plus the full `tests/test_learning_and_attack_suite.py` file pass. Concrete deployed GPU trainer and production rollback evidence remains operator-run under row 9.

Latest checkpoint (2026-06-28): T-SEC protected registry breadth is reconciled with the authoritative security playbook. `src/mnemosyne/attack_suite.py` now returns all 22 canonical curated/active/protected `T-SEC` cases (`T-SEC-001` through `T-SEC-021` plus `T-SEC-016b`) instead of the original two seed cases, and stale tests/planning notes were updated so future parity checks assert the full registry including audit-completeness and benign-utility negative controls. Verification passes for the affected 46-test learning/security slice. This is local/source registry parity only; Tier-B production security evidence remains operator-run.

### Blockers/Concerns

- Exact 1:1 blueprint parity is not yet achieved; `.planning/STRICT-BLUEPRINT-PARITY-AUDIT.md` is the controlling status artifact.
- Historical Desktop/Projects paths may appear in older phase notes; the current canonical checkout is `/Users/admin/Mnemosyne`.
- Docker/Postgres parity was verified live after launching Docker Desktop and recreating the schema volume.
- Production legal-erasure policy and operator evidence still need deployment validation beyond local/clean-DSN enforcement.

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Production storage | Production Postgres/ParadeDB/AGE/pgvector evidence over deployed infrastructure | Operator evidence pending | Strict parity audit |
| Retrieval | Production embedding/BM25/graph/reranker adapter evidence beyond local/compose and provider-contract smokes | Operator evidence pending | Strict parity audit |
| Security | Production IdP/JWKS, TLS, C2PA trust-root, KMS/residency, and delete evidence | Operator evidence pending | Strict parity audit |
| Runtime | Hosted MCP endpoint, worker supervision, observability, and full live parity-suite evidence | Operator evidence pending | Strict parity audit |

## Session Continuity

Last session: 2026-07-07T05:36:49.795Z
Stopped at: context exhaustion at 75% (2026-07-07)

Latest active checkpoint (2026-06-30): Continuing the unpaused exact-parity goal from the canonical checkout `/Users/admin/Mnemosyne`, with focus on Tier-B production/control gaps rather than redundant gates. The current source-hardening lane is closing the Phase 8 MFA elevation defect: elevated OIDC authz rules now require non-tenant claim evidence, configured `required_acr`/`required_amr`, positive `max_auth_age_seconds`, and fresh token `auth_time` before minting operator/consolidator or trust-tier≤1 sessions. This is source/control progress only; live Keycloak/MFA rollout evidence remains operator-run and required before Tier-B production parity can be claimed.

Latest active checkpoint (2026-06-30): Phase 8 provenance fail-open source control is next in the Tier-B hardening lane. C2PA trust policy defaults now require a configured trusted issuer, so unconfigured or mismatched C2PA verifier reports quarantine instead of silently improving trust. Explicit root-only trust remains possible only when a policy opts out of issuer matching and requires a configured certificate root. This is source/control progress only; live production C2PA issuer/root rotation and quarantine evidence remains operator-run and required before Tier-B provenance parity can be claimed.
