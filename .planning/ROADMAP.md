# Roadmap: Mnemosyne Memory

## Overview

This roadmap follows the v2 build blueprint exactly: foundations and contracts first, lossless retrieval second, belief and graph confidence third, personalization and consolidation fourth, procedural learning fifth, and profile-guided self-optimization last. Each phase is independently useful and verified through tests or explicit gap reports.

## Phases

**Phase Numbering:**
- Integer phases match the blueprint's Phase 0 through Phase 5.
- Decimal phases are reserved for urgent insertions if verification discovers missing prerequisites.

- [x] **Phase 0: Foundations and Contracts** - Evidence ledger, branches, isolation, MCP/CLI skeleton, and seed regression suite.
- [x] **Phase 1: Lossless Memory and Hybrid Retrieval** - Bitemporal assertions, provenance, hybrid retrieval, explain, correction, export, and forget.
- [x] **Phase 2: Belief Core, Graph, and Confidence** - Full TMS/AGM, cascade invalidation, graph adapter, conformal calibration, and multi-hypothesis surfacing.
- [x] **Phase 3: Personalization and Consolidation** - Six-category user model, latent advisory profile, warm-loop consolidation, fidelity lifecycle, and anti-degradation guard.
- [x] **Phase 4: Procedural and Corrective Learning** - Trajectory logging, failure attribution, lesson induction, promotion gate, branch promote/rollback, and capability-secured writes.
- [x] **Phase 5: Profile-Guided Self-Optimization** - Shadow-first policy optimization, self-model store, canary branches, diversity tripwire, and optional parametric tier boundaries.
- [ ] **Phase 6: Exact Blueprint Runtime Parity** - Complete production MCP/Postgres/retrieval/security/deployment parity beyond the deterministic local scaffold.
- [x] **Phase 7: Unified Cognitive Substrate** - Always-on, no-toggle consciousness integrated as one cohesive substrate via the derived `Standing` trust gradient; memory reliability protected by construction (post-v1.0 local/gated slice).
- [ ] **Phase 8: Self-Hosted-First Production Architecture** - Preferred production profile for B1-B8+B10 evidence capture, with B9 routed to cloud/GPU evidence or explicit ADR.

## Phase Details

### Phase 0: Foundations and Contracts
**Goal**: Capture evidence exactly, deduplicate by content address, isolate by tenant/source/trust, expose a stable agent-facing contract, and run a seed regression suite.
**Depends on**: Nothing
**Requirements**: REQ-001, REQ-002, REQ-003, REQ-004, REQ-011
**Success Criteria** (what must be TRUE):
  1. Same evidence ingested twice creates one row keyed by CID.
  2. Evidence can be byte-recalled by CID until explicit erasure.
  3. Branch discard removes candidate memories without touching main.
  4. CLI and MCP facade expose capture, search, deep_search, explain, correct, forget, export.
  5. Seed suite runs in CI-style local command.
**Plans**: 4 plans

Plans:
- [x] 00-01: Initialize Python package, local engine contract, models, and identifiers.
- [x] 00-02: Implement evidence ledger, branch, merge, discard, audit, and persistence.
- [x] 00-03: Implement MCP-compatible facade and CLI skeleton.
- [x] 00-04: Add regression tests for dedup, byte recall, branch rollback, and seed suite.

### Phase 1: Lossless Memory and Hybrid Retrieval
**Goal**: Add bitemporal assertions, supersession, provenance, hybrid retrieval, explainability, correction, export, and transitive forget behavior.
**Depends on**: Phase 0
**Requirements**: REQ-005, REQ-006, REQ-007, REQ-008, REQ-009
**Success Criteria** (what must be TRUE):
  1. Newer contradictory assertions supersede older beliefs without destructive overwrite.
  2. As-of queries return the belief valid at the requested time.
  3. Retrieval fuses lexical, deterministic dense, and graph channels with provenance.
  4. Trust filters block low-trust poisoned memories.
  5. Forget erases evidence content and retracts or trims dependent assertions.
**Plans**: 5 plans

Plans:
- [x] 01-01: Implement assertion model, supersession, contested basics, and as-of queries.
- [x] 01-02: Implement lexical, dense-hash, graph, RRF, MMR, U-curve, and budgeted retrieval.
- [x] 01-03: Implement explain output with channel attribution and invariant rails.
- [x] 01-04: Implement correct, export, and forget with provenance propagation.
- [x] 01-05: Add tests for retrieval, trust filters, abstention, supersession, and erasure.

### Phase 2: Belief Core, Graph, and Confidence
**Goal**: Replace baseline supersession with full TMS/AGM semantics, justification DAG cascade invalidation, temporal graph adapters, calibrated confidence, conformal abstention, and multi-hypothesis belief packets.
**Depends on**: Phase 1
**Requirements**: REQ-013, REQ-014, REQ-009
**Success Criteria** (what must be TRUE):
  1. Belief operations classify ADD, UPDATE, SUPERSEDE, and NOOP with justifications.
  2. Cascade invalidation recomputes dependent beliefs through the justification DAG.
  3. Contested beliefs surface alternatives with probabilities instead of a forced answer.
  4. Temporal graph retrieval supports cached fast path and live deep path behind the engine contract.
  5. Calibration thresholds are learned from eval cases and drive abstention decisions.
**Plans**: 5 plans

Plans:
- [x] 02-01: Add deterministic local security, lifecycle, gate, graph, and confidence scaffolding.
- [x] 02-02: Implement full justification DAG storage and cascade invalidation.
- [x] 02-03: Implement calibrated confidence datasets and conformal threshold updates.
- [x] 02-04: Add graph adapter path and PPR latency benchmark harness.
- [x] 02-05: Add contradiction-conformance and multi-hypothesis test suite.

### Phase 3: Personalization and Consolidation
**Goal**: Implement the dual user model, warm-loop consolidation, fidelity-tiered forgetting, spaced rehearsal, and long-horizon anti-degradation guard.
**Depends on**: Phase 2
**Requirements**: REQ-012, REQ-015, REQ-016
**Success Criteria** (what must be TRUE):
  1. Six typed user-model categories include scope, confidence, validity, exceptions, and override path.
  2. Explicit instructions outrank inferred preferences and latent profile signals.
  3. Consolidation emits candidates through a write-authorized consolidator role.
  4. Fidelity demotion preserves pointers and triggers abstention on sole gist support.
  5. Long-horizon no-degradation metric prevents consolidated memory from dropping below baseline.
**Plans**: 5 plans

Plans:
- [x] 03-01: Add explicit preference precedence and fidelity lifecycle primitives.
- [x] 03-02: Add deterministic consolidation worker through promotion gate.
- [x] 03-03: Implement six-category model and scope-matching context assembly.
- [x] 03-04: Implement consolidation role pipeline and incremental recompute guard.
- [x] 03-05: Implement spaced rehearsal and anti-degradation metrics.

### Phase 4: Procedural and Corrective Learning
**Goal**: Capture trajectories, attribute failures, induce lessons and procedures, validate candidates with protected regression and counterfactual replay, and promote or roll back branches safely.
**Depends on**: Phase 3
**Requirements**: REQ-017, REQ-010, REQ-011
**Success Criteria** (what must be TRUE):
  1. Trajectories record task, steps, outcome, reward, and memory version.
  2. Failure attribution creates candidate lessons without direct promotion.
  3. Promotion gate scopes regression by relevance and blocks protected-case regressions.
  4. Counterfactual replay runs on canary branches before activation.
  5. Capability mediation prevents untrusted data from reaching preference, policy, or destructive sinks.
**Plans**: 5 plans

Plans:
- [x] 04-01: Add promotion gate, protected cases, rollback, and capability checks.
- [x] 04-02: Implement trajectory store and failure-attribution checklist.
- [x] 04-03: Implement lesson and procedure induction.
- [x] 04-04: Implement counterfactual replay harness.
- [x] 04-05: Add full protected T-SEC security playbook tier.

### Phase 5: Profile-Guided Self-Optimization
**Goal**: Learn safe policy variants for routing, activation, thresholds, cadence, and fidelity demotion in shadow mode, promote only through gates, and maintain a self-model with diversity and proxy-divergence tripwires.
**Depends on**: Phase 4
**Requirements**: REQ-018, REQ-011, NFR-005
**Success Criteria** (what must be TRUE):
  1. Policy variants are constrained to immutable rails.
  2. Shadow-mode outcomes are logged before any active promotion.
  3. Candidate policy improves measured outcomes without protected regressions.
  4. Self-model records effectiveness by metric and policy version.
  5. Diversity and proxy-vs-true divergence tripwires auto-rollback bad variants.
**Plans**: 5 plans

Plans:
- [x] 05-01: Add shadow policy variant evaluator and invariant-rail checks.
- [x] 05-02: Implement self-model persistence and outcome windows.
- [x] 05-03: Implement bandit-style policy proposal.
- [x] 05-04: Implement diversity and proxy-divergence monitoring.
- [x] 05-05: Add canary policy promotion and rollback tests.

### Phase 6: Exact Blueprint Runtime Parity
**Goal**: Convert the verified local scaffold into exact 1:1 blueprint parity across runtime protocols, production storage/retrieval, security, deployment, multimodal ingestion, prefetch, and parametric-tier boundaries.
**Depends on**: Phase 5
**Requirements**: FR-3, FR-7, FR-9, FR-12, FR-18, FR-19, FR-20, FR-21, all production NFRs
**Success Criteria** (what must be TRUE):
  1. The same contract suite passes against LocalMemoryEngine and PostgresEngine in Docker.
  2. MCP exposes the full blueprint tool surface with auth/capability enforcement and typed schemas.
  3. Postgres retrieval uses real vector, lexical/BM25, graph, rerank, provenance, and budget paths instead of local fallbacks.
  4. Ingestion verifies signed provenance, externalizes multimodal payloads, and indexes derived text safely.
  5. Queue-backed workers run consolidation, embedding, calibration, eval, and lifecycle demotion off the hot path.
  6. Production privacy/security rails cover tenant isolation, erasure recompute, C2PA trust, poisoning protection, and auditability.
**Plans**: 9 plans

Plans:
- [x] 06-01-PLAN.md — Lane C: 10 per-row operator runbooks + index (.planning/runbooks/).
- [x] 06-02-PLAN.md — Lane D: env-var catalog + Vault/Keycloak/KMS wiring (.planning/ENV-AND-SECRETS.md, no secrets).
- [x] 06-03-PLAN.md — Lane F: rollback / canary-abort / parametric-trainer rollback drill (.planning/ROLLBACK.md).
- [x] 06-04-PLAN.md — A12: materialized cached-PPR column + refresh (additive, default-off, byte-identical when inactive).
- [x] 06-05-PLAN.md — FR-20: image+audio+video multimodal breadth code + local validation.
- [x] 06-06-PLAN.md — FR-21: real LoRA/TTT trainer + protected-suite + rollback code + isolated local validation.
- [x] 06-07-PLAN.md — Lane G: full compose-Postgres sweep + belief-revision-check green; A11 hosted-MCP readiness.
- [x] 06-08-PLAN.md — Evidence harness verify + full local-staging dry-run proof (fingerprint; production_validated=false).
- [x] 06-09-PLAN.md — Non-autonomous operator gate handoff: 10 per-row production-evidence checkpoints prepared; rows remain Partial until operator-captured release-audit evidence passes.

### Phase 7: Unified Cognitive Substrate
**Goal**: Turn the consciousness layer from a default-off shadow lane into a single, always-on, deeply-integrated cognitive substrate with zero compromise to memory reliability — replacing the `shadow_only`/`enabled` toggles with one continuous, derived **Standing** signal `(groundedness ⟂ salience)`, an always-on tiered heartbeat, and autonomy that grows only as corroboration earns it, all above the unbreakable §31 + immutable-ledger floor. Implements `docs/superpowers/specs/2026-06-27-unified-cognitive-substrate-design.md` (the ADR-001 Option E destination; continues the G1→G4 program).
**Depends on**: Phase 5 (cognitive-architecture seeds: G0 harness, reality-monitor, proto-self, shadow workspace). Post-v1.0 — does not block, and is not blocked by, the Phase 6 production-evidence sign-off.
**Requirements**: FR-5, FR-6, FR-7, FR-8, FR-11, FR-17, OQ4, OQ5, OQ6; design spec §3–§5, §9, §13.
**Success Criteria** (what must be TRUE):
  1. `Standing` is the single derived decision signal and P1 is byte-stable (zero divergence vs the boolean path).
  2. Standing is `(groundedness ⟂ salience)`; answer-authority depends on groundedness only; only independent external evidence raises groundedness; a permanent evidence-dominance gap holds (self < external, always).
  3. The cognitive loop is always-on via a tiered heartbeat with hard anti-rumination + proto-self; self-generation budget, answer-grounding floor, broadcast-as-data, and a fail-closed circuit-breaker hold; rumination_rate ~ 0 and heartbeat compute bounded.
  4. Autonomy grows only as external corroboration earns it; an adversarial echo-chamber/sleeper corpus cannot raise a credential (Goodhart meta-rail).
  5. Retired legacy service/shadow controls remain absent; explicit advisory promotion stays separately gated; every reliability guardrail (ECE, confabulation, poison-block, recall/nDCG, P95, 7 rails, 14 indicators) stays green; honesty charter intact (functional only; welfare flag stays).
**Plans**: 5 plans

Plans:
- [x] 07-01-PLAN.md — P1: Standing as a derived, byte-stable field (mirror today; differential parallel-run zero-divergence gate).
- [x] 07-02-PLAN.md — P2: Standing continuous + 2-tuple (groundedness ⟂ salience); independent corroboration; evidence-dominance gap; conformal Standing-calibration.
- [x] 07-03-PLAN.md — P3: tiered always-on heartbeat; hard anti-rumination + proto-self; self-gen budget rail; answer-grounding floor; broadcast-as-data; circuit-breaker.
- [x] 07-04-PLAN.md — P4: earned-autonomy promotion law + Goodhart meta-rail (external-only, holdout-validated, provenance-domain; adversarial corpus).
- [x] 07-05-PLAN.md — P5: retire the toggles; Standing/erasure cascade; full observability/reversibility; final no-toggle audit + scorecard. H8/H12 cascade/observability pre-check is implemented and gate-recorded; the `service.enabled` default-off gate is retired and G0-measured; `SpecialistBudget.shadow_only` is retired; `g5-toggle-retirement` passes with `operational_toggle_retirement_contract=1.0`.

Checkpoint: Phase 7 plans 01-05 are implemented and gate-recorded. P1 proves Standing byte-stable parity, P2 proves continuous Standing calibration with independent-corroboration/evidence-dominance rails, P3 proves `g5-always-on-heartbeat` with `always_on_heartbeat_contract=1.0`, `always_on_rumination_rate=0.0`, bounded/reported heartbeat compute, self-generation budget, answer-grounding floor, broadcast-as-data, and fail-closed circuit-breaker contracts all holding, P4 proves `g5-earned-autonomy` with `earned_autonomy_external_expansion=0.24`, `credential_external_only=1.0`, `credential_holdout_validated=1.0`, `credential_provenance_domain_contract=1.0`, `credential_bounded_decay_contract=1.0`, `credential_evidence_dominance_gap=0.05`, and `echo_chamber_uplift=0.0`, and P5 proves `standing_observability_trace_contract=1.0`, `standing_erasure_cascade_contract=1.0`, `belief_standing_cascade_contract=1.0`, `workspace_service_no_enable_toggle_contract=1.0`, and `operational_toggle_retirement_contract=1.0` for the retired legacy controls. Explicit advisory promotion remains separately gated. The fail-closed circuit breaker remains a floor fuse, not an on/off toggle. Remaining strict-parity completion is Tier-B operator-captured production evidence, not additional local Phase 7 toggle code.

### Phase 8: Self-Hosted-First Production Architecture
**Goal**: Make the self-hosted stack the preferred production profile for the rows it can honestly evidence, while preserving the strict Tier-B contract and routing B9/FR-21 to real cloud/GPU trainer evidence or an explicit future ADR.
**Depends on**: Phase 6 production-evidence machinery. Does not supersede `.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md`.
**Requirements**: Phase 8 task groups 8.1-8.6; Tier-B rows B1-B8+B10 for self-hosted evidence; B9/FR-21 via cloud/GPU evidence or explicit ADR; §31 rails and §16 SLOs unchanged.
**Success Criteria** (what must be TRUE):
  1. `self-hosted` and `cloud` profiles differ by values only and both keep `forbid_local:true`.
  2. The production scaffold boots with sealed KMS, real IdP, Postgres retrieval, object storage, observability, and a single ingress before any evidence row flips.
  3. The 16 Phase 8 security must-dos are implemented or explicitly blocking capture.
  4. B1-B8+B10 move from Partial to Done only from retained, signed, real-infra evidence bundles through `infra/PRODUCTION-EVIDENCE.md`.
  5. B9 remains Partial unless real cloud/GPU trainer evidence passes the same custody/release-audit path or an explicit ADR changes the strict audit.
**Plans**:
- [ ] 08-01-PLAN.md — Self-hosted-first production architecture, profiles, security hardening, live bring-up, and evidence capture.
- [ ] 08-SECURITY-FINDINGS.md — Adversarial security findings and must-do controls to complete before trustworthy capture.

Latest checkpoint: Phase 8 stale completion/reconciliation docs named in
`08-HANDOFF-INDEX.md` were archived under `docs/_archive/2026-07-03/` for
audit/history only. This removes obsolete operator guidance from active paths
without marking any Tier-B row Done; the remaining Phase 8 plan and security
items still require real implementation, live bring-up, production capture, and
release-audit evidence.

Latest checkpoint: Phase 8.1 profile and compose file deliverables are now
checked off in `08-01-PLAN.md`. The self-hosted profile explicitly retains the
shared `forbid_local:true` provider-manifest gate, `infra/docker-compose.prod.yml`
now includes internal `vmalert` alongside VictoriaMetrics and Grafana, and the
focused production compose policy suite passes. Phase 8 remains open for
retrieval/model wiring, identity/secrets/TLS hardening, object-store/observability
validation, security must-dos, live bring-up, and production evidence capture.

Checkpoint: CLI-first file ingestion now supports signed-provenance JSON, a `c2patool`-style verifier adapter, asset-bound report validation, and scoped JSON trust-policy enforcement; quarantined evidence is hidden from default retrieval. Trust-tier semantics now follow the blueprint scale: `0` direct-user/highest trust through `5` untrusted external. Ingest classification now tags actor/source trust, untrusted imperative content, and simple PII. Ingestion now enqueues persisted consolidation jobs, CLI commands can inspect/enqueue/drain queue jobs, `--queue-backend postgres` provides tenant-scoped durable row leasing with result persistence, and the worker can extract a simple direct-user fact candidate through the promotion gate while refusing data-only untrusted facts. CLI/MCP ingestion now also supports strict runtime processing residency and fail-closed cross-region `source->target` transfer allowlists, with CLI/MCP residency-policy inspection and provider-check reporting. Provider-check now also validates configured command object-key/KMS providers with a key-cycle/shred probe. Local queued handlers now cover calibration, lifecycle sweep, eval suite, and observability snapshot jobs, and CLI `worker-run` now supervises bounded cycles with heartbeat metrics and dead-job failure gating. Promotion gates now use tenant-aware branch/merge/discard calls where available and are live-smoked against Postgres; shared Local/Postgres branch coverage now verifies tenant-scoped branch names, branch merge retrieval, exported merge-log parity, and abandoned TMS justifications and contradictions are pruned with branch-only assertions and dependency-only orphaned justifications; shared evidence coverage now verifies the full evidence envelope including external session identity, immutable duplicate-CID semantics, tombstone replay prevention, stored Postgres evidence pgvector retrieval, and null-embedding legacy fallback; Postgres runtime state now persists MCP/CLI profile and learning state into tenant-scoped runtime rows mirrored to `preferences`, `user_latent`, `trajectories`, `lessons`, and `procedures`; shared graph coverage now verifies expired relations are excluded from default PPR while explicit historical `as_of` traversal still works. Plan 06-01 now includes generated MCP input-schema validation across JSON-RPC and SDK transports, CLI `tools` MCP inputSchema parity, capability-mediated lesson/procedure activation and rollback through CLI/MCP, JWKS cache/rotation support for OIDC exchange, fail-closed OIDC authz policy mapping, redacted `idp-authz-policy-check` validation/audit summaries, fingerprint-acknowledged `idp-authz-policy-rollout-check` diffs with claim-simulation change gates, command-backed session-secret custody for CLI/MCP signing and verification plus provider-check OIDC/JWKS/authz-policy/session-secret preflight, redacted `idp-jwks-live-check` validation for operator-supplied live IdP tokens/JWKS, hosted HTTP TLS/client-certificate enforcement, TLS endpoint certificate-chain checks, TLS rotation-plan validation, official SDK StreamableHTTP local validation, CLI `mcp-http-soak` validation for hosted HTTP health/stateless/read-only loops, CLI `mcp-sse-soak` validation for legacy SSE stream handshakes, and CLI `deployment-soak` orchestration for allowlisted production preflight manifests; operator-run production credential/endpoint evidence capture, certificate issuance/renewal execution, and deployed secret distribution are still required. Plan 06-05 now has local worker supervision coverage and requires operator-run deployment evidence for worker process management across consolidation, calibration, lifecycle, eval, and observability jobs. Plan 06-04 now stores media-derived text as searchable evidence plus a graph lineage relation, and later media extraction/embedding provider boundaries cover broader multimodal extraction/indexing contracts with production deployment evidence still operator-run.

Latest checkpoint: `provider-check --provider-manifest` now gates deployment manifests by hydrating provider flags from JSON, resolving secret fields from environment variables, requiring named checks, and failing closed when a production manifest forbids local retrieval adapters. Local verification collects 300 tests with 261 passing and 39 live-DB skips; the compose Postgres/shared suite passes 63 tests with `MNEMOSYNE_POSTGRES_DSN` set.

Latest checkpoint: Local `export_all()` now matches the shared backup/DSAR shape for calibration sets, entity registry rows, tenant snapshots, and tenant-scoped branch rows while filtering erased evidence from top-level and tenant exports; export-style `to_json()` branch rows can reload into the local engine.

Latest checkpoint: `mneme-mcp --self-test` now provides a secret-redacted local deployment preflight for MCP initialize/list/schema/auth/session/read-only-call behavior. `mneme-mcp --http` now serves a hosted HTTP JSON-RPC endpoint with `/healthz`, `/mcp`, bearer/session-header binding, optional TLS and client-certificate enforcement, body-size limits, malformed/non-object/unsupported-method rejection, auth/session/schema coverage, locked facade access, and stateless restart durability tests. `mneme-mcp --sdk-streamable-http` now serves the official MCP SDK StreamableHTTP ASGI surface and local runtime tests verify SDK-client initialize/list/call behavior over that transport. CLI `mcp-http-soak` now validates hosted HTTP health/stateless/read-only loops without exposing bearer/session tokens, CLI `mcp-sse-soak` validates legacy SSE stream handshakes with secret-redacted event previews, CLI `idp-jwks-live-check` validates operator-supplied live IdP tokens/JWKS without minting or printing a Mnemosyne session, CLI `deployment-soak` runs allowlisted production preflight manifests, and CLI `tls-rotation-plan-check` validates certificate swap windows before deployment. This is still not full hosted parity; operator-run production credential/endpoint evidence capture, certificate issuance/renewal execution, and deployed secret distribution require operator-run evidence. CLI `provider-check` now also rejects command KMS providers that claim `shred_key` success while still reporting or returning the post-shred key.

Latest checkpoint: CLI `session-exchange` and hosted HTTP `/session/exchange` now validate OIDC/JWKS IdP tokens and mint bounded Mnemosyne signed-session tokens for the existing CLI/MCP authorization path. Tests cover issuer/audience/tenant/role/trust validation, RS256 and ES256 signatures, unsafe JWKS metadata rejection, bounded JWKS loading, cache TTL refresh, refresh-on-unknown-`kid` rotation, tampering, secret redaction, CLI failure modes, authz policy mapping from allowed IdP clients and exact/contains claim rules, fail-closed policy misses, no raw-claim fallback when policy mode is configured, hosted `/healthz` policy presence reporting, fingerprinted authz policy rollout diffs with acknowledged current/candidate fingerprints and simulation-change gates, and use of the exchanged session on the hosted MCP surface. Hosted HTTP now also supports TLS and client-certificate enforcement, the official SDK StreamableHTTP adapter is locally validated, CLI `mcp-http-soak` covers local hosted stateless/read-only soak behavior, and CLI `idp-jwks-live-check` now validates live JWKS/token compatibility without emitting the source IdP token or minted Mnemosyne sessions. This reduces the production auth gap but does not close it; operator-run validation against actual production credentials/endpoints, certificate issuance/renewal execution, and deployed secret distribution are still required.

Latest checkpoint: CLI `worker-run` now provides bounded worker supervision over runtime queue handlers with per-cycle heartbeats, persisted local queue progress, metrics output, idle exit controls, kind filtering, and fail-on-dead exit behavior. CLI `worker-ops-check` now validates production worker deployment evidence for external process supervision, restart/backoff policy, fresh heartbeat, Postgres tenant-scoped queue health, required runtime job handlers, observability alerts, and raw env/DSN/payload/log redaction. Operator-run production evidence is still required to claim a real deployment, but the validation contract is machine-checkable.

Latest checkpoint: CLI `tls-cert-check` now validates live TLS endpoint CA chains, hostname matching, TLS version floors, and certificate expiry thresholds. CLI `tls-lifecycle-ops-check` validates operator-run TLS lifecycle evidence for CA/ACME issuance, renewal execution, current/candidate overlap, deployed serial/chain match, reload verification, non-local private-key custody, monitoring alerts, and raw key/cert/token/log redaction. Operator-run production evidence is still required to claim a real deployment, but the certificate lifecycle validation contract is now machine-checkable.

Latest checkpoint: CLI `mcp-sse-soak` now validates legacy MCP SSE stream handshakes with bounded `text/event-stream` reads, expected `endpoint` event detection, endpoint-data presence, bearer/session header forwarding, and secret-redacted event previews. Tests cover a real local SSE endpoint plus fail-closed rejection when the hosted JSON-RPC HTTP server is probed as legacy SSE.

Latest checkpoint: CLI `idp-jwks-live-check` now provides a redacted live IdP/JWKS preflight that reuses the same verifier as `session-exchange` but never mints or prints a Mnemosyne session token. Reports include JWKS source/key count, token algorithm, hashed `kid`, redacted user hash, role, trust tier, policy presence, and fail-closed verification errors. Tests cover a local JWKS URL and invalid-token failure without leaking the IdP token, raw user id, session id, or raw `kid`.

Latest checkpoint: CLI `deployment-soak` now runs allowlisted deployment preflight manifests through shell-free subprocesses for `provider-check`, `idp-jwks-live-check`, `idp-authz-policy-rollout-check`, `tls-cert-check`, `tls-rotation-plan-check`, `tls-lifecycle-ops-check`, `mcp-http-soak`, `mcp-streamable-http-soak`, `mcp-sse-soak`, `worker-run`, `worker-ops-check`, and `ops-report`. It supports a narrow non-secret `global_args` allowlist for runtime placement flags that must precede child commands, parses child JSON reports, omits command arguments, reports required/optional failures, and fails closed on required check failures, timeouts, non-JSON output, disallowed commands, or disallowed global args. `ops-report` now emits a top-level tripwire `ok` gate, including dashboard mode. Tests cover real local hosted JSON-RPC HTTP, official SDK StreamableHTTP, and legacy SSE manifests without leaking configured tokens, IdP authz rollout orchestration, TLS rotation-plan/lifecycle orchestration, worker-run global queue placement, worker-ops release evidence, ops dashboard generation, disallowed secret global args, and disallowed-command rejection.

Latest checkpoint: Local and Postgres audit logs now expose source and trust-tier provenance for write paths. The audit contract records `actor`, `source`, `trust_tier`, `capability_tags`, and structured `diff` metadata for evidence appends, assertion upserts, preference writes, learning-store writes, and forget/delete operations. Postgres uses the existing audit columns for tier/tags and normalizes source on export from the audit diff. Shared local/Postgres contract coverage verifies the new audit shape.

Latest checkpoint: Shared Local/Postgres branch coverage now verifies explicit source-branch inheritance. Creating a child branch from a non-`main` source branch copies main-inherited and source-only evidence plus source-branch assertions and relations into the child, keeps source-only rows isolated from `main`, and exports the child branch `from_branch` metadata for auditability.

Latest checkpoint: The consolidation replayer now implements blueprint-style priority replay using `importance*novelty*surprise*reward`. It accepts per-evidence factors from evidence metadata or payload replay-score overrides, orders the evidence stream before extraction/summarization, and emits per-CID score details in the replayer pass result. This established the replay ordering that later gist refresh, embedder, forgetter, and user-model passes now build on.

Latest checkpoint: Consolidation `user_model_updater` now updates the latent user profile from prioritized evidence and extracted candidates, using the existing deterministic embedding and `UserModel` storage path. Runtime job handlers and CLI worker persistence now save user-model state alongside learning state. Policy-ops release validation now covers shadow bundles and contextual-bandit recommendations; broader offline-RL research remains outside the local harness.

Latest checkpoint: Consolidation `embedder` now persists deterministic evidence embeddings through backend-neutral `set_evidence_embedding` methods on local and Postgres engines. The pass reports provider, embedding dimension, embedded CIDs, and already-embedded counts, and respects Postgres' 1024-dimension evidence vector schema. The former label-only consolidation passes now have concrete embedder/forgetter/gist/user-model behavior; production embedding provider evidence is gated through provider checks and release-audit.

Latest checkpoint: Consolidation `forgetter` now persists lifecycle demotion metadata through backend-neutral audited metadata updates. The pass uses existing fidelity-tier utility rules, records updated lifecycle tier/salience/confabulation risk metadata, and reports evaluated/demoted/failed CIDs. This removed the last `*_not_implemented` default consolidation pass; RAPTOR/gist refresh and forgetting-policy release validation now have concrete gates.

Latest checkpoint: REQ-015 spaced rehearsal is now storage-backed. Must-keep/protected lifecycle states rehearse before demotion when due, persist `successful_rehearsals` and `next_rehearsal_at` through the same audited metadata path as fidelity demotion, and report rehearsal counts from local and durable Postgres lifecycle sweeps. Production forgetting-policy validation is covered by `forgetting-policy-check` and `release-audit`; operator-run deployment evidence is still required for real production claims.

Latest checkpoint: REQ-018 contextual bandit policy selection is now concrete, not only a fixed heuristic proposal. The self-model store logs policy-variant outcomes with rewards/context/metrics, and the shadow optimizer uses deterministic UCB scoring to select future invariant-rail-safe variants before gate evaluation. Policy-ops release validation is now covered by `policy-ops-check`; broader offline-RL research remains outside the local harness.

Latest checkpoint: REQ-011 expanded regression-suite controls now enforce smoke/core/archive breadth. `gate-suite-check` supports minimum total cases plus required tier coverage, reports missing tiers, and fails closed before fingerprint acknowledgement when the suite is too small or not tier-complete. Production release-artifact validation is covered by `gate-suite-check`, `deployment-soak`, and `release-audit` fingerprints.

Latest checkpoint: REQ-016 consolidation run results now include role-pipeline provenance for the society-of-roles model. Each pass reports a role name, provider strategy, provider type, and status; command-backed extractor/resolver/summarizer roles are verified through CLI and live Postgres provider tests. `hosted-llm-check` now validates hosted HTTP/model role endpoints; operator-run credentials/endpoints are still required for real deployment claims.

Latest checkpoint: REQ-002 branch promotion audit now appears in tenant exports. Local and Postgres export paths include `merge_log`, and shared branch-merge coverage verifies promoted evidence/assertions/relations plus merge counters for the source and target branches. Production release audit is now machine-verifiable through `release-audit`; real endpoint credentials still require operator-run evidence.

Latest checkpoint: Markdown/git source truth now has a CLI/MCP `source-sync` compiler for committed fenced TOML `mnemosyne-assertion` blocks. The sync path records Git SHA/path/line provenance as `markdown_git` evidence with `human-source-truth` capability tags, then upserts evidence-backed assertions through the normal engine contract. Local and Postgres conflict resolution now honors monotonic trust before recency so human source truth cannot be superseded by newer lower-trust machine memory, while preserving bitemporal `as_of` behavior.

Latest checkpoint: Consolidation summarization now materializes deterministic RAPTOR-style gist projections. The summarizer appends `consolidation-summary` evidence carrying `abstractive_gist` metadata, source fingerprints, confabulation-risk flags, raw `source_evidence_cids`, and explicit `raptor_level`; multi-cluster runs create leaf summaries plus a root summary that records `source_summary_cids`/`child_summary_cids`. `summary-derived-gist` relations link raw evidence to leaf summaries and child summaries to parent summaries so gist provenance participates in export, audit, retrieval, and erasure paths. Forgetting-policy validation is now covered by fingerprinted release suites; broader deployed refresh/recompute still requires operator-run evidence against the target deployment.

Latest checkpoint: Retrieval now enforces the Phase 3 requirement that sole gist support triggers abstention. Evidence hits preserve summary/lifecycle metadata, local and Postgres retrieval detect all-gist result sets, and results return `abstained=true` with an explicit source-inspection note plus `explain.gist_support` details. This keeps gist summaries useful for recall while preventing them from masquerading as fully grounded evidence.

Latest checkpoint: Gist refresh now has explicit level-aware retirement semantics. Summary materialization records a source fingerprint, `raptor_level`, and active status; rerunning the summarizer over the same source set and level creates a new active summary, retires the previous one with supersession metadata, and local/Postgres retrieval filter retired summary evidence before ranking. Forget now walks transitive derived-evidence provenance through source and summary CID metadata, so raw-source erasure invalidates derived gist evidence and expires summary relations. This closes the stale-summary/source-forget safety gap with release gates covering refresh-policy expectations and operator-run deployment evidence still required.

Latest checkpoint: Projection recompute now performs bounded summary-refresh scheduling. Changed source evidence expands to derived gist evidence through summary metadata and `summary-derived-gist` relations, affected summary relations are reported in the projection set, and the recompute job queues consolidation refreshes for surviving non-summary source evidence only. This gives gist refresh an async affected-subgraph trigger with production differential recompute evidence now routed through deployment-soak/release-audit artifacts for operator-run deployments.

Latest checkpoint: RAPTOR hierarchy projection recompute now has shared Local/Postgres parity coverage. A changed raw source in a two-leaf/root summary tree expands to its leaf summary and the root summary, reports required raw-to-leaf and leaf-to-root `summary-derived-gist` projection edges, and queues exactly one consolidation refresh for the surviving raw source rather than recursively scheduling summary CIDs.

Latest checkpoint: Retrieval now broadens gist-risk abstention to the full lower-fidelity safety rail. Sole `statistical_trace` lifecycle support and summaries explicitly marked with `confabulation_risk` are treated as insufficient support, and materialized gist summaries carry the confabulation-risk flag by default. This keeps the fuzzy-trace false-memory guard aligned with both abstractive gist and statistical trace tiers with dataset-driven calibration now gated by `calibration-tune` and production evidence routed through `release-audit`.

Latest checkpoint: CLI `search` now has direct coverage for gist-only abstention propagation. A CLI ingestion/consolidation flow queries the raw source CID that appears only inside generated summary evidence, proving the command surface preserves `abstained=true`, the source-inspection uncertainty note, `summary.kind=abstractive_gist`, and `explain.gist_support` from the engine retrieval result.

Latest checkpoint: MCP `search` now has direct JSON-RPC facade coverage for the same gist-only abstention rail. A stateful MCP server seeds a real consolidation summary, then `tools/call` search proves the structured result preserves `abstained=true`, the source-inspection uncertainty note, abstractive-gist hit metadata, and `explain.gist_support`.

Latest checkpoint: CLI `mcp-streamable-http-soak` now validates the official MCP SDK StreamableHTTP transport with health transport checks plus repeated SDK `initialize`, `tools/list`, and read-only `tools/call` loops. The command supports bearer/session headers for deployed gateways, injects auth only into the read-only call contract when needed, redacts configured token values from output, and is allowlisted for `deployment-soak`.

Latest checkpoint: CLI `tls-rotation-plan-check` now validates current/candidate certificate files before a rotation by checking SAN hostname coverage on both certs, current/candidate validity windows, overlap days, optional issuer continuity, and hashed serial reporting. CLI `tls-lifecycle-ops-check` now validates the production evidence bundle around the rotation plan: non-local issuer, renewal execution, deployed serial match, reload verification, secret-manager private-key custody, monitoring alerts, and raw material redaction. Tests cover passing and fail-closed TLS lifecycle evidence; actual CA issuance/renewal/distribution remains operator-run evidence, not a local side effect.

Latest checkpoint: Media extraction jobs now add an explicit `media-derived-text` relation from the source asset evidence CID to the derived text evidence CID, with both CIDs as provenance. Local CLI and live Postgres tests verify the relation is exported, the derived text stays searchable, and forgetting the source asset erases derived evidence and expires the lineage relation. This moves Plan 06-04 toward a single associative substrate for multimodal projections, while broader production extractor/embedder evidence is routed through provider validation and operator-run deployment evidence.

Latest checkpoint: CLI `provider-check` now validates command-backed session-secret custody from flags or a manifest `session_secret` provider block. The check loads the shell-free `get_session_secret` adapter, validates secret/keyring active-key semantics, proves a signed-session round trip, reports only redacted shape metadata, and fails closed on bad rotation state without leaking key material. This reduces the deployed secret-distribution gap but does not close production secret-manager rotation evidence capture.

Latest checkpoint: Live Postgres CLI coverage now exercises HTTP-compatible retrieval providers end to end. A local fake embedding provider captures through `--embedding-provider http`, persists provider-generated 1024-dim pgvector evidence embeddings, and is verified directly in Postgres under tenant RLS; `search`/`explain` report `http-embedding` and `http-reranker` adapter metadata; the HTTP reranker controls final hit ordering; and malformed zero-vector provider output exits nonzero without silently falling back to local hashing or inserting evidence. This reduces Plan 06-03 provider parity risk but does not close validation against real Qwen3/Gemini/Voyage/Cohere deployments or calibration datasets.

Latest checkpoint: Command-backed raw-media embeddings now participate in actual ingestion and retrieval, not only provider checks. `IngestionPipeline` can attach a media vector to externalized image/audio/video evidence without changing content-addressed object identity; LocalMemoryEngine uses stored media vectors for dense retrieval before OCR/transcript extraction; PostgresEngine writes the vector into `evidence.embedding` and exposes safe media-embedding metadata in hits; and live CLI/Postgres coverage verifies command media embedding storage plus raw image retrieval without derived text. This reduces Plan 06-04 multimodal retrieval risk but does not close real production extractor/embedder deployment validation.

Latest checkpoint: Parametric protected-suite orchestration now uses persisted runtime gate cases before falling back to synthetic smoke cases. CLI/MCP parametric evaluation reports protected-suite source, IDs, and tier counts; rollback persists the same suite snapshot, records rollback protected-case metrics, and sends protected cases to command-backed rollback providers. CLI `provider-check` now validates that rollback suite contract and reports the protected-suite snapshot. Focused parametric CLI/MCP/provider-check tests plus the full affected runtime/CLI test files pass. This reduces the local protected-suite and rollback-orchestration gap while deployed trainer, immutable-rail service, and production rollback validation require operator-run evidence.

Latest checkpoint: Ops dashboard packaging now writes a local deployment bundle. `ops-report --dashboard-package-dir` emits `ops-dashboard.html`, `ops-report.json`, and `manifest.json`; the manifest records tenant, ok state, report counts, tripwires, and package file names. Deployment-soak coverage verifies the package output through an allowlisted `ops-report` check. This reduces the observability packaging gap while hosted dashboard deployment and production operations require operator-run evidence.

Latest checkpoint: Deployment-soak can now preserve operator-run validation evidence. `deployment-soak --evidence-dir` writes `deployment-soak-report.json`, per-check JSON files under `checks/`, and a manifest that records source manifest, ok state, summary, and check file paths without storing raw command lines. The ops dashboard soak test verifies the evidence bundle alongside dashboard packaging. This strengthens production validation handoff while live hosted endpoint runs require operator-run evidence.

Latest checkpoint: Consolidation now supports command-backed entity resolution. A configured command resolver receives tenant-scoped candidate payloads after extraction, must return explicit candidate-to-entity mappings, and fails closed on malformed, missing, duplicate, or unknown mappings; successful promotions persist resolver-provided entity keys into the entity registry. CLI runtime workers, `ingest --run-consolidation-once`, provider manifests, and `provider-check` can configure and validate the resolver boundary. Focused CLI tests cover runtime consolidation, manifest-backed provider checks, and malformed resolver failure. This reduces the richer production entity-resolution gap and later hosted-LLM/projection-recompute gates cover resolver, model, and incremental recompute contracts; real deployed runs require operator-run evidence.

Latest checkpoint: Runtime jobs now support affected-subgraph projection recompute. `projection_recompute` accepts changed evidence CIDs, expands through derived evidence/provenance edges, reports the affected assertion/preference/relation/entity projection IDs, and can queue consolidation only for dirty surviving source inputs with projection-specific pass selection. CLI `projection-recompute-enqueue` and `projection-recompute-once` expose the job, and `deployment-soak` can run the bounded once command as an allowlisted preflight. Focused CLI/runtime tests cover direct fact projection impact and media-derived evidence dependency expansion. This reduces the incremental recompute gap while broader production IVM/differential recompute evidence is routed through deployment-soak/release-audit for operator-run deployments.

Latest checkpoint: CLI blueprint alias coverage now includes explicit profile recording and direct trajectory attribution. `profile-record-explicit` is verified through the write result plus scoped `profile-context` readback for scope, confidence, source evidence CIDs, and authorization metadata; `trajectory-attribute` is verified against a persisted failed trajectory for cause, signature, confidence, and evidence. Local and clean Postgres DSN full suites pass at 361 collected tests. This tightens CLI/MCP runtime parity while production endpoint validation requires operator-run evidence.

Latest checkpoint: CLI runtime handler coverage now reaches every command surface by static mention scan. New direct tests cover `prefetch`, `discard`, `trajectory-log`, `lesson-induce`, and `procedure-induce`; they verify warm prefetch retrieval and threshold skip behavior, authorized branch discard removing candidate-branch retrieval, and direct learning alias persistence. Local and clean Postgres DSN full suites pass at 362 collected tests. This closes the CLI handler coverage audit loop while live hosted endpoint and production soak validation require operator-run evidence.

Latest checkpoint: MCP behavioral parity now extends beyond `tools/list` schema equality and capture/search smoke tests. In-process JSON-RPC `tools/call` coverage directly exercises source-truth sync, scoped profile record/infer/correct/get-relevant, graph timeline/as-of reads, direct trajectory attribution, lesson/procedure induction/search, and outcome evaluation. Local and clean Postgres DSN full suites pass at 363 collected tests. This reduces the CLI/MCP runtime parity gap while hosted production endpoint validation requires operator-run evidence.

Latest checkpoint: Runtime maintenance jobs now have durable Postgres queue-worker parity coverage. `PostgresQueue` plus `QueueWorker` drains calibration, lifecycle sweep, eval suite, observability snapshot, and projection recompute jobs over live Postgres state, and persists complete job results for each. The slice also fixed eval-suite result serialization for slotted/dataclass outcomes. Local and clean Postgres DSN full suites pass at 364 collected tests. This reduces the deployed worker parity gap while hosted worker supervision and production operations require operator-run evidence.

Latest checkpoint: Deployment-soak evidence bundles now distinguish local validation harness evidence from production endpoint evidence. The report and bundle manifest carry local orchestrator scope, explicit `production_validated=false`, and redaction metadata that documents omitted raw commands/stderr while retaining bounded JSON check output. Local and clean Postgres DSN full suites pass at 364 collected tests. This strengthens operator handoff without overstating real provider, certificate, IdP, KMS, or hosted endpoint validation.

Latest checkpoint: The user model coverage now explicitly exercises all six blueprint categories with scope-aware authority ordering: identity, hard instruction, explicit preference, inferred preference, situational preference, and temporary state. REQ-012 now reflects implemented model/runtime behavior while keeping dedicated product UX out of the blocking parity path. Local and clean Postgres DSN full suites pass at 364 collected tests.

Latest checkpoint: Consolidation now supports command-backed candidate extraction and evidence summarization providers. The extractor contract accepts tenant/payload/evidence JSON and returns strict candidate rows; the summarizer contract accepts tenant/evidence JSON and returns a non-empty summary. Runtime workers, `ingest --run-consolidation-once`, provider manifests, and `provider-check` can configure both providers, and malformed provider output fails closed. Focused CLI tests cover model-style extraction of non-deterministic evidence, command summarizer pass details, manifest-backed provider checks, and bad-provider failure modes. This closes the local/contract model-backed extraction-summarization gap; real deployed model/provider claims now require `hosted-llm-check` and operator-run endpoint evidence.

Latest checkpoint: Protected regression suites now have a deployment check surface. `gate-suite-check` reports persisted suite shape, tier counts, protected-case IDs, and a stable fingerprint; it can include full cases for review/export and fails closed on insufficient protected cases or mismatched expected fingerprints. The command is allowlisted for `deployment-soak`, and CLI tests cover successful fingerprint acknowledgement plus mismatch failure. This makes protected-suite review part of the production release-artifact validation path.

Latest checkpoint: Live Postgres parity coverage now exercises command-backed consolidation providers. A new live test runs `PostgresEngine` with command candidate extraction and command summarization, gates a non-deterministic evidence note through protected regression checks, and verifies exported assertions/entities reflect the provider-produced candidate. This broadens production adapter parity coverage when `MNEMOSYNE_POSTGRES_DSN` is configured.

Latest checkpoint: Live Postgres command-backed consolidation parity now includes entity resolution in the same provider path. The live test wires command candidate extraction, command entity resolution, and command summarization into `ConsolidationWorker`, then verifies protected promotion, resolver pass metadata, and the resolver-supplied entity canonical persisted in Postgres. `hosted-llm-check` now covers hosted resolver/model contracts while real deployments still require operator-run evidence.

Latest checkpoint: Provider manifest deployment gates now include explicit lexical/graph retrieval backend validation. `provider-check` emits a `retrieval_backends` check with backend names plus local-fallback flags, and `forbid_local` rejects local lexical or graph backends alongside local embedding/reranker providers. Tests cover a production-style `paradedb-bm25` + `apache-age` manifest and fail-closed `local-bm25-lite` + `local-ppr` fallback rejection. This tightens Plan 06-03 production retrieval adapter handoff without requiring local ParadeDB/AGE deployment.

Latest checkpoint: Production retrieval release validation now has a fingerprinted `retrieval-ops-check` command. The checker validates operator-run evidence bundles with `provider_check`, `retrieval`, `calibration`, and `redaction` sections, requiring forbid-local provider manifests, non-local embedding/reranker providers, non-local lexical and graph backends, Postgres production validation, hashed tenant/query cases proving lexical/vector/graph/reranked/calibrated retrieval paths, production calibration dataset fingerprints, sample-shape counts, empirical coverage and false-accept thresholds, raw-query/document/embedding/credential redaction, and expected fingerprint acknowledgement. It is part of the `deployment-soak` allowlist and production `release-audit` command profile.

Latest checkpoint: Deployment evidence now has a second-stage `release-audit` gate. The command reads a `deployment-soak` report or evidence manifest, verifies required production command coverage, required provider-check subchecks, non-local retrieval backends, provider `forbid_local`, redaction flags, optional production validation scope, and a stable release fingerprint. Focused CLI tests cover a passing production evidence bundle plus fail-closed missing-command/local-provider evidence. This converts the remaining production release-audit gap into a machine-verifiable artifact while still requiring operator-run credentials/endpoints for real production claims.

Latest checkpoint: Calibration threshold tuning now accepts labeled eval datasets through `calibration-tune` and the queue-backed calibration job. The tuner derives conformal scores from correct examples, reports threshold/coverage/false-accept/abstention metrics, fails closed on insufficient sample shape, low empirical coverage, or high false-accept rates, and only persists calibration when the gates pass. Shared engine coverage exercises the tuned calibration through local/Postgres backends, and `release-audit` now requires `calibration-tune` evidence in the production command profile.

Latest checkpoint: Forgetting policy release validation now has a fingerprinted `forgetting-policy-check` command. The checker evaluates JSON policy cases for lifecycle demotion, must-keep spaced rehearsal, and gist-risk abstention expectations, reports per-case actuals/findings, supports required case IDs plus expected fingerprint acknowledgement, and is part of the `deployment-soak` allowlist and production `release-audit` command profile. Focused CLI coverage verifies both a passing policy suite and fail-closed expectation mismatches.

Latest checkpoint: Policy-ops release validation now has a fingerprinted `policy-ops-check` command. The checker validates production shadow evidence bundles for invariant-rail-safe variants, explicit shadow mode, external reward outcomes, contextual-bandit recommendation agreement, proxy-vs-true tripwires, and cadence bounds. It is allowlisted for `deployment-soak` and required by the production `release-audit` profile, closing the REQ-018 production policy-ops validation gate while still leaving broader offline-RL research outside the local harness.

Latest checkpoint: Belief-revision release validation now has a fingerprinted `belief-revision-check` command. The checker runs JSON TMS/AGM suites through the existing `BeliefRevisionCore`, verifies supersession and contradiction expectations, proves dependency cascade invalidation, surfaces contested hypotheses with normalized probabilities, supports required case IDs plus expected fingerprint acknowledgement, and is required by the production `release-audit` command profile.

Latest checkpoint: Hosted LLM/provider validation now has a redacted `hosted-llm-check` command. The checker reads a manifest of candidate-extractor, summarizer, and entity-resolver HTTP/model endpoints, enforces HTTPS by default with localhost-only test escape hatch, supports native role JSON and OpenAI chat JSON response shapes, validates each role contract without printing request/response bodies or bearer tokens, fingerprints the sanitized result, and is part of the production `release-audit` command profile.

Latest checkpoint: Production retrieval adapter breadth now includes shell-free command lexical and graph retrievers. `--lexical-provider command` and `--graph-provider command` route PostgresEngine lexical/BM25 and specialist graph/PPR calls through JSON command contracts while preserving native Postgres FTS and recursive-PPR defaults. Provider manifests can configure the same commands, and `provider-check` now probes command lexical and graph adapters with validated hit output before accepting production retrieval backend evidence.

Latest checkpoint: Tenant isolation and auth release validation now has a fingerprinted `auth-ops-check` command. The checker validates operator-run evidence bundles with `idp_jwks`, `authz_rollout`, `session_secret`, `tls`, `tenant_isolation`, and `redaction` sections. It requires live issuer/audience/JWKS validation, multi-key JWKS rotation with refresh-on-unknown-kid and previous-key rejection, safe token headers, hashed tenant/user identity, configured authz policy, acknowledged current/candidate authz fingerprints with allow/deny simulations, external command-backed session-secret custody and rotation, TLS certificate and rotation-plan controls, Postgres RLS and signed-session tenant binding, cross-tenant deny and same-tenant allow cases, raw token/claim/secret/private-key redaction, and expected fingerprint acknowledgement. It is part of the `deployment-soak` allowlist and production `release-audit` command profile.

Latest checkpoint: Hosted MCP runtime release validation now has a fingerprinted `mcp-ops-check` command. The checker validates operator-run evidence bundles with `http_json_rpc`, `streamable_http`, optional or required `legacy_sse`, `tls`, and `redaction` sections. It requires HTTPS non-local hosted endpoints, health/initialize/tools-list/read-only-call/stateless loop controls, bearer-token and signed-session binding, loop-count and latency thresholds, optional legacy SSE endpoint event evidence, TLS certificate validity, optional client-certificate enforcement, raw token/request/response redaction, and expected fingerprint acknowledgement. It is part of the `deployment-soak` allowlist and production `release-audit` command profile.

Latest checkpoint: Consolidation role-pipeline release validation now has a fingerprinted `consolidation-ops-check` command. The checker validates operator-run evidence bundles with `validation_scope`, `worker_run`, `provider_check`, `hosted_llm`, `projection_recompute`, `gate_suite`, `embedding`, `consolidation_run`, `calibration`, `lifecycle`, `ops_report`, `deployment`, and `redaction` sections. It requires production operator assertion, Postgres-backed supervised worker cycles across consolidation/calibration/lifecycle/observability/projection jobs, forbid-local provider and hosted extractor/summarizer/entity-resolver role checks, production embedding evidence, projection recompute with consolidation re-enqueue, protected-suite tier coverage, model-backed consolidator write authority, calibration/lifecycle/ops tripwire health, deployment supervision/alert/fingerprint/count binding with bounded latency, raw-field redaction, stable fingerprints, `deployment-soak` allowlist coverage, and production `release-audit` command-profile coverage.

Latest checkpoint: Multimodal retrieval release validation now has a fingerprinted `multimodal-ops-check` command. The checker validates operator-run evidence bundles with `validation_scope`, `provider_check`, `object_store`, `extraction`, `media_embedding`, `retrieval`, `media_jobs`, `deployment`, and `redaction` sections. It requires production operator assertion, forbid-local media extractor/embedder provider checks, encrypted external object storage, image/audio/video extraction cases with asset and derived-CID hashes, raw media embedding indexing, Postgres production retrieval over stored media vectors and derived text, Postgres-backed `media_extract` job processing with fail-closed dead-job handling, supervised extraction/embedding/object-store/job/retrieval operations, alert routing, execution fingerprints, deployment count binding, raw-field redaction, stable fingerprints, `deployment-soak` allowlist coverage, and production `release-audit` command-profile coverage.

Latest checkpoint: Signed-provenance production validation now has a fingerprinted `provenance-ops-check` command. The checker validates operator-run C2PA evidence bundles with `validation_scope`, `c2pa_verifier`, `trust_roots`, `provenance_trust`, `asset_bound_cases`, `quarantine`, `ingestion`, `deployment`, and `redaction` sections. It requires production operator assertion, command/container/hosted verifier identity, trusted issuer/root policy fingerprints, trust-root rotation and stale-root rejection, asset-bound trusted plus quarantined cases, quarantine exclusion from default retrieval, Postgres ingestion evidence with hashed tenant/evidence IDs and provenance capability tags, supervised verifier health, trust-root refresh, quarantine drill, ingestion-pipeline supervision, alert routing, deployment policy/ingestion fingerprint binding, raw asset/manifest/verifier/certificate/credential redaction, stable fingerprints, `deployment-soak` allowlist coverage, and production `release-audit` command-profile coverage.

Latest checkpoint: Signed-provenance release validation now has a fingerprinted `provenance-trust-check` command. The checker runs C2PA verifier suites against asset-bound manifests, requires configured trusted issuers and certificate roots, verifies signer/root expectations without printing raw asset bytes, manifests, verifier stdout, or verifier stderr, supports required case IDs plus expected fingerprint acknowledgement, and is part of the `deployment-soak` allowlist and production `release-audit` command profile.

Latest checkpoint: Ops dashboard release validation now has a fingerprinted `ops-dashboard-check` command. The checker validates `ops-report --dashboard-package-dir` bundles for package manifest shape, snapshot health, tripwire pass state, dashboard HTML markers, and tenant binding, validates hosted dashboard URLs with HTTPS-by-default policy and raw HTML/JSON redaction, and optionally validates production dashboard operations evidence for refresh freshness, source snapshot fingerprints, access control, alert delivery, and raw snapshot/token/user-data redaction. It is part of the `deployment-soak` allowlist and production `release-audit` command profile.

Latest checkpoint: Production Postgres retrieval validation now requires concrete specialist adapter probes in `retrieval-ops-check` bundles. The checker requires lexical, vector, graph, and reranker probe evidence with production validation, non-local backend/provider names, SHA-256 command/source/query/tenant/result/top-id fingerprints, hit counts, bounded latency, and redaction of raw query/document/result/stdout/stderr/command/env/credential fields. The command remains part of the `deployment-soak` allowlist and production `release-audit` command profile; actual deployed adapter evidence remains operator-run.

Latest checkpoint: Privacy and erasure release validation now has a fingerprinted `privacy-ops-check` command. The checker validates production evidence bundles for non-local KMS/key lifecycle, key rotation and shred verification, strict residency allow/deny enforcement, tombstone recompute safety, legal hard-delete safety, required case IDs, explicit key/object/subject/KMS-response redaction flags, recursive raw-field rejection, and expected fingerprint acknowledgement. It is part of the `deployment-soak` allowlist and production `release-audit` command profile.

Latest checkpoint: Production differential recompute evidence is now part of the default `release-audit` command profile. `projection-recompute-once` was already allowlisted for `deployment-soak`; release bundles must now include it alongside worker, provider, suite, policy, and dashboard gates so affected-subgraph recompute evidence is not optional in production handoff.

Latest checkpoint: Parametric trainer release validation now has a fingerprinted `parametric-trainer-check` command. The checker validates production evidence bundles with explicit `trainer`, `protected_suite`, `gate`, `rollback`, `deployment`, `rail_report`, `metrics`, and `redaction` sections. It requires non-local trainer providers, immutable artifact URI/hash proof, canonical protected suite fields (`case_count`, `protected_case_count`, `case_ids`, `protected_case_ids`, non-synthetic `source`, `fingerprint`, and smoke/core/archive `tier_counts`), gate candidate/artifact matching, passed protected cases, margin above the enforced floor, no rollback branch on promoted gates, HTTPS supervised deployment/canary/alert evidence, protected-suite/artifact/rollback fingerprint binding, rollback drill verification, bounded deployment latency, runtime rail metadata for external reward/monotonic trust/non-negative trust delta/no system-prompt sink/no eval-source overlap, rollback authorization, mutation/reward/sink thresholds, raw-field redaction checks, and expected fingerprint acknowledgement. It is part of the `deployment-soak` allowlist and production `release-audit` command profile, leaving only operator-run trainer infrastructure evidence for real production claims.

Latest checkpoint: DSN-backed full-suite verification uncovered and fixed an explicit-empty Postgres DSN edge case. CLI backend, runtime-state, and Postgres queue loading now use the parser-selected DSN directly, so `--postgres-dsn ""` fails even when `MNEMOSYNE_POSTGRES_DSN` is set. Focused CLI coverage sets an env DSN to lock the behavior, and the full suite passes against the local compose pgvector container.

Latest checkpoint: Hosted HTTP JSON-RPC MCP `search` now preserves gist-only abstention through the deployed `/mcp` transport, including capture persistence, consolidation summary materialization, server restart, and `tools/call` search over generated gist support. CLI `tools` output now has exact parity coverage against MCP `tools/list`, pinning the agent-facing tool contract across both surfaces. Local verification now collects 352 tests with 298 passing tests plus 54 skipped live-DB tests, and the clean compose DSN run passes all 352 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Deep retrieval now abstains when its only support is generated-gist evidence plus graph/PPR relations derived from that gist. Local and Postgres graph hits expose relation source/predicate/target/confidence/source-evidence metadata, retrieval classifies `summary-derived-gist` relations as gist-tier support, and MCP `deep_search`/`explain` preserve the same abstention contract. Local verification now collects 355 tests with 300 passing tests plus 55 skipped live-DB tests, and the clean compose DSN run passes all 355 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Official MCP SDK StreamableHTTP validation now exercises a full local write/read path through the official in-process client: initialize, `tools/list`, read-only `residency_policy`, `capture`, and `search` with provenance verification. Local verification remains 355 collected tests with 300 passing tests plus 55 skipped live-DB tests, and the clean compose DSN run passes all 355 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Hosted HTTP JSON-RPC MCP `deep_search` and `explain` now preserve gist-derived graph abstention over `/mcp` when relation-only `summary-derived-gist` support is retrieved. Clean DSN verification also surfaced and fixed a Postgres forget-propagation timestamp edge case by expiring relations with `GREATEST(clock_timestamp(), valid_from + interval '1 microsecond')` instead of transaction `now()`. Local verification now collects 356 tests with 301 passing tests plus 55 skipped live-DB tests, and the clean compose DSN run passes all 356 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Shared Local/Postgres direct primitive retrieval coverage now locks caller budget enforcement. `lexical_search`, `vector_search`, and `graph_ppr` are verified to honor `k=1` across multiple matching rows while preserving tenant, branch, and backend channel metadata. Local verification now collects 358 tests with 302 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 358 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: CLI `deep-search` and `explain` now preserve gist-derived graph abstention for relation-only `summary-derived-gist` support, matching the engine, MCP facade, and hosted HTTP JSON-RPC safety rail. Local verification now collects 359 tests with 303 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 359 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: CLI `projection-recompute-enqueue` now has direct payload persistence coverage. The command queues `projection_recompute` with tenant/user/branch, multiple changed evidence CIDs, custom max attempts, and `--no-enqueue-consolidation` encoded in the job payload. Local verification now collects 360 tests with 304 passing tests plus 56 skipped live-DB tests, and the clean compose DSN run passes all 360 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Shared runtime side-state parity now directly round-trips `RuntimeState` and `PostgresRuntimeState` for user-model entries, latent profiles, learning trajectories/attributions/lessons/procedures, queued job payloads, metrics snapshots, and protected gate cases. Focused verification covers local skip behavior plus live Postgres equality, full local verification now collects 410 tests with 350 passing tests plus 60 skipped live-DB tests, and the clean compose DSN run passes all 410 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Live helper parity now covers public setup paths that were previously only indirectly exercised. A DSN-gated test directly verifies idempotent `PostgresEngine.ensure_tenant_and_branch`, exported branch metadata, idempotent `PostgresQueue.ensure_schema`, tenant-scoped `PostgresQueue.list_jobs`, and `RuntimeState.from_store_path` sidecar derivation. Focused local verification skips the DSN-gated case, focused Postgres verification passes it, full local verification now collects 411 tests with 350 passing tests plus 61 skipped live-DB tests, and the clean compose DSN run passes all 411 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Shared evidence-embedding parity now covers the fail-closed and observability sides of `set_evidence_embedding`, not only successful vector persistence. The Local/Postgres contract asserts missing-CID updates return false, successful updates persist through recall/export, and audit rows retain embedder source, trust tier, source type, and embedding dimension metadata. Focused local verification covers the local engine and skips the DSN fixture, focused Postgres verification passes both engines, full local verification now collects 411 tests with 350 passing tests plus 61 skipped live-DB tests, and the clean compose DSN run passes all 411 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Runtime job handler parity now directly exercises the public `RuntimeJobHandlers` methods that were previously verified mainly through queue-worker dispatch. The focused test invokes `run_media_extract`, `run_calibration`, `run_lifecycle_sweep`, `run_eval_suite`, and `run_observability_snapshot`, asserting structured status/details plus emitted counters. Focused local and DSN-configured verification each pass 2 tests, full local verification now collects 412 tests with 351 passing tests plus 61 skipped live-DB tests, and the clean compose DSN run passes all 412 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Direct `MemoryTools` facade parity now covers wrapper methods separately from the JSON-RPC transport. The focused test seeds evidence, an assertion, and a relation, then directly invokes profile record/infer/correct/get, graph neighbors/query/timeline/as-of, trajectory attribution, lesson/procedure induction, procedure validate/promote/rollback, and outcome evaluation. Focused verification passes 1 direct facade test, full local verification now collects 413 tests with 352 passing tests plus 61 skipped live-DB tests, and the clean compose DSN run passes all 413 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Direct `MemoryTools.source_sync` coverage now verifies the wrapper path for committed Markdown/git source truth, not only CLI or JSON-RPC transport entrypoints. The focused test builds a clean temporary Git repository with a fenced `mnemosyne-assertion`, calls `source_sync(apply=True)` directly, and checks security authorization, source identity, appended `markdown_git` evidence, active assertion provenance, trust tier, capability tags, and source-truth Git metadata. Focused direct `MemoryTools` verification passes 2 tests, full local verification now collects 414 tests with 353 passing tests plus 61 skipped live-DB tests, and the clean compose DSN run passes all 414 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Durable Postgres queue failure-path parity now covers worker exceptions, not only successful leasing/completion. A live DSN-gated test runs a failing handler twice with `max_attempts=2` and verifies persisted `retry` then `dead` status, attempts, `last_error`, status snapshots, no cross-tenant lease/list visibility, and no leasing after the job is dead. Focused local verification skips the DSN case, focused Postgres verification passes it, full local verification now collects 415 tests with 353 passing tests plus 62 skipped live-DB tests, and the clean compose DSN run passes all 415 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Postgres runtime side-state tenant isolation now has direct two-tenant coverage. A DSN-gated test saves mixed tenant user-model and learning state through tenant-scoped `PostgresRuntimeState` instances, plus distinct queues, metrics, and gate cases, then verifies each tenant reloads only its own state and mirror-table counts stay scoped for `runtime_state`, `preferences`, `user_latent`, `trajectories`, `lessons`, `procedures`, and `eval_cases`. Focused local verification skips the DSN case, focused Postgres verification passes it, full local verification now collects 416 tests with 353 passing tests plus 63 skipped live-DB tests, and the clean compose DSN run passes all 416 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: CLI `eval` now has direct command behavior coverage and a serialization fix for slotted `EvalOutcome` rows. The regression runs the real subprocess entrypoint with an intentionally empty Postgres DSN, proving the seed-suite command remains backend-independent while emitting stable JSON outcome names, pass flags, details, and no extra fields. Focused verification passes 1 CLI eval test, full local verification now collects 417 tests with 354 passing tests plus 63 skipped live-DB tests, and the clean compose DSN run passes all 417 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Production release-audit validation now requires complete production-scope attestation, not just a lone `production_validated=true` boolean. When `--require-production-validated` is set, forged or incomplete deployment-soak reports fail closed unless they also declare `target_environment=production` and `operator_asserted=true`; the focused regression verifies the new `production_target_missing` and `operator_attestation_missing` findings. Focused release-audit verification passes 3 tests, full local verification now collects 418 tests with 355 passing tests plus 63 skipped live-DB tests, and the clean compose DSN run passes all 418 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Production release-audit now validates the required output shape for every command in the production release profile instead of accepting placeholder `{ok: true}` stdout. Required evidence from auth, MCP, retrieval, consolidation, multimodal, privacy, provenance, trainer, worker, TLS lifecycle, projection, dashboard, and ops-report checks must expose the stable top-level sections those commands emit, while `ops-report` accepts either direct report counters/tripwires or a dashboard-wrapped report. Focused release-audit verification passes 4 tests, full local verification now collects 419 tests with 356 passing tests plus 63 skipped live-DB tests, and the clean compose DSN run passes all 419 tests with `MNEMOSYNE_POSTGRES_DSN=postgresql://mnemosyne:<redacted>@127.0.0.1:54329/mnemosyne?connect_timeout=5`.

Latest checkpoint: Production manifest rendering is now deterministic and fail-closed for the Tier B operator evidence pass. The current 2026-06-30 flow creates an external Tier-B custody packet, fills that packet's strict `production-render.env`, and passes it via `infra/scripts/render-production-soak-manifest.sh --env-file <packet>/production-render.env`; shell sourcing and ad hoc template-copy instructions are retired. `infra/scripts/render-production-soak-manifest.sh` renders an external `0600` manifest only after every required value is present, refuses repository-local output by default, and validates the 28-command production profile before writing; `infra/scripts/capture-production-evidence.sh` rejects unresolved production placeholders before creating an evidence directory or running `deployment-soak`. This tightens the production evidence handoff, but Phase 6 remains open until the operator-run production bundles pass `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated --require-provider-forbid-local` against deployed infrastructure.

Latest checkpoint: Production evidence capture now has explicit redaction-scan hardening. `src/mnemosyne/evidence_redaction.py` centralizes high-confidence secret-shape detection, and `infra/scripts/capture-production-evidence.sh` rejects rendered manifests with obvious secret material, writes `redaction-scan.json` during preflight, scans generated text evidence after the soak/audit path, and fails before `summary.json` when the bundle is contaminated. Focused redaction/preflight tests, the full venv-backed pytest suite, ruff, unit/drift CI, and Postgres integration CI pass on `fe64422`. This is evidence hygiene only; the remaining Tier B rows still require operator-captured production bundles over real infrastructure.

Latest checkpoint: Strict production evidence now enforces the exact frozen 28-command profile across the renderer, capture wrapper, and manifest-bound release audit. Manual production manifests fail preflight on duplicate or unknown commands, and `release-audit --evidence-manifest "$OUT_ROOT/evidence/manifest.json" --require-production-validated` fails closed on duplicate required command evidence or unexpected command evidence outside the production profile. Focused runtime/preflight/runbook verification and the full venv-backed pytest suite pass locally. This prevents evidence drift and accidental new gates; Phase 6 still remains open until operator-run production bundles pass against deployed infrastructure.

Latest checkpoint: Phase 8.2 retrieval provider wiring now enforces the self-hosted embedding contract's `query:` prefix only on query paths. `HttpEmbeddingProvider.embed()` still sends document text unchanged, while `embed_query()` prefixes query text without double-prefixing; Local/Postgres/SQLite dense query vectors, MMR query vectors, local similarity rerank queries, HTTP rerank queries, and provider-check embedding probes all route through the query-aware helper. Focused provider-contract verification passes locally. TEI/Infinity deployment, real cross-encoder evidence, CPU P95 evidence, and Tier-B production bundles remain operator-run.

Latest checkpoint: Production release-audit now has an explicit source-side audit-evidence gate for the Phase 8.5 tamper-evidence row. `ops-report` evidence must include retained Vault-HMAC hash-chain verification, enabled/retained pgaudit evidence, and external retained WORM-copy evidence; weak or hollow audit sections fail with `required_ops_report_audit_evidence_incomplete`. Focused release-audit verification passes locally. This does not create operator artifacts; real pgaudit/WORM/Vault-HMAC bundles remain required.

Latest checkpoint: Production provider evidence now carries a source-side CPU P95 gate for the embedding/reranker path. `provider-check` can collect repeated embedding and reranker latency samples, fail on provider p95 over threshold, and emit structured p95 timing; production `release-audit` rejects provider evidence that lacks repeated p95 timing for the embedding and reranker subchecks. Focused provider/release-audit verification passes locally. This does not create target-host measurements; TEI/Infinity deployment, real cross-encoder evidence, and retained CPU P95 artifacts remain operator-run.

Latest checkpoint: Production release-audit now has an explicit source-side MCP runtime evidence gate for the fail-closed production-defaults row. `mcp-ops-check` output carries bearer-token and signed-session proof in the retained transport checks, and production `release-audit` rejects MCP evidence that does not prove hosted non-local transports, bearer-token enforcement, signed-session binding, required client certificates, control-loop checks, latency bounds, and raw token/request/response redaction. Focused MCP/release-audit verification passes locally. This does not create hosted MCP operator artifacts; real production MCP evidence remains required.
