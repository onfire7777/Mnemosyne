# W3 P3 B — SQLite working-memory persistence and TTL

Authoritative requirement:
- `docs/superpowers/plans/2026-07-15-W3-taxonomy-completion-plan.md`, Phase 3 and its validation commands.
- Frozen contract and adversarial handoffs in Kanban parents `t_b99b2aa2` and `t_65611e82`.

Exclusive allowed write set:
- `src/mnemosyne/sqlite_engine.py`
- `src/mnemosyne/sqlite_schema.py`
- `tests/test_sqlite_working_memory.py` (new)

Forbidden/shared files:
- Everything outside the exclusive write set, especially `src/mnemosyne/models.py`, `src/mnemosyne/engine.py`, `src/mnemosyne/postgres_engine.py`, `sql/schema.sql`, `tests/test_working_memory.py`, `tests/test_shared_engine_contract.py`, retrieval files, `.planning/**`, and `docs/**`.
- Do not implement Phase 4, change planning state, push, merge, bypass hooks, or modify dependencies.

Acceptance criteria:
- Implement the exact `put_working`, `get_working`, `list_working`, and `expire_working` SQLite contract against the Local/Protocol model supplied by the parent implementation, with composite tenant/session/item identity.
- Persist working rows and audit events transactionally; use explicit aware UTC clocks, half-open TTL visibility, inclusive 24-hour cap, deterministic ordering, monotonic logical expiry, exact-once audit behavior, detached returns, provenance/erasure validation, and no infrastructure fallback.
- Preserve tenant/session/owner isolation and existing data-only, trust, capability, audit, and provenance rails. Do not add implicit promotion.
- Add focused SQLite tests for shared cases 2–10 and 15–17 plus SQLite cases 22–25, including transaction rollback, same-database contention, unavailable database fail-closed behavior, and canonical UTC offset comparison.

### Task 1: Implement SQLite working-memory persistence
- [x] Add schema/table/index definitions without disturbing existing schema.
- [x] Add transactional row marshalling and exact contract methods using parameterized SQL and per-tenant locking.
- [x] Integrate existing audit/provenance/erasure helpers without Local-oracle fallback.

### Task 2: Add focused SQLite behavioral coverage
- [x] Add deterministic tests for TTL, isolation, detached values, provenance, transactionality, contention, unavailable storage, and offset normalization.
- [x] Keep all test fixtures explicit-clock and independent of wall-clock sleeps.

### Task 3: Validate and reconcile
- [x] Run the focused SQLite working-memory tests and required quality checks. The prescribed `tests/test_sqlite_prospective_memory.py` path is absent in this checkout; the equivalent existing `tests/test_prospective_memory.py` suite passed.
- [x] Confirm the final diff contains only the exclusive write set and no tracked `.ralphex` metadata.

Focused validation commands:
- `git diff --check`
- `.venv/bin/ruff check --quiet src/mnemosyne/sqlite_engine.py src/mnemosyne/sqlite_schema.py tests/test_sqlite_working_memory.py`
- `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_sqlite_working_memory.py tests/test_sqlite_engine_core.py tests/test_prospective_memory.py`
