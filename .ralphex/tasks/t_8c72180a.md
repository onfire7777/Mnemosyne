# W3 P5 — subject-bound prospective mutation, recurrence, and session parity

Authoritative requirement: Hermes task `t_8c72180a`, accepted W3 A1 parent `478bef5dae841c83062549f5791be76500f72c8b`, and the frozen W3 governing plan. Execution and review model: `gpt-5.6-sol:low`.

Allowed files:
- src/mnemosyne/engine.py
- src/mnemosyne/sqlite_engine.py
- src/mnemosyne/postgres_engine.py
- src/mnemosyne/mcp_tools.py
- src/mnemosyne/cli.py
- sql/schema.sql
- tests/test_prospective_memory.py
- tests/test_sqlite_prospective_memory.py
- tests/test_postgres_prospective_memory.py
- tests/test_prospective_memory_api.py
- tests/test_prospective_memory_security.py
- tests/test_shared_engine_contract.py
- tests/test_planning_traceability.py only if the public contract list requires it

Forbidden files:
- eval/public/** and benchmark fixtures
- unrelated retrieval/deletion/security work
- planning/GSD/docs
- dependencies and lockfiles
- network, push, merge, skips, xfails, and assertion weakening
- benchmark-only task, clock.inject, event.inject, query, or action-selection engine primitives

### Task 1: Implement production prospective mutation/session parity
- [x] Trace existing Local/SQLite/Postgres prospective contracts and authenticated CLI/MCP authorization before changing behavior.
- [x] Add one authenticated production `update_intention` contract covering reschedule and data-only action override; keep existing cancellation canonical.
- [x] Add explicit recurrence policy/state and session ownership/binding with additive schema/backward-compatible safe defaults.
- [x] Make Local, SQLite, and Postgres behavior atomic and equivalent with audit/provenance, detached reads, replay/idempotency, and terminal-state protection.
- [x] Reject cross-tenant/user/agent/session mutation, fired/cancelled mutation, invalid recurrence, provenance loss, and caller-controlled role/trust. Explicitly resolve the A1 reader-mutation policy from the existing role contract rather than leaving it implicit.
- [x] Reuse `evaluated_at`, `trigger_context`, `operating_point`, and dependency evaluation for time/event/dependency inputs.
- [x] Add focused positive, negative, backcompat, schema, replay, and shared-engine tests without weakening unrelated behavior.
- [x] Review the exact lease, run the gates, and commit one clean candidate.

Validation:
- `/Users/admin/Mnemosyne/.venv/bin/python -m pytest -q tests/test_prospective_memory.py tests/test_sqlite_prospective_memory.py tests/test_postgres_prospective_memory.py tests/test_prospective_memory_api.py tests/test_prospective_memory_security.py tests/test_shared_engine_contract.py`
- `/Users/admin/Mnemosyne/.venv/bin/python -m ruff check src/mnemosyne/engine.py src/mnemosyne/sqlite_engine.py src/mnemosyne/postgres_engine.py src/mnemosyne/mcp_tools.py src/mnemosyne/cli.py tests/test_prospective_memory.py tests/test_sqlite_prospective_memory.py tests/test_postgres_prospective_memory.py tests/test_prospective_memory_api.py tests/test_prospective_memory_security.py tests/test_shared_engine_contract.py`
- `git diff --check 478bef5dae841c83062549f5791be76500f72c8b..HEAD`
- `python3 -c "import subprocess; allowed={'src/mnemosyne/engine.py','src/mnemosyne/sqlite_engine.py','src/mnemosyne/postgres_engine.py','src/mnemosyne/mcp_tools.py','src/mnemosyne/cli.py','sql/schema.sql','tests/test_prospective_memory.py','tests/test_sqlite_prospective_memory.py','tests/test_postgres_prospective_memory.py','tests/test_prospective_memory_api.py','tests/test_prospective_memory_security.py','tests/test_shared_engine_contract.py','tests/test_planning_traceability.py'}; changed=set(subprocess.check_output(['git','diff','--name-only','478bef5dae841c83062549f5791be76500f72c8b..HEAD'],text=True).splitlines()); assert not changed-allowed, sorted(changed-allowed)"`
- `python3 -c "import subprocess; tracked=subprocess.check_output(['git','ls-files'],text=True).splitlines(); assert not any(p.startswith('.ralphex/') for p in tracked); diff=subprocess.check_output(['git','diff','478bef5dae841c83062549f5791be76500f72c8b..HEAD','--'],text=True); needles=('AKIA','BEGIN PRIVATE KEY','ghp_'); assert not any(n in diff for n in needles)"`

Evidence boundary: production contract and deterministic tests only. No official benchmark execution, protected evidence, quality claim, publication claim, or W3 final parity claim.

### Task 2: Run the final bounded Sol-low acceptance pass
- [x] Review the full cumulative diff from `478bef5dae841c83062549f5791be76500f72c8b` through the current clean head, re-run the focused gates, fix only confirmed in-scope findings within the exact allowed files, and leave one exact RalphEx acceptance receipt. Treat the independently reproduced PostgreSQL propose/confirm ID mismatch as a separate P5C contract repair; do not weaken that test or broaden P5. (Accepted at `9602db2`: the 506-case focused selection passed with expected PostgreSQL skips because `MNEMOSYNE_POSTGRES_DSN` is unset; Ruff, diff check, lease check, and secret check passed. No confirmed in-scope findings.)

## Repair receipt (2026-07-18)

- Repaired PostgreSQL trigger-expression persistence for reschedules.
- Added deterministic occurrence advancement, per-occurrence receipts, replay suppression, and terminal max-occurrence behavior across Local, SQLite, and PostgreSQL.
- Made identical update replay a detached zero-mutation return with no history, audit, or persistence duplication.
- Required authenticated non-empty sessions for new MCP/CLI schedules and updates; legacy sessionless persisted rows bind exactly once on first update.
- Exact scoped tests and Ruff pass. Adjacent MCP/planning tests pass.
- Live PostgreSQL cases are present but were not executed locally because `MNEMOSYNE_POSTGRES_DSN` is unset; deterministic PostgreSQL schema/SQL contract coverage passed.

## Additive receipt-ledger repair (2026-07-18)

- Restored `intention_firing_receipts` to its legacy three-column primary key without altering or rebuilding the table.
- Added `intention_firing_receipts_v2` as the occurrence-aware append-only ledger. New firings write only to v2; replay/reuse checks read both legacy and v2 ledgers.
- Added a live upgrade regression that creates the exact old table and receipt, records the table/constraint identities, applies the schema repeatedly, proves both identities and the row survive, proves the legacy receipt still blocks reuse, and proves two v2 recurrence receipts survive another schema application.
- Executed all live PostgreSQL prospective tests against the authorized disposable database: 55 passed, 0 skipped.
- Executed the P5-scoped expanded live gate: 278 passed, 235 deselected. The full requested expanded selection reached 506 passed, 6 skipped, with one PostgreSQL `MemoryTools.propose/confirm` facade failure reproduced unchanged at exact accepted base `478bef5` in an isolated schema.
- Ruff passed for all changed Python files. `sql/schema.sql` was applied repeatedly by the live regression and is not a Ruff input.
- P5B drift follow-up must include `intentions`, `intention_firing_receipts`, and `intention_firing_receipts_v2`.
