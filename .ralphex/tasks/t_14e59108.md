# W3 P3 D — distinct working-memory retrieval route

Authoritative requirement:
- `/Users/admin/Mnemosyne/docs/superpowers/plans/2026-07-15-W3-taxonomy-completion-plan.md`, Phase 3 lines 88-100.
- Frozen contract parent `t_b99b2aa2`: tenant/session-scoped active working items, explicit UTC evaluation, deterministic recency/task-relevance route, fourth shared-budget channel, data-only treatment, no implicit promotion.

Exclusive allowed write set:
- `src/mnemosyne/pipeline.py`
- `src/mnemosyne/retrieval.py`
- `tests/test_working_memory_retrieval.py` (new)

Forbidden/shared files:
- All files outside the exclusive write set, especially engine/storage/schema/model/policy/gate/API files owned by sibling or integration lanes.
- Do not edit `.planning/**`, `docs/**`, Phase 4 surfaces, planning state, lockfiles, or configuration.
- Do not push, merge, bypass hooks, or weaken tenancy, capability, trust, audit, provenance, data-only, deterministic TTL, or explicit-promotion rails.

Acceptance criteria:
- Add one distinct session-scoped working-memory retrieval route weighted by task relevance and recency at an explicit evaluation instant.
- Fuse the route as a stable fourth input with dense/lexical/graph so it competes in existing MMR/U-curve and the single `fit_budget` evidence budget; no reserved second budget.
- Preserve tenant/session isolation; absent session selector returns zero working hits; expired-at-boundary items are excluded without mutating retrieval rows.
- Preserve provenance/evidence IDs and source trust/sensitivity/access/reality metadata; retrieved working text goes through the existing data-only sanitizer and cannot become instruction authority.
- Keep sequential/parallel route identity deterministic, cache-safe across TTL/evaluation changes, and old non-working route behavior unchanged when no working items exist.
- Add focused regression coverage for isolation, deterministic TTL clock, relevance+recency/tie ordering, fourth channel/explain, shared budget/oversize behavior, provenance, data-only sink refusal, absent-session fail-closed, cache expiry, and sequential/parallel identity.

Validation commands:
- `git diff --check`
- `.venv/bin/ruff check --quiet src/mnemosyne/pipeline.py src/mnemosyne/retrieval.py tests/test_working_memory_retrieval.py`
- `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_working_memory_retrieval.py`
- `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_pipeline_extraction.py tests/test_engine_perf_lanes.py`

### Task 1: Implement and verify the bounded retrieval route
- [x] Inspect current pipeline/retrieval APIs and sibling working-memory contracts without editing forbidden files.
- [x] Implement the fourth route and shared evidence-budget participation in the leased files only.
- [x] Add focused regression tests in the leased new test file.
- [x] Run focused validation independently and reconcile a lease-clean diff.
