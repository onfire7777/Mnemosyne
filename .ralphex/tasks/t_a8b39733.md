# W5 I3 reader ABI and synthetic TRAIN-only lane

Authority: W5 compact-answering plan; reader/reranker preregistration comment 99 on task t_29f14275; accepted parity/custody and Rust skeleton parent receipts.

Allowed write set (exclusive):
- services/answering-ort/src/reader.rs
- eval/compact_answering/reader_dev.py
- eval/compact_answering/fixtures/reader-train-dev.json
- tests/test_compact_answering_reader.py

Forbidden/shared files and actions:
- Do not modify any other repository file, especially lib.rs, deployment, providers, docs, planning, lockfiles, or shared parity files.
- No model/data download, model execution, export, fine-tune, quantization, protected/held-out data, hardware or heavy run, spend, publication, push, or merge.
- Keep all output synthetic TRAIN-only and explicitly unmeasured; do not claim a model winner, quality, parity, or 8-GiB acceptance.

### Task 1: Implement the bounded reader ABI
- [x] Add a versioned fail-closed Rust reader contract for start/end, yes/no/span, supporting facts, null margin, 512-token windows and 128 stride, raw UTF-8 offsets, identity/digest binding, finite/shape/timeout/unsupported-answer rejection.
- [x] Add the strict Python mirror and deterministic synthetic TRAIN-only reader selection/receipt validator.
- [x] Add an immutable source-owned synthetic fixture covering answer types, support facts, null/abstention, Unicode/repeated/boundary/multi-window cases and identity/preprocessing/artifact custody.
- [x] Add focused tests for valid behavior and malformed, drifted, non-finite, timeout, offset, digest, and unsupported-answer failures.

### Task 2: Validate the exact lane
- [x] Run focused reader pytest and Ruff (passed with the shared root virtualenv at ../../.venv; this worktree has no local .venv directory).
- [x] Run relevant Rust formatting/clippy/tests where the accepted sidecar crate is available without changing unleased files (standalone rustfmt passed; clippy/tests skipped because services/answering-ort has no Cargo manifest).
- [x] Run secret/risky-file sweep and git diff checks; confirm only the four leased files changed and RalphEx metadata is untracked/ignored (git diff --check passed; RalphEx metadata is excluded by .git/info/exclude).

Focused validation commands:
- PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_compact_answering_reader.py
- .venv/bin/ruff check --quiet eval/compact_answering/reader_dev.py tests/test_compact_answering_reader.py
- git diff --check
- git status --short --untracked-files=all
