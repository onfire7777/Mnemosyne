# W3 P2 C corrective PostgreSQL intention persistence

Authoritative requirements:
- `/Users/admin/Mnemosyne/docs/superpowers/plans/2026-07-15-W3-taxonomy-completion-plan.md`
- Kanban task `t_1ac765b7`, including the corrective contract after completed Local parent `t_d7de6faa`

Allowed write set (exclusive):
- `src/mnemosyne/postgres_engine.py`
- `sql/schema.sql`
- `tests/test_postgres_prospective_memory.py`

Forbidden/shared files:
- `src/mnemosyne/engine.py`
- `src/mnemosyne/sqlite_engine.py`
- `src/mnemosyne/sqlite_schema.py`
- `tests/test_shared_engine_contract.py`
- all CLI/MCP files
- `.planning/**`
- `docs/**`
- dependency and lock files

### Task 1: Reconcile PostgreSQL implementation with the canonical Local contract
- [x] Delete backend-local prospective context, operating-point, validator, comparison, containment, and evaluator copies; import and use canonical public types/helpers from `mnemosyne.engine`.
- [x] Replace every `object.__new__` bypass with canonical public `Intention` construction/deserialization.
- [x] Preserve parameterized SQL and fail-closed provenance, trust, capability, tenant, user, and agent checks.

### Task 2: Make firing transactional, deterministic, and replay/concurrency safe
- [x] Use deterministic ordered row locking or atomic `UPDATE ... RETURNING` with explicit loser detection; never swallow broad transactional failures and never return firings from a rolled-back transaction.
- [x] Add a durable, clock-independent unique firing receipt keyed by tenant, intention, and operation, with audit and state transition in one transaction.
- [x] Ensure dependency IDs are bound parameters and hostile identifiers cannot alter SQL.

### Task 3: Prove the corrective invariants on live PostgreSQL
- [ ] Replace bypass-based tests with canonical public constructors and canonical context/operating-point imports.
- [ ] Add live two-connection race, replay, RLS/tenant-user-agent isolation, hostile dependency-ID, and audit-failure rollback tests.
- [ ] Keep tests deterministic and isolated; skip only when the documented live PostgreSQL prerequisite is unavailable.

Focused validation commands:
- `.venv/bin/python -m pytest -q tests/test_postgres_prospective_memory.py tests/test_postgres_security.py`
- `.venv/bin/ruff check --quiet src/mnemosyne/postgres_engine.py tests/test_postgres_prospective_memory.py`
- `git diff --check`
