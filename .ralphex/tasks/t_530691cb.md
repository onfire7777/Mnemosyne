# PM-Bench and TriggerBench deterministic action adapters

Allowed files:
- eval/public/adapters/pm_bench_triggerbench.py
- tests/test_public_pm_bench_triggerbench.py

Forbidden files:
- eval/public/runner.py
- eval/public/registry.json
- eval/public/scoring.py
- eval/public/bundle.py
- eval/harness/cli_driver.py
- all planning state and documentation
- every file not listed in Allowed files

### Task 1: Implement normalized deterministic action probes
- [x] Add strict canonical schema validation and normalization for repository-authored PM-Bench and TriggerBench development fixtures, including fixed UTC clocks, stable sorted IDs, required PBPP nonpublishable/nonheadline/noncomparable flags, explicit operating-point configuration, and fail-closed official/upstream custody.
- [x] Execute only through a supplied public CLI seam using command invocations, isolated tenant/session stores, fixture clocks/events, query-then-act flow, and opaque action IDs; never execute payload text or pass expected/gold fields into CLI inputs.
- [x] Produce deterministic traces and exact recomputable TP/FP/FN set metrics, safety/error counts, required PM category rows, required TriggerBench dimension-by-variant rows, and hard-gate failures for missing categories, wrong-time/invalid actions, leakage, side effects, gold access, payload execution, silent operating points, or rerun digest mismatch.
- [x] Add deterministic repository-authored fixtures in tests covering PM trigger types, monitoring classes, update classes, cross-day/boundary/dependency/lure/duplicate behavior and TriggerBench dimensions/available variants, overload preservation, risk/logical negatives, and matched RM control.
- [x] Add focused tests for schema rejection, canonical normalization/digest stability, CLI-only execution, gold exclusion, no payload side effects, trace-to-metric recomputation, category completeness, unavailable official TriggerBench custody, and byte-identical reruns.

Validation:
- python -m pytest -q tests/test_public_pm_bench_triggerbench.py
- ruff check --quiet eval/public/adapters/pm_bench_triggerbench.py tests/test_public_pm_bench_triggerbench.py
- git diff --check
