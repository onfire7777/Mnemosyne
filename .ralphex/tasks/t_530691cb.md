# PM-Bench and TriggerBench deterministic action adapters

Allowed files:
- eval/public/adapters/pm_bench_triggerbench.py
- tests/test_public_pm_bench_triggerbench.py

Forbidden files:
- eval/public/runner.py
- eval/public/registry.json
- eval/public/scoring.py
- eval/public/bundle.py
- eval/harness/cli_driver.py
- all planning state and documentation
- every file not listed in Allowed files

### Task 1: Implement normalized deterministic action probes
- [x] Initial adapter and focused tests implemented.

### Task 2: Correct the adapter to the authoritative benchmark contract
- [x] Replace the invented abbreviated schema with the required canonical top-level fields: integer schema_version, benchmark, source_protocol, split_role=development, seed, clock, cases, PBPP flags publishable=false, headline_eligible=false, independent_reproduction=false, upstream_comparable=false, and explicit operating_point_id/config. Validate exact repository-authored custody; reject official/upstream TriggerBench and unlicensed PM-Bench assets.
- [x] Validate canonical cases with case_id, category, variant, tenant_id, session_id, operating_point_id, steps; tasks/intentions with task_id, label, action_id, all five trigger types (exact_time, time_window, event, condition, dependency_completion), canonical trigger payload, introduced_at, optional expires_at, regularity, temporal_scope, monitoring_class, update_class, and dependency IDs; steps with step_id, now, narrative/event/channel observations, cancel/override/reschedule updates, opaque available actions/lures, expected_due_action_ids, and expected query channels.
- [x] For TriggerBench cases require dimension from state-tracking, temporal-grounding, logical-adherence, attention-recovery, safe-coding; variant from positive_clean, positive_overloaded, negative_clean, rm_control; normalized constraint_id, trigger_id, expected_intervene, expected_action_id. Ensure gold is absent from every CLI input.
- [x] Execute task formation and updates, fixed clock/event injection, due-intention query, and opaque action selection solely through cli.run(command, *args) or an equivalent supplied public subprocess seam. Use fresh tenant/session isolation per case. Never execute action payload text or use fixture gold to choose actions. Ensure deterministic ordering and duplicate suppression.
- [x] Score per-step TP/FP/FN, micro precision/recall/set-F1, macro case set-F1, query count, and explicit miss/early/late/lure/duplicate/cancelled_action/stale_preupdate_action/dependency_violation counts. Produce required PM rows for all five trigger types, event/time, regularity, temporal scope, monitoring classes, update classes, and UTC boundary classes. Produce all available TriggerBench dimension-by-variant rows and clean recall, overload drop, negative specificity/false-alarm, and RM accuracy summaries.
- [x] Hard fail independently of F1 on wrong-time, cancelled/stale/dependency-invalid execution, tenant/session leakage, fixture-gold access, payload execution, digest mismatch, missing category row, silent operating point, or reported side effects.
- [x] Replace shallow fixtures/tests with deterministic repository-authored fixtures covering all PM trigger types; no-due, lure/early, cross-day, cancel, override, reschedule, dependency blocked/released, hidden channel queried/not queried, duplicate suppression, exact-time boundary; and every TriggerBench dimension/available variant, prefix-preserved overload, risk/logical negatives, and matched RM control. Test schema rejection, normalization/digest stability, CLI-only seam, trace recomputation, category completeness, fail-closed custody, no gold/payload side effects, isolation, and byte-identical reruns.

Validation:
- python -m pytest -q tests/test_public_pm_bench_triggerbench.py
- ruff check --quiet eval/public/adapters/pm_bench_triggerbench.py tests/test_public_pm_bench_triggerbench.py
- git diff --check
