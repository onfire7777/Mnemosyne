# W3 P4 D — working-memory action-oriented probe

Authoritative requirement: `docs/superpowers/plans/2026-07-15-W3-taxonomy-completion-plan.md`, Phase 4, plus the task's authoritative P4 design handoff. The finalized Phase-3 public seam is `working-seed`, `working-query`, and `working-expire` at parent head `b0a20bbd6ed48c408465a595698bf011440bb93f`; this branch intentionally does not own those upstream files.

Allowed files:
- `eval/public/adapters/working_memory_action_probe.py`
- `tests/test_public_working_memory_action_probe.py`

Forbidden files:
- Every other repository file, including registry, runner, scoring, bundle, CLI, source engines, planning state, docs, lockfiles, and `.ralphex` runtime metadata.

Constraints:
- Deterministic development fixture only; no protected/headline/external measurement.
- Normalize and strictly validate the frozen schema, fixed RFC3339 UTC times, stable IDs/order, explicit operating point, six required item categories, opaque action IDs, and scoring-side gold custody.
- Exercise working-memory state only through a public CLI-compatible adapter seam. Never call private engine methods. Never execute action payloads or untrusted tool-result content.
- The deterministic policy must choose an opaque action ID or abstain from visible working context without receiving expected labels.
- Emit canonical per-case traces and deterministic metrics with exact decision accuracy, positive precision/recall/F1, abstention specificity/false-action rate, seeded bootstrap macro accuracy, required category/status/isolation rows, and hard-gate violation counts.
- Cover supersession, resolution, exact TTL boundary, session/tenant isolation, irrelevant distractors, inert imperative content, repeated-run byte identity/idempotency, no automatic durable-plane promotion, no fixture-gold access, and explicit operating-point custody.
- Do not wire the registry/runner/scoring/bundle; serialized W3 P4 I0 owns integration.

### Task 1: Implement and test the bounded deterministic action probe
- [x] Add the adapter module and focused tests in the exact allowed files.
- [x] Verify schema rejection, all required categories and adversarial rails, canonical reruns, score recomputation, and public-seam-only interactions.

Validation:
- `python -m pytest -q tests/test_public_working_memory_action_probe.py`
- `ruff check eval/public/adapters/working_memory_action_probe.py tests/test_public_working_memory_action_probe.py`
- `git diff --check`
