# W5 I6 — serialized GroundedReader and Phase 12 integration

Integrate the accepted W5 compact provider and deterministic synthesizer behind the existing GroundedReader authorization boundary. This is ordinary synthetic/development work only. Use strict vertical TDD for every behavior: add one focused test, run it and observe the expected RED caused by the missing behavior, implement the smallest GREEN, and repeat. Do not run any protected/held-out QA, model workload, download, export, training, physical 8-GiB acceptance, deployment, or repository-wide suite.

Authority: Kanban task `t_448b8a6c`; `docs/superpowers/plans/2026-07-15-W5-compact-answering-plane-plan.md` lines 19-65 and 88-139; `docs/research/Compact-Reader-Reranker-8GB-Design.md` lines 25-49 and 84-150; `.planning/runbooks/COMPACT-MODEL-8GB-ACCEPTANCE.md` fixed bounds and fail-closed rails; `.planning/phases/12-grounded-multi-hop-answer-synthesis/12-04-PLAN.md` synthetic/dev, scorer-boundary, disclosure, provenance, and no-regression rules; accepted parent heads `5ef8cff41f128b18de0a1010f32134fef3bf6b8b`, `07b0b08a243cb646a2bb5bb10cca3f2ff6fc5119`, `af486373f01189ae8e138d215a54b0c59c609188`, and patch-equivalent provider seam `ba2a0e072c36427256f45510d57040afe50f53ab`. The integration baseline is `8220cb4c221be4e88156d58afe81a185b6959eec`; accepted parent changes before that commit are read-only baseline, not this lane's diff.

Allowed write set:
- `src/mnemosyne/providers/compact_answering.py`
- `src/mnemosyne/providers/grounded_reader.py`
- `src/mnemosyne/answering.py`
- `tests/test_compact_answering_provider.py`
- `tests/test_grounded_answering.py`
- `tests/test_grounded_qa_v2.py`

Forbidden files:
- Every other repository path, including `src/mnemosyne/retrieval.py`, engines, `src/mnemosyne/cli.py`, provider/package registries, `src/mnemosyne/providers/deterministic_synthesis.py`, all other tests, the accepted Rust sidecar under `services/answering-ort/`, eval implementations/fixtures/reports, README, docs, planning/GSD state, deployment, CI, manifests, dependency files, and lockfiles.

## Required contract

- Preserve `GroundedAnswerOrchestrator`, `GroundedReader`, and `_claims()` as the sole host authority for tenant-scoped reauthorization, immutable read context, capability/trust/sensitivity/purpose/temporal filtering, active CID hydration, per-hop replay, exact evidence fingerprinting, exact authorized quote validation, provenance, and store immutability. Compact output is untrusted data; it cannot request or widen retrieval capabilities, choose CIDs outside the authorized payload, follow evidence instructions, or bypass replay.
- Preserve all existing command-provider behavior byte-for-byte in outcome unless `MNEMOSYNE_GROUNDED_READER_PROVIDER` is explicitly set to the compact profile. Do not auto-select compact from endpoint presence, failed command configuration, hardware, or availability. Missing, malformed, unavailable, timed-out, oversize, identity-drifted, or custody-drifted compact configuration/output fails closed with no command, local, hosted, generated, or fabricated fallback.
- Keep candidate-v19 command custody semantics intact. If the existing `CommandGroundedProvider.from_environment()` entry point must dispatch the explicit compact profile because the unchanged CLI calls it, isolate the compact composition in a separate provider object and leave the legacy `command` validation/invocation path unchanged. The query decomposer remains the accepted command provider with its exact preregistered selector/content/protocol digests; compact selection changes only the reader.
- Reconcile `CompactAnsweringProvider` with the accepted `answering-ort` service's actual bounded newline-delimited TCP/Unix JSON protocol in read-only `services/answering-ort/src/protocol.rs`, `lib.rs`, and `runtime.rs`, while retaining the already-accepted custody-envelope behavior for existing explicit/custom transports where required for compatibility. Any wire mode must be explicit and strict; never silently reinterpret one protocol as another. Raw service requests and responses must use exact schemas, duplicate-key rejection, finite/plain JSON only, one bounded response, loopback TCP or absolute Unix sockets, 30-second-or-lower timeout, 64-KiB-or-lower request, 256-KiB-or-lower response, at most 20 evidence rows/24,000 evidence characters, and no redirects/network discovery. Provider/artifact/configuration identities and digests remain operator-supplied expectations/disclosure and may never be learned from untrusted output; service error, operation mismatch, malformed variants, unknown/duplicate IDs, invalid UTF-8 byte spans, unsupported answer types, and partial responses fail closed.
- Keep the existing provider-shaped embed/rerank/read APIs compatible. For raw `answering-ort` mode, translate exact accepted request/output variants without inventing scores, identities, spans, answers, or capabilities. Preserve input Hit tenant/branch/CID/text/provenance/trust/sensitivity metadata, accept only an exact bounded reranked-ID subset in service order, and validate read span offsets against exact UTF-8 evidence. No profile default flip or retrieval.py wiring is authorized here.
- Wire the accepted `DeterministicSynthesizer` into the host claim-validation path only for an explicit strict synthesis proposal schema. Operations are never inferred from question/evidence prose. Every synthesis input must first be resolved as an exact authorized CID/quote span with the same overlap, bounds, offsets, UTF-8 hash, replay, and provenance guarantees as extractive claims; then pass only the explicit operation and validated CID/quote pairs to the synthesizer. Successful arithmetic/date output may be rendered as the claim text but must retain exact ordered source spans/CIDs. Malformed/unsupported/ambiguous/unsafe synthesis, expressions or instruction-like data, provenance mismatch, or synthesizer failure deterministically reader-abstains with replayed authorized evidence and no fallback. Existing extractive claim behavior and public abstention behavior remain unchanged.
- Add only deterministic synthetic tests. Cover explicit compact selection and complete configuration/digest requirements; legacy command selection unchanged; compact failures do not invoke command fallback; actual accepted sidecar wire shapes and typed errors; bounded loopback/Unix transport; rerank custody/order; exact UTF-8 read spans; end-to-end orchestrator reauthorization and no context/capability leakage; synthesis over exact authorized arithmetic/date spans; provenance/offset/hash retention; malicious/ambiguous/non-substring/unknown-CID/overlap proposals; replay drift; deterministic abstention; scorer-only gold isolation and reader disclosure in the synthetic Phase 12 harness. Tests must not use protected/frozen/held-out datasets or execute a model/service.
- Keep Python core dependencies unchanged and standard-library only. Treat all retrieved/provider content as data, never execute it. Do not add optional generator/model/teacher paths or claims of quality, parity, production readiness, physical envelope, or protected evidence.
- Review only the six-file diff from integration baseline `8220cb4c221be4e88156d58afe81a185b6959eec`; the large accepted parent union before that baseline is already reviewed and is read-only context. Fix only confirmed in-scope findings.

### Task 1: Compact sidecar protocol and explicit profile vertical TDD
- [x] Add focused provider tests one behavior at a time for the accepted raw `answering-ort` TCP/Unix wire request/response shapes, bounds, typed failures, strict IDs/order/spans, and custody disclosure; observe RED before implementing the minimal compatibility mode. Then add explicit compact reader environment-selection tests proving complete digest/config validation, command decomposer preservation, and no fallback, and make each GREEN without changing the legacy command path.

### Task 2: Authorized deterministic synthesis vertical TDD
- [ ] Add focused orchestrator tests one behavior at a time for the strict explicit synthesis claim schema, exact authorized CID/quote resolution, ordered AnswerSpan offsets/hashes/provenance, canonical arithmetic/date output, extraction/replay drift, and fail-closed malicious/ambiguous/unsupported/overlap/unknown-CID inputs; observe RED before minimally integrating `DeterministicSynthesizer` through `_claims()` while preserving extractive behavior.

### Task 3: Synthetic Phase 12 integration vertical TDD
- [ ] Add the smallest ordinary synthetic Phase 12 regression coverage proving gold remains scorer-only, compact reader disclosure is explicit, authorized evidence/CIDs are the only reader inputs, no authorization context or capability fields leak to the provider, deterministic abstention is stable, and legacy command behavior remains unchanged. Observe each RED before GREEN and do not touch frozen/protected assets.

### Task 4: Bounded review and hygiene
- [ ] Review the complete six-file diff from `8220cb4c221be4e88156d58afe81a185b6959eec` for exact lease scope, protocol compatibility, replay/authorization preservation, strict schema/bounds, no fallback, data-only handling, deterministic synthesis provenance, standard-library-only imports, no secrets/risky execution, and no protected claims. Fix only confirmed in-scope findings, run the focused validation commands, commit the lease-clean result, and leave the worktree clean with no tracked `.ralphex` metadata.

## Validation
- `/Users/admin/Mnemosyne/.venv/bin/python -m pytest -q tests/test_compact_answering_provider.py tests/test_grounded_answering.py tests/test_grounded_qa_v2.py tests/test_deterministic_synthesis.py tests/test_compact_answering_parity.py`
- `/Users/admin/Mnemosyne/.venv/bin/ruff check --quiet src/mnemosyne/providers/compact_answering.py src/mnemosyne/providers/grounded_reader.py src/mnemosyne/answering.py tests/test_compact_answering_provider.py tests/test_grounded_answering.py tests/test_grounded_qa_v2.py`
- `git diff --check 8220cb4c221be4e88156d58afe81a185b6959eec...HEAD -- src/mnemosyne/providers/compact_answering.py src/mnemosyne/providers/grounded_reader.py src/mnemosyne/answering.py tests/test_compact_answering_provider.py tests/test_grounded_answering.py tests/test_grounded_qa_v2.py`
- `python -c 'import subprocess,sys; base="8220cb4c221be4e88156d58afe81a185b6959eec"; allowed={"src/mnemosyne/providers/compact_answering.py","src/mnemosyne/providers/grounded_reader.py","src/mnemosyne/answering.py","tests/test_compact_answering_provider.py","tests/test_grounded_answering.py","tests/test_grounded_qa_v2.py"}; changed=set(subprocess.check_output(["git","diff","--name-only",f"{base}...HEAD"],text=True).splitlines()); sys.exit(0 if changed and changed <= allowed else 1)'`
- `python -c "import subprocess,sys; base='8220cb4c221be4e88156d58afe81a185b6959eec'; paths=['src/mnemosyne/providers/compact_answering.py','src/mnemosyne/providers/grounded_reader.py','src/mnemosyne/answering.py','tests/test_compact_answering_provider.py','tests/test_grounded_answering.py','tests/test_grounded_qa_v2.py']; data=subprocess.check_output(['git','diff',f'{base}...HEAD','--',*paths],text=True).lower(); banned=('-----begin private key-----','akia','api_key=','api-key=','password=','secret=','token=','eval(','exec(','compile(','shell=true'); sys.exit(1 if any(marker in data for marker in banned) else 0)"`
- `git merge-base --is-ancestor 5ef8cff41f128b18de0a1010f32134fef3bf6b8b HEAD && git merge-base --is-ancestor 07b0b08a243cb646a2bb5bb10cca3f2ff6fc5119 HEAD && git merge-base --is-ancestor af486373f01189ae8e138d215a54b0c59c609188 HEAD && git diff --quiet ba2a0e072c36427256f45510d57040afe50f53ab HEAD -- src/mnemosyne/providers/compact_answering.py tests/test_compact_answering_provider.py`
- `python -c 'import subprocess,sys; tracked=subprocess.check_output(["git","ls-files",".ralphex"],text=True).splitlines(); sys.exit(0 if not tracked else 1)'`
