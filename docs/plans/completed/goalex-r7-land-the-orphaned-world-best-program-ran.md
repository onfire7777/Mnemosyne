# Plan: Land the orphaned world-best-program range (two PRs), then implement W1 Fix A + Fix B (graph resurrection) with TDD

## Overview

You are the executor for round 7 of the Mnemosyne v2.0 goal loop. Repo: `/Users/admin/Mnemosyne`, branch `main`. Authoritative docs: `docs/superpowers/plans/2026-07-15-W1-graph-substrate-fix-plan.md` (acceptance contract for the substantive slice), master spec `docs/superpowers/specs/2026-07-15-world-best-memory-platform-design.md`, trackers under `.planning/`. NOTE: the spec and W1–W5 plan docs currently exist ONLY in the unpushed local commits you land in Task 1.

**Verified current state (round-7 planning, read-only):**
- Working tree clean. Local `main` = `ac28f00b2213d1a5559d47aafe0f27e6cd408225`; `origin/main` = `4d691e80c17dc0df0618e309f28b45c854028248`; local is ahead 7 / behind 0; `gh pr list --state open` is empty.
- The 7 orphaned commits split into two logical tasks: **(docs)** `2004865` master design spec, `9c504c9` spec→GSD roadmap mapping, `68d92d9` W3 plan, `dbad101` W2/W4/W5 plans, `7bc5e13` W1 plan + operator-owned dev-host runtime ADR (7 files, +1411 lines, docs only); **(code)** `833af4f` + `ac28f00` post-PR-#23 review fixes touching `eval/datasets/v2/README.md`, `eval/datasets/v2/run_graph_ppr_baseline.py`, `eval/reports/phase-12-graph-baseline-2026-07-15.md`, `tests/test_phase12_graph_baseline.py`, and a round-6 plan-doc correction.
- **Round-5 review finding adjudication: RESOLVED/superseded — dismiss with evidence, make no R2b changes.** Commits `7b06de5`/`2520c14` landed via PR #20 (merged as `51fa898`) and tracker reconciliation via PR #21 (merged as `69cd2cf`); both verified MERGED and ancestors of `origin/main`. Record this disposition in the trackers; do not re-land anything from that range.
- `origin/main` already contains the W1 Phase-1 RED dead-graph baseline (PR #23, merged `4d691e8`).
- Stale local-only branch `goalex-r6-land-orphaned-r2b-review-fixes-via-pr-re` sits at `ac12da9`; its receipt content is subsumed into `main`'s `.planning/STATE.md` "Round-6 execution precondition receipt" section (STATE.md:301). Branch `codex/r2b-capture-rotator-lock@0826154` is intentionally preserved — leave it alone.
- **The Colima production VM is INTENTIONALLY STOPPED (operator decision).** Do NOT start the stack, run service-health/20-container/live/protected/production-evidence admission, touch Vault, run a model, do CBM-reindex/gbrain-sync/Graphify refresh, or run the full suite. Record such items as explicitly operator-gated, never as failures. All work here is deterministic, local-engine, no-model → **≥35% targeted-test tier**: before each pytest run require ≥35% free memory, load1 ≤ 10, no resident model, serialized execution.

**The W1 keystone (root cause verified in-repo):** the relation graph is empty at query time because **(Cause A)** the eval ingests via `capture_batch` (`eval/harness/cli_driver.py:186`, used by `eval/public/adapters/hipporag_multihop.py` and `eval/datasets/v2/run_grounded_qa_v2.py:78`) which only appends evidence and never consolidates — relations are created only by the consolidation belief-reviser (`src/mnemosyne/consolidation.py:1324` path via `_extract_simple_fact`), reachable via `consolidate-once` (`cmd_consolidate_once`, `src/mnemosyne/cli.py:8856`; parser at `cli.py:20382`); and **(Cause B)** the deterministic extractor `_extract_simple_fact` (`src/mnemosyne/consolidation.py:1742`) matches only a whole-first-sentence copular clause `SUBJECT is/are/was/were OBJECT`, so it emits nothing from `"{title}\n{content}"` prose or the dev fixture (`Mara owns Helios`, `Helios ships in Q3 2026`).

**Conventions (mandatory):** TDD (RED before GREEN); Ponytail (pure reuse of `cmd_consolidate_once`/existing job machinery — no new dependency, pure Python, deterministic); small conventional commits pushed incrementally; one task per PR with evidence-bearing bodies; merge only on green exact-head CI on the pushed SHA (runbook as PRs #20–#23: push branch, `gh pr create`, wait for checks on the exact head, `gh pr merge --merge`, verify merged SHA locally); NEVER reset/rebase/force-push shared history or push directly to `main`; sweep every full diff for secrets/risky/large/generated files before staging; never weaken a §31 rail or §33 test class; never tune on MuSiQue/2Wiki/HotpotQA or any held-out data (dev/synthetic fixtures only); no protected `qa_hard_v2` attempt; no publication.

## Validation Commands
- `set -e; cd "$(git rev-parse --show-toplevel)"; git diff --check; .venv/bin/ruff check --quiet .; PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_planning_traceability.py tests/test_config_drift.py`
- `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_phase12_graph_baseline.py tests/test_public_hipporag.py tests/test_grounded_qa_v2.py tests/test_shared_engine_contract.py tests/test_extractive_decomposer.py`
- `git merge-base --is-ancestor 7bc5e13 origin/main && git merge-base --is-ancestor ac28f00 origin/main` (after Task 1)
- `git fetch origin && git rev-list --left-right --count origin/main...main` (must be `0 0` at end of round)
- `gh pr list --state open` (must be empty at end of round)

### Task 1: Land the orphaned 7-commit range via two sequential merge-commit PRs (no history rewrite)
- [ ] Re-verify preconditions: clean tree; `git rev-parse main origin/main` = `ac28f00…`/`4d691e8…`; `git log --oneline origin/main..main` shows exactly the 7 commits; no open PRs. If anything differs, stop and record the mismatch in `.planning/STATE.md` instead of improvising.
- [ ] Adjudicate the round-5 review finding: confirm PRs #20/#21 are merged (`51fa898`, `69cd2cf`) and ancestors of `origin/main`, then record it as resolved/superseded — no R2b code changes.
- [ ] Inspect the full range diff (`git diff origin/main..main`) and run the secret/risky-file/large-file/generated-artifact sweep over it.
- [ ] Without moving `main`, create `codex/world-best-program-plans` at `7bc5e13` and `codex/phase12-graph-baseline-review-fixes` at `ac28f00`. If either branch already exists, reuse it only after proving the exact expected tip.
- [ ] PR 1 (docs): prove `origin/main..codex/world-best-program-plans` contains only the five docs commits touching only the spec, W1–W5 plans, and runtime ADR; run the hygiene gate; push; open a PR whose body names the governing spec, the W1–W5 dependency order, future CAP-012/CAP-013 ledger work, and states the runtime ADR is recorded as operator-owned, not actioned. Merge with a merge commit only on green exact-head CI; fetch and prove `7bc5e13` is an ancestor of `origin/main`. Do not sync local `main` yet.
- [ ] PR 2 (code): after PR 1 merges, prove the second branch's effective diff vs updated `origin/main` is limited to the graph-baseline files listed in the Overview; verify the `833af4f`/`ac28f00` custody labels (engine-under-test base vs runner/evidence commit) precisely describe what ran — if any label overclaims, fix it on this branch (regression test first); run the targeted graph-baseline tests + hygiene at the ≥35% tier; push; open a PR whose body carries the RED dead-graph baseline, custody boundary, and an explicit local-engine-only / no-production-evidence disclaimer. Merge with a merge commit on green exact-head CI.
- [ ] Fetch; prove `833af4f` and `ac28f00` are ancestors of `origin/main`; fast-forward local `main` with `git pull --ff-only` (possible because `ac28f00` is an ancestor of the final merge commit) — stop and record rather than reset if fast-forward is impossible. Delete the two merged local+remote branches after ancestry checks.
- [ ] Stale-branch hygiene: confirm the `ac12da9` receipt content is subsumed in merged `main`'s STATE.md (Round-6 receipt section, line ~301); if subsumed, delete local branch `goalex-r6-land-orphaned-r2b-review-fixes-via-pr-re`; if not, keep it and record why. Leave `codex/r2b-capture-rotator-lock` untouched.

### Task 2: W1 Fix A — consolidate the corpus before search (TDD, new branch from merged main)
- [ ] Create `codex/w1-graph-fix-a-consolidate` from the new `origin/main`. RED first: add a test (extend `tests/test_phase12_graph_baseline.py` or new `tests/test_phase12_graph_fix.py`) that captures a small synthetic corpus of currently-parseable copular sentences (e.g. `Helios is a project led by Mara.` / `Mara is the owner of Helios.`) through the real local-engine capture path with a new consolidate-after-capture option, then asserts `len(engine.relations) > 0` and a positive `graph_ppr` channel hit on a bridge query. Run it; it must FAIL (no such option exists).
- [ ] GREEN by pure reuse: add a `--consolidate` path on the eval driver's `capture_batch` (`eval/harness/cli_driver.py:186`) that drives the existing `consolidate-once` CLI (`cmd_consolidate_once`, `src/mnemosyne/cli.py:8856`) against the staged store, or an equivalent drain of the existing consolidation job. No new dependency, no engine rewrite.
- [ ] Wire it into the eval entry points that ingest before search: `eval/public/adapters/hipporag_multihop.py` (both capture sites) and `eval/datasets/v2/run_grounded_qa_v2.py:78`. Keep `eval/datasets/v2/run_graph_ppr_baseline.py` WITHOUT consolidation — it is the preserved dead-graph baseline record and its existing `relations == 0` test must keep passing unchanged.
- [ ] Verify the pre-extractor gates admit these captures (`consolidation.py:409` prediction-error gate promotes; `:456` no-write-data false for the eval's source/actor/trust settings) — assert in the test or record evidence in the test's docstring.

### Task 3: W1 Fix B — deterministic extractor emits edges from prose (TDD, same or stacked branch)
- [ ] RED: add extractor unit tests that currently FAIL: `Mara owns Helios.` and `Helios ships in Q3 2026.` each yield an edge; a `"{title}\n{content}"` payload yields edges from content sentences beyond the first; multi-sentence prose yields co-occurrence edges with the connecting verb/preposition as predicate (fallback `related_to`).
- [ ] GREEN: broaden `_extract_simple_fact` (`src/mnemosyne/consolidation.py:1742`) — iterate all sentences, strip the title prefix, emit edges between co-occurring salient entity spans, predicate = connecting verb/preposition else `related_to`. Stay pure-Python/deterministic: no dict/set-iteration-order or hash-seed dependence; sort candidates explicitly.
- [ ] GREEN the end-to-end W1 dev acceptance on the real fixture (`eval/datasets/v2/qa_scale_dev_v1.json` + the 16-case matrix named in the W1 plan doc): after capture+consolidation, `engine.relations > 0`, positive `graph_ppr` hit on the bridge query, Recall@5 0 → 1 on the bridge case, and byte-identical `traces.jsonl` across two repeat runs (hash comparison in the test). This is the post-fix regression cell required alongside the preserved pre-fix baseline cell.
- [ ] Confirm no regression at the ≥35% tier: dense/lexical single-hop dev cases unchanged under RRF; run the second Validation Command block plus the nearest consolidation test files.

### Task 4: Land the Fix A+B slice and reconcile trackers
- [ ] Update `eval/reports/phase-12-graph-baseline-2026-07-15.md` (or a sibling `phase-12-graph-fix` note) with measured post-fix dev-scale results, explicitly labeled **local-engine development iteration — not production evidence**; production-Postgres parity (`postgres-recursive-ppr`) and the eval-report disclosure audit remain OPEN W1 Phase-4 items — record them, do not attempt while the VM is down.
- [ ] Take a fresh admission sample: rerun `MNEMO_SECRETS_DIR=/Users/admin/mnemosyne-prod-secrets infra/validate/validate-production-mcp-client-tls.sh /Users/admin/mnemosyne-prod-secrets/stepca-acme-root.crt /Users/admin/mnemosyne-prod-secrets/mcp-client.crt /Users/admin/mnemosyne-prod-secrets/mcp-client.key` without starting the VM; record pass or the literal failure — never rotate certificates or weaken validation (a lapsed 24h leaf is the R3/R4 gap, not a new incident).
- [ ] Reconcile `.planning/STATE.md`: all PR numbers/merge SHAs/exact-head CI run IDs from this round; the resolved round-5 finding disposition; W1 plan Phase-1 marked complete (citing PR #23 + the merged review fixes) and Phases 2–3 status per this slice; explicit operator-gated open items (VM restart + Vault unseal for production parity and the single protected attempt, runtime-readiness repair, R3/R4 rotator-fix slice with the saved custody patch, CBM/gbrain/Graphify refresh). Touch `ROADMAP.md`/`REQUIREMENTS.md` only where status text is now stale — CAP-003/BENCH-005 stay partial (dev-scale proof does not close them).
- [ ] Run the full hygiene gate, sweep the diff, commit conventionally, push, open the PR with evidence body, merge only on green exact-head CI, fast-forward local `main`, delete the merged branch, and verify terminal invariants: `git rev-list --left-right --count origin/main...main` = `0 0`, `gh pr list --state open` empty, all Validation Commands exit 0.
