# Plan: Reconcile the externally merged PR #24, then resurrect the W1 graph substrate (Fix A + Fix B, TDD) and correct disclosure

## Overview

You are the executor for round 8 of the Mnemosyne v2.0 goal loop. Repo: `/Users/admin/Mnemosyne`. This advances Phase 12 / Plan 12-04 and CAP-001/CAP-002/CAP-003/BENCH-005 under `docs/superpowers/plans/2026-07-15-W1-graph-substrate-fix-plan.md`, governed by `docs/superpowers/specs/2026-07-15-world-best-memory-platform-design.md` (both now merged in `origin/main`). Trackers under `.planning/` are the source of truth.

**Standing rule (why round 7 failed — do not repeat):** the goalex harness commits a `goalex: round N plan` file onto the checkout each round, so NEVER gate any step on `main`'s tip SHA or an ahead-count. Pin every check to immutable content SHAs and ancestry (`git merge-base --is-ancestor`). A round-8 plan commit appearing at any time is expected bookkeeping, not an anomaly.

**Verified state at planning time (round 8, read-only):**
- `origin/main` = `fbc1857`, the merge commit of **PR #24** (`deliver/world-best-program-2026-07-15`, head `5fbd4a2`), which landed the entire world-best range: spec `2004865`, mapping `9c504c9`, W3 plan `68d92d9`, W2/W4/W5 plans `dbad101`, W1 plan + dev-host ADR `7bc5e13`, review fixes `833af4f`/`ac28f00`, and round-7 plan `5fbd4a2`. Do NOT re-land any of it.
- **PR #24 merged before all exact-head checks completed** (merged `05:35:18Z`; Postgres integration finished `05:35:19Z`, Provider conformance `05:35:23Z`, Unit + drift `05:46:23Z`). All checks ultimately succeeded and PR-head run `29474245544` on `5fbd4a2` is green, but the post-merge run `29474289329` on `fbc1857` was still `in_progress`. Record this merge-before-green delivery breach literally in the trackers; later green does not retroactively make it compliant.
- Local `main` = `5fbd4a2`, behind `origin/main` by exactly the merge commit — fast-forwardable. Working tree clean on branch `goalex-r7-land-the-orphaned-world-best-program-ran` at `3f39fed` (round-7 receipt, a 14-line `.planning/STATE.md` addition, NOT in `origin/main`). No open PRs.
- **Round-5 review finding: RESOLVED — disposition, change nothing in R2b.** `7b06de5`/`2520c14` landed via PR #20 (merge `51fa898`) and PR #21 (merge `69cd2cf`); all four verified ancestors of `origin/main`.
- Branches: local `deliver/world-best-program-2026-07-15` is merged (cleanup candidate); `goalex-r6-land-orphaned-r2b-review-fixes-via-pr-re` (`ac12da9`) and `goalex-r1-r2b-integrate-the-shared-runtime-exclusi` (`e9651c3`) are stale receipt/plan branches superseded by merged PRs #15–#23; `codex/r2b-capture-rotator-lock@0826154` (upstream gone) is intentionally preserved — leave it alone.
- **The Colima production VM is INTENTIONALLY STOPPED (operator decision).** Do NOT start the stack, run service-health/20-container/live/protected/production-evidence admission, touch Vault, load any model, run the full suite, or claim production evidence. Record such items as explicitly operator-gated, never as failures. All work here is deterministic, local-engine, no-model → **≥35% targeted-test tier**: before each pytest run take a fresh sample requiring ≥35% free memory, load1 ≤ 10, no resident model; run serialized.

**The W1 keystone (historical root cause at baseline `fbc1857`):** the relation graph was empty at query time because **(Cause A)** the eval ingested via `MnemoCLI.capture_batch` (used by `eval/public/adapters/hipporag_multihop.py` and `eval/datasets/v2/run_grounded_qa_v2.py`), which only appended evidence and never consolidated — relations were created only by `ConsolidationWorker.run_job` through `engine.add_relation`, reachable via `cmd_consolidate_once`; and **(Cause B)** the deterministic extractor `_extract_simple_fact` matched only a whole-first-sentence copular clause `SUBJECT is/are/was/were OBJECT`, so it emitted nothing from `"{title}\n{content}"` prose or the dev fixture (`Mara owns Helios`, `Helios ships in Q3 2026`).
**Committed baseline (`eval/reports/phase-12-graph-baseline-2026-07-15.md`, tested by `tests/test_phase12_graph_baseline.py`):** persisted relations 0, graph-specific bridge participation 0, direct-query Recall@5 proxy **0.5**. Post-fix targets: relations > 0, at least one positive `graph_ppr` contribution on the bridge query, direct-query Recall@5 proxy **1.0**, both bridge documents retrieved, byte-identical traces across repeat runs. Do not touch MuSiQue/2Wiki/HotpotQA or any held-out data (no test tuning); no protected `qa_hard_v2` attempt; no publication.

**Conventions (mandatory):** TDD (RED before GREEN); Ponytail (pure reuse of the existing consolidation machinery — no new dependency, pure Python, deterministic); systematic debugging; small conventional commits pushed incrementally; one task per PR with evidence-bearing bodies; **merge only after ALL exact-head checks on the pushed SHA have completed green — wait for completion, do not repeat PR #24's early merge**; verify the post-merge run on the merge SHA too; never rewrite shared history, force-push, or push directly to `main`; sweep every full diff for secrets/risky/large/generated files before staging; never weaken a §31 rail or §33 test class; preserve tenant/provenance/data-only boundaries on every new write path; update docs touched by each slice in the same PR.

## Validation Commands
- `git fetch --prune origin && git rev-parse HEAD main origin/main && git merge-base --is-ancestor 5fbd4a2 origin/main && git merge-base --is-ancestor 7bc5e13 origin/main`
- `gh pr view 24 --json state,headRefOid,mergeCommit,mergedAt && gh run view 29474245544 --json status,conclusion,headSha && gh run view 29474289329 --json status,conclusion,headSha`
- `set -e; cd "$(git rev-parse --show-toplevel)"; git diff --check; .venv/bin/ruff check --quiet .; PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_planning_traceability.py tests/test_config_drift.py`
- `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_phase12_graph_baseline.py tests/test_phase12_graph_fix.py tests/test_public_eval_cli.py tests/test_public_hipporag.py tests/test_grounded_qa_v2.py tests/test_shared_engine_contract.py tests/test_extractive_decomposer.py tests/test_public_requirement_truth.py tests/test_cli_runtime_tools.py::test_cli_projection_recompute_tracks_affected_projection_set`
- `cd "$(git rev-parse --show-toplevel)" && PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m eval.datasets.v2.run_graph_ppr_postfix --output /tmp/mnemo-fix-b-postfix`
- `git rev-list --left-right --count origin/main...main; gh pr list --state open` (must end `0 0` and empty)

### Task 1: Reconcile the externally merged range, land the orphaned receipt, establish the W1 base
- [x] `git fetch --prune origin`. Verify by content, not tip position: PR #24 state MERGED with head `5fbd4a2` and merge commit `fbc1857`; runs `29474245544` (PR head) and `29474289329` (post-merge, on `fbc1857`) both `completed`/`success` — the post-merge run was in progress at planning time, so check its final outcome now. If GitHub is unreachable, a run failed, or SHAs differ, stop before any W1 work and record the literal blocker in `.planning/STATE.md`.
- [x] Switch to local `main` and `git pull --ff-only` to `origin/main` (never reset/rebase/force-push; if fast-forward is impossible, stop and record). Confirm `7b06de5`, `2520c14`, `51fa898`, `69cd2cf` are ancestors of `origin/main` — that is the evidence dispositioning the round-5 review finding as resolved.
- [x] Create `codex/r8-reconcile-bookkeeping` from `origin/main`. Cherry-pick the `3f39fed` STATE.md receipt hunk (round-7 precondition-mismatch record) onto it, add the round-8 plan doc if the harness left one locally, and extend `.planning/STATE.md` with: (a) the round-5 finding disposition and evidence SHAs above; (b) PR #24's actual merge/CI chronology recorded as a merge-before-green delivery breach (merged `05:35:18Z`, last required check green `05:46:23Z`), with the corrective rule that future merges wait for all exact-head checks; (c) the round-7 tip-gating failure closed by this round's content-pinned discipline. Run the hygiene gate, sweep the diff, push, open a PR, and merge only after ALL exact-head checks complete green; verify the post-merge run and fast-forward local `main`.
- [x] Branch hygiene with ancestry proof: delete local (and remote where applicable) `deliver/world-best-program-2026-07-15` after proving `5fbd4a2` is an ancestor of `origin/main`; delete `goalex-r6-land-orphaned-r2b-review-fixes-via-pr-re` and `goalex-r1-r2b-integrate-the-shared-runtime-exclusi` only after confirming their unique content is subsumed by merged tracker text/PRs #15–#23 (otherwise keep and record why); leave `codex/r2b-capture-rotator-lock` and the round-7/8 goalex branches untouched. Make no R2b code changes; do not start the VM or inspect `/opt`.

### Task 2: W1 Fix A — opt-in consolidation before search (TDD, own PR)
- [x] Create `codex/w1-fix-a-consolidate` from updated `origin/main`. RED first (new `tests/test_phase12_graph_fix.py` or an extension of `tests/test_phase12_graph_baseline.py`), at the ≥35% tier: an opt-in consolidate-after-capture path on the eval driver's `capture_batch` that (i) consolidates every captured CID, (ii) yields `len(engine.relations) > 0` for currently-parseable copular sentences (e.g. `Mara is the owner of Helios.`), (iii) on a consolidation failure leaves the original atomic store untouched, and (iv) leaves the DEFAULT path unconsolidated so the historical dead-graph baseline stays reproducible. All must FAIL before implementation.
- [x] GREEN by pure reuse at the existing atomic `capture-batch` boundary: add a `--consolidate` CLI flag / driver parameter that drives the existing `cmd_consolidate_once` machinery or a deterministic bounded drain of the existing consolidation queue covering every captured row, completing before the staged store is published (`os.replace`). No duplicated consolidator, no new dependency, no unbounded worker.
- [x] Wire consolidation ON at both `eval/public/adapters/hipporag_multihop.py` capture sites and `eval/datasets/v2/run_grounded_qa_v2.py`; keep `eval/datasets/v2/run_graph_ppr_baseline.py` on the default unconsolidated path so its committed `relations == 0` receipt and test remain unchanged. Update fake-CLI/driver contract tests to assert the new option.
- [x] Verify the pre-extractor gates admit the eval's captures (`ConsolidationWorker._prediction_error_gate` and `_contains_no_write_data`, for the eval's source/actor/trust settings) — assert in a test or record the evidence in the test docstring. Preserve tenant/provenance/data-only boundaries.
- [x] Run the targeted suite + hygiene gate, sweep the full diff, commit conventionally, push, open a PR with the RED→GREEN evidence, merge only after all exact-head checks complete green, verify the post-merge run, fast-forward local `main`. PR #26 merged exact head `ac13abe` as `ee6e0e2` after all required checks passed; post-merge run `29479553530` completed successfully on `ee6e0e2` before local `main` was fast-forwarded.

### Task 3: W1 Fix B — deterministic extractor emits edges from prose, plus the dev-scale proof (TDD, own PR)
- [x] Branch from merged Fix A. RED first — extractor unit tests that currently FAIL: `Mara owns Helios.` and `Helios ships in Q3 2026.` each yield an edge; a `"{title}\n{content}"` payload yields edges from content sentences beyond the first; multi-sentence prose yields co-occurrence edges with the connecting verb/preposition as predicate (fallback `related_to`); candidate ordering is explicitly stable; every extracted fact reaches the persisted relation path (not merely the first tuple).
- [x] GREEN with the smallest deterministic pure-Python change to `_extract_simple_fact` and its caller: strip the title prefix only for an explicitly title-plus-body source layout, scan all sentences, emit edges between co-occurring salient entity spans, predicate = connecting verb/preposition else `related_to`; sort candidates explicitly (no dict/set-order or hash-seed dependence); preserve access policy, trust, sensitivity, and source-CID provenance on every emitted relation.
- [x] Add the post-fix regression cell BESIDE (never replacing) the committed baseline cell, on `eval/datasets/v2/qa_scale_dev_v1.json` + the 16-case matrix named in the W1 plan doc: persisted relations > 0, at least one positive `graph_ppr` contribution on the bridge query, both bridge documents retrieved, direct-query Recall@5 proxy 0.5 → 1.0, and byte-identical `traces.jsonl` across two repeat runs (hash comparison). The unconsolidated baseline runner must still report `relations == 0`. No reader model, no held-out datasets.
- [x] Prove no regression at the ≥35% tier: dense/lexical single-hop dev behavior unchanged under RRF; run the full targeted validation block plus the nearest consolidation test files.
- [x] Write a named results note (sibling of `eval/reports/phase-12-graph-baseline-2026-07-15.md`) explicitly labeled **"local-engine development iteration — non-headline, non-production evidence"**; land Fix B + tests + note through its own PR, merged only after all exact-head checks complete green; verify post-merge run. PR #28 merged exact head `94e8da5` as `28a6d24` after exact-head run `29483754981` completed successfully; post-merge run `29484670121` completed successfully on `28a6d24` before local `main` was fast-forwarded.

### Task 4: Disclosure audit, tracker reconciliation, fresh admission sample, close the slice
- [x] Audit every report under `eval/reports/` for disclosed backend/engine/model/custody vs. what actually ran (this is local doc/verifier work — it does not need the VM). Correct `phase-11-evidence.md` so the EXECUTED engine is disclosed as `local` and `postgres-recursive-ppr` is identified only as the incorrect configured/self-reported adapter value; never infer an unknown value. For digest-bound generated reports, use the existing source/reproduced bundles and the pinned report verifier/generator for metadata-only correction — do not hand-edit bound bytes, rerun held-out benchmarks, alter metrics/traces, or consume any attempt. Add a regression assertion separating executed-engine from configured-graph-backend disclosure. Receipt: all 37 pre-existing report artifacts were audited; the four public source/reproduced bundle pairs and external reports verified; retained traces/metrics matched byte-for-byte; the Phase 11 projection and SLO renderer/projections now distinguish executed engine metadata without changing any bound bytes or benchmark result.
- [x] Take a fresh admission sample without starting the VM: run `MNEMO_SECRETS_DIR=/Users/admin/mnemosyne-prod-secrets infra/validate/validate-production-mcp-client-tls.sh /Users/admin/mnemosyne-prod-secrets/stepca-acme-root.crt /Users/admin/mnemosyne-prod-secrets/mcp-client.crt /Users/admin/mnemosyne-prod-secrets/mcp-client.key`; record pass or the literal failure. Never rotate certificates or weaken validation — a lapsed 24h leaf is the known R3/R4 gap, not a new incident. Receipt: `production MCP client TLS chain verified` at `2026-07-16T09:39:28Z`; no VM, Vault, certificate, or secret mutation ran.
- [x] Reconcile `.planning/STATE.md` / `ROADMAP.md` / `REQUIREMENTS.md`: W1 plan Phase-1 complete (PR #23 + merged custody fixes), Fix A/Fix B receipts with PR numbers, merge SHAs, and exact-head CI run IDs; CAP-003/BENCH-005 and Plan 12-04 stay PARTIAL (dev-scale proof does not close them — production-Postgres parity on `postgres-recursive-ppr`, runtime readiness, grounded-reader dev QA, and the single protected attempt remain open). Do not add CAP-012/CAP-013 here; W3 owns those rows.
- [x] Record as explicitly operator/admission-gated (never as failures): VM restart + Vault unseal for production parity and the protected `qa_hard_v2` attempt; runtime-readiness repair (decomposer smoke, `/opt` role commands); the R3/R4 live-rotation slice including landing the saved rotator-version-check fix (`~/mnemosyne-tier-b-custody/rotator-version-check-fix.patch`) via normal TDD+CI; full suite; CBM reindex, gbrain sync, and Graphify refresh at the merged SHA.
- [x] Land the audit + reconciliation through its own PR merged only after all exact-head checks complete green. Terminal invariants: `git rev-list --left-right --count origin/main...main` = `0 0`, `gh pr list --state open` empty, all Validation Commands exit 0, merged branches deleted after ancestry proof, no stale local-only work left undisclosed. Receipt: PR #30 merged exact head `67546bb` as `0865271` only after corrected exact-head run `29489028437` completed successfully; post-merge run `29489959348` completed successfully on `0865271`. The first exact-head run `29488205019` exposed a cold-runner SQLite expiry-test timing flaw, which was fixed and revalidated rather than waived. The merged audit branch was deleted only after ancestry proof.
