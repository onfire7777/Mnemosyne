# Plan: Land orphaned R2b review fixes via PR, reconcile trackers, attempt admission, capture graph-PPR dead-graph baseline

## Overview
You are the executor for round 6 of the Mnemosyne v2.0 goal loop. Work in `/Users/admin/Mnemosyne` (git toplevel containing `.planning/`, `eval/`, `infra/`). This increment advances Phase 12 Plan 12-04 delivery discipline (acceptance criteria 6–7) and opens the Phase-12 graph-PPR keystone (CAP-003, BENCH-005); it protects but does not close CAP-001..003, BENCH-005, RAIL-001..004.

**Confirmed problem (independent round-5 review, HIGH).** Commits `7b06de5` ("fix: address code review findings" — touches `infra/scripts/runtime-exclusive-lock.sh`, `infra/scripts/capture-production-evidence.sh`, `infra/scripts/rotate-production-mcp-client-cert.sh`, `tests/test_runtime_exclusive_lock.py`, `tests/test_production_mcp_client_cert_rotator.py`, `.planning/STATE.md`) and `2520c14` (moves the round-5 plan file to `docs/plans/completed/`) sit unpushed on local `main` — `main@2520c140e35bdb470026d0b8ff6e1495391b79e1`, `origin/main@435fb5351e5f8bef85b4fd5b26bc2e1690aa6388`, ahead 2, no branch, no open PR. `.planning/STATE.md` "Latest review checkpoint (2026-07-15)" (~lines 270–284) presents these fixes in delivered terms without disclosing the unpushed state. Review lanes found NO code regression; the only defect is the landing bypass.

**Content of `7b06de5` (for the PR body):** hardens runtime-lock reentry — owner-file reads made position-independent via `os.pread` (with regression test), `run_child` now requires acquired state, a direct `--verify-child` test matrix (matching ownership + one-invariant tampering) added, and forgeable `MNEMO_RUNTIME_LOCK_ACTIVE/OPERATION/OWNER_PID/OWNER_TOKEN` env checks removed because every removed check was either forgeable by the same actor who sets `OWNER_FD` or is still enforced from the authoritative locked owner file.

**Adjudication of the review finding: accept it as CONFIRMED, but do NOT use its suggested `git reset` of local `main`.** Instead: create the delivery branch at the current head, never move `main`, land via a **merge-commit** PR (repo convention — see PRs #16–#19) so both SHAs become ancestors of `origin/main`, then `git pull --ff-only` on `main`. No history rewrite, no force-push, no direct commits to `main`, no squash/rebase merge of this branch.

**Keystone context (Phase 12, HIGH PRIORITY per GOAL.md).** The `graph_ppr` retrieval channel is completely inert (0/1000 graph hits on MuSiQue per `eval/reports/phase-11-evidence.md`) because (Cause A) the eval ingests via `capture_batch` → `engine.append_evidence` and never runs consolidation, so `engine.relations == {}`; (Cause B) `_extract_simple_fact` (`consolidation.py:1742`) only matches whole-first-sentence copular forms. Full root-cause: `~/mnemosyne-tier-b-custody/GRAPH-PPR-ROOTCAUSE-2026-07-15.md`. The fix slice is round 7; **this round captures only the deterministic dead-graph baseline on dev data** (`eval/datasets/v2/qa_scale_dev_v1.json` + 16-case matrix, LOCAL engine, unprotected — NO MuSiQue/2Wiki/HotpotQA, NO `qa_hard_v2`, no protected attempt).

**Other fixed context:** the production MCP client cert is rotated and valid — do NOT treat it as a blocker or restore the expired-cert record. PRs #17–#19 and the original R2b implementation are complete — do not redo them. Local branch `codex/r2b-capture-rotator-lock` at `0826154` is intentionally preserved (documented in STATE.md's branch-hygiene note) — never delete it. Hard constraints: never weaken §31 rails/§33 test classes; full-suite/index/model/live work requires the complete fail-closed three-sample hardware admission first (all three samples ≥55% free memory plus load/model-residency/20-container/Vault/TLS checks); never mutate services to free memory; no committed secrets.

Before acting, re-read `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/ACTIVE-GOAL-OPERATING-CONTRACT.md`, and the completed round-5 plan (`docs/plans/completed/goalex-r5-land-r2b-review-hardening-push-pr-exact-.md`). Use CBM for repository understanding where available; record literally if unavailable.

## Validation Commands
- `set -e; cd "$(git rev-parse --show-toplevel)"; git diff --check; .venv/bin/ruff check --quiet .; PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_planning_traceability.py tests/test_config_drift.py`
- `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_runtime_exclusive_lock.py tests/test_production_mcp_client_cert_rotator.py`
- `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/test_parity_learning.py tests/test_learning_and_attack_suite.py tests/test_g0_harness.py`
- `bash -n infra/scripts/runtime-exclusive-lock.sh infra/scripts/capture-production-evidence.sh infra/scripts/rotate-production-mcp-client-cert.sh && shellcheck infra/scripts/runtime-exclusive-lock.sh infra/scripts/capture-production-evidence.sh infra/scripts/rotate-production-mcp-client-cert.sh`
- `git fetch origin --prune && git merge-base --is-ancestor 7b06de5 origin/main && git merge-base --is-ancestor 2520c14 origin/main && echo ANCESTRY-OK` (must pass at end of Task 2)
- `git fetch origin && test "$(git rev-parse main)" = "$(git rev-parse origin/main)" && test -z "$(git status --porcelain)" && echo MAIN-SYNCED` (end-of-round invariant)

### Task 1: Stage the orphaned delivery on a branch with an honest tracker disclosure
- [x] Verify preconditions exactly: clean tree, `main@2520c14`, `origin/main@435fb53`, and `git log --oneline origin/main..main` shows only `2520c14` and `7b06de5`. If ANY ref or diff differs, stop and record the actual state in `.planning/STATE.md` (via the normal branch/PR path) instead of proceeding blind.
- [x] Create `codex/r2b-review-fix-landing` at `2520c14` and switch to it. If it already exists, reuse only after proving it points at `2520c14`. Do NOT reset, rebase, or move `main` at any point.
- [x] On the branch, edit `.planning/STATE.md`'s "Latest review checkpoint (2026-07-15)" so it (a) records the round-5 review finding as CONFIRMED — the review fixes existed only as unpushed local commits, bypassing landing discipline — and (b) states they are being landed via this PR. Preserve the open memory-admission blocker record (45%/46%/45% vs the all-three-at-55% floor), the `0826154` branch-hygiene note, and every open freshness row. Commit as one conventional docs commit.
- [x] Run parallel read-only review lanes over `origin/main...HEAD` (correctness/security and delivery/tracker scope): authoritative owner-file checks, `os.pread` regression test, `run_child(state)` callers, `--verify-child` matrix, no stale references to the removed env vars, staged-only behavior, no validator weakened. Any NEW defect found requires a failing regression test first (RED before GREEN) and a separate conventional commit on this branch.

### Task 2: Land through exact-head CI with a merge commit
- [x] Run all targeted/static/§31-§33/hygiene commands above on the branch head, inspect the complete `origin/main...HEAD` diff, and run the secret/risky-file sweep (credentials, certificates, binaries, oversized files) before pushing.
- [x] Push the branch and open ONE evidence-bearing PR to `main`: the confirmed round-5 finding and its remediation, the `7b06de5` hardening content (see Overview), the `2520c14` plan-file move, gate results and test counts, current cert status, and an explicit statement that no live/protected/public/Vault/CA action occurred.
- [x] Require ALL gating CI jobs green on the exact pushed head SHA (`gh pr checks --watch` / `gh run list --commit <sha>`). Diagnose any failure at root cause on the branch; never merge red, never reuse stale-head CI, never weaken a validator or hook.
- [x] Merge using GitHub's **merge-commit** method only (no squash/rebase — the two SHAs must become ancestors of `origin/main`). Then verify: `git fetch origin`; `git merge-base --is-ancestor 7b06de5 origin/main` and same for `2520c14`; post-merge `main` CI green; `git checkout main && git pull --ff-only` (stop and diagnose rather than reset if fast-forward is impossible). Record PR number, exact-head CI run ID, post-merge CI run ID, and merged `main` SHA. Delete `codex/r2b-review-fix-landing` locally and remotely (no force) — never touch `codex/r2b-capture-rotator-lock@0826154`.

### Task 3: Reconcile the authoritative trackers through a second PR
- [x] From synchronized `main`, create a fresh reconciliation branch. In `.planning/STATE.md`, record the full receipts per the file's checkpoint conventions (timestamps, full SHAs, CI run IDs): code PR number, feature head, merge SHA, exact-head CI run, post-merge CI run, and the resolution of the HIGH finding.
- [x] Reconcile the live continuation truthfully: cert admission is no longer blocked by the expired leaf; R2c/R2d and R3/R4 remain open; CAP-003 and BENCH-005 remain partial; next substantive sequence is runtime-readiness repair (decomposer smoke timeout, absent `/opt` role commands) then the dev-only graph-PPR consolidation/extractor fix slice with its PBPP disclosure correction (phase-11 report discloses `postgres-recursive-ppr` but the eval runs the local engine). Leave `.planning/ROADMAP.md` plan status and all unmet `.planning/REQUIREMENTS.md` rows unchecked — this slice closes no requirement.
- [x] Run the fast hygiene/planning gates, open the docs-only PR with receipts in the body, require exact-head CI green, merge (merge commit), verify post-merge CI, `git pull --ff-only` on `main`, delete the branch. Never leave an unpushed commit on local `main`.

### Task 4: Attempt the three-sample hardware admission; run or record the gated refreshes
- [x] On final merged `main`, run the complete fail-closed three-sample hardware/TLS/Vault/service/topology preflight per `.planning/runbooks/HARDWARE-WORKLOAD-PREFLIGHT.md` (entry point `infra/scripts/eval-window-admission.sh`; all three samples must clear the 55% free-memory floor plus load, model-residency, 20-container-health, Vault, and TLS checks). Receipt: on `main@69cd2cf`, samples at `2026-07-16T03:10:32Z`, `03:10:48Z`, and `03:11:04Z` each recorded 70% free memory, load1/load5 2.78/2.35, 2.50/2.31, and 2.30/2.28, and zero resident models. Admission was rejected because Colima was stopped, so the 20-service stack, Vault, API/stream restart counts, and single-project topology were unavailable; the unchanged TLS validator also rejected the canonical root as a symlink. No reclamation or service mutation occurred.
- [x] If ADMITTED: skipped because the complete gate rejected. The serialized full suite, CBM change-detection/reindex, gbrain sync/doctor, and GSD Graphify refresh did not run, and their freshness rows remain open.
- [x] If REJECTED: recorded all three samples above and in `.planning/STATE.md`. Owner: host operator. Next safe action: restore the canonical Colima/Vault/service topology and a regular-file canonical client-auth root through the approved operator runbooks, then rerun the entire three-sample preflight. No gated operation ran.

### Task 5: Capture the deterministic graph-PPR dead-graph baseline on dev data (keystone entry)
- [ ] On a new branch `codex/phase12-graph-baseline` from final merged `main`, reproduce the dead-graph baseline using exactly the engine the eval really runs — the LOCAL engine path (`LocalMemoryEngine.graph_ppr`, no `--graph-retrieval-command`) — over `eval/datasets/v2/qa_scale_dev_v1.json` (24 questions) and the 16-case matrix: ingest via the existing `capture_batch` path, then measure and record `engine.relations` count == 0, `graph_ppr` channel contribution == 0 across all queries, current Recall@5 per the existing harness (`eval/datasets/v2/run_grounded_qa_v2.py`), and byte-identical `traces.jsonl` across two repeat runs. Dev/synthetic data only; if any step requires an admission-gated resource, stop that step and record it rather than bypass the gate.
- [ ] Encode the baseline as a pytest regression cell that PASSES against current behavior (asserting relations==0 and zero graph participation on the dev fixture, commented as the pre-fix baseline the round-7 fix slice must flip to relations>0, a positive `graph_ppr` hit on the bridge query, and Recall@5 0→1 on it). Do not commit a failing test.
- [ ] Write the named evidence artifact `eval/reports/phase-12-graph-baseline-2026-07-15.md` with measured numbers, reproduction commands, and the explicit PBPP honesty item: disclosed backend `postgres-recursive-ppr` vs actual local engine, to be reconciled in the fix slice.
- [ ] Run the hygiene gates on the branch and push it. If complete and green, land via the standard PR discipline (evidence body, exact-head CI, merge-commit, verify, cleanup); if the round ends first, record in `.planning/STATE.md` (via PR, per Task 3–4 mechanism) the branch name, pushed head SHA, and artifact path as the checkpoint from which round 7's Fix A (run consolidation post-capture) + Fix B (broaden `_extract_simple_fact`) slice resumes. End-of-round invariant: `main == origin/main`, clean tree, no unpushed commits on `main`.
